// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client/kit";
import rawIDL from "./idl/realloc_borsh.json";
import type { ReallocBorsh } from "./idl/realloc_borsh";
const IDL = rawIDL as unknown as ReallocBorsh;
import * as instructions from "./instructions";
import * as accounts from "./accounts/kit";
import * as types from "./types";

/**
 * High-level client for the `realloc_borsh` program.
 * Uses the modern @solana/kit (v2) transaction model.
 */
export class ReallocBorshClient {
  public readonly program: naclac.Program;
  public readonly instructions = instructions;
  public readonly accounts = accounts;
  public readonly types = types;
  public readonly errors = types.ProgramErrors;

  public get constants() {
    return this.program.constants;
  }

  /** The on-chain program address. */
  public get programId(): naclac.Address {
    return this.program.idl.address as naclac.Address;
  }

  constructor(
    providerOrCluster: naclac.NaclacProvider | "devnet" | "mainnet" | "localnet" | "litesvm",
    payer?: naclac.KeyPairSigner
  ) {
    let provider: naclac.NaclacProvider;
    if (typeof providerOrCluster === "string") {
      if (!payer) throw new Error("[Naclac] A payer KeyPairSigner is required when specifying a cluster string.");
      provider = naclac.createProvider(providerOrCluster, payer);
    } else {
      provider = providerOrCluster;
    }
    this.program = new naclac.Program(IDL, provider, false);
  }

  /**
   * Builds the `initGrowable` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public initGrowable(args?: Record<string, never>, accounts?: Partial<instructions.InitGrowableAccounts>) {
    return instructions.initGrowable(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `resizeGrowable` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public resizeGrowable(args: instructions.ResizeGrowableArgs, accounts?: Partial<instructions.ResizeGrowableAccounts>) {
    return instructions.resizeGrowable(this.program, args ?? {}, accounts);
  }

  /** Derives the PDA for a `growable` account. */
  public async getGrowablePda(seeds: {
  }): Promise<readonly [naclac.Address, number]> {
    return naclac.getProgramDerivedAddress({
      programAddress: this.programId,
      seeds: [
                new Uint8Array([103, 114, 111, 119, 97, 98, 108, 101])
      ]
    });
  }

  /** Fetches a `Growable` account. Throws if it does not exist. */
  public fetchGrowable(address: naclac.Address | string) {
    return accounts.fetchGrowable(this.program.provider.rpc as any, naclac.address(address as string));
  }

  /** Fetches a `Growable` account. Returns a MaybeAccount (check `.exists`) if it does not exist. */
  public fetchMaybeGrowable(address: naclac.Address | string) {
    return accounts.fetchMaybeGrowable(this.program.provider.rpc as any, naclac.address(address as string));
  }

  /** Fetches multiple `Growable` accounts by address. Throws if any do not exist. */
  public fetchAllGrowables(addresses: Array<naclac.Address | string>) {
    return accounts.fetchAllGrowable(this.program.provider.rpc as any, addresses.map((a) => naclac.address(a as string)));
  }

  /** Fetches multiple `Growable` accounts. Missing accounts have `.exists = false`. */
  public fetchAllMaybeGrowables(addresses: Array<naclac.Address | string>) {
    return accounts.fetchAllMaybeGrowable(this.program.provider.rpc as any, addresses.map((a) => naclac.address(a as string)));
  }

  /** Fetches ALL `Growable` accounts owned by this program. */
  public fetchAllGrowableByOwner(options?: { commitment?: any; filters?: unknown[] }) {
    return accounts.fetchAllGrowableByOwner(this.program.provider.rpc as any, this.programId, options);
  }

}
