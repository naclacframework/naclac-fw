// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client/legacy";
import rawIDL from "./idl/realloc_borsh.json";
import type { ReallocBorsh } from "./idl/realloc_borsh";
const IDL = rawIDL as unknown as ReallocBorsh;
import * as instructions from "./instructions";
import * as accounts from "./accounts/legacy";
import * as types from "./types";

/**
 * High-level client for the `realloc_borsh` program.
 * Uses the classic @solana/web3.js (v1) transaction model.
 */
export class ReallocBorshClient {
  public readonly program: naclac.LegacyProgram;
  public readonly instructions = instructions;
  public readonly accounts = accounts;
  public readonly types = types;
  public readonly errors = types.ProgramErrors;

  public get constants() {
    return this.program.constants;
  }

  /** The on-chain program address as a `PublicKey`. */
  public get programId(): naclac.PublicKey {
    return this.program.programId;
  }

  constructor(
    providerOrCluster: naclac.LegacyProvider | "devnet" | "mainnet" | "localnet" | string,
    payer?: naclac.Keypair
  ) {
    let provider: naclac.LegacyProvider;
    if (typeof providerOrCluster === "string") {
      let url = providerOrCluster;
      if (providerOrCluster === "devnet") url = "https://api.devnet.solana.com";
      else if (providerOrCluster === "mainnet") url = "https://api.mainnet-beta.solana.com";
      else if (providerOrCluster === "localnet") url = "http://127.0.0.1:8899";
      const connection = new naclac.Connection(url, "confirmed");
      provider = { connection, payer, publicKey: payer ? payer.publicKey : undefined };
    } else {
      provider = providerOrCluster;
    }
    this.program = new naclac.LegacyProgram(IDL, provider);
  }

  /**
   * Builds the `initGrowable` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public initGrowable(args?: Record<string, never>, accounts?: Partial<instructions.InitGrowableAccounts>) {
    return instructions.initGrowable(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `resizeGrowable` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public resizeGrowable(args: instructions.ResizeGrowableArgs, accounts?: Partial<instructions.ResizeGrowableAccounts>) {
    return instructions.resizeGrowable(this.program, args ?? {}, accounts);
  }

  /** Derives the PDA for a `growable` account. Returns `[PublicKey, bumpSeed]`. */
  public getGrowablePda(seeds: {
  }): [naclac.PublicKey, number] {
    const [pda, bump] = naclac.PublicKey.findProgramAddressSync(
      [
                new Uint8Array([103, 114, 111, 119, 97, 98, 108, 101])
      ],
      this.programId
    );
    return [pda, bump];
  }

  /** Fetches a `Growable` account. Throws if it does not exist. */
  public fetchGrowable(address: naclac.PublicKey | string) {
    return this.program.account['Growable'].fetch(address);
  }

  /** Fetches a `Growable` account. Returns `null` if it does not exist. */
  public async fetchMaybeGrowable(address: naclac.PublicKey | string) {
    try { return await this.program.account['Growable'].fetch(address); }
    catch { return null; }
  }

  /** Fetches multiple `Growable` accounts in a single RPC call. */
  public fetchAllGrowables(addresses: Array<naclac.PublicKey | string>, opts?: { dropCorrupted?: boolean }) {
    return this.program.account['Growable'].fetchMultiple(addresses, opts);
  }

  /** Fetches all on-chain `Growable` accounts owned by this program. */
  public fetchAllGrowableByOwner(options?: { dropCorrupted?: boolean; filters?: any[] }) {
    return this.program.account['Growable'].all(options);
  }

}
