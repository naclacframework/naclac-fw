[package]
name = "events_borsh"
version = "0.1.0"
description = "Regression test: event emission round-trips correctly in all three backends (solana-borsh)"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]

[features]
no-entrypoint = []
cpi = ["no-entrypoint"]
custom-heap = []
custom-panic = []
debug-mode = []
default = ["borsh"]
borsh = ["naclac-lang/borsh"]

[dependencies]
naclac-lang = { version = "0.1.0", path = "../../../../naclac-lang" }

[dev-dependencies]
naclac-client = { version = "0.1.0", path = "../../../../naclac-client" }
events-borsh-client = { version = "0.1.0", path = "../../clients/rust/events_borsh" }

[lints]
workspace = true
