// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `emit_via_self_cpi_baseline` instruction. */
export const EMIT_VIA_SELF_CPI_BASELINE_DISCRIMINATOR = new Uint8Array([140, 85, 165, 90, 139, 159, 143, 156]);

/** Accounts for the `emitViaSelfCpiBaseline` instruction. */
export interface EmitViaSelfCpiBaselineAccounts {
  /**
   * The invoking program's own account. A self-CPI's target program_id is
   * resolved from the current instruction's own account list, not from
   * `invoke`'s arguments — so this program must appear here, the same
   * reason Anchor's `emit_cpi!` requires a `Program<'info, Self>` account.
   * SAFETY: only its address is checked; its data is never read.
   */
  program: naclac.Address | string;
}

/**
 * Builds the `emitViaSelfCpiBaseline` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 * 
 * Pure self-CPI baseline: invokes `log_event` with zero-length payload and
 * zero accounts, isolating the fixed CU cost of a self-CPI from any
 * per-byte payload cost.
 */
export function emitViaSelfCpiBaseline(
  program: any,
  args?: Record<string, never>,
  accounts?: Partial<EmitViaSelfCpiBaselineAccounts>
) {
  const builder = program.methods.emit_via_self_cpi_baseline(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
