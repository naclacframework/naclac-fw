// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `emit_via_self_cpi_signed_baseline` instruction. */
export const EMIT_VIA_SELF_CPI_SIGNED_BASELINE_DISCRIMINATOR = new Uint8Array([189, 43, 89, 129, 100, 10, 118, 18]);

/** Accounts for the `emitViaSelfCpiSignedBaseline` instruction. */
export interface EmitViaSelfCpiSignedBaselineAccounts {
  /**
   * SAFETY: only its address (against the PDA derived from
   * `SEED_EVENT_AUTHORITY`) is checked here; it's the CPI target
   * (`log_event_signed`) that checks `is_signer`, once `invoke_signed`
   * below has actually marked it as one. Its data is never read.
   */
  event_authority?: naclac.Address | string;
  /** SAFETY: only its address is checked; its data is never read. */
  program: naclac.Address | string;
}

/**
 * Builds the `emitViaSelfCpiSignedBaseline` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 * 
 * Real, security-correct self-CPI baseline: signs the `event_authority` PDA
 * via `invoke_signed` (matching Anchor's `emit_cpi!`), unlike
 * `emit_via_self_cpi_baseline`'s plain unsigned `invoke`. Measures the true
 * CU cost a spoof-resistant `emit_cpi!` feature would actually pay.
 */
export function emitViaSelfCpiSignedBaseline(
  program: any,
  args?: Record<string, never>,
  accounts?: Partial<EmitViaSelfCpiSignedBaselineAccounts>
) {
  const builder = program.methods.emit_via_self_cpi_signed_baseline(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
