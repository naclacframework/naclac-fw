// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `emit_via_sol_log_data_sized` instruction. */
export const EMIT_VIA_SOL_LOG_DATA_SIZED_DISCRIMINATOR = new Uint8Array([251, 28, 204, 156, 163, 133, 238, 156]);

/** Instruction arguments for `emitViaSolLogDataSized`. */
export interface EmitViaSolLogDataSizedArgs {
  size: number;
}

/** Accounts for the `emitViaSolLogDataSized` instruction. */
export interface EmitViaSolLogDataSizedAccounts {
  /** SAFETY: only its address is checked; its data is never read. */
  program: naclac.Address | string;
}

/**
 * Builds the `emitViaSolLogDataSized` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 * 
 * `sol_log_data` emit with a `size`-byte payload — sweeps CU cost against
 * payload size, directly comparable to `emit_via_self_cpi_sized`.
 */
export function emitViaSolLogDataSized(
  program: any,
  args: EmitViaSolLogDataSizedArgs,
  accounts?: Partial<EmitViaSolLogDataSizedAccounts>
) {
  const builder = program.methods.emit_via_sol_log_data_sized(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
