// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `no_cpi_baseline` instruction. */
export const NO_CPI_BASELINE_DISCRIMINATOR = new Uint8Array([4, 133, 150, 205, 53, 226, 164, 73]);

/** Accounts for the `noCpiBaseline` instruction. */
export interface NoCpiBaselineAccounts {
  /** SAFETY: only its address is checked; its data is never read. */
  program: naclac.Address | string;
}

/**
 * Builds the `noCpiBaseline` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function noCpiBaseline(
  program: any,
  args?: Record<string, never>,
  accounts?: Partial<NoCpiBaselineAccounts>
) {
  const builder = program.methods.no_cpi_baseline(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
