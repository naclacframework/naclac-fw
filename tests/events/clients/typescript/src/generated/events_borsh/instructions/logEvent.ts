// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `log_event` instruction. */
export const LOG_EVENT_DISCRIMINATOR = new Uint8Array([5, 9, 90, 141, 223, 134, 57, 217]);

/** Instruction arguments for `logEvent`. */
export interface LogEventArgs {
  data: Uint8Array;
}

/** Accounts for the `logEvent` instruction. */
export interface LogEventAccounts {
}

/**
 * Builds the `logEvent` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function logEvent(
  program: any,
  args: LogEventArgs,
  accounts?: Partial<LogEventAccounts>
) {
  const builder = program.methods.log_event(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
