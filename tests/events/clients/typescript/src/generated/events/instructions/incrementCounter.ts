// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `increment_counter` instruction. */
export const INCREMENT_COUNTER_DISCRIMINATOR = new Uint8Array([16, 125, 2, 171, 73, 24, 207, 229]);

/** Accounts for the `incrementCounter` instruction. */
export interface IncrementCounterAccounts {
  counter?: naclac.Address | string;
}

/**
 * Builds the `incrementCounter` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function incrementCounter(
  program: any,
  args?: Record<string, never>,
  accounts?: Partial<IncrementCounterAccounts>
) {
  const builder = program.methods.increment_counter(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
