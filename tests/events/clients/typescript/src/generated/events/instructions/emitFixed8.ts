// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `emit_fixed_8` instruction. */
export const EMIT_FIXED_8_DISCRIMINATOR = new Uint8Array([166, 42, 161, 212, 88, 155, 188, 151]);

/** Accounts for the `emitFixed8` instruction. */
export interface EmitFixed8Accounts {
  /** SAFETY: only its address is checked; its data is never read. */
  program: naclac.Address | string;
}

/**
 * Builds the `emitFixed8` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function emitFixed8(
  program: any,
  args?: Record<string, never>,
  accounts?: Partial<EmitFixed8Accounts>
) {
  const builder = program.methods.emit_fixed_8(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
