// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `emit_via_self_cpi_fixed_8` instruction. */
export const EMIT_VIA_SELF_CPI_FIXED_8_DISCRIMINATOR = new Uint8Array([62, 26, 68, 246, 107, 11, 141, 38]);

/** Accounts for the `emitViaSelfCpiFixed8` instruction. */
export interface EmitViaSelfCpiFixed8Accounts {
  /** SAFETY: only its address is checked; its data is never read. */
  program: naclac.Address | string;
}

/**
 * Builds the `emitViaSelfCpiFixed8` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 * 
 * Self-CPI carrying an 8-byte payload with no length prefix — the CU cost
 * a real emit-cpi-for-fixed-events feature would actually pay, as opposed
 * to `emit_via_self_cpi_sized`'s generic dynamic-payload encoding.
 */
export function emitViaSelfCpiFixed8(
  program: any,
  args?: Record<string, never>,
  accounts?: Partial<EmitViaSelfCpiFixed8Accounts>
) {
  const builder = program.methods.emit_via_self_cpi_fixed_8(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
