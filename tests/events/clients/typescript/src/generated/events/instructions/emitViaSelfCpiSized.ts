// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `emit_via_self_cpi_sized` instruction. */
export const EMIT_VIA_SELF_CPI_SIZED_DISCRIMINATOR = new Uint8Array([202, 145, 216, 139, 183, 147, 134, 5]);

/** Instruction arguments for `emitViaSelfCpiSized`. */
export interface EmitViaSelfCpiSizedArgs {
  size: number;
}

/** Accounts for the `emitViaSelfCpiSized` instruction. */
export interface EmitViaSelfCpiSizedAccounts {
  /** SAFETY: only its address is checked; its data is never read. */
  program: naclac.Address | string;
}

/**
 * Builds the `emitViaSelfCpiSized` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 * 
 * Self-CPI with a `size`-byte payload — sweeps CU cost against payload size.
 */
export function emitViaSelfCpiSized(
  program: any,
  args: EmitViaSelfCpiSizedArgs,
  accounts?: Partial<EmitViaSelfCpiSizedAccounts>
) {
  const builder = program.methods.emit_via_self_cpi_sized(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
