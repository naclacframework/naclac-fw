// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `emit_fixed_128` instruction. */
export const EMIT_FIXED_128_DISCRIMINATOR = new Uint8Array([23, 253, 190, 60, 160, 216, 143, 40]);

/** Accounts for the `emitFixed128` instruction. */
export interface EmitFixed128Accounts {
  /** SAFETY: only its address is checked; its data is never read. */
  program: naclac.Address | string;
}

/**
 * Builds the `emitFixed128` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function emitFixed128(
  program: any,
  args?: Record<string, never>,
  accounts?: Partial<EmitFixed128Accounts>
) {
  const builder = program.methods.emit_fixed_128(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
