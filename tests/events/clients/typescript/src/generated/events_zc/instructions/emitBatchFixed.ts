// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `emit_batch_fixed` instruction. */
export const EMIT_BATCH_FIXED_DISCRIMINATOR = new Uint8Array([39, 157, 69, 18, 179, 149, 14, 42]);

/** Accounts for the `emitBatchFixed` instruction. */
export interface EmitBatchFixedAccounts {
  counter?: naclac.Address | string;
}

/**
 * Builds the `emitBatchFixed` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function emitBatchFixed(
  program: any,
  args?: Record<string, never>,
  accounts?: Partial<EmitBatchFixedAccounts>
) {
  const builder = program.methods.emit_batch_fixed(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
