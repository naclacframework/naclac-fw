// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `emit_batch_alloc` instruction. */
export const EMIT_BATCH_ALLOC_DISCRIMINATOR = new Uint8Array([186, 210, 167, 115, 104, 189, 246, 44]);

/** Accounts for the `emitBatchAlloc` instruction. */
export interface EmitBatchAllocAccounts {
  counter?: naclac.Address | string;
}

/**
 * Builds the `emitBatchAlloc` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function emitBatchAlloc(
  program: any,
  args?: Record<string, never>,
  accounts?: Partial<EmitBatchAllocAccounts>
) {
  const builder = program.methods.emit_batch_alloc(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
