// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `emit_fixed_512` instruction. */
export const EMIT_FIXED_512_DISCRIMINATOR = new Uint8Array([96, 239, 41, 3, 76, 227, 248, 35]);

/** Accounts for the `emitFixed512` instruction. */
export interface EmitFixed512Accounts {
  /** SAFETY: only its address is checked; its data is never read. */
  program: naclac.Address | string;
}

/**
 * Builds the `emitFixed512` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function emitFixed512(
  program: any,
  args?: Record<string, never>,
  accounts?: Partial<EmitFixed512Accounts>
) {
  const builder = program.methods.emit_fixed_512(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
