// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `emit_fixed_2048` instruction. */
export const EMIT_FIXED_2048_DISCRIMINATOR = new Uint8Array([180, 35, 94, 49, 242, 85, 196, 100]);

/** Accounts for the `emitFixed2048` instruction. */
export interface EmitFixed2048Accounts {
  /** SAFETY: only its address is checked; its data is never read. */
  program: naclac.Address | string;
}

/**
 * Builds the `emitFixed2048` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function emitFixed2048(
  program: any,
  args?: Record<string, never>,
  accounts?: Partial<EmitFixed2048Accounts>
) {
  const builder = program.methods.emit_fixed_2048(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
