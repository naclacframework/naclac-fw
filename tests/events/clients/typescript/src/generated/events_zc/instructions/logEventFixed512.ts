// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `log_event_fixed_512` instruction. */
export const LOG_EVENT_FIXED_512_DISCRIMINATOR = new Uint8Array([134, 104, 190, 181, 234, 155, 142, 191]);

/** Instruction arguments for `logEventFixed512`. */
export interface LogEventFixed512Args {
  data: string | Uint8Array;
}

/** Accounts for the `logEventFixed512` instruction. */
export interface LogEventFixed512Accounts {
}

/**
 * Builds the `logEventFixed512` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function logEventFixed512(
  program: any,
  args: LogEventFixed512Args,
  accounts?: Partial<LogEventFixed512Accounts>
) {
  const builder = program.methods.log_event_fixed_512(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
