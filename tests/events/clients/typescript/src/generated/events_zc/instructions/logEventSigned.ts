// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `log_event_signed` instruction. */
export const LOG_EVENT_SIGNED_DISCRIMINATOR = new Uint8Array([251, 224, 120, 207, 75, 64, 255, 194]);

/** Instruction arguments for `logEventSigned`. */
export interface LogEventSignedArgs {
  data: string | Uint8Array;
}

/** Accounts for the `logEventSigned` instruction. */
export interface LogEventSignedAccounts {
  /**
   * SAFETY: only its address (against the PDA derived from
   * `SEED_EVENT_AUTHORITY`) and `is_signer` are checked; its data is
   * never read.
   */
  eventAuthority?: naclac.Address | string;
}

/**
 * Builds the `logEventSigned` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function logEventSigned(
  program: any,
  args: LogEventSignedArgs,
  accounts?: Partial<LogEventSignedAccounts>
) {
  const builder = program.methods.log_event_signed(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
