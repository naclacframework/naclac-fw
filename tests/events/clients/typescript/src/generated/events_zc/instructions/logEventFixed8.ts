// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `log_event_fixed_8` instruction. */
export const LOG_EVENT_FIXED_8_DISCRIMINATOR = new Uint8Array([203, 97, 44, 17, 196, 174, 17, 22]);

/** Instruction arguments for `logEventFixed8`. */
export interface LogEventFixed8Args {
  data: string | Uint8Array;
}

/** Accounts for the `logEventFixed8` instruction. */
export interface LogEventFixed8Accounts {
}

/**
 * Builds the `logEventFixed8` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function logEventFixed8(
  program: any,
  args: LogEventFixed8Args,
  accounts?: Partial<LogEventFixed8Accounts>
) {
  const builder = program.methods.log_event_fixed_8(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
