// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client/kit";
import rawIDL from "./idl/events_zc.json";
import type { EventsZc } from "./idl/events_zc";
const IDL = rawIDL as unknown as EventsZc;
import * as instructions from "./instructions";
import * as accounts from "./accounts/kit";
import * as types from "./types";

/**
 * High-level client for the `events_zc` program.
 * Uses the modern @solana/kit (v2) transaction model.
 */
export class EventsZcClient {
  public readonly program: naclac.Program;
  public readonly instructions = instructions;
  public readonly accounts = accounts;
  public readonly types = types;
  public readonly errors = types.ProgramErrors;

  public get constants() {
    return this.program.constants;
  }

  /** The on-chain program address. */
  public get programId(): naclac.Address {
    return this.program.idl.address as naclac.Address;
  }

  constructor(
    providerOrCluster: naclac.NaclacProvider | "devnet" | "mainnet" | "localnet" | "litesvm",
    payer?: naclac.KeyPairSigner
  ) {
    let provider: naclac.NaclacProvider;
    if (typeof providerOrCluster === "string") {
      if (!payer) throw new Error("[Naclac] A payer KeyPairSigner is required when specifying a cluster string.");
      provider = naclac.createProvider(providerOrCluster, payer);
    } else {
      provider = providerOrCluster;
    }
    this.program = new naclac.Program(IDL, provider, true);
  }

  /**
   * Builds the `initCounter` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public initCounter(args?: Record<string, never>, accounts?: Partial<instructions.InitCounterAccounts>) {
    return instructions.initCounter(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `incrementCounter` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public incrementCounter(args?: Record<string, never>, accounts?: Partial<instructions.IncrementCounterAccounts>) {
    return instructions.incrementCounter(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `touchCounterExplicitBump` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public touchCounterExplicitBump(args?: Record<string, never>, accounts?: Partial<instructions.TouchCounterExplicitBumpAccounts>) {
    return instructions.touchCounterExplicitBump(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `emitBatchFixed` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public emitBatchFixed(args?: Record<string, never>, accounts?: Partial<instructions.EmitBatchFixedAccounts>) {
    return instructions.emitBatchFixed(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `emitBatchAlloc` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public emitBatchAlloc(args?: Record<string, never>, accounts?: Partial<instructions.EmitBatchAllocAccounts>) {
    return instructions.emitBatchAlloc(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `logEvent` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public logEvent(args: instructions.LogEventArgs, accounts?: Partial<instructions.LogEventAccounts>) {
    return instructions.logEvent(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `noCpiBaseline` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public noCpiBaseline(args?: Record<string, never>, accounts?: Partial<instructions.NoCpiBaselineAccounts>) {
    return instructions.noCpiBaseline(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `emitViaSelfCpiBaseline` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public emitViaSelfCpiBaseline(args?: Record<string, never>, accounts?: Partial<instructions.EmitViaSelfCpiBaselineAccounts>) {
    return instructions.emitViaSelfCpiBaseline(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `emitViaSelfCpiSized` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public emitViaSelfCpiSized(args: instructions.EmitViaSelfCpiSizedArgs, accounts?: Partial<instructions.EmitViaSelfCpiSizedAccounts>) {
    return instructions.emitViaSelfCpiSized(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `emitViaSolLogDataBaseline` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public emitViaSolLogDataBaseline(args?: Record<string, never>, accounts?: Partial<instructions.EmitViaSolLogDataBaselineAccounts>) {
    return instructions.emitViaSolLogDataBaseline(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `emitViaSolLogDataSized` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public emitViaSolLogDataSized(args: instructions.EmitViaSolLogDataSizedArgs, accounts?: Partial<instructions.EmitViaSolLogDataSizedAccounts>) {
    return instructions.emitViaSolLogDataSized(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `emitFixed8` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public emitFixed8(args?: Record<string, never>, accounts?: Partial<instructions.EmitFixed8Accounts>) {
    return instructions.emitFixed8(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `emitFixed128` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public emitFixed128(args?: Record<string, never>, accounts?: Partial<instructions.EmitFixed128Accounts>) {
    return instructions.emitFixed128(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `emitFixed512` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public emitFixed512(args?: Record<string, never>, accounts?: Partial<instructions.EmitFixed512Accounts>) {
    return instructions.emitFixed512(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `emitFixed2048` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public emitFixed2048(args?: Record<string, never>, accounts?: Partial<instructions.EmitFixed2048Accounts>) {
    return instructions.emitFixed2048(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `logEventFixed8` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public logEventFixed8(args: instructions.LogEventFixed8Args, accounts?: Partial<instructions.LogEventFixed8Accounts>) {
    return instructions.logEventFixed8(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `logEventFixed128` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public logEventFixed128(args: instructions.LogEventFixed128Args, accounts?: Partial<instructions.LogEventFixed128Accounts>) {
    return instructions.logEventFixed128(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `logEventFixed512` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public logEventFixed512(args: instructions.LogEventFixed512Args, accounts?: Partial<instructions.LogEventFixed512Accounts>) {
    return instructions.logEventFixed512(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `logEventFixed2048` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public logEventFixed2048(args: instructions.LogEventFixed2048Args, accounts?: Partial<instructions.LogEventFixed2048Accounts>) {
    return instructions.logEventFixed2048(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `emitViaSelfCpiFixed8` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public emitViaSelfCpiFixed8(args?: Record<string, never>, accounts?: Partial<instructions.EmitViaSelfCpiFixed8Accounts>) {
    return instructions.emitViaSelfCpiFixed8(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `emitViaSelfCpiFixed128` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public emitViaSelfCpiFixed128(args?: Record<string, never>, accounts?: Partial<instructions.EmitViaSelfCpiFixed128Accounts>) {
    return instructions.emitViaSelfCpiFixed128(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `emitViaSelfCpiFixed512` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public emitViaSelfCpiFixed512(args?: Record<string, never>, accounts?: Partial<instructions.EmitViaSelfCpiFixed512Accounts>) {
    return instructions.emitViaSelfCpiFixed512(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `emitViaSelfCpiFixed2048` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public emitViaSelfCpiFixed2048(args?: Record<string, never>, accounts?: Partial<instructions.EmitViaSelfCpiFixed2048Accounts>) {
    return instructions.emitViaSelfCpiFixed2048(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `logEventSigned` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public logEventSigned(args: instructions.LogEventSignedArgs, accounts?: Partial<instructions.LogEventSignedAccounts>) {
    return instructions.logEventSigned(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `emitViaSelfCpiSignedBaseline` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public emitViaSelfCpiSignedBaseline(args?: Record<string, never>, accounts?: Partial<instructions.EmitViaSelfCpiSignedBaselineAccounts>) {
    return instructions.emitViaSelfCpiSignedBaseline(this.program, args ?? {}, accounts);
  }

  /** Derives the PDA for a `counter` account. */
  public async getCounterPda(seeds: {
  }): Promise<readonly [naclac.Address, number]> {
    return naclac.getProgramDerivedAddress({
      programAddress: this.programId,
      seeds: [
                new Uint8Array([99, 111, 117, 110, 116, 101, 114])
      ]
    });
  }

  /** Derives the PDA for a `event_authority` account. */
  public async getEventAuthorityPda(seeds: {
  }): Promise<readonly [naclac.Address, number]> {
    return naclac.getProgramDerivedAddress({
      programAddress: this.programId,
      seeds: [
                new Uint8Array([95, 95, 101, 118, 101, 110, 116, 95, 97, 117, 116, 104, 111, 114, 105, 116, 121])
      ]
    });
  }

  /** Fetches a `Counter` account. Throws if it does not exist. */
  public fetchCounter(address: naclac.Address | string) {
    return accounts.fetchCounter(this.program.provider.rpc as any, naclac.address(address as string));
  }

  /** Fetches a `Counter` account. Returns a MaybeAccount (check `.exists`) if it does not exist. */
  public fetchMaybeCounter(address: naclac.Address | string) {
    return accounts.fetchMaybeCounter(this.program.provider.rpc as any, naclac.address(address as string));
  }

  /** Fetches multiple `Counter` accounts by address. Throws if any do not exist. */
  public fetchAllCounters(addresses: Array<naclac.Address | string>) {
    return accounts.fetchAllCounter(this.program.provider.rpc as any, addresses.map((a) => naclac.address(a as string)));
  }

  /** Fetches multiple `Counter` accounts. Missing accounts have `.exists = false`. */
  public fetchAllMaybeCounters(addresses: Array<naclac.Address | string>) {
    return accounts.fetchAllMaybeCounter(this.program.provider.rpc as any, addresses.map((a) => naclac.address(a as string)));
  }

  /** Subscribes to `CounterIncremented` events. Returns a listener ID. */
  public onCounterIncremented(callback: (event: types.CounterIncremented, slot: number, signature: string) => void) {
    return types.addCounterIncrementedListener(this.program, callback);
  }

  /** Awaits the next `CounterIncremented` event. Resolves `null` on timeout. */
  public waitForCounterIncremented(options?: { timeoutMs?: number }) {
    return this.program.waitForEvent<types.CounterIncremented>("CounterIncremented", options);
  }

  /** Decodes every `CounterIncremented` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForCounterIncremented` — prefer this when you already know which transaction you're checking. */
  public parseCounterIncrementedEvents(logs: readonly string[]) {
    return this.program.parseEvents<types.CounterIncremented>("CounterIncremented", logs);
  }

  /** Subscribes to `BatchTouched` events. Returns a listener ID. */
  public onBatchTouched(callback: (event: types.BatchTouched, slot: number, signature: string) => void) {
    return types.addBatchTouchedListener(this.program, callback);
  }

  /** Awaits the next `BatchTouched` event. Resolves `null` on timeout. */
  public waitForBatchTouched(options?: { timeoutMs?: number }) {
    return this.program.waitForEvent<types.BatchTouched>("BatchTouched", options);
  }

  /** Decodes every `BatchTouched` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForBatchTouched` — prefer this when you already know which transaction you're checking. */
  public parseBatchTouchedEvents(logs: readonly string[]) {
    return this.program.parseEvents<types.BatchTouched>("BatchTouched", logs);
  }

  /** Subscribes to `BatchTouchedAlloc` events. Returns a listener ID. */
  public onBatchTouchedAlloc(callback: (event: types.BatchTouchedAlloc, slot: number, signature: string) => void) {
    return types.addBatchTouchedAllocListener(this.program, callback);
  }

  /** Awaits the next `BatchTouchedAlloc` event. Resolves `null` on timeout. */
  public waitForBatchTouchedAlloc(options?: { timeoutMs?: number }) {
    return this.program.waitForEvent<types.BatchTouchedAlloc>("BatchTouchedAlloc", options);
  }

  /** Decodes every `BatchTouchedAlloc` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForBatchTouchedAlloc` — prefer this when you already know which transaction you're checking. */
  public parseBatchTouchedAllocEvents(logs: readonly string[]) {
    return this.program.parseEvents<types.BatchTouchedAlloc>("BatchTouchedAlloc", logs);
  }

  /** Subscribes to `SizedPayload` events. Returns a listener ID. */
  public onSizedPayload(callback: (event: types.SizedPayload, slot: number, signature: string) => void) {
    return types.addSizedPayloadListener(this.program, callback);
  }

  /** Awaits the next `SizedPayload` event. Resolves `null` on timeout. */
  public waitForSizedPayload(options?: { timeoutMs?: number }) {
    return this.program.waitForEvent<types.SizedPayload>("SizedPayload", options);
  }

  /** Decodes every `SizedPayload` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForSizedPayload` — prefer this when you already know which transaction you're checking. */
  public parseSizedPayloadEvents(logs: readonly string[]) {
    return this.program.parseEvents<types.SizedPayload>("SizedPayload", logs);
  }

  /** Subscribes to `FixedPayload8` events. Returns a listener ID. */
  public onFixedPayload8(callback: (event: types.FixedPayload8, slot: number, signature: string) => void) {
    return types.addFixedPayload8Listener(this.program, callback);
  }

  /** Awaits the next `FixedPayload8` event. Resolves `null` on timeout. */
  public waitForFixedPayload8(options?: { timeoutMs?: number }) {
    return this.program.waitForEvent<types.FixedPayload8>("FixedPayload8", options);
  }

  /** Decodes every `FixedPayload8` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForFixedPayload8` — prefer this when you already know which transaction you're checking. */
  public parseFixedPayload8Events(logs: readonly string[]) {
    return this.program.parseEvents<types.FixedPayload8>("FixedPayload8", logs);
  }

  /** Subscribes to `FixedPayload128` events. Returns a listener ID. */
  public onFixedPayload128(callback: (event: types.FixedPayload128, slot: number, signature: string) => void) {
    return types.addFixedPayload128Listener(this.program, callback);
  }

  /** Awaits the next `FixedPayload128` event. Resolves `null` on timeout. */
  public waitForFixedPayload128(options?: { timeoutMs?: number }) {
    return this.program.waitForEvent<types.FixedPayload128>("FixedPayload128", options);
  }

  /** Decodes every `FixedPayload128` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForFixedPayload128` — prefer this when you already know which transaction you're checking. */
  public parseFixedPayload128Events(logs: readonly string[]) {
    return this.program.parseEvents<types.FixedPayload128>("FixedPayload128", logs);
  }

  /** Subscribes to `FixedPayload512` events. Returns a listener ID. */
  public onFixedPayload512(callback: (event: types.FixedPayload512, slot: number, signature: string) => void) {
    return types.addFixedPayload512Listener(this.program, callback);
  }

  /** Awaits the next `FixedPayload512` event. Resolves `null` on timeout. */
  public waitForFixedPayload512(options?: { timeoutMs?: number }) {
    return this.program.waitForEvent<types.FixedPayload512>("FixedPayload512", options);
  }

  /** Decodes every `FixedPayload512` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForFixedPayload512` — prefer this when you already know which transaction you're checking. */
  public parseFixedPayload512Events(logs: readonly string[]) {
    return this.program.parseEvents<types.FixedPayload512>("FixedPayload512", logs);
  }

  /** Subscribes to `FixedPayload2048` events. Returns a listener ID. */
  public onFixedPayload2048(callback: (event: types.FixedPayload2048, slot: number, signature: string) => void) {
    return types.addFixedPayload2048Listener(this.program, callback);
  }

  /** Awaits the next `FixedPayload2048` event. Resolves `null` on timeout. */
  public waitForFixedPayload2048(options?: { timeoutMs?: number }) {
    return this.program.waitForEvent<types.FixedPayload2048>("FixedPayload2048", options);
  }

  /** Decodes every `FixedPayload2048` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForFixedPayload2048` — prefer this when you already know which transaction you're checking. */
  public parseFixedPayload2048Events(logs: readonly string[]) {
    return this.program.parseEvents<types.FixedPayload2048>("FixedPayload2048", logs);
  }

  /** Removes a registered event listener by its ID. */
  public removeEventListener(listenerId: number) {
    return types.removeListener(this.program, listenerId);
  }
  /** Fetches ALL `Counter` accounts owned by this program. */
  public fetchAllCounterByOwner(options?: { commitment?: any; filters?: unknown[] }) {
    return accounts.fetchAllCounterByOwner(this.program.provider.rpc as any, this.programId, options);
  }

}
