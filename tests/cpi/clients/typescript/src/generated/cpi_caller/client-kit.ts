// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client/kit";
import rawIDL from "./idl/cpi_caller.json";
import type { CpiCaller } from "./idl/cpi_caller";
const IDL = rawIDL as unknown as CpiCaller;
import * as instructions from "./instructions";
import * as accounts from "./accounts/kit";
import * as types from "./types";

/**
 * High-level client for the `cpi_caller` program.
 * Uses the modern @solana/kit (v2) transaction model.
 */
export class CpiCallerClient {
  public readonly program: naclac.Program;
  public readonly instructions = instructions;
  public readonly accounts = accounts;
  public readonly types = types;
  public readonly errors = types.ProgramErrors;

  public get constants() {
    return this.program.constants;
  }

  /** The on-chain program address. */
  public get programId(): naclac.Address {
    return this.program.idl.address as naclac.Address;
  }

  constructor(
    providerOrCluster: naclac.NaclacProvider | "devnet" | "mainnet" | "localnet" | "litesvm",
    payer?: naclac.KeyPairSigner
  ) {
    let provider: naclac.NaclacProvider;
    if (typeof providerOrCluster === "string") {
      if (!payer) throw new Error("[Naclac] A payer KeyPairSigner is required when specifying a cluster string.");
      provider = naclac.createProvider(providerOrCluster, payer);
    } else {
      provider = providerOrCluster;
    }
    this.program = new naclac.Program(IDL, provider, true);
  }

  /**
   * Builds the `initCallerAuthority` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public initCallerAuthority(args?: Record<string, never>, accounts?: Partial<instructions.InitCallerAuthorityAccounts>) {
    return instructions.initCallerAuthority(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `callSystemTransfer` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public callSystemTransfer(args: instructions.CallSystemTransferArgs, accounts?: Partial<instructions.CallSystemTransferAccounts>) {
    return instructions.callSystemTransfer(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `callSetupCounter` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public callSetupCounter(args?: Record<string, never>, accounts?: Partial<instructions.CallSetupCounterAccounts>) {
    return instructions.callSetupCounter(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `callAuthorizedIncrement` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public callAuthorizedIncrement(args?: Record<string, never>, accounts?: Partial<instructions.CallAuthorizedIncrementAccounts>) {
    return instructions.callAuthorizedIncrement(this.program, args ?? {}, accounts);
  }

  /** Derives the PDA for a `caller_authority` account. */
  public async getCallerAuthorityPda(seeds: {
  }): Promise<readonly [naclac.Address, number]> {
    return naclac.getProgramDerivedAddress({
      programAddress: this.programId,
      seeds: [
                new Uint8Array([99, 97, 108, 108, 101, 114, 95, 97, 117, 116, 104, 111, 114, 105, 116, 121])
      ]
    });
  }

  /** Fetches a `CallerAuthority` account. Throws if it does not exist. */
  public fetchCallerAuthority(address: naclac.Address | string) {
    return accounts.fetchCallerAuthority(this.program.provider.rpc as any, naclac.address(address as string));
  }

  /** Fetches a `CallerAuthority` account. Returns a MaybeAccount (check `.exists`) if it does not exist. */
  public fetchMaybeCallerAuthority(address: naclac.Address | string) {
    return accounts.fetchMaybeCallerAuthority(this.program.provider.rpc as any, naclac.address(address as string));
  }

  /** Fetches multiple `CallerAuthority` accounts by address. Throws if any do not exist. */
  public fetchAllCallerAuthoritys(addresses: Array<naclac.Address | string>) {
    return accounts.fetchAllCallerAuthority(this.program.provider.rpc as any, addresses.map((a) => naclac.address(a as string)));
  }

  /** Fetches multiple `CallerAuthority` accounts. Missing accounts have `.exists = false`. */
  public fetchAllMaybeCallerAuthoritys(addresses: Array<naclac.Address | string>) {
    return accounts.fetchAllMaybeCallerAuthority(this.program.provider.rpc as any, addresses.map((a) => naclac.address(a as string)));
  }

  /** Fetches ALL `CallerAuthority` accounts owned by this program. */
  public fetchAllCallerAuthorityByOwner(options?: { commitment?: any; filters?: unknown[] }) {
    return accounts.fetchAllCallerAuthorityByOwner(this.program.provider.rpc as any, this.programId, options);
  }

}
