// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `call_authorized_increment` instruction. */
export const CALL_AUTHORIZED_INCREMENT_DISCRIMINATOR = new Uint8Array([160, 53, 80, 237, 25, 232, 122, 27]);

/** Accounts for the `callAuthorizedIncrement` instruction. */
export interface CallAuthorizedIncrementAccounts {
  /** SAFETY: `cpi_callee`'s own PDA, validated by that program. */
  counter: naclac.Address | string;
  caller_authority?: naclac.Address | string;
  callee_program: naclac.Address | string;
}

/**
 * Builds the `callAuthorizedIncrement` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function callAuthorizedIncrement(
  program: any,
  args?: Record<string, never>,
  accounts?: Partial<CallAuthorizedIncrementAccounts>
) {
  const builder = program.methods.call_authorized_increment(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
