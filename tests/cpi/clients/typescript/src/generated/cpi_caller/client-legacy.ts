// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client/legacy";
import rawIDL from "./idl/cpi_caller.json";
import type { CpiCaller } from "./idl/cpi_caller";
const IDL = rawIDL as unknown as CpiCaller;
import * as instructions from "./instructions";
import * as accounts from "./accounts/legacy";
import * as types from "./types";

/**
 * High-level client for the `cpi_caller` program.
 * Uses the classic @solana/web3.js (v1) transaction model.
 */
export class CpiCallerClient {
  public readonly program: naclac.LegacyProgram;
  public readonly instructions = instructions;
  public readonly accounts = accounts;
  public readonly types = types;
  public readonly errors = types.ProgramErrors;

  public get constants() {
    return this.program.constants;
  }

  /** The on-chain program address as a `PublicKey`. */
  public get programId(): naclac.PublicKey {
    return this.program.programId;
  }

  constructor(
    providerOrCluster: naclac.LegacyProvider | "devnet" | "mainnet" | "localnet" | string,
    payer?: naclac.Keypair
  ) {
    let provider: naclac.LegacyProvider;
    if (typeof providerOrCluster === "string") {
      let url = providerOrCluster;
      if (providerOrCluster === "devnet") url = "https://api.devnet.solana.com";
      else if (providerOrCluster === "mainnet") url = "https://api.mainnet-beta.solana.com";
      else if (providerOrCluster === "localnet") url = "http://127.0.0.1:8899";
      const connection = new naclac.Connection(url, "confirmed");
      provider = { connection, payer, publicKey: payer ? payer.publicKey : undefined };
    } else {
      provider = providerOrCluster;
    }
    this.program = new naclac.LegacyProgram(IDL, provider);
  }

  /**
   * Builds the `initCallerAuthority` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public initCallerAuthority(args?: Record<string, never>, accounts?: Partial<instructions.InitCallerAuthorityAccounts>) {
    return instructions.initCallerAuthority(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `callSystemTransfer` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public callSystemTransfer(args: instructions.CallSystemTransferArgs, accounts?: Partial<instructions.CallSystemTransferAccounts>) {
    return instructions.callSystemTransfer(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `callSetupCounter` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public callSetupCounter(args?: Record<string, never>, accounts?: Partial<instructions.CallSetupCounterAccounts>) {
    return instructions.callSetupCounter(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `callAuthorizedIncrement` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public callAuthorizedIncrement(args?: Record<string, never>, accounts?: Partial<instructions.CallAuthorizedIncrementAccounts>) {
    return instructions.callAuthorizedIncrement(this.program, args ?? {}, accounts);
  }

  /** Derives the PDA for a `caller_authority` account. Returns `[PublicKey, bumpSeed]`. */
  public getCallerAuthorityPda(seeds: {
  }): [naclac.PublicKey, number] {
    const [pda, bump] = naclac.PublicKey.findProgramAddressSync(
      [
                new Uint8Array([99, 97, 108, 108, 101, 114, 95, 97, 117, 116, 104, 111, 114, 105, 116, 121])
      ],
      this.programId
    );
    return [pda, bump];
  }

  /** Fetches a `CallerAuthority` account. Throws if it does not exist. */
  public fetchCallerAuthority(address: naclac.PublicKey | string) {
    return this.program.account['CallerAuthority'].fetch(address);
  }

  /** Fetches a `CallerAuthority` account. Returns `null` if it does not exist. */
  public async fetchMaybeCallerAuthority(address: naclac.PublicKey | string) {
    try { return await this.program.account['CallerAuthority'].fetch(address); }
    catch { return null; }
  }

  /** Fetches multiple `CallerAuthority` accounts in a single RPC call. */
  public fetchAllCallerAuthoritys(addresses: Array<naclac.PublicKey | string>, opts?: { dropCorrupted?: boolean }) {
    return this.program.account['CallerAuthority'].fetchMultiple(addresses, opts);
  }

  /** Fetches all on-chain `CallerAuthority` accounts owned by this program. */
  public fetchAllCallerAuthorityByOwner(options?: { dropCorrupted?: boolean; filters?: any[] }) {
    return this.program.account['CallerAuthority'].all(options);
  }

}
