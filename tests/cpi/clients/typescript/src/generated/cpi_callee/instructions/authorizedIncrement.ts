// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `authorized_increment` instruction. */
export const AUTHORIZED_INCREMENT_DISCRIMINATOR = new Uint8Array([149, 164, 161, 180, 189, 180, 114, 220]);

/** Accounts for the `authorizedIncrement` instruction. */
export interface AuthorizedIncrementAccounts {
  counter?: naclac.Address | string;
  authority: naclac.Address | string;
}

/**
 * Builds the `authorizedIncrement` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function authorizedIncrement(
  program: any,
  args?: Record<string, never>,
  accounts?: Partial<AuthorizedIncrementAccounts>
) {
  const builder = program.methods.authorized_increment(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
