// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client/kit";
import rawIDL from "./idl/optional_accounts.json";
import type { OptionalAccounts } from "./idl/optional_accounts";
const IDL = rawIDL as unknown as OptionalAccounts;
import * as instructions from "./instructions";
import * as accounts from "./accounts/kit";
import * as types from "./types";

/**
 * High-level client for the `optional_accounts` program.
 * Uses the modern @solana/kit (v2) transaction model.
 */
export class OptionalAccountsClient {
  public readonly program: naclac.Program;
  public readonly instructions = instructions;
  public readonly accounts = accounts;
  public readonly types = types;
  public readonly errors = types.ProgramErrors;

  public get constants() {
    return this.program.constants;
  }

  /** The on-chain program address. */
  public get programId(): naclac.Address {
    return this.program.idl.address as naclac.Address;
  }

  constructor(
    providerOrCluster: naclac.NaclacProvider | "devnet" | "mainnet" | "localnet" | "litesvm",
    payer?: naclac.KeyPairSigner
  ) {
    let provider: naclac.NaclacProvider;
    if (typeof providerOrCluster === "string") {
      if (!payer) throw new Error("[Naclac] A payer KeyPairSigner is required when specifying a cluster string.");
      provider = naclac.createProvider(providerOrCluster, payer);
    } else {
      provider = providerOrCluster;
    }
    this.program = new naclac.Program(IDL, provider, true);
  }

  /**
   * Builds the `initThingA` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public initThingA(args?: Record<string, never>, accounts?: Partial<instructions.InitThingAAccounts>) {
    return instructions.initThingA(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `initThingB` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public initThingB(args?: Record<string, never>, accounts?: Partial<instructions.InitThingBAccounts>) {
    return instructions.initThingB(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `touchOptional` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public touchOptional(args?: Record<string, never>, accounts?: Partial<instructions.TouchOptionalAccounts>) {
    return instructions.touchOptional(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `touchTwoOptional` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public touchTwoOptional(args?: Record<string, never>, accounts?: Partial<instructions.TouchTwoOptionalAccounts>) {
    return instructions.touchTwoOptional(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `initOptionalThing` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public initOptionalThing(args?: Record<string, never>, accounts?: Partial<instructions.InitOptionalThingAccounts>) {
    return instructions.initOptionalThing(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `closeOptionalThing` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public closeOptionalThing(args?: Record<string, never>, accounts?: Partial<instructions.CloseOptionalThingAccounts>) {
    return instructions.closeOptionalThing(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `reallocOptionalThing` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public reallocOptionalThing(args: instructions.ReallocOptionalThingArgs, accounts?: Partial<instructions.ReallocOptionalThingAccounts>) {
    return instructions.reallocOptionalThing(this.program, args ?? {}, accounts);
  }

  /** Derives the PDA for a `optional_thing` account. */
  public async getOptionalThingPda(seeds: {
  }): Promise<readonly [naclac.Address, number]> {
    return naclac.getProgramDerivedAddress({
      programAddress: this.programId,
      seeds: [
                new Uint8Array([111, 112, 116, 105, 111, 110, 97, 108, 95, 116, 104, 105, 110, 103])
      ]
    });
  }

  /** Derives the PDA for a `thing_a` account. */
  public async getThingAPda(seeds: {
  }): Promise<readonly [naclac.Address, number]> {
    return naclac.getProgramDerivedAddress({
      programAddress: this.programId,
      seeds: [
                new Uint8Array([116, 104, 105, 110, 103, 95, 97])
      ]
    });
  }

  /** Derives the PDA for a `thing_b` account. */
  public async getThingBPda(seeds: {
  }): Promise<readonly [naclac.Address, number]> {
    return naclac.getProgramDerivedAddress({
      programAddress: this.programId,
      seeds: [
                new Uint8Array([116, 104, 105, 110, 103, 95, 98])
      ]
    });
  }

  /** Fetches a `Thing` account. Throws if it does not exist. */
  public fetchThing(address: naclac.Address | string) {
    return accounts.fetchThing(this.program.provider.rpc as any, naclac.address(address as string));
  }

  /** Fetches a `Thing` account. Returns a MaybeAccount (check `.exists`) if it does not exist. */
  public fetchMaybeThing(address: naclac.Address | string) {
    return accounts.fetchMaybeThing(this.program.provider.rpc as any, naclac.address(address as string));
  }

  /** Fetches multiple `Thing` accounts by address. Throws if any do not exist. */
  public fetchAllThings(addresses: Array<naclac.Address | string>) {
    return accounts.fetchAllThing(this.program.provider.rpc as any, addresses.map((a) => naclac.address(a as string)));
  }

  /** Fetches multiple `Thing` accounts. Missing accounts have `.exists = false`. */
  public fetchAllMaybeThings(addresses: Array<naclac.Address | string>) {
    return accounts.fetchAllMaybeThing(this.program.provider.rpc as any, addresses.map((a) => naclac.address(a as string)));
  }

  /** Fetches ALL `Thing` accounts owned by this program. */
  public fetchAllThingByOwner(options?: { commitment?: any; filters?: unknown[] }) {
    return accounts.fetchAllThingByOwner(this.program.provider.rpc as any, this.programId, options);
  }

}
