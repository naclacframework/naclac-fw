// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `set_mint_authority` instruction. */
export const SET_MINT_AUTHORITY_DISCRIMINATOR = new Uint8Array([67, 127, 155, 187, 100, 174, 103, 121]);

/** Instruction arguments for `setMintAuthority`. */
export interface SetMintAuthorityArgs {
  authority_type: number;
}

/** Accounts for the `setMintAuthority` instruction. */
export interface SetMintAuthorityAccounts {
  mint: naclac.Address | string;
  mint_authority?: naclac.Address | string;
  /**
   * SAFETY: only used as an address — its own key becomes the mint's new
   * authority via `set_authority_signed` below; its data is never read or
   * deserialized.
   */
  new_authority: naclac.Address | string;
  token_program?: naclac.Address | string;
}

/**
 * Builds the `setMintAuthority` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function setMintAuthority(
  program: any,
  args: SetMintAuthorityArgs,
  accounts?: Partial<SetMintAuthorityAccounts>
) {
  const builder = program.methods.set_mint_authority(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
