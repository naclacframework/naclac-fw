// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `approve_vault_delegate2022` instruction. */
export const APPROVE_VAULT_DELEGATE2022_DISCRIMINATOR = new Uint8Array([153, 175, 229, 6, 234, 170, 232, 122]);

/** Instruction arguments for `approveVaultDelegate2022`. */
export interface ApproveVaultDelegate2022Args {
  amount: bigint | number;
}

/** Accounts for the `approveVaultDelegate2022` instruction. */
export interface ApproveVaultDelegate2022Accounts {
  vault: naclac.Address | string;
  /**
   * SAFETY: only used as an address — becomes the approved delegate via
   * `approve` below; its data is never read or deserialized.
   */
  delegate: naclac.Address | string;
  owner: naclac.Address | string;
  tokenProgram?: naclac.Address | string;
}

/**
 * Builds the `approveVaultDelegate2022` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function approveVaultDelegate2022(
  program: any,
  args: ApproveVaultDelegate2022Args,
  accounts?: Partial<ApproveVaultDelegate2022Accounts>
) {
  const builder = program.methods.approve_vault_delegate2022(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
