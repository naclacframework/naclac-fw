// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `create_ata_idempotent` instruction. */
export const CREATE_ATA_IDEMPOTENT_DISCRIMINATOR = new Uint8Array([81, 88, 202, 92, 134, 69, 133, 143]);

/** Accounts for the `createAtaIdempotent` instruction. */
export interface CreateAtaIdempotentAccounts {
  payer: naclac.Address | string;
  /**
   * SAFETY: only used as the ATA's `authority` argument to the
   * Associated Token Program CPI below; never read or deserialized.
   */
  owner: naclac.Address | string;
  mint: naclac.Address | string;
  /**
   * SAFETY: may not exist yet — created idempotently by the CPI below
   * (a no-op if it already exists).
   */
  associatedToken: naclac.Address | string;
  systemProgram?: naclac.Address | string;
  tokenProgram?: naclac.Address | string;
  associatedTokenProgram?: naclac.Address | string;
}

/**
 * Builds the `createAtaIdempotent` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function createAtaIdempotent(
  program: any,
  args?: Record<string, never>,
  accounts?: Partial<CreateAtaIdempotentAccounts>
) {
  const builder = program.methods.create_ata_idempotent(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
