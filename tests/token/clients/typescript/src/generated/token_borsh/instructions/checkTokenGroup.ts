// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `check_token_group` instruction. */
export const CHECK_TOKEN_GROUP_DISCRIMINATOR = new Uint8Array([64, 59, 5, 251, 75, 155, 157, 122]);

/** Instruction arguments for `checkTokenGroup`. */
export interface CheckTokenGroupArgs {
  expectedSize: bigint | number;
  expectedMaxSize: bigint | number;
}

/** Accounts for the `checkTokenGroup` instruction. */
export interface CheckTokenGroupAccounts {
  mint: naclac.Address | string;
}

/**
 * Builds the `checkTokenGroup` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function checkTokenGroup(
  program: any,
  args: CheckTokenGroupArgs,
  accounts?: Partial<CheckTokenGroupAccounts>
) {
  const builder = program.methods.check_token_group(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
