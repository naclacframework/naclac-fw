// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `check_memo_transfer` instruction. */
export const CHECK_MEMO_TRANSFER_DISCRIMINATOR = new Uint8Array([111, 90, 98, 4, 185, 241, 187, 85]);

/** Instruction arguments for `checkMemoTransfer`. */
export interface CheckMemoTransferArgs {
  expectedPresent: number;
  expectedRequired: number;
}

/** Accounts for the `checkMemoTransfer` instruction. */
export interface CheckMemoTransferAccounts {
  vault: naclac.Address | string;
}

/**
 * Builds the `checkMemoTransfer` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function checkMemoTransfer(
  program: any,
  args: CheckMemoTransferArgs,
  accounts?: Partial<CheckMemoTransferAccounts>
) {
  const builder = program.methods.check_memo_transfer(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
