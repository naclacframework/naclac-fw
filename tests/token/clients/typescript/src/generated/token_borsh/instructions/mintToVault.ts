// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `mint_to_vault` instruction. */
export const MINT_TO_VAULT_DISCRIMINATOR = new Uint8Array([197, 102, 31, 8, 93, 20, 64, 185]);

/** Instruction arguments for `mintToVault`. */
export interface MintToVaultArgs {
  amount: bigint | number;
}

/** Accounts for the `mintToVault` instruction. */
export interface MintToVaultAccounts {
  mint: naclac.Address | string;
  mint_authority?: naclac.Address | string;
  vault: naclac.Address | string;
  token_program?: naclac.Address | string;
}

/**
 * Builds the `mintToVault` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function mintToVault(
  program: any,
  args: MintToVaultArgs,
  accounts?: Partial<MintToVaultAccounts>
) {
  const builder = program.methods.mint_to_vault(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
