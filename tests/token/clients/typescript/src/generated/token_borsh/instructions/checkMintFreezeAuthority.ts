// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `check_mint_freeze_authority` instruction. */
export const CHECK_MINT_FREEZE_AUTHORITY_DISCRIMINATOR = new Uint8Array([254, 61, 30, 240, 226, 124, 124, 101]);

/** Accounts for the `checkMintFreezeAuthority` instruction. */
export interface CheckMintFreezeAuthorityAccounts {
  /**
   * SAFETY: only used as an address to compare the mint's freeze
   * authority against, via the `mint::freeze_authority` constraint below;
   * its data is never read or deserialized. Declared before `mint`
   * because field constraints reference sibling fields by local variable
   * name, and fields load sequentially in declaration order (same
   * convention as `check_vault_constraints.rs`'s `mint`/`mint_authority`
   * preceding `vault`).
   */
  freezeAuthority: naclac.Address | string;
  mint: naclac.Address | string;
}

/**
 * Builds the `checkMintFreezeAuthority` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function checkMintFreezeAuthority(
  program: any,
  args?: Record<string, never>,
  accounts?: Partial<CheckMintFreezeAuthorityAccounts>
) {
  const builder = program.methods.check_mint_freeze_authority(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
