// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `check_pausable_config` instruction. */
export const CHECK_PAUSABLE_CONFIG_DISCRIMINATOR = new Uint8Array([248, 215, 7, 252, 136, 203, 211, 202]);

/** Instruction arguments for `checkPausableConfig`. */
export interface CheckPausableConfigArgs {
  expected_authority: naclac.Address | string | null;
  expected_paused: number;
}

/** Accounts for the `checkPausableConfig` instruction. */
export interface CheckPausableConfigAccounts {
  mint: naclac.Address | string;
}

/**
 * Builds the `checkPausableConfig` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function checkPausableConfig(
  program: any,
  args: CheckPausableConfigArgs,
  accounts?: Partial<CheckPausableConfigAccounts>
) {
  const builder = program.methods.check_pausable_config(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
