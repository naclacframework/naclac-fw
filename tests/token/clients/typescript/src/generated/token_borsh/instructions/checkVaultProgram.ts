// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `check_vault_program` instruction. */
export const CHECK_VAULT_PROGRAM_DISCRIMINATOR = new Uint8Array([11, 24, 214, 180, 243, 235, 169, 238]);

/** Accounts for the `checkVaultProgram` instruction. */
export interface CheckVaultProgramAccounts {
  tokenProgram?: naclac.Address | string;
  /**
   * SAFETY: only the account's owning-program metadata is inspected via
   * the `token::program` constraint below; its data is never read or
   * deserialized.
   */
  vault: naclac.Address | string;
}

/**
 * Builds the `checkVaultProgram` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function checkVaultProgram(
  program: any,
  args?: Record<string, never>,
  accounts?: Partial<CheckVaultProgramAccounts>
) {
  const builder = program.methods.check_vault_program(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
