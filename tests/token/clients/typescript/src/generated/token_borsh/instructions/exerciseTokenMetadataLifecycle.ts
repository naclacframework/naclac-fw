// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `exercise_token_metadata_lifecycle` instruction. */
export const EXERCISE_TOKEN_METADATA_LIFECYCLE_DISCRIMINATOR = new Uint8Array([176, 177, 57, 217, 140, 170, 202, 7]);

/** Instruction arguments for `exerciseTokenMetadataLifecycle`. */
export interface ExerciseTokenMetadataLifecycleArgs {
  newName: string;
  extraKey: string;
  extraValue: string;
}

/** Accounts for the `exerciseTokenMetadataLifecycle` instruction. */
export interface ExerciseTokenMetadataLifecycleAccounts {
  mintAuthority?: naclac.Address | string;
  /**
   * SAFETY: this account is the same mint validated/created by
   * `create_mint2022_with_metadata_pointer_and_metadata` in the test that
   * calls this instruction — no naclac `Discriminator` exists to check
   * since this is a raw SPL `Mint` + `TokenMetadata` layout, not a
   * naclac component.
   */
  mint: naclac.Address | string;
  tokenProgram?: naclac.Address | string;
}

/**
 * Builds the `exerciseTokenMetadataLifecycle` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function exerciseTokenMetadataLifecycle(
  program: any,
  args: ExerciseTokenMetadataLifecycleArgs,
  accounts?: Partial<ExerciseTokenMetadataLifecycleAccounts>
) {
  const builder = program.methods.exercise_token_metadata_lifecycle(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
