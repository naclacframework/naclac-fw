// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `exercise_transfer_checked_with_hook` instruction. */
export const EXERCISE_TRANSFER_CHECKED_WITH_HOOK_DISCRIMINATOR = new Uint8Array([199, 170, 77, 203, 136, 191, 8, 139]);

/** Instruction arguments for `exerciseTransferCheckedWithHook`. */
export interface ExerciseTransferCheckedWithHookArgs {
  amount: bigint | number;
  decimals: number;
}

/** Accounts for the `exerciseTransferCheckedWithHook` instruction. */
export interface ExerciseTransferCheckedWithHookAccounts {
  mintAuthority?: naclac.Address | string;
  mint: naclac.Address | string;
  source: naclac.Address | string;
  destination: naclac.Address | string;
  /**
   * SAFETY: only used as an address (the hook program to CPI into) and
   * passed through to `transfer_checked_with_hook_signed`, which itself
   * resolves and validates the hook's required accounts against real
   * Token-2022/`spl-transfer-hook-interface` rules; its data is never
   * read or deserialized here.
   */
  hookProgram: naclac.Address | string;
  tokenProgram?: naclac.Address | string;
}

/**
 * Builds the `exerciseTransferCheckedWithHook` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function exerciseTransferCheckedWithHook(
  program: any,
  args: ExerciseTransferCheckedWithHookArgs,
  accounts?: Partial<ExerciseTransferCheckedWithHookAccounts>
) {
  const builder = program.methods.exercise_transfer_checked_with_hook(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
