// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `create_mint_with_freeze` instruction. */
export const CREATE_MINT_WITH_FREEZE_DISCRIMINATOR = new Uint8Array([196, 21, 137, 205, 211, 246, 135, 220]);

/** Instruction arguments for `createMintWithFreeze`. */
export interface CreateMintWithFreezeArgs {
  id: bigint | number;
  mintBump: number;
  decimals: number;
}

/** Accounts for the `createMintWithFreeze` instruction. */
export interface CreateMintWithFreezeAccounts {
  payer: naclac.Address | string;
  /**
   * SAFETY: `init` + `mint::decimals`/`mint::authority`/`mint::freeze_authority`
   * below fully validate and construct this account via a real CPI to the
   * token program (`init_cpi.rs`) — there is no naclac `Discriminator` to
   * check since this is a raw SPL `Mint` layout, so `AccountInfo` is
   * correct here, not a gap in coverage.
   */
  mint?: naclac.Address | string;
  mintAuthority: naclac.Address | string;
  tokenProgram?: naclac.Address | string;
  systemProgram?: naclac.Address | string;
}

/**
 * Builds the `createMintWithFreeze` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function createMintWithFreeze(
  program: any,
  args: CreateMintWithFreezeArgs,
  accounts?: Partial<CreateMintWithFreezeAccounts>
) {
  const builder = program.methods.create_mint_with_freeze(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
