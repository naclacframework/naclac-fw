// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `create_ata` instruction. */
export const CREATE_ATA_DISCRIMINATOR = new Uint8Array([26, 102, 168, 62, 117, 72, 168, 17]);

/** Accounts for the `createAta` instruction. */
export interface CreateAtaAccounts {
  payer: naclac.Address | string;
  /**
   * SAFETY: only used as the ATA's `authority` argument to the
   * Associated Token Program CPI below; never read or deserialized.
   */
  owner: naclac.Address | string;
  mint: naclac.Address | string;
  /**
   * SAFETY: this account doesn't exist yet — the Associated Token
   * Program CPI (`associated_token::mint`/`associated_token::authority`
   * below) creates it. There is no naclac `Discriminator` to check since
   * this is a raw SPL `TokenAccount` layout created by a separate
   * program.
   */
  associated_token: naclac.Address | string;
  system_program?: naclac.Address | string;
  token_program?: naclac.Address | string;
  associated_token_program?: naclac.Address | string;
}

/**
 * Builds the `createAta` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function createAta(
  program: any,
  args?: Record<string, never>,
  accounts?: Partial<CreateAtaAccounts>
) {
  const builder = program.methods.create_ata(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
