// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `check_cpi_guard` instruction. */
export const CHECK_CPI_GUARD_DISCRIMINATOR = new Uint8Array([252, 156, 176, 45, 116, 156, 24, 123]);

/** Instruction arguments for `checkCpiGuard`. */
export interface CheckCpiGuardArgs {
  expected_present: number;
  expected_locked: number;
}

/** Accounts for the `checkCpiGuard` instruction. */
export interface CheckCpiGuardAccounts {
  vault: naclac.Address | string;
}

/**
 * Builds the `checkCpiGuard` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function checkCpiGuard(
  program: any,
  args: CheckCpiGuardArgs,
  accounts?: Partial<CheckCpiGuardAccounts>
) {
  const builder = program.methods.check_cpi_guard(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
