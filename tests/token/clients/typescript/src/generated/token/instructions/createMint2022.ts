// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `create_mint2022` instruction. */
export const CREATE_MINT2022_DISCRIMINATOR = new Uint8Array([188, 181, 9, 68, 12, 33, 187, 18]);

/** Instruction arguments for `createMint2022`. */
export interface CreateMint2022Args {
  id: bigint | number;
  mint_bump: number;
  decimals: number;
}

/** Accounts for the `createMint2022` instruction. */
export interface CreateMint2022Accounts {
  payer: naclac.Address | string;
  /**
   * SAFETY: `init` + `mint::decimals`/`mint::authority` below fully
   * validate and construct this account via a real CPI to the Token-2022
   * program (`init_cpi.rs`) — there is no naclac `Discriminator` to check
   * since this is a raw SPL `Mint` layout, so `AccountInfo` is correct
   * here, not a gap in coverage.
   */
  mint?: naclac.Address | string;
  mint_authority: naclac.Address | string;
  token_program?: naclac.Address | string;
  system_program?: naclac.Address | string;
}

/**
 * Builds the `createMint2022` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function createMint2022(
  program: any,
  args: CreateMint2022Args,
  accounts?: Partial<CreateMint2022Accounts>
) {
  const builder = program.methods.create_mint2022(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
