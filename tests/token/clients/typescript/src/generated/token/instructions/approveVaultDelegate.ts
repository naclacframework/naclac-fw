// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `approve_vault_delegate` instruction. */
export const APPROVE_VAULT_DELEGATE_DISCRIMINATOR = new Uint8Array([233, 127, 64, 92, 215, 136, 181, 12]);

/** Instruction arguments for `approveVaultDelegate`. */
export interface ApproveVaultDelegateArgs {
  amount: bigint | number;
}

/** Accounts for the `approveVaultDelegate` instruction. */
export interface ApproveVaultDelegateAccounts {
  vault: naclac.Address | string;
  /**
   * SAFETY: only used as an address — becomes the approved delegate via
   * `approve_signed` below; its data is never read or deserialized.
   */
  delegate: naclac.Address | string;
  mint_authority?: naclac.Address | string;
  token_program?: naclac.Address | string;
}

/**
 * Builds the `approveVaultDelegate` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function approveVaultDelegate(
  program: any,
  args: ApproveVaultDelegateArgs,
  accounts?: Partial<ApproveVaultDelegateAccounts>
) {
  const builder = program.methods.approve_vault_delegate(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
