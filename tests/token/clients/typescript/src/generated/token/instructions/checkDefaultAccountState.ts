// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `check_default_account_state` instruction. */
export const CHECK_DEFAULT_ACCOUNT_STATE_DISCRIMINATOR = new Uint8Array([255, 107, 101, 2, 68, 186, 61, 6]);

/** Instruction arguments for `checkDefaultAccountState`. */
export interface CheckDefaultAccountStateArgs {
  expected_state: number;
}

/** Accounts for the `checkDefaultAccountState` instruction. */
export interface CheckDefaultAccountStateAccounts {
  mint: naclac.Address | string;
}

/**
 * Builds the `checkDefaultAccountState` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function checkDefaultAccountState(
  program: any,
  args: CheckDefaultAccountStateArgs,
  accounts?: Partial<CheckDefaultAccountStateAccounts>
) {
  const builder = program.methods.check_default_account_state(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
