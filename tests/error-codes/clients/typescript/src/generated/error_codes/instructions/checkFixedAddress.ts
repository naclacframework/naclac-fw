// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `check_fixed_address` instruction. */
export const CHECK_FIXED_ADDRESS_DISCRIMINATOR = new Uint8Array([171, 46, 251, 206, 177, 201, 101, 156]);

/** Accounts for the `checkFixedAddress` instruction. */
export interface CheckFixedAddressAccounts {
  /**
   * SAFETY: only this account's own address is checked against the
   * constant below — its data is never read or deserialized.
   */
  target?: naclac.Address | string;
}

/**
 * Builds the `checkFixedAddress` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function checkFixedAddress(
  program: any,
  args?: Record<string, never>,
  accounts?: Partial<CheckFixedAddressAccounts>
) {
  const builder = program.methods.check_fixed_address(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
