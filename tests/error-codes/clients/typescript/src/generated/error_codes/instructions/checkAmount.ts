// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `check_amount` instruction. */
export const CHECK_AMOUNT_DISCRIMINATOR = new Uint8Array([64, 134, 148, 88, 151, 6, 135, 30]);

/** Instruction arguments for `checkAmount`. */
export interface CheckAmountArgs {
  amount: bigint | number;
}

/** Accounts for the `checkAmount` instruction. */
export interface CheckAmountAccounts {
  payer: naclac.Address | string;
}

/**
 * Builds the `checkAmount` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function checkAmount(
  program: any,
  args: CheckAmountArgs,
  accounts?: Partial<CheckAmountAccounts>
) {
  const builder = program.methods.check_amount(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
