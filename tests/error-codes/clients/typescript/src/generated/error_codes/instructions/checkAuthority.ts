// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `check_authority` instruction. */
export const CHECK_AUTHORITY_DISCRIMINATOR = new Uint8Array([150, 20, 117, 137, 30, 218, 200, 87]);

/** Accounts for the `checkAuthority` instruction. */
export interface CheckAuthorityAccounts {
  payer: naclac.Address | string;
  /**
   * SAFETY: only this account's own address is compared against
   * `payer`'s — its data is never read or deserialized.
   */
  required: naclac.Address | string;
}

/**
 * Builds the `checkAuthority` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function checkAuthority(
  program: any,
  args?: Record<string, never>,
  accounts?: Partial<CheckAuthorityAccounts>
) {
  const builder = program.methods.check_authority(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
