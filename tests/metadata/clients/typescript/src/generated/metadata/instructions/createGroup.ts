// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `create_group` instruction. */
export const CREATE_GROUP_DISCRIMINATOR = new Uint8Array([79, 60, 158, 134, 61, 199, 56, 248]);

/** Instruction arguments for `createGroup`. */
export interface CreateGroupArgs {
  name: string;
  uri: string;
}

/** Accounts for the `createGroup` instruction. */
export interface CreateGroupAccounts {
  group: naclac.Address | string;
  payer: naclac.Address | string;
  system_program?: naclac.Address | string;
  /** SAFETY: the real Metaplex Core program — external to this workspace. */
  mpl_core_program: naclac.Address | string;
}

/**
 * Builds the `createGroup` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function createGroup(
  program: any,
  args: CreateGroupArgs,
  accounts?: Partial<CreateGroupAccounts>
) {
  const builder = program.methods.create_group(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
