// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `check_group` instruction. */
export const CHECK_GROUP_DISCRIMINATOR = new Uint8Array([102, 70, 92, 243, 192, 208, 201, 5]);

/** Instruction arguments for `checkGroup`. */
export interface CheckGroupArgs {
  expected_update_authority: naclac.Address | string;
  expected_name: string;
  expected_uri: string;
}

/** Accounts for the `checkGroup` instruction. */
export interface CheckGroupAccounts {
  /**
   * SAFETY: read-only; `fetch_group` itself validates ownership by the
   * real Metaplex Core program before parsing any bytes.
   */
  group: naclac.Address | string;
}

/**
 * Builds the `checkGroup` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function checkGroup(
  program: any,
  args: CheckGroupArgs,
  accounts?: Partial<CheckGroupAccounts>
) {
  const builder = program.methods.check_group(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
