// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `check_master_edition` instruction. */
export const CHECK_MASTER_EDITION_DISCRIMINATOR = new Uint8Array([8, 180, 253, 245, 59, 123, 110, 106]);

/** Instruction arguments for `checkMasterEdition`. */
export interface CheckMasterEditionArgs {
  expected_max_supply: number;
}

/** Accounts for the `checkMasterEdition` instruction. */
export interface CheckMasterEditionAccounts {
  /**
   * SAFETY: read-only; `fetch_collection_master_edition` itself
   * validates ownership by the real Metaplex Core program before parsing
   * any bytes.
   */
  collection: naclac.Address | string;
}

/**
 * Builds the `checkMasterEdition` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function checkMasterEdition(
  program: any,
  args: CheckMasterEditionArgs,
  accounts?: Partial<CheckMasterEditionAccounts>
) {
  const builder = program.methods.check_master_edition(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
