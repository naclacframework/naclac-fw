// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `check_freeze_delegate` instruction. */
export const CHECK_FREEZE_DELEGATE_DISCRIMINATOR = new Uint8Array([227, 66, 85, 97, 164, 1, 37, 21]);

/** Instruction arguments for `checkFreezeDelegate`. */
export interface CheckFreezeDelegateArgs {
  expected_frozen: naclac.Bool;
}

/** Accounts for the `checkFreezeDelegate` instruction. */
export interface CheckFreezeDelegateAccounts {
  /**
   * SAFETY: read-only; `fetch_asset_freeze_delegate` itself validates
   * ownership by the real Metaplex Core program before parsing any bytes.
   */
  asset: naclac.Address | string;
}

/**
 * Builds the `checkFreezeDelegate` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function checkFreezeDelegate(
  program: any,
  args: CheckFreezeDelegateArgs,
  accounts?: Partial<CheckFreezeDelegateAccounts>
) {
  const builder = program.methods.check_freeze_delegate(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
