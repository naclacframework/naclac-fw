// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `attach_verified_creators` instruction. */
export const ATTACH_VERIFIED_CREATORS_DISCRIMINATOR = new Uint8Array([93, 61, 36, 69, 167, 240, 38, 134]);

/** Accounts for the `attachVerifiedCreators` instruction. */
export interface AttachVerifiedCreatorsAccounts {
  asset: naclac.Address | string;
  payer: naclac.Address | string;
  system_program?: naclac.Address | string;
  /**
   * The external Metaplex Core program to CPI into.
   * SAFETY: the real Metaplex Core program — external to this workspace,
   * so there's no naclac `Program<T>` marker type to constrain it with;
   * callers are expected to pass the real deployed program ID.
   */
  mpl_core_program: naclac.Address | string;
}

/**
 * Builds the `attachVerifiedCreators` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function attachVerifiedCreators(
  program: any,
  args?: Record<string, never>,
  accounts?: Partial<AttachVerifiedCreatorsAccounts>
) {
  const builder = program.methods.attach_verified_creators(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
