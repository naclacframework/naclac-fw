// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `check_app_data` instruction. */
export const CHECK_APP_DATA_DISCRIMINATOR = new Uint8Array([107, 58, 191, 149, 192, 140, 20, 99]);

/** Accounts for the `checkAppData` instruction. */
export interface CheckAppDataAccounts {
  /**
   * SAFETY: read-only; `fetch_asset_app_data` itself validates ownership
   * by the real Metaplex Core program before parsing any bytes.
   */
  asset: naclac.Address | string;
}

/**
 * Builds the `checkAppData` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function checkAppData(
  program: any,
  args?: Record<string, never>,
  accounts?: Partial<CheckAppDataAccounts>
) {
  const builder = program.methods.check_app_data(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
