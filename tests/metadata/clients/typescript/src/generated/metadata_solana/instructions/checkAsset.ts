// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `check_asset` instruction. */
export const CHECK_ASSET_DISCRIMINATOR = new Uint8Array([141, 255, 118, 14, 99, 218, 24, 3]);

/** Instruction arguments for `checkAsset`. */
export interface CheckAssetArgs {
  expected_owner: naclac.Address | string;
  expected_update_authority: naclac.Address | string;
  expected_name: string;
  expected_uri: string;
}

/** Accounts for the `checkAsset` instruction. */
export interface CheckAssetAccounts {
  /**
   * SAFETY: read-only; `fetch_asset` itself validates ownership by the
   * real Metaplex Core program before parsing any bytes.
   */
  asset: naclac.Address | string;
}

/**
 * Builds the `checkAsset` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function checkAsset(
  program: any,
  args: CheckAssetArgs,
  accounts?: Partial<CheckAssetAccounts>
) {
  const builder = program.methods.check_asset(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
