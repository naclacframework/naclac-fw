// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client/legacy";
import rawIDL from "./idl/discriminator_zc.json";
import type { DiscriminatorZc } from "./idl/discriminator_zc";
const IDL = rawIDL as unknown as DiscriminatorZc;
import * as instructions from "./instructions";
import * as accounts from "./accounts/legacy";
import * as types from "./types";

/**
 * High-level client for the `discriminator_zc` program.
 * Uses the classic @solana/web3.js (v1) transaction model.
 */
export class DiscriminatorZcClient {
  public readonly program: naclac.LegacyProgram;
  public readonly instructions = instructions;
  public readonly accounts = accounts;
  public readonly types = types;
  public readonly errors = types.ProgramErrors;

  public get constants() {
    return this.program.constants;
  }

  /** The on-chain program address as a `PublicKey`. */
  public get programId(): naclac.PublicKey {
    return this.program.programId;
  }

  constructor(
    providerOrCluster: naclac.LegacyProvider | "devnet" | "mainnet" | "localnet" | string,
    payer?: naclac.Keypair
  ) {
    let provider: naclac.LegacyProvider;
    if (typeof providerOrCluster === "string") {
      let url = providerOrCluster;
      if (providerOrCluster === "devnet") url = "https://api.devnet.solana.com";
      else if (providerOrCluster === "mainnet") url = "https://api.mainnet-beta.solana.com";
      else if (providerOrCluster === "localnet") url = "http://127.0.0.1:8899";
      const connection = new naclac.Connection(url, "confirmed");
      provider = { connection, payer, publicKey: payer ? payer.publicKey : undefined };
    } else {
      provider = providerOrCluster;
    }
    this.program = new naclac.LegacyProgram(IDL, provider);
  }

  /**
   * Builds the `initVault` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public initVault(args?: Record<string, never>, accounts?: Partial<instructions.InitVaultAccounts>) {
    return instructions.initVault(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `initConfig` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public initConfig(args?: Record<string, never>, accounts?: Partial<instructions.InitConfigAccounts>) {
    return instructions.initConfig(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `readVault` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public readVault(args?: Record<string, never>, accounts?: Partial<instructions.ReadVaultAccounts>) {
    return instructions.readVault(this.program, args ?? {}, accounts);
  }

  /** Derives the PDA for a `config` account. Returns `[PublicKey, bumpSeed]`. */
  public getConfigPda(seeds: {
  }): [naclac.PublicKey, number] {
    const [pda, bump] = naclac.PublicKey.findProgramAddressSync(
      [
                new Uint8Array([99, 111, 110, 102, 105, 103])
      ],
      this.programId
    );
    return [pda, bump];
  }

  /** Derives the PDA for a `vault` account. Returns `[PublicKey, bumpSeed]`. */
  public getVaultPda(seeds: {
  }): [naclac.PublicKey, number] {
    const [pda, bump] = naclac.PublicKey.findProgramAddressSync(
      [
                new Uint8Array([118, 97, 117, 108, 116])
      ],
      this.programId
    );
    return [pda, bump];
  }

  /** Fetches a `Config` account. Throws if it does not exist. */
  public fetchConfig(address: naclac.PublicKey | string) {
    return this.program.account['Config'].fetch(address);
  }

  /** Fetches a `Config` account. Returns `null` if it does not exist. */
  public async fetchMaybeConfig(address: naclac.PublicKey | string) {
    try { return await this.program.account['Config'].fetch(address); }
    catch { return null; }
  }

  /** Fetches multiple `Config` accounts in a single RPC call. */
  public fetchAllConfigs(addresses: Array<naclac.PublicKey | string>, opts?: { dropCorrupted?: boolean }) {
    return this.program.account['Config'].fetchMultiple(addresses, opts);
  }

  /** Fetches all on-chain `Config` accounts owned by this program. */
  public fetchAllConfigByOwner(options?: { dropCorrupted?: boolean; filters?: any[] }) {
    return this.program.account['Config'].all(options);
  }

  /** Fetches a `Vault` account. Throws if it does not exist. */
  public fetchVault(address: naclac.PublicKey | string) {
    return this.program.account['Vault'].fetch(address);
  }

  /** Fetches a `Vault` account. Returns `null` if it does not exist. */
  public async fetchMaybeVault(address: naclac.PublicKey | string) {
    try { return await this.program.account['Vault'].fetch(address); }
    catch { return null; }
  }

  /** Fetches multiple `Vault` accounts in a single RPC call. */
  public fetchAllVaults(addresses: Array<naclac.PublicKey | string>, opts?: { dropCorrupted?: boolean }) {
    return this.program.account['Vault'].fetchMultiple(addresses, opts);
  }

  /** Fetches all on-chain `Vault` accounts owned by this program. */
  public fetchAllVaultByOwner(options?: { dropCorrupted?: boolean; filters?: any[] }) {
    return this.program.account['Vault'].all(options);
  }

}
