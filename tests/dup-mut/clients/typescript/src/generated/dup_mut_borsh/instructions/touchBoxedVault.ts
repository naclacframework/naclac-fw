// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `touch_boxed_vault` instruction. */
export const TOUCH_BOXED_VAULT_DISCRIMINATOR = new Uint8Array([136, 73, 198, 15, 185, 22, 147, 122]);

/** Accounts for the `touchBoxedVault` instruction. */
export interface TouchBoxedVaultAccounts {
  target: naclac.Address | string;
}

/**
 * Builds the `touchBoxedVault` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function touchBoxedVault(
  program: any,
  args?: Record<string, never>,
  accounts?: Partial<TouchBoxedVaultAccounts>
) {
  const builder = program.methods.touch_boxed_vault(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
