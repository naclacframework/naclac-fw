// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client/kit";
import rawIDL from "./idl/dup_mut_borsh.json";
import type { DupMutBorsh } from "./idl/dup_mut_borsh";
const IDL = rawIDL as unknown as DupMutBorsh;
import * as instructions from "./instructions";
import * as accounts from "./accounts/kit";
import * as types from "./types";

/**
 * High-level client for the `dup_mut_borsh` program.
 * Uses the modern @solana/kit (v2) transaction model.
 */
export class DupMutBorshClient {
  public readonly program: naclac.Program;
  public readonly instructions = instructions;
  public readonly accounts = accounts;
  public readonly types = types;
  public readonly errors = types.ProgramErrors;

  public get constants() {
    return this.program.constants;
  }

  /** The on-chain program address. */
  public get programId(): naclac.Address {
    return this.program.idl.address as naclac.Address;
  }

  constructor(
    providerOrCluster: naclac.NaclacProvider | "devnet" | "mainnet" | "localnet",
    payer?: naclac.KeyPairSigner
  ) {
    let provider: naclac.NaclacProvider;
    if (typeof providerOrCluster === "string") {
      if (!payer) throw new Error("[Naclac] A payer KeyPairSigner is required when specifying a cluster string.");
      provider = naclac.createProvider(providerOrCluster, payer);
    } else {
      provider = providerOrCluster;
    }
    this.program = new naclac.Program(IDL, provider);
  }

  /**
   * Builds the `initVaultA` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public initVaultA(args?: Record<string, never>, accounts?: Partial<instructions.InitVaultAAccounts>) {
    return instructions.initVaultA(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `initVaultB` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public initVaultB(args?: Record<string, never>, accounts?: Partial<instructions.InitVaultBAccounts>) {
    return instructions.initVaultB(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `touchPairNoAlias` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public touchPairNoAlias(args?: Record<string, never>, accounts?: Partial<instructions.TouchPairNoAliasAccounts>) {
    return instructions.touchPairNoAlias(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `touchPairWithAlias` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public touchPairWithAlias(args?: Record<string, never>, accounts?: Partial<instructions.TouchPairWithAliasAccounts>) {
    return instructions.touchPairWithAlias(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `initVaultC` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public initVaultC(args?: Record<string, never>, accounts?: Partial<instructions.InitVaultCAccounts>) {
    return instructions.initVaultC(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `touchTriplePartialAlias` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public touchTriplePartialAlias(args?: Record<string, never>, accounts?: Partial<instructions.TouchTriplePartialAliasAccounts>) {
    return instructions.touchTriplePartialAlias(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `touchBoxedVault` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public touchBoxedVault(args?: Record<string, never>, accounts?: Partial<instructions.TouchBoxedVaultAccounts>) {
    return instructions.touchBoxedVault(this.program, args ?? {}, accounts);
  }

  /** Derives the PDA for a `vault_a` account. */
  public async getVaultAPda(seeds: {
  }): Promise<readonly [naclac.Address, number]> {
    return naclac.getProgramDerivedAddress({
      programAddress: this.programId,
      seeds: [
                new Uint8Array([118, 97, 117, 108, 116, 95, 97])
      ]
    });
  }

  /** Derives the PDA for a `vault_b` account. */
  public async getVaultBPda(seeds: {
  }): Promise<readonly [naclac.Address, number]> {
    return naclac.getProgramDerivedAddress({
      programAddress: this.programId,
      seeds: [
                new Uint8Array([118, 97, 117, 108, 116, 95, 98])
      ]
    });
  }

  /** Derives the PDA for a `vault_c` account. */
  public async getVaultCPda(seeds: {
  }): Promise<readonly [naclac.Address, number]> {
    return naclac.getProgramDerivedAddress({
      programAddress: this.programId,
      seeds: [
                new Uint8Array([118, 97, 117, 108, 116, 95, 99])
      ]
    });
  }

  /** Fetches a `Vault` account. Throws if it does not exist. */
  public fetchVault(address: naclac.Address | string) {
    return accounts.fetchVault(this.program.provider.rpc as any, naclac.address(address as string));
  }

  /** Fetches a `Vault` account. Returns a MaybeAccount (check `.exists`) if it does not exist. */
  public fetchMaybeVault(address: naclac.Address | string) {
    return accounts.fetchMaybeVault(this.program.provider.rpc as any, naclac.address(address as string));
  }

  /** Fetches multiple `Vault` accounts by address. Throws if any do not exist. */
  public fetchAllVaults(addresses: Array<naclac.Address | string>) {
    return accounts.fetchAllVault(this.program.provider.rpc as any, addresses.map((a) => naclac.address(a as string)));
  }

  /** Fetches multiple `Vault` accounts. Missing accounts have `.exists = false`. */
  public fetchAllMaybeVaults(addresses: Array<naclac.Address | string>) {
    return accounts.fetchAllMaybeVault(this.program.provider.rpc as any, addresses.map((a) => naclac.address(a as string)));
  }

  /** Fetches ALL `Vault` accounts owned by this program. */
  public fetchAllVaultByOwner(options?: { commitment?: any; filters?: unknown[] }) {
    return accounts.fetchAllVaultByOwner(this.program.provider.rpc as any, this.programId, options);
  }

}
