// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client/legacy";
import type { Note } from "../../types";

/** 8-byte on-chain discriminator for `Note` accounts. */
export const NOTE_DISCRIMINATOR = new Uint8Array([203, 75, 252, 196, 81, 210, 122, 126]);

/** Fetches a `Note` account. Throws if it does not exist. */
export async function fetchNote(
  program: naclac.LegacyProgram,
  address: naclac.PublicKey | string,
): Promise<Note> {
  const result = await program.account['Note'].fetch(address);
  return result as unknown as Note;
}

/** Fetches a `Note` account. Returns `null` if it does not exist. */
export async function fetchMaybeNote(
  program: naclac.LegacyProgram,
  address: naclac.PublicKey | string,
): Promise<Note | null> {
  try {
    const result = await program.account['Note'].fetch(address);
    return result as unknown as Note;
  } catch {
    return null;
  }
}

/** Fetches multiple `Note` accounts. Missing accounts are `null`. */
export async function fetchAllNote(
  program: naclac.LegacyProgram,
  addresses: Array<naclac.PublicKey | string>,
  opts?: { dropCorrupted?: boolean },
): Promise<Array<Note | null>> {
  const results = await program.account['Note'].fetchMultiple(addresses, opts);
  return results as unknown as Array<Note | null>;
}

/** Fetches all on-chain `Note` accounts owned by this program. */
export async function fetchAllNoteByOwner(
  program: naclac.LegacyProgram,
  options?: { dropCorrupted?: boolean; filters?: any[] },
): Promise<Array<{ publicKey: naclac.PublicKey; account: Note }>> {
  const results = await program.account['Note'].all(options);
  return results as unknown as Array<{ publicKey: naclac.PublicKey; account: Note }>;
}
