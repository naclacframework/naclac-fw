// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client/legacy";
import type { Vault } from "../../types";

/** 8-byte on-chain discriminator for `Vault` accounts. */
export const VAULT_DISCRIMINATOR = new Uint8Array([211, 8, 232, 43, 2, 152, 117, 119]);

/** Fetches a `Vault` account. Throws if it does not exist. */
export async function fetchVault(
  program: naclac.LegacyProgram,
  address: naclac.PublicKey | string,
): Promise<Vault> {
  const result = await program.account['Vault'].fetch(address);
  return result as unknown as Vault;
}

/** Fetches a `Vault` account. Returns `null` if it does not exist. */
export async function fetchMaybeVault(
  program: naclac.LegacyProgram,
  address: naclac.PublicKey | string,
): Promise<Vault | null> {
  try {
    const result = await program.account['Vault'].fetch(address);
    return result as unknown as Vault;
  } catch {
    return null;
  }
}

/** Fetches multiple `Vault` accounts. Missing accounts are `null`. */
export async function fetchAllVault(
  program: naclac.LegacyProgram,
  addresses: Array<naclac.PublicKey | string>,
  opts?: { dropCorrupted?: boolean },
): Promise<Array<Vault | null>> {
  const results = await program.account['Vault'].fetchMultiple(addresses, opts);
  return results as unknown as Array<Vault | null>;
}

/** Fetches all on-chain `Vault` accounts owned by this program. */
export async function fetchAllVaultByOwner(
  program: naclac.LegacyProgram,
  options?: { dropCorrupted?: boolean; filters?: any[] },
): Promise<Array<{ publicKey: naclac.PublicKey; account: Vault }>> {
  const results = await program.account['Vault'].all(options);
  return results as unknown as Array<{ publicKey: naclac.PublicKey; account: Vault }>;
}
