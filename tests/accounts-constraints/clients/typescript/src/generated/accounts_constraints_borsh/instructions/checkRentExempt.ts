// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `check_rent_exempt` instruction. */
export const CHECK_RENT_EXEMPT_DISCRIMINATOR = new Uint8Array([218, 238, 171, 16, 41, 107, 170, 172]);

/** Accounts for the `checkRentExempt` instruction. */
export interface CheckRentExemptAccounts {
  /**
   * SAFETY: only this account's lamport balance and data length are
   * inspected via the `rent_exempt` constraint below; its data is never
   * read or deserialized.
   */
  target: naclac.Address | string;
}

/**
 * Builds the `checkRentExempt` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function checkRentExempt(
  program: any,
  args?: Record<string, never>,
  accounts?: Partial<CheckRentExemptAccounts>
) {
  const builder = program.methods.check_rent_exempt(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
