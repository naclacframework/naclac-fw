// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `check_external_pda` instruction. */
export const CHECK_EXTERNAL_PDA_DISCRIMINATOR = new Uint8Array([77, 75, 166, 158, 13, 14, 11, 209]);

/** Instruction arguments for `checkExternalPda`. */
export interface CheckExternalPdaArgs {
  bump: number;
}

/** Accounts for the `checkExternalPda` instruction. */
export interface CheckExternalPdaAccounts {
  /**
   * SAFETY: only this account's own address is compared against the PDA
   * derived from `SEED_EXTERNAL_PDA` + `bump` against the System
   * Program's ID; its data is never read or deserialized.
   */
  target?: naclac.Address | string;
}

/**
 * Builds the `checkExternalPda` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function checkExternalPda(
  program: any,
  args: CheckExternalPdaArgs,
  accounts?: Partial<CheckExternalPdaAccounts>
) {
  const builder = program.methods.check_external_pda(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
