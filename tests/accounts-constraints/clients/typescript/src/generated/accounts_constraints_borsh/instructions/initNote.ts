// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `init_note` instruction. */
export const INIT_NOTE_DISCRIMINATOR = new Uint8Array([210, 21, 99, 159, 132, 44, 76, 125]);

/** Instruction arguments for `initNote`. */
export interface InitNoteArgs {
  name: string;
}

/** Accounts for the `initNote` instruction. */
export interface InitNoteAccounts {
  payer: naclac.Address | string;
  note?: naclac.Address | string;
  system_program?: naclac.Address | string;
}

/**
 * Builds the `initNote` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function initNote(
  program: any,
  args: InitNoteArgs,
  accounts?: Partial<InitNoteAccounts>
) {
  const builder = program.methods.init_note(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
