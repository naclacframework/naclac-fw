// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `check_executable` instruction. */
export const CHECK_EXECUTABLE_DISCRIMINATOR = new Uint8Array([27, 192, 107, 127, 85, 227, 212, 55]);

/** Accounts for the `checkExecutable` instruction. */
export interface CheckExecutableAccounts {
  /**
   * SAFETY: only the account's `executable` flag is inspected via the
   * `executable` constraint below; its data is never read or deserialized.
   */
  target: naclac.Address | string;
}

/**
 * Builds the `checkExecutable` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function checkExecutable(
  program: any,
  args?: Record<string, never>,
  accounts?: Partial<CheckExecutableAccounts>
) {
  const builder = program.methods.check_executable(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
