// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `check_address_relational` instruction. */
export const CHECK_ADDRESS_RELATIONAL_DISCRIMINATOR = new Uint8Array([171, 64, 177, 170, 149, 28, 7, 185]);

/** Accounts for the `checkAddressRelational` instruction. */
export interface CheckAddressRelationalAccounts {
  /**
   * SAFETY: only the account's own address is inspected via the
   * `address` constraint below; its data is never read or deserialized.
   */
  target?: naclac.Address | string;
  /**
   * SAFETY: only its own address is read, as the expected value; never
   * deserialized.
   */
  expected_address: naclac.Address | string;
}

/**
 * Builds the `checkAddressRelational` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function checkAddressRelational(
  program: any,
  args?: Record<string, never>,
  accounts?: Partial<CheckAddressRelationalAccounts>
) {
  const builder = program.methods.check_address_relational(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
