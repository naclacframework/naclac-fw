// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client/legacy";
import type { SeededThing } from "../../types";

/** 8-byte on-chain discriminator for `SeededThing` accounts. */
export const SEEDEDTHING_DISCRIMINATOR = new Uint8Array([108, 26, 3, 60, 128, 59, 161, 172]);

/** Fetches a `SeededThing` account. Throws if it does not exist. */
export async function fetchSeededThing(
  program: naclac.LegacyProgram,
  address: naclac.PublicKey | string,
): Promise<SeededThing> {
  const result = await program.account['SeededThing'].fetch(address);
  return result as unknown as SeededThing;
}

/** Fetches a `SeededThing` account. Returns `null` if it does not exist. */
export async function fetchMaybeSeededThing(
  program: naclac.LegacyProgram,
  address: naclac.PublicKey | string,
): Promise<SeededThing | null> {
  try {
    const result = await program.account['SeededThing'].fetch(address);
    return result as unknown as SeededThing;
  } catch {
    return null;
  }
}

/** Fetches multiple `SeededThing` accounts. Missing accounts are `null`. */
export async function fetchAllSeededThing(
  program: naclac.LegacyProgram,
  addresses: Array<naclac.PublicKey | string>,
  opts?: { dropCorrupted?: boolean },
): Promise<Array<SeededThing | null>> {
  const results = await program.account['SeededThing'].fetchMultiple(addresses, opts);
  return results as unknown as Array<SeededThing | null>;
}

/** Fetches all on-chain `SeededThing` accounts owned by this program. */
export async function fetchAllSeededThingByOwner(
  program: naclac.LegacyProgram,
  options?: { dropCorrupted?: boolean; filters?: any[] },
): Promise<Array<{ publicKey: naclac.PublicKey; account: SeededThing }>> {
  const results = await program.account['SeededThing'].all(options);
  return results as unknown as Array<{ publicKey: naclac.PublicKey; account: SeededThing }>;
}
