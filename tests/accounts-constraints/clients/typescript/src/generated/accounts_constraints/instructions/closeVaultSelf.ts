// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `close_vault_self` instruction. */
export const CLOSE_VAULT_SELF_DISCRIMINATOR = new Uint8Array([165, 27, 184, 217, 241, 90, 203, 86]);

/** Accounts for the `closeVaultSelf` instruction. */
export interface CloseVaultSelfAccounts {
  /**
   * SAFETY: bare `AccountInfo` close target — this test exercises only
   * the self-close guard (target == dest), which fires before any
   * owner/data check, so no other validation is meaningful here.
   */
  target: naclac.Address | string;
  /** SAFETY: bare `AccountInfo` close destination — see `target` above. */
  destination: naclac.Address | string;
}

/**
 * Builds the `closeVaultSelf` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function closeVaultSelf(
  program: any,
  args?: Record<string, never>,
  accounts?: Partial<CloseVaultSelfAccounts>
) {
  const builder = program.methods.close_vault_self(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
