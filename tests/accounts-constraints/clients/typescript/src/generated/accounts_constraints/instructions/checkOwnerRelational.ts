// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `check_owner_relational` instruction. */
export const CHECK_OWNER_RELATIONAL_DISCRIMINATOR = new Uint8Array([179, 162, 242, 171, 88, 217, 216, 174]);

/** Accounts for the `checkOwnerRelational` instruction. */
export interface CheckOwnerRelationalAccounts {
  /**
   * SAFETY: only the account's owner field is inspected via the `owner`
   * constraint below; its data is never read or deserialized.
   */
  target: naclac.Address | string;
  /**
   * SAFETY: only its own address is read, as the expected owning program;
   * never deserialized.
   */
  expected_owner: naclac.Address | string;
}

/**
 * Builds the `checkOwnerRelational` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function checkOwnerRelational(
  program: any,
  args?: Record<string, never>,
  accounts?: Partial<CheckOwnerRelationalAccounts>
) {
  const builder = program.methods.check_owner_relational(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
