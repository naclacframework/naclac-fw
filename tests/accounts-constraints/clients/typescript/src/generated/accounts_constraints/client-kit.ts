// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client/kit";
import rawIDL from "./idl/accounts_constraints.json";
import type { AccountsConstraints } from "./idl/accounts_constraints";
const IDL = rawIDL as unknown as AccountsConstraints;
import * as instructions from "./instructions";
import * as accounts from "./accounts/kit";
import * as types from "./types";

/**
 * High-level client for the `accounts_constraints` program.
 * Uses the modern @solana/kit (v2) transaction model.
 */
export class AccountsConstraintsClient {
  public readonly program: naclac.Program;
  public readonly instructions = instructions;
  public readonly accounts = accounts;
  public readonly types = types;
  public readonly errors = types.ProgramErrors;

  public get constants() {
    return this.program.constants;
  }

  /** The on-chain program address. */
  public get programId(): naclac.Address {
    return this.program.idl.address as naclac.Address;
  }

  constructor(
    providerOrCluster: naclac.NaclacProvider | "devnet" | "mainnet" | "localnet" | "litesvm",
    payer?: naclac.KeyPairSigner
  ) {
    let provider: naclac.NaclacProvider;
    if (typeof providerOrCluster === "string") {
      if (!payer) throw new Error("[Naclac] A payer KeyPairSigner is required when specifying a cluster string.");
      provider = naclac.createProvider(providerOrCluster, payer);
    } else {
      provider = providerOrCluster;
    }
    this.program = new naclac.Program(IDL, provider, true);
  }

  /**
   * Builds the `initVault` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public initVault(args?: Record<string, never>, accounts?: Partial<instructions.InitVaultAccounts>) {
    return instructions.initVault(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `initIfNeededLedger` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public initIfNeededLedger(args: instructions.InitIfNeededLedgerArgs, accounts?: Partial<instructions.InitIfNeededLedgerAccounts>) {
    return instructions.initIfNeededLedger(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `requireSigner` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public requireSigner(args?: Record<string, never>, accounts?: Partial<instructions.RequireSignerAccounts>) {
    return instructions.requireSigner(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `checkOwner` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public checkOwner(args?: Record<string, never>, accounts?: Partial<instructions.CheckOwnerAccounts>) {
    return instructions.checkOwner(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `checkOwnerRelational` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public checkOwnerRelational(args?: Record<string, never>, accounts?: Partial<instructions.CheckOwnerRelationalAccounts>) {
    return instructions.checkOwnerRelational(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `checkAddress` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public checkAddress(args?: Record<string, never>, accounts?: Partial<instructions.CheckAddressAccounts>) {
    return instructions.checkAddress(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `checkAddressRelational` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public checkAddressRelational(args?: Record<string, never>, accounts?: Partial<instructions.CheckAddressRelationalAccounts>) {
    return instructions.checkAddressRelational(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `checkExecutable` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public checkExecutable(args?: Record<string, never>, accounts?: Partial<instructions.CheckExecutableAccounts>) {
    return instructions.checkExecutable(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `touchMutVault` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public touchMutVault(args?: Record<string, never>, accounts?: Partial<instructions.TouchMutVaultAccounts>) {
    return instructions.touchMutVault(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `relatedVault` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public relatedVault(args?: Record<string, never>, accounts?: Partial<instructions.RelatedVaultAccounts>) {
    return instructions.relatedVault(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `initSeeded` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public initSeeded(args?: Record<string, never>, accounts?: Partial<instructions.InitSeededAccounts>) {
    return instructions.initSeeded(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `touchSeeded` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public touchSeeded(args: instructions.TouchSeededArgs, accounts?: Partial<instructions.TouchSeededAccounts>) {
    return instructions.touchSeeded(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `closeVault` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public closeVault(args?: Record<string, never>, accounts?: Partial<instructions.CloseVaultAccounts>) {
    return instructions.closeVault(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `closeVaultSelf` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public closeVaultSelf(args?: Record<string, never>, accounts?: Partial<instructions.CloseVaultSelfAccounts>) {
    return instructions.closeVaultSelf(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `checkRentExempt` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public checkRentExempt(args?: Record<string, never>, accounts?: Partial<instructions.CheckRentExemptAccounts>) {
    return instructions.checkRentExempt(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `relatedVaultCustomError` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public relatedVaultCustomError(args?: Record<string, never>, accounts?: Partial<instructions.RelatedVaultCustomErrorAccounts>) {
    return instructions.relatedVaultCustomError(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `checkExternalPda` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public checkExternalPda(args: instructions.CheckExternalPdaArgs, accounts?: Partial<instructions.CheckExternalPdaAccounts>) {
    return instructions.checkExternalPda(this.program, args ?? {}, accounts);
  }

  /** Derives the PDA for a `ledger` account. */
  public async getLedgerPda(seeds: {
  }): Promise<readonly [naclac.Address, number]> {
    return naclac.getProgramDerivedAddress({
      programAddress: this.programId,
      seeds: [
                new Uint8Array([108, 101, 100, 103, 101, 114])
      ]
    });
  }

  /** Derives the PDA for a `seeded` account. */
  public async getSeededPda(seeds: {
  }): Promise<readonly [naclac.Address, number]> {
    return naclac.getProgramDerivedAddress({
      programAddress: this.programId,
      seeds: [
                new Uint8Array([115, 101, 101, 100, 101, 100])
      ]
    });
  }

  /** Derives the PDA for a `target` account. */
  public async getTargetPda(seeds: {
  }): Promise<readonly [naclac.Address, number]> {
    return naclac.getProgramDerivedAddress({
      programAddress: this.programId,
      seeds: [
                new Uint8Array([101, 120, 116, 101, 114, 110, 97, 108, 95, 112, 100, 97])
      ]
    });
  }

  /** Derives the PDA for a `vault` account. */
  public async getVaultPda(seeds: {
  }): Promise<readonly [naclac.Address, number]> {
    return naclac.getProgramDerivedAddress({
      programAddress: this.programId,
      seeds: [
                new Uint8Array([118, 97, 117, 108, 116])
      ]
    });
  }

  /** Fetches a `Ledger` account. Throws if it does not exist. */
  public fetchLedger(address: naclac.Address | string) {
    return accounts.fetchLedger(this.program.provider.rpc as any, naclac.address(address as string));
  }

  /** Fetches a `Ledger` account. Returns a MaybeAccount (check `.exists`) if it does not exist. */
  public fetchMaybeLedger(address: naclac.Address | string) {
    return accounts.fetchMaybeLedger(this.program.provider.rpc as any, naclac.address(address as string));
  }

  /** Fetches multiple `Ledger` accounts by address. Throws if any do not exist. */
  public fetchAllLedgers(addresses: Array<naclac.Address | string>) {
    return accounts.fetchAllLedger(this.program.provider.rpc as any, addresses.map((a) => naclac.address(a as string)));
  }

  /** Fetches multiple `Ledger` accounts. Missing accounts have `.exists = false`. */
  public fetchAllMaybeLedgers(addresses: Array<naclac.Address | string>) {
    return accounts.fetchAllMaybeLedger(this.program.provider.rpc as any, addresses.map((a) => naclac.address(a as string)));
  }

  /** Fetches a `SeededThing` account. Throws if it does not exist. */
  public fetchSeededThing(address: naclac.Address | string) {
    return accounts.fetchSeededThing(this.program.provider.rpc as any, naclac.address(address as string));
  }

  /** Fetches a `SeededThing` account. Returns a MaybeAccount (check `.exists`) if it does not exist. */
  public fetchMaybeSeededThing(address: naclac.Address | string) {
    return accounts.fetchMaybeSeededThing(this.program.provider.rpc as any, naclac.address(address as string));
  }

  /** Fetches multiple `SeededThing` accounts by address. Throws if any do not exist. */
  public fetchAllSeededThings(addresses: Array<naclac.Address | string>) {
    return accounts.fetchAllSeededThing(this.program.provider.rpc as any, addresses.map((a) => naclac.address(a as string)));
  }

  /** Fetches multiple `SeededThing` accounts. Missing accounts have `.exists = false`. */
  public fetchAllMaybeSeededThings(addresses: Array<naclac.Address | string>) {
    return accounts.fetchAllMaybeSeededThing(this.program.provider.rpc as any, addresses.map((a) => naclac.address(a as string)));
  }

  /** Fetches a `Vault` account. Throws if it does not exist. */
  public fetchVault(address: naclac.Address | string) {
    return accounts.fetchVault(this.program.provider.rpc as any, naclac.address(address as string));
  }

  /** Fetches a `Vault` account. Returns a MaybeAccount (check `.exists`) if it does not exist. */
  public fetchMaybeVault(address: naclac.Address | string) {
    return accounts.fetchMaybeVault(this.program.provider.rpc as any, naclac.address(address as string));
  }

  /** Fetches multiple `Vault` accounts by address. Throws if any do not exist. */
  public fetchAllVaults(addresses: Array<naclac.Address | string>) {
    return accounts.fetchAllVault(this.program.provider.rpc as any, addresses.map((a) => naclac.address(a as string)));
  }

  /** Fetches multiple `Vault` accounts. Missing accounts have `.exists = false`. */
  public fetchAllMaybeVaults(addresses: Array<naclac.Address | string>) {
    return accounts.fetchAllMaybeVault(this.program.provider.rpc as any, addresses.map((a) => naclac.address(a as string)));
  }

  /** Fetches ALL `Ledger` accounts owned by this program. */
  public fetchAllLedgerByOwner(options?: { commitment?: any; filters?: unknown[] }) {
    return accounts.fetchAllLedgerByOwner(this.program.provider.rpc as any, this.programId, options);
  }

  /** Fetches ALL `SeededThing` accounts owned by this program. */
  public fetchAllSeededThingByOwner(options?: { commitment?: any; filters?: unknown[] }) {
    return accounts.fetchAllSeededThingByOwner(this.program.provider.rpc as any, this.programId, options);
  }

  /** Fetches ALL `Vault` accounts owned by this program. */
  public fetchAllVaultByOwner(options?: { commitment?: any; filters?: unknown[] }) {
    return accounts.fetchAllVaultByOwner(this.program.provider.rpc as any, this.programId, options);
  }

}
