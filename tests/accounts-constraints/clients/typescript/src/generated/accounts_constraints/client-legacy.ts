// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client/legacy";
import rawIDL from "./idl/accounts_constraints.json";
import type { AccountsConstraints } from "./idl/accounts_constraints";
const IDL = rawIDL as unknown as AccountsConstraints;
import * as instructions from "./instructions";
import * as accounts from "./accounts/legacy";
import * as types from "./types";

/**
 * High-level client for the `accounts_constraints` program.
 * Uses the classic @solana/web3.js (v1) transaction model.
 */
export class AccountsConstraintsClient {
  public readonly program: naclac.LegacyProgram;
  public readonly instructions = instructions;
  public readonly accounts = accounts;
  public readonly types = types;
  public readonly errors = types.ProgramErrors;

  public get constants() {
    return this.program.constants;
  }

  /** The on-chain program address as a `PublicKey`. */
  public get programId(): naclac.PublicKey {
    return this.program.programId;
  }

  constructor(
    providerOrCluster: naclac.LegacyProvider | "devnet" | "mainnet" | "localnet" | string,
    payer?: naclac.Keypair
  ) {
    let provider: naclac.LegacyProvider;
    if (typeof providerOrCluster === "string") {
      let url = providerOrCluster;
      if (providerOrCluster === "devnet") url = "https://api.devnet.solana.com";
      else if (providerOrCluster === "mainnet") url = "https://api.mainnet-beta.solana.com";
      else if (providerOrCluster === "localnet") url = "http://127.0.0.1:8899";
      const connection = new naclac.Connection(url, "confirmed");
      provider = { connection, payer, publicKey: payer ? payer.publicKey : undefined };
    } else {
      provider = providerOrCluster;
    }
    this.program = new naclac.LegacyProgram(IDL, provider);
  }

  /**
   * Builds the `initVault` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public initVault(args?: Record<string, never>, accounts?: Partial<instructions.InitVaultAccounts>) {
    return instructions.initVault(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `initIfNeededLedger` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public initIfNeededLedger(args: instructions.InitIfNeededLedgerArgs, accounts?: Partial<instructions.InitIfNeededLedgerAccounts>) {
    return instructions.initIfNeededLedger(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `requireSigner` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public requireSigner(args?: Record<string, never>, accounts?: Partial<instructions.RequireSignerAccounts>) {
    return instructions.requireSigner(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `checkOwner` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public checkOwner(args?: Record<string, never>, accounts?: Partial<instructions.CheckOwnerAccounts>) {
    return instructions.checkOwner(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `checkAddress` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public checkAddress(args?: Record<string, never>, accounts?: Partial<instructions.CheckAddressAccounts>) {
    return instructions.checkAddress(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `touchMutVault` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public touchMutVault(args?: Record<string, never>, accounts?: Partial<instructions.TouchMutVaultAccounts>) {
    return instructions.touchMutVault(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `relatedVault` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public relatedVault(args?: Record<string, never>, accounts?: Partial<instructions.RelatedVaultAccounts>) {
    return instructions.relatedVault(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `initSeeded` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public initSeeded(args?: Record<string, never>, accounts?: Partial<instructions.InitSeededAccounts>) {
    return instructions.initSeeded(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `touchSeeded` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public touchSeeded(args: instructions.TouchSeededArgs, accounts?: Partial<instructions.TouchSeededAccounts>) {
    return instructions.touchSeeded(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `closeVault` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public closeVault(args?: Record<string, never>, accounts?: Partial<instructions.CloseVaultAccounts>) {
    return instructions.closeVault(this.program, args ?? {}, accounts);
  }

  /** Derives the PDA for a `ledger` account. Returns `[PublicKey, bumpSeed]`. */
  public getLedgerPda(seeds: {
  }): [naclac.PublicKey, number] {
    const [pda, bump] = naclac.PublicKey.findProgramAddressSync(
      [
                new Uint8Array([108, 101, 100, 103, 101, 114])
      ],
      this.programId
    );
    return [pda, bump];
  }

  /** Derives the PDA for a `seeded` account. Returns `[PublicKey, bumpSeed]`. */
  public getSeededPda(seeds: {
  }): [naclac.PublicKey, number] {
    const [pda, bump] = naclac.PublicKey.findProgramAddressSync(
      [
                new Uint8Array([115, 101, 101, 100, 101, 100])
      ],
      this.programId
    );
    return [pda, bump];
  }

  /** Derives the PDA for a `vault` account. Returns `[PublicKey, bumpSeed]`. */
  public getVaultPda(seeds: {
  }): [naclac.PublicKey, number] {
    const [pda, bump] = naclac.PublicKey.findProgramAddressSync(
      [
                new Uint8Array([118, 97, 117, 108, 116])
      ],
      this.programId
    );
    return [pda, bump];
  }

  /** Fetches a `Ledger` account. Throws if it does not exist. */
  public fetchLedger(address: naclac.PublicKey | string) {
    return this.program.account['Ledger'].fetch(address);
  }

  /** Fetches a `Ledger` account. Returns `null` if it does not exist. */
  public async fetchMaybeLedger(address: naclac.PublicKey | string) {
    try { return await this.program.account['Ledger'].fetch(address); }
    catch { return null; }
  }

  /** Fetches multiple `Ledger` accounts in a single RPC call. */
  public fetchAllLedgers(addresses: Array<naclac.PublicKey | string>, opts?: { dropCorrupted?: boolean }) {
    return this.program.account['Ledger'].fetchMultiple(addresses, opts);
  }

  /** Fetches all on-chain `Ledger` accounts owned by this program. */
  public fetchAllLedgerByOwner(options?: { dropCorrupted?: boolean; filters?: any[] }) {
    return this.program.account['Ledger'].all(options);
  }

  /** Fetches a `SeededThing` account. Throws if it does not exist. */
  public fetchSeededThing(address: naclac.PublicKey | string) {
    return this.program.account['SeededThing'].fetch(address);
  }

  /** Fetches a `SeededThing` account. Returns `null` if it does not exist. */
  public async fetchMaybeSeededThing(address: naclac.PublicKey | string) {
    try { return await this.program.account['SeededThing'].fetch(address); }
    catch { return null; }
  }

  /** Fetches multiple `SeededThing` accounts in a single RPC call. */
  public fetchAllSeededThings(addresses: Array<naclac.PublicKey | string>, opts?: { dropCorrupted?: boolean }) {
    return this.program.account['SeededThing'].fetchMultiple(addresses, opts);
  }

  /** Fetches all on-chain `SeededThing` accounts owned by this program. */
  public fetchAllSeededThingByOwner(options?: { dropCorrupted?: boolean; filters?: any[] }) {
    return this.program.account['SeededThing'].all(options);
  }

  /** Fetches a `Vault` account. Throws if it does not exist. */
  public fetchVault(address: naclac.PublicKey | string) {
    return this.program.account['Vault'].fetch(address);
  }

  /** Fetches a `Vault` account. Returns `null` if it does not exist. */
  public async fetchMaybeVault(address: naclac.PublicKey | string) {
    try { return await this.program.account['Vault'].fetch(address); }
    catch { return null; }
  }

  /** Fetches multiple `Vault` accounts in a single RPC call. */
  public fetchAllVaults(addresses: Array<naclac.PublicKey | string>, opts?: { dropCorrupted?: boolean }) {
    return this.program.account['Vault'].fetchMultiple(addresses, opts);
  }

  /** Fetches all on-chain `Vault` accounts owned by this program. */
  public fetchAllVaultByOwner(options?: { dropCorrupted?: boolean; filters?: any[] }) {
    return this.program.account['Vault'].all(options);
  }

}
