// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client/kit";
import rawIDL from "./idl/single_file_layout.json";
import type { SingleFileLayout } from "./idl/single_file_layout";
const IDL = rawIDL as unknown as SingleFileLayout;
import * as instructions from "./instructions";
import * as accounts from "./accounts/kit";
import * as types from "./types";

/**
 * High-level client for the `single_file_layout` program.
 * Uses the modern @solana/kit (v2) transaction model.
 */
export class SingleFileLayoutClient {
  public readonly program: naclac.Program;
  public readonly instructions = instructions;
  public readonly accounts = accounts;
  public readonly types = types;
  public readonly errors = types.ProgramErrors;

  public get constants() {
    return this.program.constants;
  }

  /** The on-chain program address. */
  public get programId(): naclac.Address {
    return this.program.idl.address as naclac.Address;
  }

  constructor(
    providerOrCluster: naclac.NaclacProvider | "devnet" | "mainnet" | "localnet",
    payer?: naclac.KeyPairSigner
  ) {
    let provider: naclac.NaclacProvider;
    if (typeof providerOrCluster === "string") {
      if (!payer) throw new Error("[Naclac] A payer KeyPairSigner is required when specifying a cluster string.");
      provider = naclac.createProvider(providerOrCluster, payer);
    } else {
      provider = providerOrCluster;
    }
    this.program = new naclac.Program(IDL, provider);
  }

  /**
   * Builds the `initCounter` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public initCounter(args?: Record<string, never>, accounts?: Partial<instructions.InitCounterAccounts>) {
    return instructions.initCounter(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `incrementCounter` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public incrementCounter(args?: Record<string, never>, accounts?: Partial<instructions.IncrementCounterAccounts>) {
    return instructions.incrementCounter(this.program, args ?? {}, accounts);
  }

  /** Derives the PDA for a `counter` account. */
  public async getCounterPda(seeds: {
  }): Promise<readonly [naclac.Address, number]> {
    return naclac.getProgramDerivedAddress({
      programAddress: this.programId,
      seeds: [
                new Uint8Array([99, 111, 117, 110, 116, 101, 114])
      ]
    });
  }

  /** Fetches a `Counter` account. Throws if it does not exist. */
  public fetchCounter(address: naclac.Address | string) {
    return accounts.fetchCounter(this.program.provider.rpc as any, naclac.address(address as string));
  }

  /** Fetches a `Counter` account. Returns a MaybeAccount (check `.exists`) if it does not exist. */
  public fetchMaybeCounter(address: naclac.Address | string) {
    return accounts.fetchMaybeCounter(this.program.provider.rpc as any, naclac.address(address as string));
  }

  /** Fetches multiple `Counter` accounts by address. Throws if any do not exist. */
  public fetchAllCounters(addresses: Array<naclac.Address | string>) {
    return accounts.fetchAllCounter(this.program.provider.rpc as any, addresses.map((a) => naclac.address(a as string)));
  }

  /** Fetches multiple `Counter` accounts. Missing accounts have `.exists = false`. */
  public fetchAllMaybeCounters(addresses: Array<naclac.Address | string>) {
    return accounts.fetchAllMaybeCounter(this.program.provider.rpc as any, addresses.map((a) => naclac.address(a as string)));
  }

  /** Fetches ALL `Counter` accounts owned by this program. */
  public fetchAllCounterByOwner(options?: { commitment?: any; filters?: unknown[] }) {
    return accounts.fetchAllCounterByOwner(this.program.provider.rpc as any, this.programId, options);
  }

}
