// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `init_child_safe` instruction. */
export const INIT_CHILD_SAFE_DISCRIMINATOR = new Uint8Array([28, 228, 30, 126, 154, 251, 158, 96]);

/** Accounts for the `initChildSafe` instruction. */
export interface InitChildSafeAccounts {
  payer: naclac.Address | string;
  registry: naclac.Address | string;
  childSafe?: naclac.Address | string;
  systemProgram?: naclac.Address | string;
}

/**
 * Builds the `initChildSafe` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function initChildSafe(
  program: any,
  args?: Record<string, never>,
  accounts?: Partial<InitChildSafeAccounts>
) {
  const builder = program.methods.init_child_safe(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
