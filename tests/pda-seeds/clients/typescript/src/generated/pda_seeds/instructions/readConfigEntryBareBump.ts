// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `read_config_entry_bare_bump` instruction. */
export const READ_CONFIG_ENTRY_BARE_BUMP_DISCRIMINATOR = new Uint8Array([210, 17, 27, 227, 111, 244, 4, 34]);

/** Instruction arguments for `readConfigEntryBareBump`. */
export interface ReadConfigEntryBareBumpArgs {
  isPumpPool: naclac.Bool;
  marketCapLamports: bigint | number;
  tradeSizeLamports: bigint | number;
  isNewQuoteMint: naclac.Bool;
}

/** Accounts for the `readConfigEntryBareBump` instruction. */
export interface ReadConfigEntryBareBumpAccounts {
  /**
   * SAFETY: only used as PDA seed material for `config_entry` below; never
   * deserialized, invoked, or otherwise trusted for its own contents.
   */
  configProgramId: naclac.Address | string;
  configEntry?: naclac.Address | string;
}

/**
 * Builds the `readConfigEntryBareBump` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function readConfigEntryBareBump(
  program: any,
  args: ReadConfigEntryBareBumpArgs,
  accounts?: Partial<ReadConfigEntryBareBumpAccounts>
) {
  const builder = program.methods.read_config_entry_bare_bump(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
