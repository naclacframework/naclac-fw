// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `touch_config_entry_bare_bump` instruction. */
export const TOUCH_CONFIG_ENTRY_BARE_BUMP_DISCRIMINATOR = new Uint8Array([138, 240, 56, 162, 1, 92, 68, 26]);

/** Accounts for the `touchConfigEntryBareBump` instruction. */
export interface TouchConfigEntryBareBumpAccounts {
  /**
   * SAFETY: only used as PDA seed material for `config_entry` below; never
   * deserialized, invoked, or otherwise trusted for its own contents.
   */
  config_program_id: naclac.Address | string;
  config_entry?: naclac.Address | string;
}

/**
 * Builds the `touchConfigEntryBareBump` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function touchConfigEntryBareBump(
  program: any,
  args?: Record<string, never>,
  accounts?: Partial<TouchConfigEntryBareBumpAccounts>
) {
  const builder = program.methods.touch_config_entry_bare_bump(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
