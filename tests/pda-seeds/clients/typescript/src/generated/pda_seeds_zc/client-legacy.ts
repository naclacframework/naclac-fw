// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client/legacy";
import rawIDL from "./idl/pda_seeds_zc.json";
import type { PdaSeedsZc } from "./idl/pda_seeds_zc";
const IDL = rawIDL as unknown as PdaSeedsZc;
import * as instructions from "./instructions";
import * as accounts from "./accounts/legacy";
import * as types from "./types";

/**
 * High-level client for the `pda_seeds_zc` program.
 * Uses the classic @solana/web3.js (v1) transaction model.
 */
export class PdaSeedsZcClient {
  public readonly program: naclac.LegacyProgram;
  public readonly instructions = instructions;
  public readonly accounts = accounts;
  public readonly types = types;
  public readonly errors = types.ProgramErrors;

  public get constants() {
    return this.program.constants;
  }

  /** The on-chain program address as a `PublicKey`. */
  public get programId(): naclac.PublicKey {
    return this.program.programId;
  }

  constructor(
    providerOrCluster: naclac.LegacyProvider | "devnet" | "mainnet" | "localnet" | string,
    payer?: naclac.Keypair
  ) {
    let provider: naclac.LegacyProvider;
    if (typeof providerOrCluster === "string") {
      let url = providerOrCluster;
      if (providerOrCluster === "devnet") url = "https://api.devnet.solana.com";
      else if (providerOrCluster === "mainnet") url = "https://api.mainnet-beta.solana.com";
      else if (providerOrCluster === "localnet") url = "http://127.0.0.1:8899";
      const connection = new naclac.Connection(url, "confirmed");
      provider = { connection, payer, publicKey: payer ? payer.publicKey : undefined };
    } else {
      provider = providerOrCluster;
    }
    this.program = new naclac.LegacyProgram(IDL, provider);
  }

  /**
   * Builds the `initRegistry` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public initRegistry(args?: Record<string, never>, accounts?: Partial<instructions.InitRegistryAccounts>) {
    return instructions.initRegistry(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `initEntry` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public initEntry(args: instructions.InitEntryArgs, accounts?: Partial<instructions.InitEntryAccounts>) {
    return instructions.initEntry(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `touchEntryBareBump` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public touchEntryBareBump(args?: Record<string, never>, accounts?: Partial<instructions.TouchEntryBareBumpAccounts>) {
    return instructions.touchEntryBareBump(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `touchRegistryExplicitBump` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public touchRegistryExplicitBump(args: instructions.TouchRegistryExplicitBumpArgs, accounts?: Partial<instructions.TouchRegistryExplicitBumpAccounts>) {
    return instructions.touchRegistryExplicitBump(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `initChild` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public initChild(args: instructions.InitChildArgs, accounts?: Partial<instructions.InitChildAccounts>) {
    return instructions.initChild(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `initTaggedChild` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public initTaggedChild(args: instructions.InitTaggedChildArgs, accounts?: Partial<instructions.InitTaggedChildAccounts>) {
    return instructions.initTaggedChild(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `touchConfigEntryBareBump` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public touchConfigEntryBareBump(args?: Record<string, never>, accounts?: Partial<instructions.TouchConfigEntryBareBumpAccounts>) {
    return instructions.touchConfigEntryBareBump(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `touchConfigEntryBareBumpWithArgs` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public touchConfigEntryBareBumpWithArgs(args: instructions.TouchConfigEntryBareBumpWithArgsArgs, accounts?: Partial<instructions.TouchConfigEntryBareBumpWithArgsAccounts>) {
    return instructions.touchConfigEntryBareBumpWithArgs(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `readConfigEntryBareBump` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public readConfigEntryBareBump(args: instructions.ReadConfigEntryBareBumpArgs, accounts?: Partial<instructions.ReadConfigEntryBareBumpAccounts>) {
    return instructions.readConfigEntryBareBump(this.program, args ?? {}, accounts);
  }

  /** Derives the PDA for a `child` account. Returns `[PublicKey, bumpSeed]`. */
  public getChildPda(seeds: {
    registry_bump: number;
  }): [naclac.PublicKey, number] {
    const [pda, bump] = naclac.PublicKey.findProgramAddressSync(
      [
                new Uint8Array([99, 104, 105, 108, 100]),
        new Uint8Array(naclac.getIdlCodec(JSON.parse('"u8"')).encode(seeds.registry_bump))
      ],
      this.programId
    );
    return [pda, bump];
  }

  /** Derives the PDA for a `config_entry` account. Returns `[PublicKey, bumpSeed]`. */
  public getConfigEntryPda(seeds: {
    config_program_id: naclac.PublicKey | string;
  }): [naclac.PublicKey, number] {
    const [pda, bump] = naclac.PublicKey.findProgramAddressSync(
      [
                new Uint8Array([99, 111, 110, 102, 105, 103, 95, 101, 110, 116, 114, 121]),
        new naclac.PublicKey(seeds.config_program_id).toBuffer()
      ],
      this.programId
    );
    return [pda, bump];
  }

  /** Derives the PDA for a `entry` account. Returns `[PublicKey, bumpSeed]`. */
  public getEntryPda(seeds: {
    registry: naclac.PublicKey | string;
  }): [naclac.PublicKey, number] {
    const [pda, bump] = naclac.PublicKey.findProgramAddressSync(
      [
                new Uint8Array([101, 110, 116, 114, 121]),
        new naclac.PublicKey(seeds.registry).toBuffer()
      ],
      this.programId
    );
    return [pda, bump];
  }

  /** Derives the PDA for a `registry` account. Returns `[PublicKey, bumpSeed]`. */
  public getRegistryPda(seeds: {
  }): [naclac.PublicKey, number] {
    const [pda, bump] = naclac.PublicKey.findProgramAddressSync(
      [
                new Uint8Array([114, 101, 103, 105, 115, 116, 114, 121])
      ],
      this.programId
    );
    return [pda, bump];
  }

// getTaggedChildPda intentionally not generated: this PDA's seeds include
// `registry.label`, a field read from another account whose type is not a plain
// primitive — naclac-syn couldn't resolve it from the backing
// component's struct definition. Derive it manually — fetch the
// account, read the field, and call
// naclac.PublicKey.findProgramAddressSync yourself with the correct
// byte encoding for that field's type.

  /** Fetches a `Child` account. Throws if it does not exist. */
  public fetchChild(address: naclac.PublicKey | string) {
    return this.program.account['Child'].fetch(address);
  }

  /** Fetches a `Child` account. Returns `null` if it does not exist. */
  public async fetchMaybeChild(address: naclac.PublicKey | string) {
    try { return await this.program.account['Child'].fetch(address); }
    catch { return null; }
  }

  /** Fetches multiple `Child` accounts in a single RPC call. */
  public fetchAllChilds(addresses: Array<naclac.PublicKey | string>, opts?: { dropCorrupted?: boolean }) {
    return this.program.account['Child'].fetchMultiple(addresses, opts);
  }

  /** Fetches all on-chain `Child` accounts owned by this program. */
  public fetchAllChildByOwner(options?: { dropCorrupted?: boolean; filters?: any[] }) {
    return this.program.account['Child'].all(options);
  }

  /** Fetches a `ConfigEntry` account. Throws if it does not exist. */
  public fetchConfigEntry(address: naclac.PublicKey | string) {
    return this.program.account['ConfigEntry'].fetch(address);
  }

  /** Fetches a `ConfigEntry` account. Returns `null` if it does not exist. */
  public async fetchMaybeConfigEntry(address: naclac.PublicKey | string) {
    try { return await this.program.account['ConfigEntry'].fetch(address); }
    catch { return null; }
  }

  /** Fetches multiple `ConfigEntry` accounts in a single RPC call. */
  public fetchAllConfigEntrys(addresses: Array<naclac.PublicKey | string>, opts?: { dropCorrupted?: boolean }) {
    return this.program.account['ConfigEntry'].fetchMultiple(addresses, opts);
  }

  /** Fetches all on-chain `ConfigEntry` accounts owned by this program. */
  public fetchAllConfigEntryByOwner(options?: { dropCorrupted?: boolean; filters?: any[] }) {
    return this.program.account['ConfigEntry'].all(options);
  }

  /** Fetches a `Entry` account. Throws if it does not exist. */
  public fetchEntry(address: naclac.PublicKey | string) {
    return this.program.account['Entry'].fetch(address);
  }

  /** Fetches a `Entry` account. Returns `null` if it does not exist. */
  public async fetchMaybeEntry(address: naclac.PublicKey | string) {
    try { return await this.program.account['Entry'].fetch(address); }
    catch { return null; }
  }

  /** Fetches multiple `Entry` accounts in a single RPC call. */
  public fetchAllEntrys(addresses: Array<naclac.PublicKey | string>, opts?: { dropCorrupted?: boolean }) {
    return this.program.account['Entry'].fetchMultiple(addresses, opts);
  }

  /** Fetches all on-chain `Entry` accounts owned by this program. */
  public fetchAllEntryByOwner(options?: { dropCorrupted?: boolean; filters?: any[] }) {
    return this.program.account['Entry'].all(options);
  }

  /** Fetches a `Registry` account. Throws if it does not exist. */
  public fetchRegistry(address: naclac.PublicKey | string) {
    return this.program.account['Registry'].fetch(address);
  }

  /** Fetches a `Registry` account. Returns `null` if it does not exist. */
  public async fetchMaybeRegistry(address: naclac.PublicKey | string) {
    try { return await this.program.account['Registry'].fetch(address); }
    catch { return null; }
  }

  /** Fetches multiple `Registry` accounts in a single RPC call. */
  public fetchAllRegistrys(addresses: Array<naclac.PublicKey | string>, opts?: { dropCorrupted?: boolean }) {
    return this.program.account['Registry'].fetchMultiple(addresses, opts);
  }

  /** Fetches all on-chain `Registry` accounts owned by this program. */
  public fetchAllRegistryByOwner(options?: { dropCorrupted?: boolean; filters?: any[] }) {
    return this.program.account['Registry'].all(options);
  }

  /** Fetches a `TaggedChild` account. Throws if it does not exist. */
  public fetchTaggedChild(address: naclac.PublicKey | string) {
    return this.program.account['TaggedChild'].fetch(address);
  }

  /** Fetches a `TaggedChild` account. Returns `null` if it does not exist. */
  public async fetchMaybeTaggedChild(address: naclac.PublicKey | string) {
    try { return await this.program.account['TaggedChild'].fetch(address); }
    catch { return null; }
  }

  /** Fetches multiple `TaggedChild` accounts in a single RPC call. */
  public fetchAllTaggedChilds(addresses: Array<naclac.PublicKey | string>, opts?: { dropCorrupted?: boolean }) {
    return this.program.account['TaggedChild'].fetchMultiple(addresses, opts);
  }

  /** Fetches all on-chain `TaggedChild` accounts owned by this program. */
  public fetchAllTaggedChildByOwner(options?: { dropCorrupted?: boolean; filters?: any[] }) {
    return this.program.account['TaggedChild'].all(options);
  }

}
