// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client/kit";
import rawIDL from "./idl/pda_seeds_zc.json";
import type { PdaSeedsZc } from "./idl/pda_seeds_zc";
const IDL = rawIDL as unknown as PdaSeedsZc;
import * as instructions from "./instructions";
import * as accounts from "./accounts/kit";
import * as types from "./types";

/**
 * High-level client for the `pda_seeds_zc` program.
 * Uses the modern @solana/kit (v2) transaction model.
 */
export class PdaSeedsZcClient {
  public readonly program: naclac.Program;
  public readonly instructions = instructions;
  public readonly accounts = accounts;
  public readonly types = types;
  public readonly errors = types.ProgramErrors;

  public get constants() {
    return this.program.constants;
  }

  /** The on-chain program address. */
  public get programId(): naclac.Address {
    return this.program.idl.address as naclac.Address;
  }

  constructor(
    providerOrCluster: naclac.NaclacProvider | "devnet" | "mainnet" | "localnet",
    payer?: naclac.KeyPairSigner
  ) {
    let provider: naclac.NaclacProvider;
    if (typeof providerOrCluster === "string") {
      if (!payer) throw new Error("[Naclac] A payer KeyPairSigner is required when specifying a cluster string.");
      provider = naclac.createProvider(providerOrCluster, payer);
    } else {
      provider = providerOrCluster;
    }
    this.program = new naclac.Program(IDL, provider);
  }

  /**
   * Builds the `initRegistry` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public initRegistry(args?: Record<string, never>, accounts?: Partial<instructions.InitRegistryAccounts>) {
    return instructions.initRegistry(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `initEntry` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public initEntry(args: instructions.InitEntryArgs, accounts?: Partial<instructions.InitEntryAccounts>) {
    return instructions.initEntry(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `touchEntryBareBump` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public touchEntryBareBump(args?: Record<string, never>, accounts?: Partial<instructions.TouchEntryBareBumpAccounts>) {
    return instructions.touchEntryBareBump(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `touchRegistryExplicitBump` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public touchRegistryExplicitBump(args: instructions.TouchRegistryExplicitBumpArgs, accounts?: Partial<instructions.TouchRegistryExplicitBumpAccounts>) {
    return instructions.touchRegistryExplicitBump(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `initChild` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public initChild(args: instructions.InitChildArgs, accounts?: Partial<instructions.InitChildAccounts>) {
    return instructions.initChild(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `initTaggedChild` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public initTaggedChild(args: instructions.InitTaggedChildArgs, accounts?: Partial<instructions.InitTaggedChildAccounts>) {
    return instructions.initTaggedChild(this.program, args ?? {}, accounts);
  }

  /** Derives the PDA for a `child` account. */
  public async getChildPda(seeds: {
    registry_bump: number;
  }): Promise<readonly [naclac.Address, number]> {
    return naclac.getProgramDerivedAddress({
      programAddress: this.programId,
      seeds: [
                new Uint8Array([99, 104, 105, 108, 100]),
        new Uint8Array(naclac.getIdlCodec(JSON.parse('"u8"')).encode(seeds.registry_bump))
      ]
    });
  }

  /** Derives the PDA for a `entry` account. */
  public async getEntryPda(seeds: {
    registry: naclac.Address | string;
  }): Promise<readonly [naclac.Address, number]> {
    return naclac.getProgramDerivedAddress({
      programAddress: this.programId,
      seeds: [
                new Uint8Array([101, 110, 116, 114, 121]),
        new Uint8Array(naclac.getAddressEncoder().encode(typeof seeds.registry === 'string' ? naclac.address(seeds.registry) : seeds.registry))
      ]
    });
  }

  /** Derives the PDA for a `registry` account. */
  public async getRegistryPda(seeds: {
  }): Promise<readonly [naclac.Address, number]> {
    return naclac.getProgramDerivedAddress({
      programAddress: this.programId,
      seeds: [
                new Uint8Array([114, 101, 103, 105, 115, 116, 114, 121])
      ]
    });
  }

// getTaggedChildPda intentionally not generated: this PDA's seeds include
// `registry.label`, a field read from another account whose type is not a plain
// primitive — naclac-syn couldn't resolve it from the backing
// component's struct definition. Derive it manually — fetch the
// account, read the field, and call naclac.getProgramDerivedAddress
// yourself with the correct encoding for that field's type.

  /** Fetches a `Child` account. Throws if it does not exist. */
  public fetchChild(address: naclac.Address | string) {
    return accounts.fetchChild(this.program.provider.rpc as any, naclac.address(address as string));
  }

  /** Fetches a `Child` account. Returns a MaybeAccount (check `.exists`) if it does not exist. */
  public fetchMaybeChild(address: naclac.Address | string) {
    return accounts.fetchMaybeChild(this.program.provider.rpc as any, naclac.address(address as string));
  }

  /** Fetches multiple `Child` accounts by address. Throws if any do not exist. */
  public fetchAllChilds(addresses: Array<naclac.Address | string>) {
    return accounts.fetchAllChild(this.program.provider.rpc as any, addresses.map((a) => naclac.address(a as string)));
  }

  /** Fetches multiple `Child` accounts. Missing accounts have `.exists = false`. */
  public fetchAllMaybeChilds(addresses: Array<naclac.Address | string>) {
    return accounts.fetchAllMaybeChild(this.program.provider.rpc as any, addresses.map((a) => naclac.address(a as string)));
  }

  /** Fetches a `Entry` account. Throws if it does not exist. */
  public fetchEntry(address: naclac.Address | string) {
    return accounts.fetchEntry(this.program.provider.rpc as any, naclac.address(address as string));
  }

  /** Fetches a `Entry` account. Returns a MaybeAccount (check `.exists`) if it does not exist. */
  public fetchMaybeEntry(address: naclac.Address | string) {
    return accounts.fetchMaybeEntry(this.program.provider.rpc as any, naclac.address(address as string));
  }

  /** Fetches multiple `Entry` accounts by address. Throws if any do not exist. */
  public fetchAllEntrys(addresses: Array<naclac.Address | string>) {
    return accounts.fetchAllEntry(this.program.provider.rpc as any, addresses.map((a) => naclac.address(a as string)));
  }

  /** Fetches multiple `Entry` accounts. Missing accounts have `.exists = false`. */
  public fetchAllMaybeEntrys(addresses: Array<naclac.Address | string>) {
    return accounts.fetchAllMaybeEntry(this.program.provider.rpc as any, addresses.map((a) => naclac.address(a as string)));
  }

  /** Fetches a `Registry` account. Throws if it does not exist. */
  public fetchRegistry(address: naclac.Address | string) {
    return accounts.fetchRegistry(this.program.provider.rpc as any, naclac.address(address as string));
  }

  /** Fetches a `Registry` account. Returns a MaybeAccount (check `.exists`) if it does not exist. */
  public fetchMaybeRegistry(address: naclac.Address | string) {
    return accounts.fetchMaybeRegistry(this.program.provider.rpc as any, naclac.address(address as string));
  }

  /** Fetches multiple `Registry` accounts by address. Throws if any do not exist. */
  public fetchAllRegistrys(addresses: Array<naclac.Address | string>) {
    return accounts.fetchAllRegistry(this.program.provider.rpc as any, addresses.map((a) => naclac.address(a as string)));
  }

  /** Fetches multiple `Registry` accounts. Missing accounts have `.exists = false`. */
  public fetchAllMaybeRegistrys(addresses: Array<naclac.Address | string>) {
    return accounts.fetchAllMaybeRegistry(this.program.provider.rpc as any, addresses.map((a) => naclac.address(a as string)));
  }

  /** Fetches a `TaggedChild` account. Throws if it does not exist. */
  public fetchTaggedChild(address: naclac.Address | string) {
    return accounts.fetchTaggedChild(this.program.provider.rpc as any, naclac.address(address as string));
  }

  /** Fetches a `TaggedChild` account. Returns a MaybeAccount (check `.exists`) if it does not exist. */
  public fetchMaybeTaggedChild(address: naclac.Address | string) {
    return accounts.fetchMaybeTaggedChild(this.program.provider.rpc as any, naclac.address(address as string));
  }

  /** Fetches multiple `TaggedChild` accounts by address. Throws if any do not exist. */
  public fetchAllTaggedChilds(addresses: Array<naclac.Address | string>) {
    return accounts.fetchAllTaggedChild(this.program.provider.rpc as any, addresses.map((a) => naclac.address(a as string)));
  }

  /** Fetches multiple `TaggedChild` accounts. Missing accounts have `.exists = false`. */
  public fetchAllMaybeTaggedChilds(addresses: Array<naclac.Address | string>) {
    return accounts.fetchAllMaybeTaggedChild(this.program.provider.rpc as any, addresses.map((a) => naclac.address(a as string)));
  }

  /** Fetches ALL `Child` accounts owned by this program. */
  public fetchAllChildByOwner(options?: { commitment?: any; filters?: unknown[] }) {
    return accounts.fetchAllChildByOwner(this.program.provider.rpc as any, this.programId, options);
  }

  /** Fetches ALL `Entry` accounts owned by this program. */
  public fetchAllEntryByOwner(options?: { commitment?: any; filters?: unknown[] }) {
    return accounts.fetchAllEntryByOwner(this.program.provider.rpc as any, this.programId, options);
  }

  /** Fetches ALL `Registry` accounts owned by this program. */
  public fetchAllRegistryByOwner(options?: { commitment?: any; filters?: unknown[] }) {
    return accounts.fetchAllRegistryByOwner(this.program.provider.rpc as any, this.programId, options);
  }

  /** Fetches ALL `TaggedChild` accounts owned by this program. */
  public fetchAllTaggedChildByOwner(options?: { commitment?: any; filters?: unknown[] }) {
    return accounts.fetchAllTaggedChildByOwner(this.program.provider.rpc as any, this.programId, options);
  }

}
