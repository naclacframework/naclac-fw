// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client/kit";
import rawIDL from "./idl/stack_safety.json";
import type { StackSafety } from "./idl/stack_safety";
const IDL = rawIDL as unknown as StackSafety;
import * as instructions from "./instructions";
import * as accounts from "./accounts/kit";
import * as types from "./types";

/**
 * High-level client for the `stack_safety` program.
 * Uses the modern @solana/kit (v2) transaction model.
 */
export class StackSafetyClient {
  public readonly program: naclac.Program;
  public readonly instructions = instructions;
  public readonly accounts = accounts;
  public readonly types = types;
  public readonly errors = types.ProgramErrors;

  public get constants() {
    return this.program.constants;
  }

  /** The on-chain program address. */
  public get programId(): naclac.Address {
    return this.program.idl.address as naclac.Address;
  }

  constructor(
    providerOrCluster: naclac.NaclacProvider | "devnet" | "mainnet" | "localnet",
    payer?: naclac.KeyPairSigner
  ) {
    let provider: naclac.NaclacProvider;
    if (typeof providerOrCluster === "string") {
      if (!payer) throw new Error("[Naclac] A payer KeyPairSigner is required when specifying a cluster string.");
      provider = naclac.createProvider(providerOrCluster, payer);
    } else {
      provider = providerOrCluster;
    }
    this.program = new naclac.Program(IDL, provider);
  }

  /**
   * Builds the `initSmall` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public initSmall(args?: Record<string, never>, accounts?: Partial<instructions.InitSmallAccounts>) {
    return instructions.initSmall(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `touchSmall` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public touchSmall(args?: Record<string, never>, accounts?: Partial<instructions.TouchSmallAccounts>) {
    return instructions.touchSmall(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `initBig` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public initBig(args?: Record<string, never>, accounts?: Partial<instructions.InitBigAccounts>) {
    return instructions.initBig(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `touchBig` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public touchBig(args?: Record<string, never>, accounts?: Partial<instructions.TouchBigAccounts>) {
    return instructions.touchBig(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `closeBig` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public closeBig(args?: Record<string, never>, accounts?: Partial<instructions.CloseBigAccounts>) {
    return instructions.closeBig(this.program, args ?? {}, accounts);
  }

  /** Derives the PDA for a `big` account. */
  public async getBigPda(seeds: {
  }): Promise<readonly [naclac.Address, number]> {
    return naclac.getProgramDerivedAddress({
      programAddress: this.programId,
      seeds: [
                new Uint8Array([98, 105, 103])
      ]
    });
  }

  /** Derives the PDA for a `small` account. */
  public async getSmallPda(seeds: {
  }): Promise<readonly [naclac.Address, number]> {
    return naclac.getProgramDerivedAddress({
      programAddress: this.programId,
      seeds: [
                new Uint8Array([115, 109, 97, 108, 108])
      ]
    });
  }

  /** Fetches a `BigData` account. Throws if it does not exist. */
  public fetchBigData(address: naclac.Address | string) {
    return accounts.fetchBigData(this.program.provider.rpc as any, naclac.address(address as string));
  }

  /** Fetches a `BigData` account. Returns a MaybeAccount (check `.exists`) if it does not exist. */
  public fetchMaybeBigData(address: naclac.Address | string) {
    return accounts.fetchMaybeBigData(this.program.provider.rpc as any, naclac.address(address as string));
  }

  /** Fetches multiple `BigData` accounts by address. Throws if any do not exist. */
  public fetchAllBigDatas(addresses: Array<naclac.Address | string>) {
    return accounts.fetchAllBigData(this.program.provider.rpc as any, addresses.map((a) => naclac.address(a as string)));
  }

  /** Fetches multiple `BigData` accounts. Missing accounts have `.exists = false`. */
  public fetchAllMaybeBigDatas(addresses: Array<naclac.Address | string>) {
    return accounts.fetchAllMaybeBigData(this.program.provider.rpc as any, addresses.map((a) => naclac.address(a as string)));
  }

  /** Fetches a `SmallData` account. Throws if it does not exist. */
  public fetchSmallData(address: naclac.Address | string) {
    return accounts.fetchSmallData(this.program.provider.rpc as any, naclac.address(address as string));
  }

  /** Fetches a `SmallData` account. Returns a MaybeAccount (check `.exists`) if it does not exist. */
  public fetchMaybeSmallData(address: naclac.Address | string) {
    return accounts.fetchMaybeSmallData(this.program.provider.rpc as any, naclac.address(address as string));
  }

  /** Fetches multiple `SmallData` accounts by address. Throws if any do not exist. */
  public fetchAllSmallDatas(addresses: Array<naclac.Address | string>) {
    return accounts.fetchAllSmallData(this.program.provider.rpc as any, addresses.map((a) => naclac.address(a as string)));
  }

  /** Fetches multiple `SmallData` accounts. Missing accounts have `.exists = false`. */
  public fetchAllMaybeSmallDatas(addresses: Array<naclac.Address | string>) {
    return accounts.fetchAllMaybeSmallData(this.program.provider.rpc as any, addresses.map((a) => naclac.address(a as string)));
  }

  /** Fetches ALL `BigData` accounts owned by this program. */
  public fetchAllBigDataByOwner(options?: { commitment?: any; filters?: unknown[] }) {
    return accounts.fetchAllBigDataByOwner(this.program.provider.rpc as any, this.programId, options);
  }

  /** Fetches ALL `SmallData` accounts owned by this program. */
  public fetchAllSmallDataByOwner(options?: { commitment?: any; filters?: unknown[] }) {
    return accounts.fetchAllSmallDataByOwner(this.program.provider.rpc as any, this.programId, options);
  }

}
