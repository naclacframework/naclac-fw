// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `touch_big` instruction. */
export const TOUCH_BIG_DISCRIMINATOR = new Uint8Array([66, 178, 192, 160, 63, 148, 8, 37]);

/** Accounts for the `touchBig` instruction. */
export interface TouchBigAccounts {
  big: naclac.Address | string;
}

/**
 * Builds the `touchBig` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function touchBig(
  program: any,
  args?: Record<string, never>,
  accounts?: Partial<TouchBigAccounts>
) {
  const builder = program.methods.touch_big(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
