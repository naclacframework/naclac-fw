// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `close_big` instruction. */
export const CLOSE_BIG_DISCRIMINATOR = new Uint8Array([80, 248, 96, 7, 7, 207, 31, 221]);

/** Accounts for the `closeBig` instruction. */
export interface CloseBigAccounts {
  payer: naclac.Address | string;
  big: naclac.Address | string;
}

/**
 * Builds the `closeBig` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function closeBig(
  program: any,
  args?: Record<string, never>,
  accounts?: Partial<CloseBigAccounts>
) {
  const builder = program.methods.close_big(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
