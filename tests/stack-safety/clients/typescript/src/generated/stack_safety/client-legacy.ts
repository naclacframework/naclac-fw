// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client/legacy";
import rawIDL from "./idl/stack_safety.json";
import type { StackSafety } from "./idl/stack_safety";
const IDL = rawIDL as unknown as StackSafety;
import * as instructions from "./instructions";
import * as accounts from "./accounts/legacy";
import * as types from "./types";

/**
 * High-level client for the `stack_safety` program.
 * Uses the classic @solana/web3.js (v1) transaction model.
 */
export class StackSafetyClient {
  public readonly program: naclac.LegacyProgram;
  public readonly instructions = instructions;
  public readonly accounts = accounts;
  public readonly types = types;
  public readonly errors = types.ProgramErrors;

  public get constants() {
    return this.program.constants;
  }

  /** The on-chain program address as a `PublicKey`. */
  public get programId(): naclac.PublicKey {
    return this.program.programId;
  }

  constructor(
    providerOrCluster: naclac.LegacyProvider | "devnet" | "mainnet" | "localnet" | string,
    payer?: naclac.Keypair
  ) {
    let provider: naclac.LegacyProvider;
    if (typeof providerOrCluster === "string") {
      let url = providerOrCluster;
      if (providerOrCluster === "devnet") url = "https://api.devnet.solana.com";
      else if (providerOrCluster === "mainnet") url = "https://api.mainnet-beta.solana.com";
      else if (providerOrCluster === "localnet") url = "http://127.0.0.1:8899";
      const connection = new naclac.Connection(url, "confirmed");
      provider = { connection, payer, publicKey: payer ? payer.publicKey : undefined };
    } else {
      provider = providerOrCluster;
    }
    this.program = new naclac.LegacyProgram(IDL, provider);
  }

  /**
   * Builds the `initSmall` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public initSmall(args?: Record<string, never>, accounts?: Partial<instructions.InitSmallAccounts>) {
    return instructions.initSmall(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `touchSmall` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public touchSmall(args?: Record<string, never>, accounts?: Partial<instructions.TouchSmallAccounts>) {
    return instructions.touchSmall(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `initBig` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public initBig(args?: Record<string, never>, accounts?: Partial<instructions.InitBigAccounts>) {
    return instructions.initBig(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `touchBig` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public touchBig(args?: Record<string, never>, accounts?: Partial<instructions.TouchBigAccounts>) {
    return instructions.touchBig(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `closeBig` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public closeBig(args?: Record<string, never>, accounts?: Partial<instructions.CloseBigAccounts>) {
    return instructions.closeBig(this.program, args ?? {}, accounts);
  }

  /** Derives the PDA for a `big` account. Returns `[PublicKey, bumpSeed]`. */
  public getBigPda(seeds: {
  }): [naclac.PublicKey, number] {
    const [pda, bump] = naclac.PublicKey.findProgramAddressSync(
      [
                new Uint8Array([98, 105, 103])
      ],
      this.programId
    );
    return [pda, bump];
  }

  /** Derives the PDA for a `small` account. Returns `[PublicKey, bumpSeed]`. */
  public getSmallPda(seeds: {
  }): [naclac.PublicKey, number] {
    const [pda, bump] = naclac.PublicKey.findProgramAddressSync(
      [
                new Uint8Array([115, 109, 97, 108, 108])
      ],
      this.programId
    );
    return [pda, bump];
  }

  /** Fetches a `BigData` account. Throws if it does not exist. */
  public fetchBigData(address: naclac.PublicKey | string) {
    return this.program.account['BigData'].fetch(address);
  }

  /** Fetches a `BigData` account. Returns `null` if it does not exist. */
  public async fetchMaybeBigData(address: naclac.PublicKey | string) {
    try { return await this.program.account['BigData'].fetch(address); }
    catch { return null; }
  }

  /** Fetches multiple `BigData` accounts in a single RPC call. */
  public fetchAllBigDatas(addresses: Array<naclac.PublicKey | string>, opts?: { dropCorrupted?: boolean }) {
    return this.program.account['BigData'].fetchMultiple(addresses, opts);
  }

  /** Fetches all on-chain `BigData` accounts owned by this program. */
  public fetchAllBigDataByOwner(options?: { dropCorrupted?: boolean; filters?: any[] }) {
    return this.program.account['BigData'].all(options);
  }

  /** Fetches a `SmallData` account. Throws if it does not exist. */
  public fetchSmallData(address: naclac.PublicKey | string) {
    return this.program.account['SmallData'].fetch(address);
  }

  /** Fetches a `SmallData` account. Returns `null` if it does not exist. */
  public async fetchMaybeSmallData(address: naclac.PublicKey | string) {
    try { return await this.program.account['SmallData'].fetch(address); }
    catch { return null; }
  }

  /** Fetches multiple `SmallData` accounts in a single RPC call. */
  public fetchAllSmallDatas(addresses: Array<naclac.PublicKey | string>, opts?: { dropCorrupted?: boolean }) {
    return this.program.account['SmallData'].fetchMultiple(addresses, opts);
  }

  /** Fetches all on-chain `SmallData` accounts owned by this program. */
  public fetchAllSmallDataByOwner(options?: { dropCorrupted?: boolean; filters?: any[] }) {
    return this.program.account['SmallData'].all(options);
  }

}
