name: Release Naclac CLI

on:
  push:
    tags:
      - 'v*' # Trigger on version tags (e.g. v0.1.0)

permissions:
  contents: write

jobs:
  build-release:
    name: Build for ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
            artifact_name: naclac
            use_shell: true
          - target: x86_64-apple-darwin
            os: macos-latest
            artifact_name: naclac
            use_shell: true
          - target: aarch64-apple-darwin
            os: macos-latest
            artifact_name: naclac
            use_shell: true
          - target: x86_64-pc-windows-msvc
            os: windows-latest
            artifact_name: naclac.exe
            use_shell: false

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Install Rust
      uses: dtolnay/rust-toolchain@stable
      with:
        targets: ${{ matrix.target }}

    - name: Build CLI in Release Mode
      run: cargo build --release --bin naclac --target ${{ matrix.target }}

    - name: Package Binary (Unix)
      if: matrix.use_shell
      run: |
        cd target/${{ matrix.target }}/release
        tar -czf ../../../naclac-${{ matrix.target }}.tar.gz ${{ matrix.artifact_name }}

    - name: Package Binary (Windows)
      if: ${{ !matrix.use_shell }}
      run: |
        cd target/${{ matrix.target }}/release
        tar -czf ../../../naclac-${{ matrix.target }}.zip ${{ matrix.artifact_name }}

    - name: Upload Release Artifacts
      uses: softprops/action-gh-release@v1
      with:
        files: |
          naclac-${{ matrix.target }}.tar.gz
          naclac-${{ matrix.target }}.zip
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
