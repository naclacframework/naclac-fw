// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client/kit";
import rawIDL from "./idl/token_creator.json";
import type { TokenCreator } from "./idl/token_creator";
const IDL = rawIDL as unknown as TokenCreator;
import * as instructions from "./instructions";
import * as accounts from "./accounts/kit";
import * as types from "./types";

/**
 * High-level client for the `token_creator` program.
 * Uses the modern @solana/kit (v2) transaction model.
 */
export class TokenCreatorClient {
  public readonly program: naclac.Program;
  public readonly instructions = instructions;
  public readonly accounts = accounts;
  public readonly types = types;
  public readonly errors = types.ProgramErrors;

  public get constants() {
    return this.program.constants;
  }

  /** The on-chain program address. */
  public get programId(): naclac.Address {
    return this.program.idl.address as naclac.Address;
  }

  constructor(
    providerOrCluster: naclac.NaclacProvider | "devnet" | "mainnet" | "localnet",
    payer?: naclac.KeyPairSigner
  ) {
    let provider: naclac.NaclacProvider;
    if (typeof providerOrCluster === "string") {
      if (!payer) throw new Error("[Naclac] A payer KeyPairSigner is required when specifying a cluster string.");
      provider = naclac.createProvider(providerOrCluster, payer);
    } else {
      provider = providerOrCluster;
    }
    this.program = new naclac.Program(IDL, provider);
  }

  /**
   * Builds the `launchToken` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public launchToken(args: instructions.LaunchTokenArgs, accounts?: Partial<instructions.LaunchTokenAccounts>) {
    return instructions.launchToken(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `createMint` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public createMint(args: instructions.CreateMintArgs, accounts?: Partial<instructions.CreateMintAccounts>) {
    return instructions.createMint(this.program, args ?? {}, accounts);
  }

  /** Derives the PDA for a `launchRecord` account. */
  public async getLaunchRecordPda(seeds: {
    payer: naclac.Address | string;
    id: bigint | number;
  }): Promise<readonly [naclac.Address, number]> {
    return naclac.getProgramDerivedAddress({
      programAddress: this.programId,
      seeds: [
                new Uint8Array([108, 97, 117, 110, 99, 104]),
        new Uint8Array(naclac.getAddressEncoder().encode(typeof seeds.payer === 'string' ? naclac.address(seeds.payer) : seeds.payer)),
        new Uint8Array(naclac.getIdlCodec(JSON.parse('"u64"')).encode(seeds.id))
      ]
    });
  }

  /** Derives the PDA for a `mint` account. */
  public async getMintPda(seeds: {
    id: bigint | number;
  }): Promise<readonly [naclac.Address, number]> {
    return naclac.getProgramDerivedAddress({
      programAddress: this.programId,
      seeds: [
                new Uint8Array([109, 105, 110, 116]),
        new Uint8Array(naclac.getIdlCodec(JSON.parse('"u64"')).encode(seeds.id))
      ]
    });
  }

  /** Fetches a `LaunchRecord` account. Throws if it does not exist. */
  public fetchLaunchRecord(address: naclac.Address | string) {
    return accounts.fetchLaunchRecord(this.program.provider.rpc as any, naclac.address(address as string));
  }

  /** Fetches a `LaunchRecord` account. Returns a MaybeAccount (check `.exists`) if it does not exist. */
  public fetchMaybeLaunchRecord(address: naclac.Address | string) {
    return accounts.fetchMaybeLaunchRecord(this.program.provider.rpc as any, naclac.address(address as string));
  }

  /** Fetches multiple `LaunchRecord` accounts by address. Throws if any do not exist. */
  public fetchAllLaunchRecords(addresses: Array<naclac.Address | string>) {
    return accounts.fetchAllLaunchRecord(this.program.provider.rpc as any, addresses.map((a) => naclac.address(a as string)));
  }

  /** Fetches multiple `LaunchRecord` accounts. Missing accounts have `.exists = false`. */
  public fetchAllMaybeLaunchRecords(addresses: Array<naclac.Address | string>) {
    return accounts.fetchAllMaybeLaunchRecord(this.program.provider.rpc as any, addresses.map((a) => naclac.address(a as string)));
  }

  /** Subscribes to `TokenLaunched` events. Returns a listener ID. */
  public onTokenLaunched(callback: (event: types.TokenLaunched, slot: number, signature: string) => void) {
    return types.addTokenLaunchedListener(this.program, callback);
  }

  /** Awaits the next `TokenLaunched` event. Resolves `null` on timeout. */
  public waitForTokenLaunched(options?: { timeoutMs?: number }) {
    return this.program.waitForEvent<types.TokenLaunched>("TokenLaunched", options);
  }

  /** Subscribes to `MintCreated` events. Returns a listener ID. */
  public onMintCreated(callback: (event: types.MintCreated, slot: number, signature: string) => void) {
    return types.addMintCreatedListener(this.program, callback);
  }

  /** Awaits the next `MintCreated` event. Resolves `null` on timeout. */
  public waitForMintCreated(options?: { timeoutMs?: number }) {
    return this.program.waitForEvent<types.MintCreated>("MintCreated", options);
  }

  /** Removes a registered event listener by its ID. */
  public removeEventListener(listenerId: number) {
    return types.removeListener(this.program, listenerId);
  }
  /** Fetches ALL `LaunchRecord` accounts owned by this program. */
  public fetchAllLaunchRecordByOwner(options?: { commitment?: any; filters?: unknown[] }) {
    return accounts.fetchAllLaunchRecordByOwner(this.program.provider.rpc as any, this.programId, options);
  }

}
