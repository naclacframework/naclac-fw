// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

#[cfg(feature = "borsh")]
use crate::sdk_core::borsh::{BorshDeserialize, BorshSerialize};

#[cfg_attr(feature = "borsh", derive(Clone, Debug, BorshSerialize, BorshDeserialize))]
#[cfg_attr(feature = "borsh", borsh(crate = "crate::sdk_core::borsh"))]
#[cfg_attr(not(feature = "borsh"), derive(Copy, Clone, Debug))]
#[cfg_attr(not(feature = "borsh"), repr(C))]
pub struct LaunchRecord {
    pub creator: crate::sdk_core::Address,
    pub mint: crate::sdk_core::Address,
    pub amount_token: u64,
    pub amount_quote: u64,
}

/// 8-byte on-chain discriminator for `LaunchRecord` accounts.
pub const LAUNCHRECORD_DISCRIMINATOR: [u8; 8] = [146, 240, 197, 204, 6, 6, 87, 75];
#[cfg(not(feature = "borsh"))]
unsafe impl crate::sdk_core::bytemuck::Zeroable for LaunchRecord {}
#[cfg(not(feature = "borsh"))]
unsafe impl crate::sdk_core::bytemuck::Pod for LaunchRecord {}
#[cfg(feature = "offchain")]
#[cfg(not(feature = "borsh"))]
/// Borrows account data as `LaunchRecord` after skipping the 8-byte discriminator.
/// The returned reference is zero-copy: no allocation, no deserialization.
pub fn fetch_launch_record(data: &[u8]) -> &LaunchRecord {
    naclac_client::bytemuck::from_bytes::<LaunchRecord>(&data[8..])
}

#[cfg(feature = "offchain")]
#[cfg(not(feature = "borsh"))]
/// Borrows account data mutably as `LaunchRecord` (zero-copy write access).
pub fn fetch_launch_record_mut(data: &mut [u8]) -> &mut LaunchRecord {
    naclac_client::bytemuck::from_bytes_mut::<LaunchRecord>(&mut data[8..])
}

#[cfg(feature = "offchain")]
#[cfg(not(feature = "borsh"))]
/// Fetches ALL on-chain `LaunchRecord` accounts owned by `program_id` (Zero-Copy).
pub fn fetch_all_launch_record(
    provider: &naclac_client::NaclacProvider,
    program_id: &naclac_client::Address,
) -> Result<crate::sdk_core::Vec<(naclac_client::Address, LaunchRecord)>, naclac_client::NaclacClientError> {
    let raw_accounts = provider.get_program_accounts(program_id, Some(&LAUNCHRECORD_DISCRIMINATOR))?;
    let mut results = crate::sdk_core::Vec::new();
    for (address, data) in raw_accounts {
        if data.len() < 8 { continue; }
        let value = fetch_launch_record(&data);
        results.push((address, *value));
    }
    Ok(results)
}
#[cfg(feature = "offchain")]
#[cfg(feature = "borsh")]
/// Fetches and deserializes a `LaunchRecord` account from the chain (Borsh).
pub fn fetch_launch_record(
    provider: &naclac_client::NaclacProvider,
    address: &naclac_client::Address,
) -> Result<LaunchRecord, naclac_client::NaclacClientError> {
    let raw = provider.get_account_data(address)?;
    if raw.len() < 8 || raw[..8] != LAUNCHRECORD_DISCRIMINATOR {
        return Err(naclac_client::NaclacClientError::General(
            format!("Invalid discriminator for account {}", address)
        ));
    }
    naclac_client::borsh::from_slice::<LaunchRecord>(&raw[8..])
        .map_err(|e| naclac_client::NaclacClientError::DeserializationError(e.to_string()))
}

#[cfg(feature = "offchain")]
#[cfg(feature = "borsh")]
/// Fetches a `LaunchRecord` account (Borsh). Returns `None` if the account does not exist.
pub fn fetch_maybe_launch_record(
    provider: &naclac_client::NaclacProvider,
    address: &naclac_client::Address,
) -> Result<Option<LaunchRecord>, naclac_client::NaclacClientError> {
    match fetch_launch_record(provider, address) {
        Ok(a) => Ok(Some(a)),
        Err(naclac_client::NaclacClientError::AccountNotFound(_)) => Ok(None),
        Err(e) => Err(e),
    }
}

#[cfg(feature = "offchain")]
#[cfg(feature = "borsh")]
/// Fetches ALL on-chain `LaunchRecord` accounts owned by `program_id` (Borsh).
pub fn fetch_all_launch_record(
    provider: &naclac_client::NaclacProvider,
    program_id: &naclac_client::Address,
) -> Result<crate::sdk_core::Vec<(naclac_client::Address, LaunchRecord)>, naclac_client::NaclacClientError> {
    let raw_accounts = provider.get_program_accounts(program_id, Some(&LAUNCHRECORD_DISCRIMINATOR))?;
    let mut results = crate::sdk_core::Vec::new();
    for (address, data) in raw_accounts {
        if data.len() < 8 { continue; }
        match naclac_client::borsh::from_slice::<LaunchRecord>(&data[8..]) {
            Ok(account) => results.push((address, account)),
            Err(_) => continue,
        }
    }
    Ok(results)
}
