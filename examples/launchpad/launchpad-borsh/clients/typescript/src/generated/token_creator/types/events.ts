// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for `TokenLaunched` events in transaction logs. */
export const TOKENLAUNCHED_EVENT_DISCRIMINATOR = new Uint8Array([225, 232, 190, 147, 213, 192, 220, 168]);

/** Auto-generated event interface from the program IDL. */
export interface TokenLaunched {
  id: bigint | number;
  mint: naclac.Address | string;
  amountToken: bigint | number;
  amountQuote: bigint | number;
}

/** Subscribes to `TokenLaunched` events. Returns a listener ID for cleanup. */
export function addTokenLaunchedListener(
  program: any,
  callback: (event: TokenLaunched, slot: number, signature: string) => void
): number {
  return program.addEventListener("TokenLaunched", callback);
}

/** Waits for the next `TokenLaunched` event. Resolves `null` if the timeout expires. */
export async function waitForTokenLaunched(
  program: any,
  options?: { timeoutMs?: number }
): Promise<TokenLaunched | null> {
  return program.waitForEvent("TokenLaunched", options) as Promise<TokenLaunched | null>;
}

/** 8-byte discriminator for `MintCreated` events in transaction logs. */
export const MINTCREATED_EVENT_DISCRIMINATOR = new Uint8Array([254, 157, 196, 76, 231, 48, 27, 150]);

/** Auto-generated event interface from the program IDL. */
export interface MintCreated {
  id: bigint | number;
  mint: naclac.Address | string;
  decimals: number;
}

/** Subscribes to `MintCreated` events. Returns a listener ID for cleanup. */
export function addMintCreatedListener(
  program: any,
  callback: (event: MintCreated, slot: number, signature: string) => void
): number {
  return program.addEventListener("MintCreated", callback);
}

/** Waits for the next `MintCreated` event. Resolves `null` if the timeout expires. */
export async function waitForMintCreated(
  program: any,
  options?: { timeoutMs?: number }
): Promise<MintCreated | null> {
  return program.waitForEvent("MintCreated", options) as Promise<MintCreated | null>;
}

/** Removes a previously registered event listener. */
export function removeListener(program: any, listenerId: number) {
  program.removeEventListener(listenerId);
}
