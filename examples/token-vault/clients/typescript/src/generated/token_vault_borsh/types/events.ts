// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for `TokenDeposited` events in transaction logs. */
export const TOKENDEPOSITED_EVENT_DISCRIMINATOR = new Uint8Array([104, 7, 18, 187, 94, 141, 251, 120]);

/** Auto-generated event interface from the program IDL. */
export interface TokenDeposited {
  user: naclac.Address | string;
  amount: bigint | number;
  totalVaultBalance: bigint | number;
}

/** Subscribes to `TokenDeposited` events. Returns a listener ID for cleanup. */
export function addTokenDepositedListener(
  program: any,
  callback: (event: TokenDeposited, slot: number, signature: string) => void
): number {
  return program.addEventListener("TokenDeposited", callback);
}

/** Waits for the next `TokenDeposited` event. Resolves `null` if the timeout expires. */
export async function waitForTokenDeposited(
  program: any,
  options?: { timeoutMs?: number }
): Promise<TokenDeposited | null> {
  return program.waitForEvent("TokenDeposited", options) as Promise<TokenDeposited | null>;
}

/** Decodes every `TokenDeposited` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForTokenDeposited`/`addTokenDepositedListener` — prefer this when you already know which transaction you're checking. */
export function parseTokenDepositedEvents(
  program: any,
  logs: readonly string[]
): TokenDeposited[] {
  return program.parseEvents("TokenDeposited", logs) as TokenDeposited[];
}

/** 8-byte discriminator for `TokenWithdrawn` events in transaction logs. */
export const TOKENWITHDRAWN_EVENT_DISCRIMINATOR = new Uint8Array([35, 57, 130, 51, 154, 247, 155, 142]);

/** Auto-generated event interface from the program IDL. */
export interface TokenWithdrawn {
  user: naclac.Address | string;
  amount: bigint | number;
  totalVaultBalance: bigint | number;
}

/** Subscribes to `TokenWithdrawn` events. Returns a listener ID for cleanup. */
export function addTokenWithdrawnListener(
  program: any,
  callback: (event: TokenWithdrawn, slot: number, signature: string) => void
): number {
  return program.addEventListener("TokenWithdrawn", callback);
}

/** Waits for the next `TokenWithdrawn` event. Resolves `null` if the timeout expires. */
export async function waitForTokenWithdrawn(
  program: any,
  options?: { timeoutMs?: number }
): Promise<TokenWithdrawn | null> {
  return program.waitForEvent("TokenWithdrawn", options) as Promise<TokenWithdrawn | null>;
}

/** Decodes every `TokenWithdrawn` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForTokenWithdrawn`/`addTokenWithdrawnListener` — prefer this when you already know which transaction you're checking. */
export function parseTokenWithdrawnEvents(
  program: any,
  logs: readonly string[]
): TokenWithdrawn[] {
  return program.parseEvents("TokenWithdrawn", logs) as TokenWithdrawn[];
}

/** Removes a previously registered event listener. */
export function removeListener(program: any, listenerId: number) {
  program.removeEventListener(listenerId);
}
