// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client/kit";
import rawIDL from "./idl/token_vault.json";
import type { TokenVault } from "./idl/token_vault";
const IDL = rawIDL as unknown as TokenVault;
import * as instructions from "./instructions";
import * as accounts from "./accounts/kit";
import * as types from "./types";

/**
 * High-level client for the `token_vault` program.
 * Uses the modern @solana/kit (v2) transaction model.
 */
export class TokenVaultClient {
  public readonly program: naclac.Program;
  public readonly instructions = instructions;
  public readonly accounts = accounts;
  public readonly types = types;
  public readonly errors = types.ProgramErrors;

  public get constants() {
    return this.program.constants;
  }

  /** The on-chain program address. */
  public get programId(): naclac.Address {
    return this.program.idl.address as naclac.Address;
  }

  constructor(
    providerOrCluster: naclac.NaclacProvider | "devnet" | "mainnet" | "localnet",
    payer?: naclac.KeyPairSigner
  ) {
    let provider: naclac.NaclacProvider;
    if (typeof providerOrCluster === "string") {
      if (!payer) throw new Error("[Naclac] A payer KeyPairSigner is required when specifying a cluster string.");
      provider = naclac.createProvider(providerOrCluster, payer);
    } else {
      provider = providerOrCluster;
    }
    this.program = new naclac.Program(IDL, provider);
  }

  /**
   * Builds the `initialize` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public initialize(args: instructions.InitializeArgs, accounts?: Partial<instructions.InitializeAccounts>) {
    return instructions.initialize(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `deposit` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public deposit(args: instructions.DepositArgs, accounts?: Partial<instructions.DepositAccounts>) {
    return instructions.deposit(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `withdraw` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public withdraw(args: instructions.WithdrawArgs, accounts?: Partial<instructions.WithdrawAccounts>) {
    return instructions.withdraw(this.program, args ?? {}, accounts);
  }

  /** Derives the PDA for a `userAccount` account. */
  public async getUserAccountPda(seeds: {
    vaultId: bigint | number;
    payer: naclac.Address | string;
    mint: naclac.Address | string;
  }): Promise<readonly [naclac.Address, number]> {
    return naclac.getProgramDerivedAddress({
      programAddress: this.programId,
      seeds: [
                new Uint8Array([117, 115, 101, 114]),
        new Uint8Array(naclac.getIdlCodec(JSON.parse('"u64"')).encode(seeds.vaultId)),
        new Uint8Array(naclac.getAddressEncoder().encode(typeof seeds.payer === 'string' ? naclac.address(seeds.payer) : seeds.payer)),
        new Uint8Array(naclac.getAddressEncoder().encode(typeof seeds.mint === 'string' ? naclac.address(seeds.mint) : seeds.mint))
      ]
    });
  }

  /** Derives the PDA for a `vaultAccount` account. */
  public async getVaultAccountPda(seeds: {
    vaultId: bigint | number;
  }): Promise<readonly [naclac.Address, number]> {
    return naclac.getProgramDerivedAddress({
      programAddress: this.programId,
      seeds: [
                new Uint8Array([118, 97, 117, 108, 116]),
        new Uint8Array(naclac.getIdlCodec(JSON.parse('"u64"')).encode(seeds.vaultId))
      ]
    });
  }

  /** Fetches a `UserAccount` account. Throws if it does not exist. */
  public fetchUserAccount(address: naclac.Address | string) {
    return accounts.fetchUserAccount(this.program.provider.rpc as any, naclac.address(address as string));
  }

  /** Fetches a `UserAccount` account. Returns a MaybeAccount (check `.exists`) if it does not exist. */
  public fetchMaybeUserAccount(address: naclac.Address | string) {
    return accounts.fetchMaybeUserAccount(this.program.provider.rpc as any, naclac.address(address as string));
  }

  /** Fetches multiple `UserAccount` accounts by address. Throws if any do not exist. */
  public fetchAllUserAccounts(addresses: Array<naclac.Address | string>) {
    return accounts.fetchAllUserAccount(this.program.provider.rpc as any, addresses.map((a) => naclac.address(a as string)));
  }

  /** Fetches multiple `UserAccount` accounts. Missing accounts have `.exists = false`. */
  public fetchAllMaybeUserAccounts(addresses: Array<naclac.Address | string>) {
    return accounts.fetchAllMaybeUserAccount(this.program.provider.rpc as any, addresses.map((a) => naclac.address(a as string)));
  }

  /** Fetches a `Vault` account. Throws if it does not exist. */
  public fetchVault(address: naclac.Address | string) {
    return accounts.fetchVault(this.program.provider.rpc as any, naclac.address(address as string));
  }

  /** Fetches a `Vault` account. Returns a MaybeAccount (check `.exists`) if it does not exist. */
  public fetchMaybeVault(address: naclac.Address | string) {
    return accounts.fetchMaybeVault(this.program.provider.rpc as any, naclac.address(address as string));
  }

  /** Fetches multiple `Vault` accounts by address. Throws if any do not exist. */
  public fetchAllVaults(addresses: Array<naclac.Address | string>) {
    return accounts.fetchAllVault(this.program.provider.rpc as any, addresses.map((a) => naclac.address(a as string)));
  }

  /** Fetches multiple `Vault` accounts. Missing accounts have `.exists = false`. */
  public fetchAllMaybeVaults(addresses: Array<naclac.Address | string>) {
    return accounts.fetchAllMaybeVault(this.program.provider.rpc as any, addresses.map((a) => naclac.address(a as string)));
  }

  /** Subscribes to `TokenDeposited` events. Returns a listener ID. */
  public onTokenDeposited(callback: (event: types.TokenDeposited, slot: number, signature: string) => void) {
    return types.addTokenDepositedListener(this.program, callback);
  }

  /** Awaits the next `TokenDeposited` event. Resolves `null` on timeout. */
  public waitForTokenDeposited(options?: { timeoutMs?: number }) {
    return this.program.waitForEvent<types.TokenDeposited>("TokenDeposited", options);
  }

  /** Decodes every `TokenDeposited` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForTokenDeposited` — prefer this when you already know which transaction you're checking. */
  public parseTokenDepositedEvents(logs: readonly string[]) {
    return this.program.parseEvents<types.TokenDeposited>("TokenDeposited", logs);
  }

  /** Subscribes to `TokenWithdrawn` events. Returns a listener ID. */
  public onTokenWithdrawn(callback: (event: types.TokenWithdrawn, slot: number, signature: string) => void) {
    return types.addTokenWithdrawnListener(this.program, callback);
  }

  /** Awaits the next `TokenWithdrawn` event. Resolves `null` on timeout. */
  public waitForTokenWithdrawn(options?: { timeoutMs?: number }) {
    return this.program.waitForEvent<types.TokenWithdrawn>("TokenWithdrawn", options);
  }

  /** Decodes every `TokenWithdrawn` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForTokenWithdrawn` — prefer this when you already know which transaction you're checking. */
  public parseTokenWithdrawnEvents(logs: readonly string[]) {
    return this.program.parseEvents<types.TokenWithdrawn>("TokenWithdrawn", logs);
  }

  /** Removes a registered event listener by its ID. */
  public removeEventListener(listenerId: number) {
    return types.removeListener(this.program, listenerId);
  }
  /** Fetches ALL `UserAccount` accounts owned by this program. */
  public fetchAllUserAccountByOwner(options?: { commitment?: any; filters?: unknown[] }) {
    return accounts.fetchAllUserAccountByOwner(this.program.provider.rpc as any, this.programId, options);
  }

  /** Fetches ALL `Vault` accounts owned by this program. */
  public fetchAllVaultByOwner(options?: { commitment?: any; filters?: unknown[] }) {
    return accounts.fetchAllVaultByOwner(this.program.provider.rpc as any, this.programId, options);
  }

}
