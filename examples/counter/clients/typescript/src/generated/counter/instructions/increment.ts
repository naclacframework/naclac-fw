// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `increment` instruction. */
export const INCREMENT_DISCRIMINATOR = new Uint8Array([11, 18, 104, 9, 104, 174, 59, 33]);

/** Accounts for the `increment` instruction. */
export interface IncrementAccounts {
  authority: naclac.Address | string;
  counter_account?: naclac.Address | string;
}

/**
 * Builds the `increment` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function increment(
  program: any,
  args?: Record<string, never>,
  accounts?: Partial<IncrementAccounts>
) {
  const builder = program.methods.increment(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
