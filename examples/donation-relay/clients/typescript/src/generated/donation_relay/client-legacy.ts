// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client/legacy";
import rawIDL from "./idl/donation_relay.json";
import type { DonationRelay } from "./idl/donation_relay";
const IDL = rawIDL as unknown as DonationRelay;
import * as instructions from "./instructions";
import * as accounts from "./accounts/legacy";
import * as types from "./types";

/**
 * High-level client for the `donation_relay` program.
 * Uses the classic @solana/web3.js (v1) transaction model.
 */
export class DonationRelayClient {
  public readonly program: naclac.LegacyProgram;
  public readonly instructions = instructions;
  public readonly accounts = accounts;
  public readonly types = types;
  public readonly errors = types.ProgramErrors;

  public get constants() {
    return this.program.constants;
  }

  /** The on-chain program address as a `PublicKey`. */
  public get programId(): naclac.PublicKey {
    return this.program.programId;
  }

  constructor(
    providerOrCluster: naclac.LegacyProvider | "devnet" | "mainnet" | "localnet" | string,
    payer?: naclac.Keypair
  ) {
    let provider: naclac.LegacyProvider;
    if (typeof providerOrCluster === "string") {
      let url = providerOrCluster;
      if (providerOrCluster === "devnet") url = "https://api.devnet.solana.com";
      else if (providerOrCluster === "mainnet") url = "https://api.mainnet-beta.solana.com";
      else if (providerOrCluster === "localnet") url = "http://127.0.0.1:8899";
      const connection = new naclac.Connection(url, "confirmed");
      provider = { connection, payer, publicKey: payer ? payer.publicKey : undefined };
    } else {
      provider = providerOrCluster;
    }
    this.program = new naclac.LegacyProgram(IDL, provider);
  }

  /**
   * Builds the `donatePubkeyConfigIdWithPayerV1` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public donatePubkeyConfigIdWithPayerV1(args: instructions.DonatePubkeyConfigIdWithPayerV1Args, accounts?: Partial<instructions.DonatePubkeyConfigIdWithPayerV1Accounts>) {
    return instructions.donatePubkeyConfigIdWithPayerV1(this.program, args ?? {}, accounts);
  }

  /** Derives the PDA for a `debouncer` account. Returns `[PublicKey, bumpSeed]`. */
  public getDebouncerPda(seeds: {
    args: types.DonatePubkeyConfigIdWithPayerV1Args;
    mint: naclac.PublicKey | string;
  }): [naclac.PublicKey, number] {
    const [pda, bump] = naclac.PublicKey.findProgramAddressSync(
      [
                new Uint8Array([100, 101, 98, 111, 117, 110, 99, 101, 114, 95, 118, 49]),
        new Uint8Array(naclac.getIdlCodec(JSON.parse('"publicKey"')).encode(seeds.args.config_id)),
        new naclac.PublicKey(seeds.mint).toBuffer()
      ],
      this.programId
    );
    return [pda, bump];
  }

  /** Derives the PDA for a `epoch_tracker` account. Returns `[PublicKey, bumpSeed]`. */
  public getEpochTrackerPda(seeds: {
    args: types.DonatePubkeyConfigIdWithPayerV1Args;
    mint: naclac.PublicKey | string;
  }): [naclac.PublicKey, number] {
    const [pda, bump] = naclac.PublicKey.findProgramAddressSync(
      [
                new Uint8Array([101, 112, 111, 99, 104, 95, 116, 114, 97, 99, 107, 101, 114, 95, 118, 49]),
        new Uint8Array(naclac.getIdlCodec(JSON.parse('"publicKey"')).encode(seeds.args.config_id)),
        new naclac.PublicKey(seeds.mint).toBuffer()
      ],
      this.programId
    );
    return [pda, bump];
  }

  /** Fetches a `Debouncer` account. Throws if it does not exist. */
  public fetchDebouncer(address: naclac.PublicKey | string) {
    return this.program.account['Debouncer'].fetch(address);
  }

  /** Fetches a `Debouncer` account. Returns `null` if it does not exist. */
  public async fetchMaybeDebouncer(address: naclac.PublicKey | string) {
    try { return await this.program.account['Debouncer'].fetch(address); }
    catch { return null; }
  }

  /** Fetches multiple `Debouncer` accounts in a single RPC call. */
  public fetchAllDebouncers(addresses: Array<naclac.PublicKey | string>, opts?: { dropCorrupted?: boolean }) {
    return this.program.account['Debouncer'].fetchMultiple(addresses, opts);
  }

  /** Fetches all on-chain `Debouncer` accounts owned by this program. */
  public fetchAllDebouncerByOwner(options?: { dropCorrupted?: boolean; filters?: any[] }) {
    return this.program.account['Debouncer'].all(options);
  }

  /** Fetches a `EpochTracker` account. Throws if it does not exist. */
  public fetchEpochTracker(address: naclac.PublicKey | string) {
    return this.program.account['EpochTracker'].fetch(address);
  }

  /** Fetches a `EpochTracker` account. Returns `null` if it does not exist. */
  public async fetchMaybeEpochTracker(address: naclac.PublicKey | string) {
    try { return await this.program.account['EpochTracker'].fetch(address); }
    catch { return null; }
  }

  /** Fetches multiple `EpochTracker` accounts in a single RPC call. */
  public fetchAllEpochTrackers(addresses: Array<naclac.PublicKey | string>, opts?: { dropCorrupted?: boolean }) {
    return this.program.account['EpochTracker'].fetchMultiple(addresses, opts);
  }

  /** Fetches all on-chain `EpochTracker` accounts owned by this program. */
  public fetchAllEpochTrackerByOwner(options?: { dropCorrupted?: boolean; filters?: any[] }) {
    return this.program.account['EpochTracker'].all(options);
  }

  /** Subscribes to `DonationMadeV1Event` events. Returns a listener ID. */
  public onDonationMadeV1Event(callback: (event: types.DonationMadeV1Event, slot: number, signature: string) => void) {
    return types.addDonationMadeV1EventListener(this.program, callback);
  }

  /** Awaits the next `DonationMadeV1Event` event. Resolves `null` on timeout. */
  public waitForDonationMadeV1Event(options?: { timeoutMs?: number }) {
    return this.program.waitForEvent<types.DonationMadeV1Event>("DonationMadeV1Event", options);
  }

  /** Decodes every `DonationMadeV1Event` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForDonationMadeV1Event` — prefer this when you already know which transaction you're checking. */
  public parseDonationMadeV1EventEvents(logs: readonly string[]) {
    return this.program.parseEvents<types.DonationMadeV1Event>("DonationMadeV1Event", logs);
  }

  /** Removes a registered event listener by its ID. */
  public removeEventListener(listenerId: number) {
    return types.removeListener(this.program, listenerId);
  }
}
