// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client/kit";
import rawIDL from "./idl/donation_relay.json";
import type { DonationRelay } from "./idl/donation_relay";
const IDL = rawIDL as unknown as DonationRelay;
import * as instructions from "./instructions";
import * as accounts from "./accounts/kit";
import * as types from "./types";

/**
 * High-level client for the `donation_relay` program.
 * Uses the modern @solana/kit (v2) transaction model.
 */
export class DonationRelayClient {
  public readonly program: naclac.Program;
  public readonly instructions = instructions;
  public readonly accounts = accounts;
  public readonly types = types;
  public readonly errors = types.ProgramErrors;

  public get constants() {
    return this.program.constants;
  }

  /** The on-chain program address. */
  public get programId(): naclac.Address {
    return this.program.idl.address as naclac.Address;
  }

  constructor(
    providerOrCluster: naclac.NaclacProvider | "devnet" | "mainnet" | "localnet",
    payer?: naclac.KeyPairSigner
  ) {
    let provider: naclac.NaclacProvider;
    if (typeof providerOrCluster === "string") {
      if (!payer) throw new Error("[Naclac] A payer KeyPairSigner is required when specifying a cluster string.");
      provider = naclac.createProvider(providerOrCluster, payer);
    } else {
      provider = providerOrCluster;
    }
    this.program = new naclac.Program(IDL, provider);
  }

  /**
   * Builds the `donatePubkeyConfigIdWithPayerV1` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public donatePubkeyConfigIdWithPayerV1(args: instructions.DonatePubkeyConfigIdWithPayerV1Args, accounts?: Partial<instructions.DonatePubkeyConfigIdWithPayerV1Accounts>) {
    return instructions.donatePubkeyConfigIdWithPayerV1(this.program, args ?? {}, accounts);
  }

  /** Derives the PDA for a `debouncer` account. */
  public async getDebouncerPda(seeds: {
    args: types.DonatePubkeyConfigIdWithPayerV1Args;
    mint: naclac.Address | string;
  }): Promise<readonly [naclac.Address, number]> {
    return naclac.getProgramDerivedAddress({
      programAddress: this.programId,
      seeds: [
                new Uint8Array([100, 101, 98, 111, 117, 110, 99, 101, 114, 95, 118, 49]),
        new Uint8Array(naclac.getIdlCodec(JSON.parse('"publicKey"')).encode(seeds.args.config_id)),
        new Uint8Array(naclac.getAddressEncoder().encode(typeof seeds.mint === 'string' ? naclac.address(seeds.mint) : seeds.mint))
      ]
    });
  }

  /** Derives the PDA for a `epoch_tracker` account. */
  public async getEpochTrackerPda(seeds: {
    args: types.DonatePubkeyConfigIdWithPayerV1Args;
    mint: naclac.Address | string;
  }): Promise<readonly [naclac.Address, number]> {
    return naclac.getProgramDerivedAddress({
      programAddress: this.programId,
      seeds: [
                new Uint8Array([101, 112, 111, 99, 104, 95, 116, 114, 97, 99, 107, 101, 114, 95, 118, 49]),
        new Uint8Array(naclac.getIdlCodec(JSON.parse('"publicKey"')).encode(seeds.args.config_id)),
        new Uint8Array(naclac.getAddressEncoder().encode(typeof seeds.mint === 'string' ? naclac.address(seeds.mint) : seeds.mint))
      ]
    });
  }

  /** Fetches a `Debouncer` account. Throws if it does not exist. */
  public fetchDebouncer(address: naclac.Address | string) {
    return accounts.fetchDebouncer(this.program.provider.rpc as any, naclac.address(address as string));
  }

  /** Fetches a `Debouncer` account. Returns a MaybeAccount (check `.exists`) if it does not exist. */
  public fetchMaybeDebouncer(address: naclac.Address | string) {
    return accounts.fetchMaybeDebouncer(this.program.provider.rpc as any, naclac.address(address as string));
  }

  /** Fetches multiple `Debouncer` accounts by address. Throws if any do not exist. */
  public fetchAllDebouncers(addresses: Array<naclac.Address | string>) {
    return accounts.fetchAllDebouncer(this.program.provider.rpc as any, addresses.map((a) => naclac.address(a as string)));
  }

  /** Fetches multiple `Debouncer` accounts. Missing accounts have `.exists = false`. */
  public fetchAllMaybeDebouncers(addresses: Array<naclac.Address | string>) {
    return accounts.fetchAllMaybeDebouncer(this.program.provider.rpc as any, addresses.map((a) => naclac.address(a as string)));
  }

  /** Fetches a `EpochTracker` account. Throws if it does not exist. */
  public fetchEpochTracker(address: naclac.Address | string) {
    return accounts.fetchEpochTracker(this.program.provider.rpc as any, naclac.address(address as string));
  }

  /** Fetches a `EpochTracker` account. Returns a MaybeAccount (check `.exists`) if it does not exist. */
  public fetchMaybeEpochTracker(address: naclac.Address | string) {
    return accounts.fetchMaybeEpochTracker(this.program.provider.rpc as any, naclac.address(address as string));
  }

  /** Fetches multiple `EpochTracker` accounts by address. Throws if any do not exist. */
  public fetchAllEpochTrackers(addresses: Array<naclac.Address | string>) {
    return accounts.fetchAllEpochTracker(this.program.provider.rpc as any, addresses.map((a) => naclac.address(a as string)));
  }

  /** Fetches multiple `EpochTracker` accounts. Missing accounts have `.exists = false`. */
  public fetchAllMaybeEpochTrackers(addresses: Array<naclac.Address | string>) {
    return accounts.fetchAllMaybeEpochTracker(this.program.provider.rpc as any, addresses.map((a) => naclac.address(a as string)));
  }

  /** Subscribes to `DonationMadeV1Event` events. Returns a listener ID. */
  public onDonationMadeV1Event(callback: (event: types.DonationMadeV1Event, slot: number, signature: string) => void) {
    return types.addDonationMadeV1EventListener(this.program, callback);
  }

  /** Awaits the next `DonationMadeV1Event` event. Resolves `null` on timeout. */
  public waitForDonationMadeV1Event(options?: { timeoutMs?: number }) {
    return this.program.waitForEvent<types.DonationMadeV1Event>("DonationMadeV1Event", options);
  }

  /** Decodes every `DonationMadeV1Event` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForDonationMadeV1Event` — prefer this when you already know which transaction you're checking. */
  public parseDonationMadeV1EventEvents(logs: readonly string[]) {
    return this.program.parseEvents<types.DonationMadeV1Event>("DonationMadeV1Event", logs);
  }

  /** Removes a registered event listener by its ID. */
  public removeEventListener(listenerId: number) {
    return types.removeListener(this.program, listenerId);
  }
  /** Fetches ALL `Debouncer` accounts owned by this program. */
  public fetchAllDebouncerByOwner(options?: { commitment?: any; filters?: unknown[] }) {
    return accounts.fetchAllDebouncerByOwner(this.program.provider.rpc as any, this.programId, options);
  }

  /** Fetches ALL `EpochTracker` accounts owned by this program. */
  public fetchAllEpochTrackerByOwner(options?: { commitment?: any; filters?: unknown[] }) {
    return accounts.fetchAllEpochTrackerByOwner(this.program.provider.rpc as any, this.programId, options);
  }

}
