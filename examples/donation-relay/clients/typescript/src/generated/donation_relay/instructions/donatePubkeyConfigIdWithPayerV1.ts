// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";
import * as types from "../types/typedefs";

/** 8-byte discriminator for the `donate_pubkey_config_id_with_payer_v1` instruction. */
export const DONATE_PUBKEY_CONFIG_ID_WITH_PAYER_V1_DISCRIMINATOR = new Uint8Array([120, 217, 57, 241, 135, 104, 139, 184]);

/** Instruction arguments for `donatePubkeyConfigIdWithPayerV1`. */
export interface DonatePubkeyConfigIdWithPayerV1Args {
  args: types.DonatePubkeyConfigIdWithPayerV1Args;
}

/** Accounts for the `donatePubkeyConfigIdWithPayerV1` instruction. */
export interface DonatePubkeyConfigIdWithPayerV1Accounts {
  payer: naclac.Address | string;
  from: naclac.Address | string;
  mint?: naclac.Address | string;
  epoch_tracker?: naclac.Address | string;
  debouncer?: naclac.Address | string;
  /**
   * SAFETY: may not exist yet — created idempotently by the
   * `init_if_needed`/`associated_token::mint`/`::authority` constraint
   * below (a no-op if it already exists), then reinterpreted as a
   * `TokenAccount` in the instruction body.
   */
  debouncer_token_account: naclac.Address | string;
  from_token_account: naclac.Address | string;
  /**
   * SAFETY: dead in this scoped WSOL-only pass — the real program reads
   * this to bypass its own mint-safety check for non-whitelisted mints,
   * but WSOL already passes that check unconditionally (confirmed via
   * `reference/donation-relay-probe/src/bin/probe2.rs`), so this
   * reimplementation never needs to read it.
   */
  mint_whitelist: naclac.Address | string;
  associated_token_program?: naclac.Address | string;
  token_program?: naclac.Address | string;
  system_program?: naclac.Address | string;
}

/**
 * Builds the `donatePubkeyConfigIdWithPayerV1` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function donatePubkeyConfigIdWithPayerV1(
  program: any,
  args: DonatePubkeyConfigIdWithPayerV1Args,
  accounts?: Partial<DonatePubkeyConfigIdWithPayerV1Accounts>
) {
  const builder = program.methods.donate_pubkey_config_id_with_payer_v1(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
