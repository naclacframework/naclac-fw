// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client/legacy";
import rawIDL from "./idl/vault.json";
import type { Vault } from "./idl/vault";
const IDL = rawIDL as unknown as Vault;
import * as instructions from "./instructions";
import * as accounts from "./accounts/legacy";
import * as types from "./types";

/**
 * High-level client for the `vault` program.
 * Uses the classic @solana/web3.js (v1) transaction model.
 */
export class VaultClient {
  public readonly program: naclac.LegacyProgram;
  public readonly instructions = instructions;
  public readonly accounts = accounts;
  public readonly types = types;
  public readonly errors = types.ProgramErrors;

  public get constants() {
    return this.program.constants;
  }

  /** The on-chain program address as a `PublicKey`. */
  public get programId(): naclac.PublicKey {
    return this.program.programId;
  }

  constructor(
    providerOrCluster: naclac.LegacyProvider | "devnet" | "mainnet" | "localnet" | string,
    payer?: naclac.Keypair
  ) {
    let provider: naclac.LegacyProvider;
    if (typeof providerOrCluster === "string") {
      let url = providerOrCluster;
      if (providerOrCluster === "devnet") url = "https://api.devnet.solana.com";
      else if (providerOrCluster === "mainnet") url = "https://api.mainnet-beta.solana.com";
      else if (providerOrCluster === "localnet") url = "http://127.0.0.1:8899";
      const connection = new naclac.Connection(url, "confirmed");
      provider = { connection, payer, publicKey: payer ? payer.publicKey : undefined };
    } else {
      provider = providerOrCluster;
    }
    this.program = new naclac.LegacyProgram(IDL, provider);
  }

  /**
   * Builds the `initialize` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public initialize(args?: Record<string, never>, accounts?: Partial<instructions.InitializeAccounts>) {
    return instructions.initialize(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `deposit` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public deposit(args: instructions.DepositArgs, accounts?: Partial<instructions.DepositAccounts>) {
    return instructions.deposit(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `withdraw` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public withdraw(args: instructions.WithdrawArgs, accounts?: Partial<instructions.WithdrawAccounts>) {
    return instructions.withdraw(this.program, args ?? {}, accounts);
  }

  /** Derives the PDA for a `userAccount` account. Returns `[PublicKey, bumpSeed]`. */
  public getUserAccountPda(seeds: {
    user: naclac.PublicKey | string;
  }): [naclac.PublicKey, number] {
    const [pda, bump] = naclac.PublicKey.findProgramAddressSync(
      [
                new Uint8Array([117, 115, 101, 114, 95, 118, 97, 117, 108, 116]),
        new naclac.PublicKey(seeds.user).toBuffer()
      ],
      this.programId
    );
    return [pda, bump];
  }

  /** Derives the PDA for a `vaultAccount` account. Returns `[PublicKey, bumpSeed]`. */
  public getVaultAccountPda(seeds: {
  }): [naclac.PublicKey, number] {
    const [pda, bump] = naclac.PublicKey.findProgramAddressSync(
      [
                new Uint8Array([118, 97, 117, 108, 116])
      ],
      this.programId
    );
    return [pda, bump];
  }

  /** Fetches a `UserAccount` account. Throws if it does not exist. */
  public fetchUserAccount(address: naclac.PublicKey | string) {
    return this.program.account['UserAccount'].fetch(address);
  }

  /** Fetches a `UserAccount` account. Returns `null` if it does not exist. */
  public async fetchMaybeUserAccount(address: naclac.PublicKey | string) {
    try { return await this.program.account['UserAccount'].fetch(address); }
    catch { return null; }
  }

  /** Fetches multiple `UserAccount` accounts in a single RPC call. */
  public fetchAllUserAccounts(addresses: Array<naclac.PublicKey | string>, opts?: { dropCorrupted?: boolean }) {
    return this.program.account['UserAccount'].fetchMultiple(addresses, opts);
  }

  /** Fetches all on-chain `UserAccount` accounts owned by this program. */
  public fetchAllUserAccountByOwner(options?: { dropCorrupted?: boolean; filters?: any[] }) {
    return this.program.account['UserAccount'].all(options);
  }

  /** Fetches a `Vault` account. Throws if it does not exist. */
  public fetchVault(address: naclac.PublicKey | string) {
    return this.program.account['Vault'].fetch(address);
  }

  /** Fetches a `Vault` account. Returns `null` if it does not exist. */
  public async fetchMaybeVault(address: naclac.PublicKey | string) {
    try { return await this.program.account['Vault'].fetch(address); }
    catch { return null; }
  }

  /** Fetches multiple `Vault` accounts in a single RPC call. */
  public fetchAllVaults(addresses: Array<naclac.PublicKey | string>, opts?: { dropCorrupted?: boolean }) {
    return this.program.account['Vault'].fetchMultiple(addresses, opts);
  }

  /** Fetches all on-chain `Vault` accounts owned by this program. */
  public fetchAllVaultByOwner(options?: { dropCorrupted?: boolean; filters?: any[] }) {
    return this.program.account['Vault'].all(options);
  }

  /** Subscribes to `FundsDeposited` events. Returns a listener ID. */
  public onFundsDeposited(callback: (event: types.FundsDeposited, slot: number, signature: string) => void) {
    return types.addFundsDepositedListener(this.program, callback);
  }

  /** Awaits the next `FundsDeposited` event. Resolves `null` on timeout. */
  public waitForFundsDeposited(options?: { timeoutMs?: number }) {
    return this.program.waitForEvent<types.FundsDeposited>("FundsDeposited", options);
  }

  /** Decodes every `FundsDeposited` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForFundsDeposited` — prefer this when you already know which transaction you're checking. */
  public parseFundsDepositedEvents(logs: readonly string[]) {
    return this.program.parseEvents<types.FundsDeposited>("FundsDeposited", logs);
  }

  /** Subscribes to `FundsWithdrawn` events. Returns a listener ID. */
  public onFundsWithdrawn(callback: (event: types.FundsWithdrawn, slot: number, signature: string) => void) {
    return types.addFundsWithdrawnListener(this.program, callback);
  }

  /** Awaits the next `FundsWithdrawn` event. Resolves `null` on timeout. */
  public waitForFundsWithdrawn(options?: { timeoutMs?: number }) {
    return this.program.waitForEvent<types.FundsWithdrawn>("FundsWithdrawn", options);
  }

  /** Decodes every `FundsWithdrawn` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForFundsWithdrawn` — prefer this when you already know which transaction you're checking. */
  public parseFundsWithdrawnEvents(logs: readonly string[]) {
    return this.program.parseEvents<types.FundsWithdrawn>("FundsWithdrawn", logs);
  }

  /** Removes a registered event listener by its ID. */
  public removeEventListener(listenerId: number) {
    return types.removeListener(this.program, listenerId);
  }
}
