// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client/legacy";
import type { UserAccount } from "../../types";

/** 8-byte on-chain discriminator for `UserAccount` accounts. */
export const USERACCOUNT_DISCRIMINATOR = new Uint8Array([211, 33, 136, 16, 186, 110, 242, 127]);

/** Fetches a `UserAccount` account. Throws if it does not exist. */
export async function fetchUserAccount(
  program: naclac.LegacyProgram,
  address: naclac.PublicKey | string,
): Promise<UserAccount> {
  const result = await program.account['UserAccount'].fetch(address);
  return result as unknown as UserAccount;
}

/** Fetches a `UserAccount` account. Returns `null` if it does not exist. */
export async function fetchMaybeUserAccount(
  program: naclac.LegacyProgram,
  address: naclac.PublicKey | string,
): Promise<UserAccount | null> {
  try {
    const result = await program.account['UserAccount'].fetch(address);
    return result as unknown as UserAccount;
  } catch {
    return null;
  }
}

/** Fetches multiple `UserAccount` accounts. Missing accounts are `null`. */
export async function fetchAllUserAccount(
  program: naclac.LegacyProgram,
  addresses: Array<naclac.PublicKey | string>,
  opts?: { dropCorrupted?: boolean },
): Promise<Array<UserAccount | null>> {
  const results = await program.account['UserAccount'].fetchMultiple(addresses, opts);
  return results as unknown as Array<UserAccount | null>;
}

/** Fetches all on-chain `UserAccount` accounts owned by this program. */
export async function fetchAllUserAccountByOwner(
  program: naclac.LegacyProgram,
  options?: { dropCorrupted?: boolean; filters?: any[] },
): Promise<Array<{ publicKey: naclac.PublicKey; account: UserAccount }>> {
  const results = await program.account['UserAccount'].all(options);
  return results as unknown as Array<{ publicKey: naclac.PublicKey; account: UserAccount }>;
}
