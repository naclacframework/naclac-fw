// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for `FundsDeposited` events in transaction logs. */
export const FUNDSDEPOSITED_EVENT_DISCRIMINATOR = new Uint8Array([157, 209, 100, 95, 59, 100, 3, 68]);

/** Auto-generated event interface from the program IDL. */
export interface FundsDeposited {
  user: naclac.Address | string;
  amount: bigint | number;
  totalVaultBalance: bigint | number;
}

/** Subscribes to `FundsDeposited` events. Returns a listener ID for cleanup. */
export function addFundsDepositedListener(
  program: any,
  callback: (event: FundsDeposited, slot: number, signature: string) => void
): number {
  return program.addEventListener("FundsDeposited", callback);
}

/** Waits for the next `FundsDeposited` event. Resolves `null` if the timeout expires. */
export async function waitForFundsDeposited(
  program: any,
  options?: { timeoutMs?: number }
): Promise<FundsDeposited | null> {
  return program.waitForEvent("FundsDeposited", options) as Promise<FundsDeposited | null>;
}

/** Decodes every `FundsDeposited` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForFundsDeposited`/`addFundsDepositedListener` — prefer this when you already know which transaction you're checking. */
export function parseFundsDepositedEvents(
  program: any,
  logs: readonly string[]
): FundsDeposited[] {
  return program.parseEvents("FundsDeposited", logs) as FundsDeposited[];
}

/** 8-byte discriminator for `FundsWithdrawn` events in transaction logs. */
export const FUNDSWITHDRAWN_EVENT_DISCRIMINATOR = new Uint8Array([56, 130, 230, 154, 35, 92, 11, 118]);

/** Auto-generated event interface from the program IDL. */
export interface FundsWithdrawn {
  user: naclac.Address | string;
  amount: bigint | number;
  totalVaultBalance: bigint | number;
}

/** Subscribes to `FundsWithdrawn` events. Returns a listener ID for cleanup. */
export function addFundsWithdrawnListener(
  program: any,
  callback: (event: FundsWithdrawn, slot: number, signature: string) => void
): number {
  return program.addEventListener("FundsWithdrawn", callback);
}

/** Waits for the next `FundsWithdrawn` event. Resolves `null` if the timeout expires. */
export async function waitForFundsWithdrawn(
  program: any,
  options?: { timeoutMs?: number }
): Promise<FundsWithdrawn | null> {
  return program.waitForEvent("FundsWithdrawn", options) as Promise<FundsWithdrawn | null>;
}

/** Decodes every `FundsWithdrawn` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForFundsWithdrawn`/`addFundsWithdrawnListener` — prefer this when you already know which transaction you're checking. */
export function parseFundsWithdrawnEvents(
  program: any,
  logs: readonly string[]
): FundsWithdrawn[] {
  return program.parseEvents("FundsWithdrawn", logs) as FundsWithdrawn[];
}

/** Removes a previously registered event listener. */
export function removeListener(program: any, listenerId: number) {
  program.removeEventListener(listenerId);
}
