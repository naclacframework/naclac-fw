// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `set_metaplex_creator` instruction. */
export const SET_METAPLEX_CREATOR_DISCRIMINATOR = new Uint8Array([138, 96, 174, 217, 48, 85, 197, 246]);

/** Instruction arguments for `setMetaplexCreator`. */
export interface SetMetaplexCreatorArgs {
  metadataBump: number;
  bondingCurveBump: number;
}

/** Accounts for the `setMetaplexCreator` instruction. */
export interface SetMetaplexCreatorAccounts {
  /**
   * SAFETY: only used as a seed input for `metadata`/`bonding_curve`
   * below, never read or written.
   */
  mint: naclac.Address | string;
  /**
   * SAFETY: the `owner`/`seeds`/`bump` constraints below already prove
   * this is the real, canonical Metaplex `Metadata` PDA for `mint` —
   * only its foreign, non-naclac Borsh-encoded data content (not a
   * naclac component, so not eligible for `Account<T>`'s own typed
   * validation) is read manually via `read_first_metaplex_creator`.
   */
  metadata?: naclac.Address | string;
  bondingCurve?: naclac.Address | string;
}

/**
 * Builds the `setMetaplexCreator` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function setMetaplexCreator(
  program: any,
  args: SetMetaplexCreatorArgs,
  accounts?: Partial<SetMetaplexCreatorAccounts>
) {
  const builder = program.methods.set_metaplex_creator(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
