// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `toggle_cashback_enabled` instruction. */
export const TOGGLE_CASHBACK_ENABLED_DISCRIMINATOR = new Uint8Array([115, 103, 224, 255, 189, 89, 86, 195]);

/** Instruction arguments for `toggleCashbackEnabled`. */
export interface ToggleCashbackEnabledArgs {
  enabled: naclac.Bool;
}

/** Accounts for the `toggleCashbackEnabled` instruction. */
export interface ToggleCashbackEnabledAccounts {
  global?: naclac.Address | string;
  authority: naclac.Address | string;
}

/**
 * Builds the `toggleCashbackEnabled` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function toggleCashbackEnabled(
  program: any,
  args: ToggleCashbackEnabledArgs,
  accounts?: Partial<ToggleCashbackEnabledAccounts>
) {
  const builder = program.methods.toggle_cashback_enabled(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
