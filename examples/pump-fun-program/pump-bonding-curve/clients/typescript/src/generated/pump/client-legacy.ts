// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client/legacy";
import rawIDL from "./idl/pump.json";
import type { Pump } from "./idl/pump";
const IDL = rawIDL as unknown as Pump;
import * as instructions from "./instructions";
import * as accounts from "./accounts/legacy";
import * as types from "./types";

/**
 * High-level client for the `pump` program.
 * Uses the classic @solana/web3.js (v1) transaction model.
 */
export class PumpClient {
  public readonly program: naclac.LegacyProgram;
  public readonly instructions = instructions;
  public readonly accounts = accounts;
  public readonly types = types;
  public readonly errors = types.ProgramErrors;

  public get constants() {
    return this.program.constants;
  }

  /** The on-chain program address as a `PublicKey`. */
  public get programId(): naclac.PublicKey {
    return this.program.programId;
  }

  constructor(
    providerOrCluster: naclac.LegacyProvider | "devnet" | "mainnet" | "localnet" | string,
    payer?: naclac.Keypair
  ) {
    let provider: naclac.LegacyProvider;
    if (typeof providerOrCluster === "string") {
      let url = providerOrCluster;
      if (providerOrCluster === "devnet") url = "https://api.devnet.solana.com";
      else if (providerOrCluster === "mainnet") url = "https://api.mainnet-beta.solana.com";
      else if (providerOrCluster === "localnet") url = "http://127.0.0.1:8899";
      const connection = new naclac.Connection(url, "confirmed");
      provider = { connection, payer, publicKey: payer ? payer.publicKey : undefined };
    } else {
      provider = providerOrCluster;
    }
    this.program = new naclac.LegacyProgram(IDL, provider);
  }

  /**
   * Builds the `initialize` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public initialize(args?: Record<string, never>, accounts?: Partial<instructions.InitializeAccounts>) {
    return instructions.initialize(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `create` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public create(args: instructions.CreateArgs, accounts?: Partial<instructions.CreateAccounts>) {
    return instructions.create(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `createV2` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public createV2(args: instructions.CreateV2Args, accounts?: Partial<instructions.CreateV2Accounts>) {
    return instructions.createV2(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `migrateBondingCurveCreator` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public migrateBondingCurveCreator(args: instructions.MigrateBondingCurveCreatorArgs, accounts?: Partial<instructions.MigrateBondingCurveCreatorAccounts>) {
    return instructions.migrateBondingCurveCreator(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `setParams` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public setParams(args: instructions.SetParamsArgs, accounts?: Partial<instructions.SetParamsAccounts>) {
    return instructions.setParams(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `updateBuybackConfig` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public updateBuybackConfig(args: instructions.UpdateBuybackConfigArgs, accounts?: Partial<instructions.UpdateBuybackConfigAccounts>) {
    return instructions.updateBuybackConfig(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `updateGlobalAuthority` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public updateGlobalAuthority(args?: Record<string, never>, accounts?: Partial<instructions.UpdateGlobalAuthorityAccounts>) {
    return instructions.updateGlobalAuthority(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `adminSetCreator` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public adminSetCreator(args: instructions.AdminSetCreatorArgs, accounts?: Partial<instructions.AdminSetCreatorAccounts>) {
    return instructions.adminSetCreator(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `toggleCreateV2` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public toggleCreateV2(args: instructions.ToggleCreateV2Args, accounts?: Partial<instructions.ToggleCreateV2Accounts>) {
    return instructions.toggleCreateV2(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `toggleMayhemMode` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public toggleMayhemMode(args: instructions.ToggleMayhemModeArgs, accounts?: Partial<instructions.ToggleMayhemModeAccounts>) {
    return instructions.toggleMayhemMode(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `toggleCashbackEnabled` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public toggleCashbackEnabled(args: instructions.ToggleCashbackEnabledArgs, accounts?: Partial<instructions.ToggleCashbackEnabledAccounts>) {
    return instructions.toggleCashbackEnabled(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `setReservedFeeRecipients` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public setReservedFeeRecipients(args: instructions.SetReservedFeeRecipientsArgs, accounts?: Partial<instructions.SetReservedFeeRecipientsAccounts>) {
    return instructions.setReservedFeeRecipients(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `setCreator` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public setCreator(args: instructions.SetCreatorArgs, accounts?: Partial<instructions.SetCreatorAccounts>) {
    return instructions.setCreator(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `setMetaplexCreator` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public setMetaplexCreator(args: instructions.SetMetaplexCreatorArgs, accounts?: Partial<instructions.SetMetaplexCreatorAccounts>) {
    return instructions.setMetaplexCreator(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `distributeCreatorFees` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public distributeCreatorFees(args: instructions.DistributeCreatorFeesArgs, accounts?: Partial<instructions.DistributeCreatorFeesAccounts>) {
    return instructions.distributeCreatorFees(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `distributeCreatorFeesV2` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public distributeCreatorFeesV2(args: instructions.DistributeCreatorFeesV2Args, accounts?: Partial<instructions.DistributeCreatorFeesV2Accounts>) {
    return instructions.distributeCreatorFeesV2(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `buy` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public buy(args: instructions.BuyArgs, accounts?: Partial<instructions.BuyAccounts>) {
    return instructions.buy(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `buyExactSolIn` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public buyExactSolIn(args: instructions.BuyExactSolInArgs, accounts?: Partial<instructions.BuyExactSolInAccounts>) {
    return instructions.buyExactSolIn(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `buyV2` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public buyV2(args: instructions.BuyV2Args, accounts?: Partial<instructions.BuyV2Accounts>) {
    return instructions.buyV2(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `buyExactQuoteInV2` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public buyExactQuoteInV2(args: instructions.BuyExactQuoteInV2Args, accounts?: Partial<instructions.BuyExactQuoteInV2Accounts>) {
    return instructions.buyExactQuoteInV2(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `sell` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public sell(args: instructions.SellArgs, accounts?: Partial<instructions.SellAccounts>) {
    return instructions.sell(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `sellV2` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public sellV2(args: instructions.SellV2Args, accounts?: Partial<instructions.SellV2Accounts>) {
    return instructions.sellV2(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `migrate` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public migrate(args: instructions.MigrateArgs, accounts?: Partial<instructions.MigrateAccounts>) {
    return instructions.migrate(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `migrateV2` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public migrateV2(args: instructions.MigrateV2Args, accounts?: Partial<instructions.MigrateV2Accounts>) {
    return instructions.migrateV2(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `extendAccount` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public extendAccount(args?: Record<string, never>, accounts?: Partial<instructions.ExtendAccountAccounts>) {
    return instructions.extendAccount(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `addQuoteMint` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public addQuoteMint(args: instructions.AddQuoteMintArgs, accounts?: Partial<instructions.AddQuoteMintAccounts>) {
    return instructions.addQuoteMint(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `removeQuoteMint` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public removeQuoteMint(args: instructions.RemoveQuoteMintArgs, accounts?: Partial<instructions.RemoveQuoteMintAccounts>) {
    return instructions.removeQuoteMint(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `setVirtualQuoteReserves` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public setVirtualQuoteReserves(args: instructions.SetVirtualQuoteReservesArgs, accounts?: Partial<instructions.SetVirtualQuoteReservesAccounts>) {
    return instructions.setVirtualQuoteReserves(this.program, args ?? {}, accounts);
  }

  /** Derives the PDA for a `amm_global_config` account. Returns `[PublicKey, bumpSeed]`. */
  public getAmmGlobalConfigPda(seeds: {
  }): [naclac.PublicKey, number] {
    const [pda, bump] = naclac.PublicKey.findProgramAddressSync(
      [
                new Uint8Array([103, 108, 111, 98, 97, 108, 95, 99, 111, 110, 102, 105, 103])
      ],
      new naclac.PublicKey(new Uint8Array([252, 69, 211, 229, 209, 250, 91, 185, 95, 119, 96, 125, 211, 65, 169, 106, 209, 112, 144, 171, 161, 186, 45, 122, 136, 122, 226, 73, 138, 146, 134, 211]))
    );
    return [pda, bump];
  }

  /** Derives the PDA for a `bonding_curve` account. Returns `[PublicKey, bumpSeed]`. */
  public getBondingCurvePda(seeds: {
    mint: naclac.PublicKey | string;
  }): [naclac.PublicKey, number] {
    const [pda, bump] = naclac.PublicKey.findProgramAddressSync(
      [
                new Uint8Array([98, 111, 110, 100, 105, 110, 103, 45, 99, 117, 114, 118, 101]),
        new naclac.PublicKey(seeds.mint).toBuffer()
      ],
      this.programId
    );
    return [pda, bump];
  }

  /** Derives the PDA for a `bonding_curve_v2` account. Returns `[PublicKey, bumpSeed]`. */
  public getBondingCurveV2Pda(seeds: {
    mint: naclac.PublicKey | string;
  }): [naclac.PublicKey, number] {
    const [pda, bump] = naclac.PublicKey.findProgramAddressSync(
      [
                new Uint8Array([98, 111, 110, 100, 105, 110, 103, 45, 99, 117, 114, 118, 101, 45, 118, 50]),
        new naclac.PublicKey(seeds.mint).toBuffer()
      ],
      this.programId
    );
    return [pda, bump];
  }

  /** Derives the PDA for a `boost_vault` account. Returns `[PublicKey, bumpSeed]`. */
  public getBoostVaultPda(seeds: {
    boost_vault_authority: naclac.PublicKey | string;
    quote_token_program: naclac.PublicKey | string;
    quote_mint: naclac.PublicKey | string;
  }): [naclac.PublicKey, number] {
    const [pda, bump] = naclac.PublicKey.findProgramAddressSync(
      [
                new naclac.PublicKey(seeds.boost_vault_authority).toBuffer(),
        new naclac.PublicKey(seeds.quote_token_program).toBuffer(),
        new naclac.PublicKey(seeds.quote_mint).toBuffer()
      ],
      new naclac.PublicKey(new Uint8Array([140, 151, 37, 143, 78, 36, 137, 241, 187, 61, 16, 41, 20, 142, 13, 131, 11, 90, 19, 153, 218, 255, 16, 132, 4, 142, 123, 216, 219, 233, 248, 89]))
    );
    return [pda, bump];
  }

  /** Derives the PDA for a `boost_vault_authority` account. Returns `[PublicKey, bumpSeed]`. */
  public getBoostVaultAuthorityPda(seeds: {
    pool: naclac.PublicKey | string;
  }): [naclac.PublicKey, number] {
    const [pda, bump] = naclac.PublicKey.findProgramAddressSync(
      [
                new Uint8Array([98, 111, 111, 115, 116, 95, 118, 97, 117, 108, 116]),
        new naclac.PublicKey(seeds.pool).toBuffer()
      ],
      new naclac.PublicKey(new Uint8Array([252, 69, 211, 229, 209, 250, 91, 185, 95, 119, 96, 125, 211, 65, 169, 106, 209, 112, 144, 171, 161, 186, 45, 122, 136, 122, 226, 73, 138, 146, 134, 211]))
    );
    return [pda, bump];
  }

  /** Derives the PDA for a `buyback_fee_recipient` account. Returns `[PublicKey, bumpSeed]`. */
  public getBuybackFeeRecipientPda(seeds: {
    args: types.SellV2Args;
  }): [naclac.PublicKey, number] {
    const [pda, bump] = naclac.PublicKey.findProgramAddressSync(
      [
                new Uint8Array([98, 117, 121, 98, 97, 99, 107, 45, 118, 97, 117, 108, 116]),
        new Uint8Array(naclac.getIdlCodec(JSON.parse('"u8"')).encode(seeds.args.buyback_index))
      ],
      new naclac.PublicKey(new Uint8Array([116, 160, 86, 82, 248, 105, 32, 166, 17, 57, 245, 99, 88, 171, 78, 132, 34, 207, 106, 105, 27, 23, 177, 13, 84, 100, 151, 96, 23, 63, 63, 197]))
    );
    return [pda, bump];
  }

  /** Derives the PDA for a `creator_vault` account. Returns `[PublicKey, bumpSeed]`. */
  public getCreatorVaultPda(seeds: {
    bonding_curve_creator: naclac.Address | string;
  }): [naclac.PublicKey, number] {
    const [pda, bump] = naclac.PublicKey.findProgramAddressSync(
      [
                new Uint8Array([99, 114, 101, 97, 116, 111, 114, 45, 118, 97, 117, 108, 116]),
        new Uint8Array(naclac.getIdlCodec(JSON.parse('"publicKey"')).encode(seeds.bonding_curve_creator))
      ],
      this.programId
    );
    return [pda, bump];
  }

  /** Derives the PDA for a `fee_config` account. Returns `[PublicKey, bumpSeed]`. */
  public getFeeConfigPda(seeds: {
    program: naclac.PublicKey | string;
  }): [naclac.PublicKey, number] {
    const [pda, bump] = naclac.PublicKey.findProgramAddressSync(
      [
                new Uint8Array([102, 101, 101, 95, 99, 111, 110, 102, 105, 103]),
        new naclac.PublicKey(seeds.program).toBuffer()
      ],
      new naclac.PublicKey(new Uint8Array([116, 160, 86, 82, 248, 105, 32, 166, 17, 57, 245, 99, 88, 171, 78, 132, 34, 207, 106, 105, 27, 23, 177, 13, 84, 100, 151, 96, 23, 63, 63, 197]))
    );
    return [pda, bump];
  }

  /** Derives the PDA for a `global` account. Returns `[PublicKey, bumpSeed]`. */
  public getGlobalPda(seeds: {
  }): [naclac.PublicKey, number] {
    const [pda, bump] = naclac.PublicKey.findProgramAddressSync(
      [
                new Uint8Array([103, 108, 111, 98, 97, 108])
      ],
      this.programId
    );
    return [pda, bump];
  }

  /** Derives the PDA for a `global_volume_accumulator` account. Returns `[PublicKey, bumpSeed]`. */
  public getGlobalVolumeAccumulatorPda(seeds: {
  }): [naclac.PublicKey, number] {
    const [pda, bump] = naclac.PublicKey.findProgramAddressSync(
      [
                new Uint8Array([103, 108, 111, 98, 97, 108, 95, 118, 111, 108, 117, 109, 101, 95, 97, 99, 99, 117, 109, 117, 108, 97, 116, 111, 114])
      ],
      this.programId
    );
    return [pda, bump];
  }

  /** Derives the PDA for a `lp_mint` account. Returns `[PublicKey, bumpSeed]`. */
  public getLpMintPda(seeds: {
    pool: naclac.PublicKey | string;
  }): [naclac.PublicKey, number] {
    const [pda, bump] = naclac.PublicKey.findProgramAddressSync(
      [
                new Uint8Array([112, 111, 111, 108, 95, 108, 112, 95, 109, 105, 110, 116]),
        new naclac.PublicKey(seeds.pool).toBuffer()
      ],
      new naclac.PublicKey(new Uint8Array([252, 69, 211, 229, 209, 250, 91, 185, 95, 119, 96, 125, 211, 65, 169, 106, 209, 112, 144, 171, 161, 186, 45, 122, 136, 122, 226, 73, 138, 146, 134, 211]))
    );
    return [pda, bump];
  }

  /** Derives the PDA for a `metadata` account. Returns `[PublicKey, bumpSeed]`. */
  public getMetadataPda(seeds: {
    mint: naclac.PublicKey | string;
  }): [naclac.PublicKey, number] {
    const [pda, bump] = naclac.PublicKey.findProgramAddressSync(
      [
                new Uint8Array([109, 101, 116, 97, 100, 97, 116, 97]),
        new Uint8Array([11, 112, 101, 177, 227, 209, 124, 69, 56, 157, 82, 127, 107, 4, 195, 205, 88, 184, 108, 115, 26, 160, 253, 181, 73, 182, 209, 188, 3, 248, 41, 70]),
        new naclac.PublicKey(seeds.mint).toBuffer()
      ],
      new naclac.PublicKey(new Uint8Array([11, 112, 101, 177, 227, 209, 124, 69, 56, 157, 82, 127, 107, 4, 195, 205, 88, 184, 108, 115, 26, 160, 253, 181, 73, 182, 209, 188, 3, 248, 41, 70]))
    );
    return [pda, bump];
  }

  /** Derives the PDA for a `mint_authority` account. Returns `[PublicKey, bumpSeed]`. */
  public getMintAuthorityPda(seeds: {
  }): [naclac.PublicKey, number] {
    const [pda, bump] = naclac.PublicKey.findProgramAddressSync(
      [
                new Uint8Array([109, 105, 110, 116, 45, 97, 117, 116, 104, 111, 114, 105, 116, 121])
      ],
      this.programId
    );
    return [pda, bump];
  }

  /** Derives the PDA for a `pool` account. Returns `[PublicKey, bumpSeed]`. */
  public getPoolPda(seeds: {
    pool_authority: naclac.PublicKey | string;
    mint: naclac.PublicKey | string;
  }): [naclac.PublicKey, number] {
    const [pda, bump] = naclac.PublicKey.findProgramAddressSync(
      [
                new Uint8Array([112, 111, 111, 108]),
        new Uint8Array([0, 0]),
        new naclac.PublicKey(seeds.pool_authority).toBuffer(),
        new naclac.PublicKey(seeds.mint).toBuffer(),
        new Uint8Array([6, 155, 136, 87, 254, 171, 129, 132, 251, 104, 127, 99, 70, 24, 192, 53, 218, 196, 57, 220, 26, 235, 59, 85, 152, 160, 240, 0, 0, 0, 0, 1])
      ],
      new naclac.PublicKey(new Uint8Array([252, 69, 211, 229, 209, 250, 91, 185, 95, 119, 96, 125, 211, 65, 169, 106, 209, 112, 144, 171, 161, 186, 45, 122, 136, 122, 226, 73, 138, 146, 134, 211]))
    );
    return [pda, bump];
  }

  /** Derives the PDA for a `pool_authority` account. Returns `[PublicKey, bumpSeed]`. */
  public getPoolAuthorityPda(seeds: {
    mint: naclac.PublicKey | string;
  }): [naclac.PublicKey, number] {
    const [pda, bump] = naclac.PublicKey.findProgramAddressSync(
      [
                new Uint8Array([112, 111, 111, 108, 45, 97, 117, 116, 104, 111, 114, 105, 116, 121]),
        new naclac.PublicKey(seeds.mint).toBuffer()
      ],
      this.programId
    );
    return [pda, bump];
  }

  /** Derives the PDA for a `pool_authority_mint_account` account. Returns `[PublicKey, bumpSeed]`. */
  public getPoolAuthorityMintAccountPda(seeds: {
    pool_authority: naclac.PublicKey | string;
    token_program: naclac.PublicKey | string;
    mint: naclac.PublicKey | string;
  }): [naclac.PublicKey, number] {
    const [pda, bump] = naclac.PublicKey.findProgramAddressSync(
      [
                new naclac.PublicKey(seeds.pool_authority).toBuffer(),
        new naclac.PublicKey(seeds.token_program).toBuffer(),
        new naclac.PublicKey(seeds.mint).toBuffer()
      ],
      new naclac.PublicKey(new Uint8Array([140, 151, 37, 143, 78, 36, 137, 241, 187, 61, 16, 41, 20, 142, 13, 131, 11, 90, 19, 153, 218, 255, 16, 132, 4, 142, 123, 216, 219, 233, 248, 89]))
    );
    return [pda, bump];
  }

  /** Derives the PDA for a `pool_authority_quote_account` account. Returns `[PublicKey, bumpSeed]`. */
  public getPoolAuthorityQuoteAccountPda(seeds: {
    pool_authority: naclac.PublicKey | string;
    quote_token_program: naclac.PublicKey | string;
    quote_mint: naclac.PublicKey | string;
  }): [naclac.PublicKey, number] {
    const [pda, bump] = naclac.PublicKey.findProgramAddressSync(
      [
                new naclac.PublicKey(seeds.pool_authority).toBuffer(),
        new naclac.PublicKey(seeds.quote_token_program).toBuffer(),
        new naclac.PublicKey(seeds.quote_mint).toBuffer()
      ],
      new naclac.PublicKey(new Uint8Array([140, 151, 37, 143, 78, 36, 137, 241, 187, 61, 16, 41, 20, 142, 13, 131, 11, 90, 19, 153, 218, 255, 16, 132, 4, 142, 123, 216, 219, 233, 248, 89]))
    );
    return [pda, bump];
  }

  /** Derives the PDA for a `pool_authority_wsol_account` account. Returns `[PublicKey, bumpSeed]`. */
  public getPoolAuthorityWsolAccountPda(seeds: {
    pool_authority: naclac.PublicKey | string;
    token_program: naclac.PublicKey | string;
    wsol_mint: naclac.PublicKey | string;
  }): [naclac.PublicKey, number] {
    const [pda, bump] = naclac.PublicKey.findProgramAddressSync(
      [
                new naclac.PublicKey(seeds.pool_authority).toBuffer(),
        new naclac.PublicKey(seeds.token_program).toBuffer(),
        new naclac.PublicKey(seeds.wsol_mint).toBuffer()
      ],
      new naclac.PublicKey(new Uint8Array([140, 151, 37, 143, 78, 36, 137, 241, 187, 61, 16, 41, 20, 142, 13, 131, 11, 90, 19, 153, 218, 255, 16, 132, 4, 142, 123, 216, 219, 233, 248, 89]))
    );
    return [pda, bump];
  }

  /** Derives the PDA for a `pool_base_token_account` account. Returns `[PublicKey, bumpSeed]`. */
  public getPoolBaseTokenAccountPda(seeds: {
    pool: naclac.PublicKey | string;
    token_program: naclac.PublicKey | string;
    mint: naclac.PublicKey | string;
  }): [naclac.PublicKey, number] {
    const [pda, bump] = naclac.PublicKey.findProgramAddressSync(
      [
                new naclac.PublicKey(seeds.pool).toBuffer(),
        new naclac.PublicKey(seeds.token_program).toBuffer(),
        new naclac.PublicKey(seeds.mint).toBuffer()
      ],
      new naclac.PublicKey(new Uint8Array([140, 151, 37, 143, 78, 36, 137, 241, 187, 61, 16, 41, 20, 142, 13, 131, 11, 90, 19, 153, 218, 255, 16, 132, 4, 142, 123, 216, 219, 233, 248, 89]))
    );
    return [pda, bump];
  }

  /** Derives the PDA for a `pool_quote_token_account` account. Returns `[PublicKey, bumpSeed]`. */
  public getPoolQuoteTokenAccountPda(seeds: {
    pool: naclac.PublicKey | string;
    token_program: naclac.PublicKey | string;
    wsol_mint: naclac.PublicKey | string;
  }): [naclac.PublicKey, number] {
    const [pda, bump] = naclac.PublicKey.findProgramAddressSync(
      [
                new naclac.PublicKey(seeds.pool).toBuffer(),
        new naclac.PublicKey(seeds.token_program).toBuffer(),
        new naclac.PublicKey(seeds.wsol_mint).toBuffer()
      ],
      new naclac.PublicKey(new Uint8Array([140, 151, 37, 143, 78, 36, 137, 241, 187, 61, 16, 41, 20, 142, 13, 131, 11, 90, 19, 153, 218, 255, 16, 132, 4, 142, 123, 216, 219, 233, 248, 89]))
    );
    return [pda, bump];
  }

  /** Derives the PDA for a `pump_authority` account. Returns `[PublicKey, bumpSeed]`. */
  public getPumpAuthorityPda(seeds: {
  }): [naclac.PublicKey, number] {
    const [pda, bump] = naclac.PublicKey.findProgramAddressSync(
      [
                new Uint8Array([112, 117, 109, 112, 45, 97, 117, 116, 104, 111, 114, 105, 116, 121])
      ],
      this.programId
    );
    return [pda, bump];
  }

  /** Derives the PDA for a `pump_fees_authority` account. Returns `[PublicKey, bumpSeed]`. */
  public getPumpFeesAuthorityPda(seeds: {
  }): [naclac.PublicKey, number] {
    const [pda, bump] = naclac.PublicKey.findProgramAddressSync(
      [
                new Uint8Array([112, 117, 109, 112, 45, 102, 101, 101, 115, 45, 97, 117, 116, 104, 111, 114, 105, 116, 121])
      ],
      new naclac.PublicKey(new Uint8Array([116, 160, 86, 82, 248, 105, 32, 166, 17, 57, 245, 99, 88, 171, 78, 132, 34, 207, 106, 105, 27, 23, 177, 13, 84, 100, 151, 96, 23, 63, 63, 197]))
    );
    return [pda, bump];
  }

  /** Derives the PDA for a `sharing_config` account. Returns `[PublicKey, bumpSeed]`. */
  public getSharingConfigPda(seeds: {
    mint: naclac.PublicKey | string;
  }): [naclac.PublicKey, number] {
    const [pda, bump] = naclac.PublicKey.findProgramAddressSync(
      [
                new Uint8Array([115, 104, 97, 114, 105, 110, 103, 45, 99, 111, 110, 102, 105, 103]),
        new naclac.PublicKey(seeds.mint).toBuffer()
      ],
      new naclac.PublicKey(new Uint8Array([116, 160, 86, 82, 248, 105, 32, 166, 17, 57, 245, 99, 88, 171, 78, 132, 34, 207, 106, 105, 27, 23, 177, 13, 84, 100, 151, 96, 23, 63, 63, 197]))
    );
    return [pda, bump];
  }

  /** Derives the PDA for a `user_pool_token_account` account. Returns `[PublicKey, bumpSeed]`. */
  public getUserPoolTokenAccountPda(seeds: {
    pool_authority: naclac.PublicKey | string;
    token_2022_program: naclac.PublicKey | string;
    lp_mint: naclac.PublicKey | string;
  }): [naclac.PublicKey, number] {
    const [pda, bump] = naclac.PublicKey.findProgramAddressSync(
      [
                new naclac.PublicKey(seeds.pool_authority).toBuffer(),
        new naclac.PublicKey(seeds.token_2022_program).toBuffer(),
        new naclac.PublicKey(seeds.lp_mint).toBuffer()
      ],
      new naclac.PublicKey(new Uint8Array([140, 151, 37, 143, 78, 36, 137, 241, 187, 61, 16, 41, 20, 142, 13, 131, 11, 90, 19, 153, 218, 255, 16, 132, 4, 142, 123, 216, 219, 233, 248, 89]))
    );
    return [pda, bump];
  }

  /** Derives the PDA for a `user_volume_accumulator` account. Returns `[PublicKey, bumpSeed]`. */
  public getUserVolumeAccumulatorPda(seeds: {
    user: naclac.PublicKey | string;
  }): [naclac.PublicKey, number] {
    const [pda, bump] = naclac.PublicKey.findProgramAddressSync(
      [
                new Uint8Array([117, 115, 101, 114, 95, 118, 111, 108, 117, 109, 101, 95, 97, 99, 99, 117, 109, 117, 108, 97, 116, 111, 114]),
        new naclac.PublicKey(seeds.user).toBuffer()
      ],
      this.programId
    );
    return [pda, bump];
  }

  /** Fetches a `BondingCurve` account. Throws if it does not exist. */
  public fetchBondingCurve(address: naclac.PublicKey | string) {
    return this.program.account['BondingCurve'].fetch(address);
  }

  /** Fetches a `BondingCurve` account. Returns `null` if it does not exist. */
  public async fetchMaybeBondingCurve(address: naclac.PublicKey | string) {
    try { return await this.program.account['BondingCurve'].fetch(address); }
    catch { return null; }
  }

  /** Fetches multiple `BondingCurve` accounts in a single RPC call. */
  public fetchAllBondingCurves(addresses: Array<naclac.PublicKey | string>, opts?: { dropCorrupted?: boolean }) {
    return this.program.account['BondingCurve'].fetchMultiple(addresses, opts);
  }

  /** Fetches all on-chain `BondingCurve` accounts owned by this program. */
  public fetchAllBondingCurveByOwner(options?: { dropCorrupted?: boolean; filters?: any[] }) {
    return this.program.account['BondingCurve'].all(options);
  }

  /** Fetches a `Global` account. Throws if it does not exist. */
  public fetchGlobal(address: naclac.PublicKey | string) {
    return this.program.account['Global'].fetch(address);
  }

  /** Fetches a `Global` account. Returns `null` if it does not exist. */
  public async fetchMaybeGlobal(address: naclac.PublicKey | string) {
    try { return await this.program.account['Global'].fetch(address); }
    catch { return null; }
  }

  /** Fetches multiple `Global` accounts in a single RPC call. */
  public fetchAllGlobals(addresses: Array<naclac.PublicKey | string>, opts?: { dropCorrupted?: boolean }) {
    return this.program.account['Global'].fetchMultiple(addresses, opts);
  }

  /** Fetches all on-chain `Global` accounts owned by this program. */
  public fetchAllGlobalByOwner(options?: { dropCorrupted?: boolean; filters?: any[] }) {
    return this.program.account['Global'].all(options);
  }

  /** Fetches a `GlobalVolumeAccumulator` account. Throws if it does not exist. */
  public fetchGlobalVolumeAccumulator(address: naclac.PublicKey | string) {
    return this.program.account['GlobalVolumeAccumulator'].fetch(address);
  }

  /** Fetches a `GlobalVolumeAccumulator` account. Returns `null` if it does not exist. */
  public async fetchMaybeGlobalVolumeAccumulator(address: naclac.PublicKey | string) {
    try { return await this.program.account['GlobalVolumeAccumulator'].fetch(address); }
    catch { return null; }
  }

  /** Fetches multiple `GlobalVolumeAccumulator` accounts in a single RPC call. */
  public fetchAllGlobalVolumeAccumulators(addresses: Array<naclac.PublicKey | string>, opts?: { dropCorrupted?: boolean }) {
    return this.program.account['GlobalVolumeAccumulator'].fetchMultiple(addresses, opts);
  }

  /** Fetches all on-chain `GlobalVolumeAccumulator` accounts owned by this program. */
  public fetchAllGlobalVolumeAccumulatorByOwner(options?: { dropCorrupted?: boolean; filters?: any[] }) {
    return this.program.account['GlobalVolumeAccumulator'].all(options);
  }

  /** Fetches a `SharingConfig` account. Throws if it does not exist. */
  public fetchSharingConfig(address: naclac.PublicKey | string) {
    return this.program.account['SharingConfig'].fetch(address);
  }

  /** Fetches a `SharingConfig` account. Returns `null` if it does not exist. */
  public async fetchMaybeSharingConfig(address: naclac.PublicKey | string) {
    try { return await this.program.account['SharingConfig'].fetch(address); }
    catch { return null; }
  }

  /** Fetches multiple `SharingConfig` accounts in a single RPC call. */
  public fetchAllSharingConfigs(addresses: Array<naclac.PublicKey | string>, opts?: { dropCorrupted?: boolean }) {
    return this.program.account['SharingConfig'].fetchMultiple(addresses, opts);
  }

  /** Fetches all on-chain `SharingConfig` accounts owned by this program. */
  public fetchAllSharingConfigByOwner(options?: { dropCorrupted?: boolean; filters?: any[] }) {
    return this.program.account['SharingConfig'].all(options);
  }

  /** Fetches a `UserVolumeAccumulator` account. Throws if it does not exist. */
  public fetchUserVolumeAccumulator(address: naclac.PublicKey | string) {
    return this.program.account['UserVolumeAccumulator'].fetch(address);
  }

  /** Fetches a `UserVolumeAccumulator` account. Returns `null` if it does not exist. */
  public async fetchMaybeUserVolumeAccumulator(address: naclac.PublicKey | string) {
    try { return await this.program.account['UserVolumeAccumulator'].fetch(address); }
    catch { return null; }
  }

  /** Fetches multiple `UserVolumeAccumulator` accounts in a single RPC call. */
  public fetchAllUserVolumeAccumulators(addresses: Array<naclac.PublicKey | string>, opts?: { dropCorrupted?: boolean }) {
    return this.program.account['UserVolumeAccumulator'].fetchMultiple(addresses, opts);
  }

  /** Fetches all on-chain `UserVolumeAccumulator` accounts owned by this program. */
  public fetchAllUserVolumeAccumulatorByOwner(options?: { dropCorrupted?: boolean; filters?: any[] }) {
    return this.program.account['UserVolumeAccumulator'].all(options);
  }

  /** Subscribes to `CreateEvent` events. Returns a listener ID. */
  public onCreateEvent(callback: (event: types.CreateEvent, slot: number, signature: string) => void) {
    return types.addCreateEventListener(this.program, callback);
  }

  /** Awaits the next `CreateEvent` event. Resolves `null` on timeout. */
  public waitForCreateEvent(options?: { timeoutMs?: number }) {
    return this.program.waitForEvent<types.CreateEvent>("CreateEvent", options);
  }

  /** Decodes every `CreateEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForCreateEvent` — prefer this when you already know which transaction you're checking. */
  public parseCreateEventEvents(logs: readonly string[]) {
    return this.program.parseEvents<types.CreateEvent>("CreateEvent", logs);
  }

  /** Subscribes to `MigrateBondingCurveCreatorEvent` events. Returns a listener ID. */
  public onMigrateBondingCurveCreatorEvent(callback: (event: types.MigrateBondingCurveCreatorEvent, slot: number, signature: string) => void) {
    return types.addMigrateBondingCurveCreatorEventListener(this.program, callback);
  }

  /** Awaits the next `MigrateBondingCurveCreatorEvent` event. Resolves `null` on timeout. */
  public waitForMigrateBondingCurveCreatorEvent(options?: { timeoutMs?: number }) {
    return this.program.waitForEvent<types.MigrateBondingCurveCreatorEvent>("MigrateBondingCurveCreatorEvent", options);
  }

  /** Decodes every `MigrateBondingCurveCreatorEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForMigrateBondingCurveCreatorEvent` — prefer this when you already know which transaction you're checking. */
  public parseMigrateBondingCurveCreatorEventEvents(logs: readonly string[]) {
    return this.program.parseEvents<types.MigrateBondingCurveCreatorEvent>("MigrateBondingCurveCreatorEvent", logs);
  }

  /** Subscribes to `SetParamsEvent` events. Returns a listener ID. */
  public onSetParamsEvent(callback: (event: types.SetParamsEvent, slot: number, signature: string) => void) {
    return types.addSetParamsEventListener(this.program, callback);
  }

  /** Awaits the next `SetParamsEvent` event. Resolves `null` on timeout. */
  public waitForSetParamsEvent(options?: { timeoutMs?: number }) {
    return this.program.waitForEvent<types.SetParamsEvent>("SetParamsEvent", options);
  }

  /** Decodes every `SetParamsEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForSetParamsEvent` — prefer this when you already know which transaction you're checking. */
  public parseSetParamsEventEvents(logs: readonly string[]) {
    return this.program.parseEvents<types.SetParamsEvent>("SetParamsEvent", logs);
  }

  /** Subscribes to `DistributeCreatorFeesEvent` events. Returns a listener ID. */
  public onDistributeCreatorFeesEvent(callback: (event: types.DistributeCreatorFeesEvent, slot: number, signature: string) => void) {
    return types.addDistributeCreatorFeesEventListener(this.program, callback);
  }

  /** Awaits the next `DistributeCreatorFeesEvent` event. Resolves `null` on timeout. */
  public waitForDistributeCreatorFeesEvent(options?: { timeoutMs?: number }) {
    return this.program.waitForEvent<types.DistributeCreatorFeesEvent>("DistributeCreatorFeesEvent", options);
  }

  /** Decodes every `DistributeCreatorFeesEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForDistributeCreatorFeesEvent` — prefer this when you already know which transaction you're checking. */
  public parseDistributeCreatorFeesEventEvents(logs: readonly string[]) {
    return this.program.parseEvents<types.DistributeCreatorFeesEvent>("DistributeCreatorFeesEvent", logs);
  }

  /** Subscribes to `CompleteEvent` events. Returns a listener ID. */
  public onCompleteEvent(callback: (event: types.CompleteEvent, slot: number, signature: string) => void) {
    return types.addCompleteEventListener(this.program, callback);
  }

  /** Awaits the next `CompleteEvent` event. Resolves `null` on timeout. */
  public waitForCompleteEvent(options?: { timeoutMs?: number }) {
    return this.program.waitForEvent<types.CompleteEvent>("CompleteEvent", options);
  }

  /** Decodes every `CompleteEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForCompleteEvent` — prefer this when you already know which transaction you're checking. */
  public parseCompleteEventEvents(logs: readonly string[]) {
    return this.program.parseEvents<types.CompleteEvent>("CompleteEvent", logs);
  }

  /** Subscribes to `CompletePumpAmmMigrationEvent` events. Returns a listener ID. */
  public onCompletePumpAmmMigrationEvent(callback: (event: types.CompletePumpAmmMigrationEvent, slot: number, signature: string) => void) {
    return types.addCompletePumpAmmMigrationEventListener(this.program, callback);
  }

  /** Awaits the next `CompletePumpAmmMigrationEvent` event. Resolves `null` on timeout. */
  public waitForCompletePumpAmmMigrationEvent(options?: { timeoutMs?: number }) {
    return this.program.waitForEvent<types.CompletePumpAmmMigrationEvent>("CompletePumpAmmMigrationEvent", options);
  }

  /** Decodes every `CompletePumpAmmMigrationEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForCompletePumpAmmMigrationEvent` — prefer this when you already know which transaction you're checking. */
  public parseCompletePumpAmmMigrationEventEvents(logs: readonly string[]) {
    return this.program.parseEvents<types.CompletePumpAmmMigrationEvent>("CompletePumpAmmMigrationEvent", logs);
  }

  /** Subscribes to `ExtendAccountEvent` events. Returns a listener ID. */
  public onExtendAccountEvent(callback: (event: types.ExtendAccountEvent, slot: number, signature: string) => void) {
    return types.addExtendAccountEventListener(this.program, callback);
  }

  /** Awaits the next `ExtendAccountEvent` event. Resolves `null` on timeout. */
  public waitForExtendAccountEvent(options?: { timeoutMs?: number }) {
    return this.program.waitForEvent<types.ExtendAccountEvent>("ExtendAccountEvent", options);
  }

  /** Decodes every `ExtendAccountEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForExtendAccountEvent` — prefer this when you already know which transaction you're checking. */
  public parseExtendAccountEventEvents(logs: readonly string[]) {
    return this.program.parseEvents<types.ExtendAccountEvent>("ExtendAccountEvent", logs);
  }

  /** Subscribes to `TradeEvent` events. Returns a listener ID. */
  public onTradeEvent(callback: (event: types.TradeEvent, slot: number, signature: string) => void) {
    return types.addTradeEventListener(this.program, callback);
  }

  /** Awaits the next `TradeEvent` event. Resolves `null` on timeout. */
  public waitForTradeEvent(options?: { timeoutMs?: number }) {
    return this.program.waitForEvent<types.TradeEvent>("TradeEvent", options);
  }

  /** Decodes every `TradeEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForTradeEvent` — prefer this when you already know which transaction you're checking. */
  public parseTradeEventEvents(logs: readonly string[]) {
    return this.program.parseEvents<types.TradeEvent>("TradeEvent", logs);
  }

  /** Subscribes to `UpdateGlobalAuthorityEvent` events. Returns a listener ID. */
  public onUpdateGlobalAuthorityEvent(callback: (event: types.UpdateGlobalAuthorityEvent, slot: number, signature: string) => void) {
    return types.addUpdateGlobalAuthorityEventListener(this.program, callback);
  }

  /** Awaits the next `UpdateGlobalAuthorityEvent` event. Resolves `null` on timeout. */
  public waitForUpdateGlobalAuthorityEvent(options?: { timeoutMs?: number }) {
    return this.program.waitForEvent<types.UpdateGlobalAuthorityEvent>("UpdateGlobalAuthorityEvent", options);
  }

  /** Decodes every `UpdateGlobalAuthorityEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForUpdateGlobalAuthorityEvent` — prefer this when you already know which transaction you're checking. */
  public parseUpdateGlobalAuthorityEventEvents(logs: readonly string[]) {
    return this.program.parseEvents<types.UpdateGlobalAuthorityEvent>("UpdateGlobalAuthorityEvent", logs);
  }

  /** Subscribes to `ReservedFeeRecipientsEvent` events. Returns a listener ID. */
  public onReservedFeeRecipientsEvent(callback: (event: types.ReservedFeeRecipientsEvent, slot: number, signature: string) => void) {
    return types.addReservedFeeRecipientsEventListener(this.program, callback);
  }

  /** Awaits the next `ReservedFeeRecipientsEvent` event. Resolves `null` on timeout. */
  public waitForReservedFeeRecipientsEvent(options?: { timeoutMs?: number }) {
    return this.program.waitForEvent<types.ReservedFeeRecipientsEvent>("ReservedFeeRecipientsEvent", options);
  }

  /** Decodes every `ReservedFeeRecipientsEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForReservedFeeRecipientsEvent` — prefer this when you already know which transaction you're checking. */
  public parseReservedFeeRecipientsEventEvents(logs: readonly string[]) {
    return this.program.parseEvents<types.ReservedFeeRecipientsEvent>("ReservedFeeRecipientsEvent", logs);
  }

  /** Subscribes to `SetCreatorEvent` events. Returns a listener ID. */
  public onSetCreatorEvent(callback: (event: types.SetCreatorEvent, slot: number, signature: string) => void) {
    return types.addSetCreatorEventListener(this.program, callback);
  }

  /** Awaits the next `SetCreatorEvent` event. Resolves `null` on timeout. */
  public waitForSetCreatorEvent(options?: { timeoutMs?: number }) {
    return this.program.waitForEvent<types.SetCreatorEvent>("SetCreatorEvent", options);
  }

  /** Decodes every `SetCreatorEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForSetCreatorEvent` — prefer this when you already know which transaction you're checking. */
  public parseSetCreatorEventEvents(logs: readonly string[]) {
    return this.program.parseEvents<types.SetCreatorEvent>("SetCreatorEvent", logs);
  }

  /** Subscribes to `SetMetaplexCreatorEvent` events. Returns a listener ID. */
  public onSetMetaplexCreatorEvent(callback: (event: types.SetMetaplexCreatorEvent, slot: number, signature: string) => void) {
    return types.addSetMetaplexCreatorEventListener(this.program, callback);
  }

  /** Awaits the next `SetMetaplexCreatorEvent` event. Resolves `null` on timeout. */
  public waitForSetMetaplexCreatorEvent(options?: { timeoutMs?: number }) {
    return this.program.waitForEvent<types.SetMetaplexCreatorEvent>("SetMetaplexCreatorEvent", options);
  }

  /** Decodes every `SetMetaplexCreatorEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForSetMetaplexCreatorEvent` — prefer this when you already know which transaction you're checking. */
  public parseSetMetaplexCreatorEventEvents(logs: readonly string[]) {
    return this.program.parseEvents<types.SetMetaplexCreatorEvent>("SetMetaplexCreatorEvent", logs);
  }

  /** Subscribes to `AdminSetCreatorEvent` events. Returns a listener ID. */
  public onAdminSetCreatorEvent(callback: (event: types.AdminSetCreatorEvent, slot: number, signature: string) => void) {
    return types.addAdminSetCreatorEventListener(this.program, callback);
  }

  /** Awaits the next `AdminSetCreatorEvent` event. Resolves `null` on timeout. */
  public waitForAdminSetCreatorEvent(options?: { timeoutMs?: number }) {
    return this.program.waitForEvent<types.AdminSetCreatorEvent>("AdminSetCreatorEvent", options);
  }

  /** Decodes every `AdminSetCreatorEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForAdminSetCreatorEvent` — prefer this when you already know which transaction you're checking. */
  public parseAdminSetCreatorEventEvents(logs: readonly string[]) {
    return this.program.parseEvents<types.AdminSetCreatorEvent>("AdminSetCreatorEvent", logs);
  }

  /** Removes a registered event listener by its ID. */
  public removeEventListener(listenerId: number) {
    return types.removeListener(this.program, listenerId);
  }
}
