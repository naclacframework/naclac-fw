// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";
import * as types from "../types/typedefs";

/** 8-byte discriminator for the `claim_cashback_v2` instruction. */
export const CLAIM_CASHBACK_V2_DISCRIMINATOR = new Uint8Array([122, 243, 204, 65, 94, 116, 29, 55]);

/** Instruction arguments for `claimCashbackV2`. */
export interface ClaimCashbackV2Args {
  args: types.ClaimCashbackV2Args;
}

/** Accounts for the `claimCashbackV2` instruction. */
export interface ClaimCashbackV2Accounts {
  /**
   * SAFETY: only PDA seed material for `user_volume_accumulator`/
   * `associated_quote_user` below; never deserialized.
   */
  user: naclac.Address | string;
  userVolumeAccumulator?: naclac.Address | string;
  quoteMint: naclac.Address | string;
  quoteTokenProgram: naclac.Address | string;
  associatedTokenProgram?: naclac.Address | string;
  associatedUserVolumeAccumulator: naclac.Address | string;
  associatedQuoteUser: naclac.Address | string;
  systemProgram?: naclac.Address | string;
}

/**
 * Builds the `claimCashbackV2` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 * 
 * Sweeps `associated_user_volume_accumulator`'s full SPL-token "stable
 * cashback" balance to `associated_quote_user` -- the SPL-token sibling of
 * `claim_cashback` (native SOL). No rent-exempt floor to preserve (an SPL
 * token account's rent is a fixed lamport reserve separate from its token
 * balance), so the entire balance is swept, matching
 * `collect_creator_fee_v2`'s identical shape. Permissionless (no signer
 * requirement on `user`).
 */
export function claimCashbackV2(
  program: any,
  args: ClaimCashbackV2Args,
  accounts?: Partial<ClaimCashbackV2Accounts>
) {
  const builder = program.methods.claim_cashback_v2(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
