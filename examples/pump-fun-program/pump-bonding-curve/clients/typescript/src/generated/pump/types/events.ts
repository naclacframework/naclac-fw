// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";
import { Shareholder } from "./typedefs";

/** 8-byte discriminator for `CreateEvent` events in transaction logs. */
export const CREATEEVENT_EVENT_DISCRIMINATOR = new Uint8Array([27, 114, 169, 77, 222, 235, 99, 118]);

/** Auto-generated event interface from the program IDL. */
export interface CreateEvent {
  name: string;
  symbol: string;
  uri: string;
  mint: naclac.Address | string;
  bondingCurve: naclac.Address | string;
  user: naclac.Address | string;
  creator: naclac.Address | string;
  timestamp: bigint | number;
  virtualTokenReserves: bigint | number;
  virtualSolReserves: bigint | number;
  realTokenReserves: bigint | number;
  tokenTotalSupply: bigint | number;
  tokenProgram: naclac.Address | string;
  isMayhemMode: naclac.Bool;
  isCashbackEnabled: naclac.Bool;
  quoteMint: naclac.Address | string;
  virtualQuoteReserves: bigint | number;
}

/** Subscribes to `CreateEvent` events. Returns a listener ID for cleanup. */
export function addCreateEventListener(
  program: any,
  callback: (event: CreateEvent, slot: number, signature: string) => void
): number {
  return program.addEventListener("CreateEvent", callback);
}

/** Waits for the next `CreateEvent` event. Resolves `null` if the timeout expires. */
export async function waitForCreateEvent(
  program: any,
  options?: { timeoutMs?: number }
): Promise<CreateEvent | null> {
  return program.waitForEvent("CreateEvent", options) as Promise<CreateEvent | null>;
}

/** Decodes every `CreateEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForCreateEvent`/`addCreateEventListener` — prefer this when you already know which transaction you're checking. */
export function parseCreateEventEvents(
  program: any,
  logs: readonly string[]
): CreateEvent[] {
  return program.parseEvents("CreateEvent", logs) as CreateEvent[];
}

/** 8-byte discriminator for `MigrateBondingCurveCreatorEvent` events in transaction logs. */
export const MIGRATEBONDINGCURVECREATOREVENT_EVENT_DISCRIMINATOR = new Uint8Array([155, 167, 104, 220, 213, 108, 243, 3]);

/** Auto-generated event interface from the program IDL. */
export interface MigrateBondingCurveCreatorEvent {
  timestamp: bigint | number;
  mint: naclac.Address | string;
  bondingCurve: naclac.Address | string;
  sharingConfig: naclac.Address | string;
  oldCreator: naclac.Address | string;
  newCreator: naclac.Address | string;
}

/** Subscribes to `MigrateBondingCurveCreatorEvent` events. Returns a listener ID for cleanup. */
export function addMigrateBondingCurveCreatorEventListener(
  program: any,
  callback: (event: MigrateBondingCurveCreatorEvent, slot: number, signature: string) => void
): number {
  return program.addEventListener("MigrateBondingCurveCreatorEvent", callback);
}

/** Waits for the next `MigrateBondingCurveCreatorEvent` event. Resolves `null` if the timeout expires. */
export async function waitForMigrateBondingCurveCreatorEvent(
  program: any,
  options?: { timeoutMs?: number }
): Promise<MigrateBondingCurveCreatorEvent | null> {
  return program.waitForEvent("MigrateBondingCurveCreatorEvent", options) as Promise<MigrateBondingCurveCreatorEvent | null>;
}

/** Decodes every `MigrateBondingCurveCreatorEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForMigrateBondingCurveCreatorEvent`/`addMigrateBondingCurveCreatorEventListener` — prefer this when you already know which transaction you're checking. */
export function parseMigrateBondingCurveCreatorEventEvents(
  program: any,
  logs: readonly string[]
): MigrateBondingCurveCreatorEvent[] {
  return program.parseEvents("MigrateBondingCurveCreatorEvent", logs) as MigrateBondingCurveCreatorEvent[];
}

/** 8-byte discriminator for `SetParamsEvent` events in transaction logs. */
export const SETPARAMSEVENT_EVENT_DISCRIMINATOR = new Uint8Array([223, 195, 159, 246, 62, 48, 143, 131]);

/** Auto-generated event interface from the program IDL. */
export interface SetParamsEvent {
  initialVirtualTokenReserves: bigint | number;
  initialVirtualSolReserves: bigint | number;
  initialRealTokenReserves: bigint | number;
  finalRealSolReserves: bigint | number;
  tokenTotalSupply: bigint | number;
  feeBasisPoints: bigint | number;
  withdrawAuthority: naclac.Address | string;
  poolMigrationFee: bigint | number;
  creatorFeeBasisPoints: bigint | number;
  feeRecipients: Array<naclac.Address | string>;
  timestamp: bigint | number;
  setCreatorAuthority: naclac.Address | string;
  adminSetCreatorAuthority: naclac.Address | string;
  enableMigrate: naclac.Bool;
  padding: string | Uint8Array;
}

/** Subscribes to `SetParamsEvent` events. Returns a listener ID for cleanup. */
export function addSetParamsEventListener(
  program: any,
  callback: (event: SetParamsEvent, slot: number, signature: string) => void
): number {
  return program.addEventListener("SetParamsEvent", callback);
}

/** Waits for the next `SetParamsEvent` event. Resolves `null` if the timeout expires. */
export async function waitForSetParamsEvent(
  program: any,
  options?: { timeoutMs?: number }
): Promise<SetParamsEvent | null> {
  return program.waitForEvent("SetParamsEvent", options) as Promise<SetParamsEvent | null>;
}

/** Decodes every `SetParamsEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForSetParamsEvent`/`addSetParamsEventListener` — prefer this when you already know which transaction you're checking. */
export function parseSetParamsEventEvents(
  program: any,
  logs: readonly string[]
): SetParamsEvent[] {
  return program.parseEvents("SetParamsEvent", logs) as SetParamsEvent[];
}

/** 8-byte discriminator for `DistributeCreatorFeesEvent` events in transaction logs. */
export const DISTRIBUTECREATORFEESEVENT_EVENT_DISCRIMINATOR = new Uint8Array([165, 55, 129, 112, 4, 179, 202, 40]);

/** Auto-generated event interface from the program IDL. */
export interface DistributeCreatorFeesEvent {
  timestamp: bigint | number;
  mint: naclac.Address | string;
  bondingCurve: naclac.Address | string;
  sharingConfig: naclac.Address | string;
  admin: naclac.Address | string;
  shareholders: Array<Shareholder>;
  distributed: bigint | number;
  quoteMint: naclac.Address | string;
}

/** Subscribes to `DistributeCreatorFeesEvent` events. Returns a listener ID for cleanup. */
export function addDistributeCreatorFeesEventListener(
  program: any,
  callback: (event: DistributeCreatorFeesEvent, slot: number, signature: string) => void
): number {
  return program.addEventListener("DistributeCreatorFeesEvent", callback);
}

/** Waits for the next `DistributeCreatorFeesEvent` event. Resolves `null` if the timeout expires. */
export async function waitForDistributeCreatorFeesEvent(
  program: any,
  options?: { timeoutMs?: number }
): Promise<DistributeCreatorFeesEvent | null> {
  return program.waitForEvent("DistributeCreatorFeesEvent", options) as Promise<DistributeCreatorFeesEvent | null>;
}

/** Decodes every `DistributeCreatorFeesEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForDistributeCreatorFeesEvent`/`addDistributeCreatorFeesEventListener` — prefer this when you already know which transaction you're checking. */
export function parseDistributeCreatorFeesEventEvents(
  program: any,
  logs: readonly string[]
): DistributeCreatorFeesEvent[] {
  return program.parseEvents("DistributeCreatorFeesEvent", logs) as DistributeCreatorFeesEvent[];
}

/** 8-byte discriminator for `CompleteEvent` events in transaction logs. */
export const COMPLETEEVENT_EVENT_DISCRIMINATOR = new Uint8Array([95, 114, 97, 156, 212, 46, 152, 8]);

/** Auto-generated event interface from the program IDL. */
export interface CompleteEvent {
  user: naclac.Address | string;
  mint: naclac.Address | string;
  bondingCurve: naclac.Address | string;
  timestamp: bigint | number;
  quoteMint: naclac.Address | string;
}

/** Subscribes to `CompleteEvent` events. Returns a listener ID for cleanup. */
export function addCompleteEventListener(
  program: any,
  callback: (event: CompleteEvent, slot: number, signature: string) => void
): number {
  return program.addEventListener("CompleteEvent", callback);
}

/** Waits for the next `CompleteEvent` event. Resolves `null` if the timeout expires. */
export async function waitForCompleteEvent(
  program: any,
  options?: { timeoutMs?: number }
): Promise<CompleteEvent | null> {
  return program.waitForEvent("CompleteEvent", options) as Promise<CompleteEvent | null>;
}

/** Decodes every `CompleteEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForCompleteEvent`/`addCompleteEventListener` — prefer this when you already know which transaction you're checking. */
export function parseCompleteEventEvents(
  program: any,
  logs: readonly string[]
): CompleteEvent[] {
  return program.parseEvents("CompleteEvent", logs) as CompleteEvent[];
}

/** 8-byte discriminator for `CompletePumpAmmMigrationEvent` events in transaction logs. */
export const COMPLETEPUMPAMMMIGRATIONEVENT_EVENT_DISCRIMINATOR = new Uint8Array([189, 233, 93, 185, 92, 148, 234, 148]);

/**
 * Field-for-field mirror of the real `pump::CompletePumpAmmMigrationEvent`
 * (confirmed against `reference/pump-rust-client/idls/pump.json`) — this is
 * the event the real `migrate` instruction emits; there is no separate
 * "MigrateEvent" in the real IDL.
 */
export interface CompletePumpAmmMigrationEvent {
  timestamp: bigint | number;
  mintAmount: bigint | number;
  solAmount: bigint | number;
  poolMigrationFee: bigint | number;
  user: naclac.Address | string;
  mint: naclac.Address | string;
  bondingCurve: naclac.Address | string;
  pool: naclac.Address | string;
  quoteMint: naclac.Address | string;
}

/** Subscribes to `CompletePumpAmmMigrationEvent` events. Returns a listener ID for cleanup. */
export function addCompletePumpAmmMigrationEventListener(
  program: any,
  callback: (event: CompletePumpAmmMigrationEvent, slot: number, signature: string) => void
): number {
  return program.addEventListener("CompletePumpAmmMigrationEvent", callback);
}

/** Waits for the next `CompletePumpAmmMigrationEvent` event. Resolves `null` if the timeout expires. */
export async function waitForCompletePumpAmmMigrationEvent(
  program: any,
  options?: { timeoutMs?: number }
): Promise<CompletePumpAmmMigrationEvent | null> {
  return program.waitForEvent("CompletePumpAmmMigrationEvent", options) as Promise<CompletePumpAmmMigrationEvent | null>;
}

/** Decodes every `CompletePumpAmmMigrationEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForCompletePumpAmmMigrationEvent`/`addCompletePumpAmmMigrationEventListener` — prefer this when you already know which transaction you're checking. */
export function parseCompletePumpAmmMigrationEventEvents(
  program: any,
  logs: readonly string[]
): CompletePumpAmmMigrationEvent[] {
  return program.parseEvents("CompletePumpAmmMigrationEvent", logs) as CompletePumpAmmMigrationEvent[];
}

/** 8-byte discriminator for `ExtendAccountEvent` events in transaction logs. */
export const EXTENDACCOUNTEVENT_EVENT_DISCRIMINATOR = new Uint8Array([97, 97, 215, 144, 93, 146, 22, 124]);

/** Auto-generated event interface from the program IDL. */
export interface ExtendAccountEvent {
  account: naclac.Address | string;
  user: naclac.Address | string;
  currentSize: bigint | number;
  newSize: bigint | number;
  timestamp: bigint | number;
}

/** Subscribes to `ExtendAccountEvent` events. Returns a listener ID for cleanup. */
export function addExtendAccountEventListener(
  program: any,
  callback: (event: ExtendAccountEvent, slot: number, signature: string) => void
): number {
  return program.addEventListener("ExtendAccountEvent", callback);
}

/** Waits for the next `ExtendAccountEvent` event. Resolves `null` if the timeout expires. */
export async function waitForExtendAccountEvent(
  program: any,
  options?: { timeoutMs?: number }
): Promise<ExtendAccountEvent | null> {
  return program.waitForEvent("ExtendAccountEvent", options) as Promise<ExtendAccountEvent | null>;
}

/** Decodes every `ExtendAccountEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForExtendAccountEvent`/`addExtendAccountEventListener` — prefer this when you already know which transaction you're checking. */
export function parseExtendAccountEventEvents(
  program: any,
  logs: readonly string[]
): ExtendAccountEvent[] {
  return program.parseEvents("ExtendAccountEvent", logs) as ExtendAccountEvent[];
}

/** 8-byte discriminator for `TradeEvent` events in transaction logs. */
export const TRADEEVENT_EVENT_DISCRIMINATOR = new Uint8Array([189, 219, 127, 211, 78, 230, 97, 238]);

/** Auto-generated event interface from the program IDL. */
export interface TradeEvent {
  mint: naclac.Address | string;
  solAmount: bigint | number;
  tokenAmount: bigint | number;
  isBuy: naclac.Bool;
  user: naclac.Address | string;
  timestamp: bigint | number;
  virtualSolReserves: bigint | number;
  virtualTokenReserves: bigint | number;
  realSolReserves: bigint | number;
  realTokenReserves: bigint | number;
  feeRecipient: naclac.Address | string;
  feeBasisPoints: bigint | number;
  fee: bigint | number;
  creator: naclac.Address | string;
  creatorFeeBasisPoints: bigint | number;
  creatorFee: bigint | number;
  trackVolume: naclac.Bool;
  totalUnclaimedTokens: bigint | number;
  totalClaimedTokens: bigint | number;
  currentSolVolume: bigint | number;
  lastUpdateTimestamp: bigint | number;
  ixName: string;
  mayhemMode: naclac.Bool;
  cashbackFeeBasisPoints: bigint | number;
  cashback: bigint | number;
  buybackFeeBasisPoints: bigint | number;
  buybackFee: bigint | number;
  shareholders: Array<Shareholder>;
  quoteMint: naclac.Address | string;
  quoteAmount: bigint | number;
  virtualQuoteReserves: bigint | number;
  realQuoteReserves: bigint | number;
}

/** Subscribes to `TradeEvent` events. Returns a listener ID for cleanup. */
export function addTradeEventListener(
  program: any,
  callback: (event: TradeEvent, slot: number, signature: string) => void
): number {
  return program.addEventListener("TradeEvent", callback);
}

/** Waits for the next `TradeEvent` event. Resolves `null` if the timeout expires. */
export async function waitForTradeEvent(
  program: any,
  options?: { timeoutMs?: number }
): Promise<TradeEvent | null> {
  return program.waitForEvent("TradeEvent", options) as Promise<TradeEvent | null>;
}

/** Decodes every `TradeEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForTradeEvent`/`addTradeEventListener` — prefer this when you already know which transaction you're checking. */
export function parseTradeEventEvents(
  program: any,
  logs: readonly string[]
): TradeEvent[] {
  return program.parseEvents("TradeEvent", logs) as TradeEvent[];
}

/** 8-byte discriminator for `UpdateGlobalAuthorityEvent` events in transaction logs. */
export const UPDATEGLOBALAUTHORITYEVENT_EVENT_DISCRIMINATOR = new Uint8Array([182, 195, 137, 42, 35, 206, 207, 247]);

/** Auto-generated event interface from the program IDL. */
export interface UpdateGlobalAuthorityEvent {
  global: naclac.Address | string;
  authority: naclac.Address | string;
  newAuthority: naclac.Address | string;
  timestamp: bigint | number;
}

/** Subscribes to `UpdateGlobalAuthorityEvent` events. Returns a listener ID for cleanup. */
export function addUpdateGlobalAuthorityEventListener(
  program: any,
  callback: (event: UpdateGlobalAuthorityEvent, slot: number, signature: string) => void
): number {
  return program.addEventListener("UpdateGlobalAuthorityEvent", callback);
}

/** Waits for the next `UpdateGlobalAuthorityEvent` event. Resolves `null` if the timeout expires. */
export async function waitForUpdateGlobalAuthorityEvent(
  program: any,
  options?: { timeoutMs?: number }
): Promise<UpdateGlobalAuthorityEvent | null> {
  return program.waitForEvent("UpdateGlobalAuthorityEvent", options) as Promise<UpdateGlobalAuthorityEvent | null>;
}

/** Decodes every `UpdateGlobalAuthorityEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForUpdateGlobalAuthorityEvent`/`addUpdateGlobalAuthorityEventListener` — prefer this when you already know which transaction you're checking. */
export function parseUpdateGlobalAuthorityEventEvents(
  program: any,
  logs: readonly string[]
): UpdateGlobalAuthorityEvent[] {
  return program.parseEvents("UpdateGlobalAuthorityEvent", logs) as UpdateGlobalAuthorityEvent[];
}

/** 8-byte discriminator for `ReservedFeeRecipientsEvent` events in transaction logs. */
export const RESERVEDFEERECIPIENTSEVENT_EVENT_DISCRIMINATOR = new Uint8Array([43, 188, 250, 18, 221, 75, 187, 95]);

/** Auto-generated event interface from the program IDL. */
export interface ReservedFeeRecipientsEvent {
  timestamp: bigint | number;
  reservedFeeRecipient: naclac.Address | string;
  reservedFeeRecipients: Array<naclac.Address | string>;
}

/** Subscribes to `ReservedFeeRecipientsEvent` events. Returns a listener ID for cleanup. */
export function addReservedFeeRecipientsEventListener(
  program: any,
  callback: (event: ReservedFeeRecipientsEvent, slot: number, signature: string) => void
): number {
  return program.addEventListener("ReservedFeeRecipientsEvent", callback);
}

/** Waits for the next `ReservedFeeRecipientsEvent` event. Resolves `null` if the timeout expires. */
export async function waitForReservedFeeRecipientsEvent(
  program: any,
  options?: { timeoutMs?: number }
): Promise<ReservedFeeRecipientsEvent | null> {
  return program.waitForEvent("ReservedFeeRecipientsEvent", options) as Promise<ReservedFeeRecipientsEvent | null>;
}

/** Decodes every `ReservedFeeRecipientsEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForReservedFeeRecipientsEvent`/`addReservedFeeRecipientsEventListener` — prefer this when you already know which transaction you're checking. */
export function parseReservedFeeRecipientsEventEvents(
  program: any,
  logs: readonly string[]
): ReservedFeeRecipientsEvent[] {
  return program.parseEvents("ReservedFeeRecipientsEvent", logs) as ReservedFeeRecipientsEvent[];
}

/** 8-byte discriminator for `SetCreatorEvent` events in transaction logs. */
export const SETCREATOREVENT_EVENT_DISCRIMINATOR = new Uint8Array([237, 52, 123, 37, 245, 251, 72, 210]);

/** Auto-generated event interface from the program IDL. */
export interface SetCreatorEvent {
  timestamp: bigint | number;
  mint: naclac.Address | string;
  bondingCurve: naclac.Address | string;
  creator: naclac.Address | string;
}

/** Subscribes to `SetCreatorEvent` events. Returns a listener ID for cleanup. */
export function addSetCreatorEventListener(
  program: any,
  callback: (event: SetCreatorEvent, slot: number, signature: string) => void
): number {
  return program.addEventListener("SetCreatorEvent", callback);
}

/** Waits for the next `SetCreatorEvent` event. Resolves `null` if the timeout expires. */
export async function waitForSetCreatorEvent(
  program: any,
  options?: { timeoutMs?: number }
): Promise<SetCreatorEvent | null> {
  return program.waitForEvent("SetCreatorEvent", options) as Promise<SetCreatorEvent | null>;
}

/** Decodes every `SetCreatorEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForSetCreatorEvent`/`addSetCreatorEventListener` — prefer this when you already know which transaction you're checking. */
export function parseSetCreatorEventEvents(
  program: any,
  logs: readonly string[]
): SetCreatorEvent[] {
  return program.parseEvents("SetCreatorEvent", logs) as SetCreatorEvent[];
}

/** 8-byte discriminator for `SetMetaplexCreatorEvent` events in transaction logs. */
export const SETMETAPLEXCREATOREVENT_EVENT_DISCRIMINATOR = new Uint8Array([142, 203, 6, 32, 127, 105, 191, 162]);

/** Auto-generated event interface from the program IDL. */
export interface SetMetaplexCreatorEvent {
  timestamp: bigint | number;
  mint: naclac.Address | string;
  bondingCurve: naclac.Address | string;
  metadata: naclac.Address | string;
  creator: naclac.Address | string;
}

/** Subscribes to `SetMetaplexCreatorEvent` events. Returns a listener ID for cleanup. */
export function addSetMetaplexCreatorEventListener(
  program: any,
  callback: (event: SetMetaplexCreatorEvent, slot: number, signature: string) => void
): number {
  return program.addEventListener("SetMetaplexCreatorEvent", callback);
}

/** Waits for the next `SetMetaplexCreatorEvent` event. Resolves `null` if the timeout expires. */
export async function waitForSetMetaplexCreatorEvent(
  program: any,
  options?: { timeoutMs?: number }
): Promise<SetMetaplexCreatorEvent | null> {
  return program.waitForEvent("SetMetaplexCreatorEvent", options) as Promise<SetMetaplexCreatorEvent | null>;
}

/** Decodes every `SetMetaplexCreatorEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForSetMetaplexCreatorEvent`/`addSetMetaplexCreatorEventListener` — prefer this when you already know which transaction you're checking. */
export function parseSetMetaplexCreatorEventEvents(
  program: any,
  logs: readonly string[]
): SetMetaplexCreatorEvent[] {
  return program.parseEvents("SetMetaplexCreatorEvent", logs) as SetMetaplexCreatorEvent[];
}

/** 8-byte discriminator for `AdminSetCreatorEvent` events in transaction logs. */
export const ADMINSETCREATOREVENT_EVENT_DISCRIMINATOR = new Uint8Array([64, 69, 192, 104, 29, 30, 25, 107]);

/** Auto-generated event interface from the program IDL. */
export interface AdminSetCreatorEvent {
  timestamp: bigint | number;
  adminSetCreatorAuthority: naclac.Address | string;
  mint: naclac.Address | string;
  bondingCurve: naclac.Address | string;
  oldCreator: naclac.Address | string;
  newCreator: naclac.Address | string;
}

/** Subscribes to `AdminSetCreatorEvent` events. Returns a listener ID for cleanup. */
export function addAdminSetCreatorEventListener(
  program: any,
  callback: (event: AdminSetCreatorEvent, slot: number, signature: string) => void
): number {
  return program.addEventListener("AdminSetCreatorEvent", callback);
}

/** Waits for the next `AdminSetCreatorEvent` event. Resolves `null` if the timeout expires. */
export async function waitForAdminSetCreatorEvent(
  program: any,
  options?: { timeoutMs?: number }
): Promise<AdminSetCreatorEvent | null> {
  return program.waitForEvent("AdminSetCreatorEvent", options) as Promise<AdminSetCreatorEvent | null>;
}

/** Decodes every `AdminSetCreatorEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForAdminSetCreatorEvent`/`addAdminSetCreatorEventListener` — prefer this when you already know which transaction you're checking. */
export function parseAdminSetCreatorEventEvents(
  program: any,
  logs: readonly string[]
): AdminSetCreatorEvent[] {
  return program.parseEvents("AdminSetCreatorEvent", logs) as AdminSetCreatorEvent[];
}

/** 8-byte discriminator for `CollectCreatorFeeEvent` events in transaction logs. */
export const COLLECTCREATORFEEEVENT_EVENT_DISCRIMINATOR = new Uint8Array([122, 2, 127, 1, 14, 191, 12, 175]);

/** Auto-generated event interface from the program IDL. */
export interface CollectCreatorFeeEvent {
  timestamp: bigint | number;
  creator: naclac.Address | string;
  creatorFee: bigint | number;
  quoteMint: naclac.Address | string;
}

/** Subscribes to `CollectCreatorFeeEvent` events. Returns a listener ID for cleanup. */
export function addCollectCreatorFeeEventListener(
  program: any,
  callback: (event: CollectCreatorFeeEvent, slot: number, signature: string) => void
): number {
  return program.addEventListener("CollectCreatorFeeEvent", callback);
}

/** Waits for the next `CollectCreatorFeeEvent` event. Resolves `null` if the timeout expires. */
export async function waitForCollectCreatorFeeEvent(
  program: any,
  options?: { timeoutMs?: number }
): Promise<CollectCreatorFeeEvent | null> {
  return program.waitForEvent("CollectCreatorFeeEvent", options) as Promise<CollectCreatorFeeEvent | null>;
}

/** Decodes every `CollectCreatorFeeEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForCollectCreatorFeeEvent`/`addCollectCreatorFeeEventListener` — prefer this when you already know which transaction you're checking. */
export function parseCollectCreatorFeeEventEvents(
  program: any,
  logs: readonly string[]
): CollectCreatorFeeEvent[] {
  return program.parseEvents("CollectCreatorFeeEvent", logs) as CollectCreatorFeeEvent[];
}

/** 8-byte discriminator for `InitUserVolumeAccumulatorEvent` events in transaction logs. */
export const INITUSERVOLUMEACCUMULATOREVENT_EVENT_DISCRIMINATOR = new Uint8Array([134, 36, 13, 72, 232, 101, 130, 216]);

/** Auto-generated event interface from the program IDL. */
export interface InitUserVolumeAccumulatorEvent {
  payer: naclac.Address | string;
  user: naclac.Address | string;
  timestamp: bigint | number;
}

/** Subscribes to `InitUserVolumeAccumulatorEvent` events. Returns a listener ID for cleanup. */
export function addInitUserVolumeAccumulatorEventListener(
  program: any,
  callback: (event: InitUserVolumeAccumulatorEvent, slot: number, signature: string) => void
): number {
  return program.addEventListener("InitUserVolumeAccumulatorEvent", callback);
}

/** Waits for the next `InitUserVolumeAccumulatorEvent` event. Resolves `null` if the timeout expires. */
export async function waitForInitUserVolumeAccumulatorEvent(
  program: any,
  options?: { timeoutMs?: number }
): Promise<InitUserVolumeAccumulatorEvent | null> {
  return program.waitForEvent("InitUserVolumeAccumulatorEvent", options) as Promise<InitUserVolumeAccumulatorEvent | null>;
}

/** Decodes every `InitUserVolumeAccumulatorEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForInitUserVolumeAccumulatorEvent`/`addInitUserVolumeAccumulatorEventListener` — prefer this when you already know which transaction you're checking. */
export function parseInitUserVolumeAccumulatorEventEvents(
  program: any,
  logs: readonly string[]
): InitUserVolumeAccumulatorEvent[] {
  return program.parseEvents("InitUserVolumeAccumulatorEvent", logs) as InitUserVolumeAccumulatorEvent[];
}

/** 8-byte discriminator for `CloseUserVolumeAccumulatorEvent` events in transaction logs. */
export const CLOSEUSERVOLUMEACCUMULATOREVENT_EVENT_DISCRIMINATOR = new Uint8Array([146, 159, 189, 172, 146, 88, 56, 244]);

/** Auto-generated event interface from the program IDL. */
export interface CloseUserVolumeAccumulatorEvent {
  user: naclac.Address | string;
  timestamp: bigint | number;
  totalUnclaimedTokens: bigint | number;
  totalClaimedTokens: bigint | number;
  currentSolVolume: bigint | number;
  lastUpdateTimestamp: bigint | number;
}

/** Subscribes to `CloseUserVolumeAccumulatorEvent` events. Returns a listener ID for cleanup. */
export function addCloseUserVolumeAccumulatorEventListener(
  program: any,
  callback: (event: CloseUserVolumeAccumulatorEvent, slot: number, signature: string) => void
): number {
  return program.addEventListener("CloseUserVolumeAccumulatorEvent", callback);
}

/** Waits for the next `CloseUserVolumeAccumulatorEvent` event. Resolves `null` if the timeout expires. */
export async function waitForCloseUserVolumeAccumulatorEvent(
  program: any,
  options?: { timeoutMs?: number }
): Promise<CloseUserVolumeAccumulatorEvent | null> {
  return program.waitForEvent("CloseUserVolumeAccumulatorEvent", options) as Promise<CloseUserVolumeAccumulatorEvent | null>;
}

/** Decodes every `CloseUserVolumeAccumulatorEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForCloseUserVolumeAccumulatorEvent`/`addCloseUserVolumeAccumulatorEventListener` — prefer this when you already know which transaction you're checking. */
export function parseCloseUserVolumeAccumulatorEventEvents(
  program: any,
  logs: readonly string[]
): CloseUserVolumeAccumulatorEvent[] {
  return program.parseEvents("CloseUserVolumeAccumulatorEvent", logs) as CloseUserVolumeAccumulatorEvent[];
}

/** 8-byte discriminator for `ClaimCashbackEvent` events in transaction logs. */
export const CLAIMCASHBACKEVENT_EVENT_DISCRIMINATOR = new Uint8Array([226, 214, 246, 33, 7, 242, 147, 229]);

/** Auto-generated event interface from the program IDL. */
export interface ClaimCashbackEvent {
  user: naclac.Address | string;
  amount: bigint | number;
  timestamp: bigint | number;
  totalClaimed: bigint | number;
  totalCashbackEarned: bigint | number;
}

/** Subscribes to `ClaimCashbackEvent` events. Returns a listener ID for cleanup. */
export function addClaimCashbackEventListener(
  program: any,
  callback: (event: ClaimCashbackEvent, slot: number, signature: string) => void
): number {
  return program.addEventListener("ClaimCashbackEvent", callback);
}

/** Waits for the next `ClaimCashbackEvent` event. Resolves `null` if the timeout expires. */
export async function waitForClaimCashbackEvent(
  program: any,
  options?: { timeoutMs?: number }
): Promise<ClaimCashbackEvent | null> {
  return program.waitForEvent("ClaimCashbackEvent", options) as Promise<ClaimCashbackEvent | null>;
}

/** Decodes every `ClaimCashbackEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForClaimCashbackEvent`/`addClaimCashbackEventListener` — prefer this when you already know which transaction you're checking. */
export function parseClaimCashbackEventEvents(
  program: any,
  logs: readonly string[]
): ClaimCashbackEvent[] {
  return program.parseEvents("ClaimCashbackEvent", logs) as ClaimCashbackEvent[];
}

/** 8-byte discriminator for `ClaimTokenIncentivesEvent` events in transaction logs. */
export const CLAIMTOKENINCENTIVESEVENT_EVENT_DISCRIMINATOR = new Uint8Array([79, 172, 246, 49, 205, 91, 206, 232]);

/** Auto-generated event interface from the program IDL. */
export interface ClaimTokenIncentivesEvent {
  user: naclac.Address | string;
  mint: naclac.Address | string;
  amount: bigint | number;
  timestamp: bigint | number;
  totalClaimedTokens: bigint | number;
  currentSolVolume: bigint | number;
}

/** Subscribes to `ClaimTokenIncentivesEvent` events. Returns a listener ID for cleanup. */
export function addClaimTokenIncentivesEventListener(
  program: any,
  callback: (event: ClaimTokenIncentivesEvent, slot: number, signature: string) => void
): number {
  return program.addEventListener("ClaimTokenIncentivesEvent", callback);
}

/** Waits for the next `ClaimTokenIncentivesEvent` event. Resolves `null` if the timeout expires. */
export async function waitForClaimTokenIncentivesEvent(
  program: any,
  options?: { timeoutMs?: number }
): Promise<ClaimTokenIncentivesEvent | null> {
  return program.waitForEvent("ClaimTokenIncentivesEvent", options) as Promise<ClaimTokenIncentivesEvent | null>;
}

/** Decodes every `ClaimTokenIncentivesEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForClaimTokenIncentivesEvent`/`addClaimTokenIncentivesEventListener` — prefer this when you already know which transaction you're checking. */
export function parseClaimTokenIncentivesEventEvents(
  program: any,
  logs: readonly string[]
): ClaimTokenIncentivesEvent[] {
  return program.parseEvents("ClaimTokenIncentivesEvent", logs) as ClaimTokenIncentivesEvent[];
}

/** Removes a previously registered event listener. */
export function removeListener(program: any, listenerId: number) {
  program.removeEventListener(listenerId);
}
