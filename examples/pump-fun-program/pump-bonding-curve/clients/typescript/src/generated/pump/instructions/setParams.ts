// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";
import * as types from "../types/typedefs";

/** 8-byte discriminator for the `set_params` instruction. */
export const SET_PARAMS_DISCRIMINATOR = new Uint8Array([27, 234, 178, 52, 147, 2, 187, 141]);

/** Instruction arguments for `setParams`. */
export interface SetParamsArgs {
  args: types.SetParamsArgs;
}

/** Accounts for the `setParams` instruction. */
export interface SetParamsAccounts {
  global?: naclac.Address | string;
  authority: naclac.Address | string;
}

/**
 * Builds the `setParams` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function setParams(
  program: any,
  args: SetParamsArgs,
  accounts?: Partial<SetParamsAccounts>
) {
  const builder = program.methods.set_params(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
