// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `close_user_volume_accumulator` instruction. */
export const CLOSE_USER_VOLUME_ACCUMULATOR_DISCRIMINATOR = new Uint8Array([249, 69, 164, 218, 150, 103, 84, 138]);

/** Accounts for the `closeUserVolumeAccumulator` instruction. */
export interface CloseUserVolumeAccumulatorAccounts {
  user: naclac.Address | string;
  userVolumeAccumulator?: naclac.Address | string;
}

/**
 * Builds the `closeUserVolumeAccumulator` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 * 
 * Closes `user`'s own `UserVolumeAccumulator`, reclaiming its rent to
 * `user`. Unconditional -- no precondition on any pending reward field,
 * confirmed against the real deployed program.
 */
export function closeUserVolumeAccumulator(
  program: any,
  args?: Record<string, never>,
  accounts?: Partial<CloseUserVolumeAccumulatorAccounts>
) {
  const builder = program.methods.close_user_volume_accumulator(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
