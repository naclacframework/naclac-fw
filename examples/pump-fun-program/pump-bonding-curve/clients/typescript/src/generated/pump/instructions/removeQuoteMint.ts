// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `remove_quote_mint` instruction. */
export const REMOVE_QUOTE_MINT_DISCRIMINATOR = new Uint8Array([177, 65, 223, 38, 88, 209, 158, 155]);

/** Instruction arguments for `removeQuoteMint`. */
export interface RemoveQuoteMintArgs {
  quoteMint: naclac.Address | string;
}

/** Accounts for the `removeQuoteMint` instruction. */
export interface RemoveQuoteMintAccounts {
  global?: naclac.Address | string;
  authority: naclac.Address | string;
}

/**
 * Builds the `removeQuoteMint` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function removeQuoteMint(
  program: any,
  args: RemoveQuoteMintArgs,
  accounts?: Partial<RemoveQuoteMintAccounts>
) {
  const builder = program.methods.remove_quote_mint(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
