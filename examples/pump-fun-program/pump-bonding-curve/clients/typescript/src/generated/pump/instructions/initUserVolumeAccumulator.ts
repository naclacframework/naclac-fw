// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `init_user_volume_accumulator` instruction. */
export const INIT_USER_VOLUME_ACCUMULATOR_DISCRIMINATOR = new Uint8Array([94, 6, 202, 115, 255, 96, 232, 183]);

/** Instruction arguments for `initUserVolumeAccumulator`. */
export interface InitUserVolumeAccumulatorArgs {
  userVolumeAccumulatorBump: number;
}

/** Accounts for the `initUserVolumeAccumulator` instruction. */
export interface InitUserVolumeAccumulatorAccounts {
  payer: naclac.Address | string;
  /**
   * SAFETY: only used as PDA seed material for `user_volume_accumulator`
   * below; never read or invoked.
   */
  user: naclac.Address | string;
  userVolumeAccumulator?: naclac.Address | string;
  systemProgram?: naclac.Address | string;
}

/**
 * Builds the `initUserVolumeAccumulator` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 * 
 * Creates a brand-new, zeroed `UserVolumeAccumulator` PDA for `user`,
 * funded by `payer` -- permissionless, matching the real program's own
 * account list (no signer requirement on `user` at all).
 */
export function initUserVolumeAccumulator(
  program: any,
  args: InitUserVolumeAccumulatorArgs,
  accounts?: Partial<InitUserVolumeAccumulatorAccounts>
) {
  const builder = program.methods.init_user_volume_accumulator(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
