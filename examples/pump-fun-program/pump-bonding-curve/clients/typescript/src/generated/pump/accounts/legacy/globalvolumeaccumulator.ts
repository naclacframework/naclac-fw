// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client/legacy";
import type { GlobalVolumeAccumulator } from "../../types";

/** 8-byte on-chain discriminator for `GlobalVolumeAccumulator` accounts. */
export const GLOBALVOLUMEACCUMULATOR_DISCRIMINATOR = new Uint8Array([202, 42, 246, 43, 142, 190, 30, 255]);

/** Fetches a `GlobalVolumeAccumulator` account. Throws if it does not exist. */
export async function fetchGlobalVolumeAccumulator(
  program: naclac.LegacyProgram,
  address: naclac.PublicKey | string,
): Promise<GlobalVolumeAccumulator> {
  const result = await program.account['GlobalVolumeAccumulator'].fetch(address);
  return result as unknown as GlobalVolumeAccumulator;
}

/** Fetches a `GlobalVolumeAccumulator` account. Returns `null` if it does not exist. */
export async function fetchMaybeGlobalVolumeAccumulator(
  program: naclac.LegacyProgram,
  address: naclac.PublicKey | string,
): Promise<GlobalVolumeAccumulator | null> {
  try {
    const result = await program.account['GlobalVolumeAccumulator'].fetch(address);
    return result as unknown as GlobalVolumeAccumulator;
  } catch {
    return null;
  }
}

/** Fetches multiple `GlobalVolumeAccumulator` accounts. Missing accounts are `null`. */
export async function fetchAllGlobalVolumeAccumulator(
  program: naclac.LegacyProgram,
  addresses: Array<naclac.PublicKey | string>,
  opts?: { dropCorrupted?: boolean },
): Promise<Array<GlobalVolumeAccumulator | null>> {
  const results = await program.account['GlobalVolumeAccumulator'].fetchMultiple(addresses, opts);
  return results as unknown as Array<GlobalVolumeAccumulator | null>;
}

/** Fetches all on-chain `GlobalVolumeAccumulator` accounts owned by this program. */
export async function fetchAllGlobalVolumeAccumulatorByOwner(
  program: naclac.LegacyProgram,
  options?: { dropCorrupted?: boolean; filters?: any[] },
): Promise<Array<{ publicKey: naclac.PublicKey; account: GlobalVolumeAccumulator }>> {
  const results = await program.account['GlobalVolumeAccumulator'].all(options);
  return results as unknown as Array<{ publicKey: naclac.PublicKey; account: GlobalVolumeAccumulator }>;
}
