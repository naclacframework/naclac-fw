// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client/legacy";
import type { Global } from "../../types";

/** 8-byte on-chain discriminator for `Global` accounts. */
export const GLOBAL_DISCRIMINATOR = new Uint8Array([167, 232, 232, 177, 200, 108, 114, 127]);

/** Fetches a `Global` account. Throws if it does not exist. */
export async function fetchGlobal(
  program: naclac.LegacyProgram,
  address: naclac.PublicKey | string,
): Promise<Global> {
  const result = await program.account['Global'].fetch(address);
  return result as unknown as Global;
}

/** Fetches a `Global` account. Returns `null` if it does not exist. */
export async function fetchMaybeGlobal(
  program: naclac.LegacyProgram,
  address: naclac.PublicKey | string,
): Promise<Global | null> {
  try {
    const result = await program.account['Global'].fetch(address);
    return result as unknown as Global;
  } catch {
    return null;
  }
}

/** Fetches multiple `Global` accounts. Missing accounts are `null`. */
export async function fetchAllGlobal(
  program: naclac.LegacyProgram,
  addresses: Array<naclac.PublicKey | string>,
  opts?: { dropCorrupted?: boolean },
): Promise<Array<Global | null>> {
  const results = await program.account['Global'].fetchMultiple(addresses, opts);
  return results as unknown as Array<Global | null>;
}

/** Fetches all on-chain `Global` accounts owned by this program. */
export async function fetchAllGlobalByOwner(
  program: naclac.LegacyProgram,
  options?: { dropCorrupted?: boolean; filters?: any[] },
): Promise<Array<{ publicKey: naclac.PublicKey; account: Global }>> {
  const results = await program.account['Global'].all(options);
  return results as unknown as Array<{ publicKey: naclac.PublicKey; account: Global }>;
}
