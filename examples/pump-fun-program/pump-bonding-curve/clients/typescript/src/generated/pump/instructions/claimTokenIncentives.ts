// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";
import * as types from "../types/typedefs";

/** 8-byte discriminator for the `claim_token_incentives` instruction. */
export const CLAIM_TOKEN_INCENTIVES_DISCRIMINATOR = new Uint8Array([16, 4, 71, 28, 204, 1, 40, 27]);

/** Instruction arguments for `claimTokenIncentives`. */
export interface ClaimTokenIncentivesArgs {
  args: types.ClaimTokenIncentivesArgs;
}

/** Accounts for the `claimTokenIncentives` instruction. */
export interface ClaimTokenIncentivesAccounts {
  /**
   * SAFETY: only PDA seed material for `user_ata`/`user_volume_accumulator`
   * below; never deserialized.
   */
  user: naclac.Address | string;
  userAta: naclac.Address | string;
  globalVolumeAccumulator?: naclac.Address | string;
  globalIncentiveTokenAccount: naclac.Address | string;
  userVolumeAccumulator?: naclac.Address | string;
  mint: naclac.Address | string;
  tokenProgram: naclac.Address | string;
  systemProgram?: naclac.Address | string;
  associatedTokenProgram?: naclac.Address | string;
  payer: naclac.Address | string;
}

/**
 * Builds the `claimTokenIncentives` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 * 
 * Sweeps `user_volume_accumulator`'s full `total_unclaimed_tokens` balance
 * out of the shared `global_incentive_token_account` pool into `user_ata`,
 * signed by `global_volume_accumulator`'s own literal-seed PDA authority
 * (same `bare bump` + `ctx.bumps.*` idiom already established for
 * `pump_fees_authority` elsewhere in this program). Mirrors
 * `collect_creator_fee_v2`'s full-balance-sweep shape (no partial claims).
 */
export function claimTokenIncentives(
  program: any,
  args: ClaimTokenIncentivesArgs,
  accounts?: Partial<ClaimTokenIncentivesAccounts>
) {
  const builder = program.methods.claim_token_incentives(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
