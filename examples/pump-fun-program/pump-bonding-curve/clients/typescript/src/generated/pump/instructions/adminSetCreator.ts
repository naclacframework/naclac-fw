// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `admin_set_creator` instruction. */
export const ADMIN_SET_CREATOR_DISCRIMINATOR = new Uint8Array([69, 25, 171, 142, 57, 239, 13, 4]);

/** Instruction arguments for `adminSetCreator`. */
export interface AdminSetCreatorArgs {
  creator: naclac.Address | string;
  bondingCurveBump: number;
}

/** Accounts for the `adminSetCreator` instruction. */
export interface AdminSetCreatorAccounts {
  adminSetCreatorAuthority: naclac.Address | string;
  global?: naclac.Address | string;
  /**
   * SAFETY: only used as a seed input for `bonding_curve` below, never
   * read or written -- a wrong value just fails that seed check.
   */
  mint: naclac.Address | string;
  bondingCurve?: naclac.Address | string;
}

/**
 * Builds the `adminSetCreator` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function adminSetCreator(
  program: any,
  args: AdminSetCreatorArgs,
  accounts?: Partial<AdminSetCreatorAccounts>
) {
  const builder = program.methods.admin_set_creator(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
