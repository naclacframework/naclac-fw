// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `add_quote_mint` instruction. */
export const ADD_QUOTE_MINT_DISCRIMINATOR = new Uint8Array([111, 121, 21, 56, 40, 24, 94, 209]);

/** Instruction arguments for `addQuoteMint`. */
export interface AddQuoteMintArgs {
  quoteMint: naclac.Address | string;
}

/** Accounts for the `addQuoteMint` instruction. */
export interface AddQuoteMintAccounts {
  global?: naclac.Address | string;
  authority: naclac.Address | string;
}

/**
 * Builds the `addQuoteMint` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function addQuoteMint(
  program: any,
  args: AddQuoteMintArgs,
  accounts?: Partial<AddQuoteMintAccounts>
) {
  const builder = program.methods.add_quote_mint(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
