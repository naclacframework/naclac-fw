// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `migrate_bonding_curve_creator` instruction. */
export const MIGRATE_BONDING_CURVE_CREATOR_DISCRIMINATOR = new Uint8Array([87, 124, 52, 191, 52, 38, 214, 232]);

/** Instruction arguments for `migrateBondingCurveCreator`. */
export interface MigrateBondingCurveCreatorArgs {
  bondingCurveBump: number;
  sharingConfigBump: number;
}

/** Accounts for the `migrateBondingCurveCreator` instruction. */
export interface MigrateBondingCurveCreatorAccounts {
  /**
   * SAFETY: only used as a seed input for `bonding_curve`/`sharing_config` below, never
   * read or written — a wrong value just fails those seed checks.
   */
  mint: naclac.Address | string;
  bondingCurve?: naclac.Address | string;
  /**
   * SAFETY: the `seeds`/`bump`/`seeds::program` constraint already verifies its address;
   * only the address is used below, never its data, so no further validation is needed.
   */
  sharingConfig?: naclac.Address | string;
  /**
   * SAFETY: `signer` + the `seeds`/`seeds::program` constraint together
   * prove this call was CPI'd (via `invoke_signed`) by `pump_fees` itself
   * — only that program can ever produce a valid signature for its own
   * `PUMP_FEES_AUTHORITY_SEED` PDA. This is the entire authorization
   * model for this instruction; never deserialized.
   */
  pumpFeesAuthority?: naclac.Address | string;
}

/**
 * Builds the `migrateBondingCurveCreator` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function migrateBondingCurveCreator(
  program: any,
  args: MigrateBondingCurveCreatorArgs,
  accounts?: Partial<MigrateBondingCurveCreatorAccounts>
) {
  const builder = program.methods.migrate_bonding_curve_creator(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
