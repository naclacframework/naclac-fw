// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";
import * as types from "../types/typedefs";

/** 8-byte discriminator for the `collect_creator_fee_v2` instruction. */
export const COLLECT_CREATOR_FEE_V2_DISCRIMINATOR = new Uint8Array([207, 17, 138, 242, 4, 34, 19, 56]);

/** Instruction arguments for `collectCreatorFeeV2`. */
export interface CollectCreatorFeeV2Args {
  args: types.CollectCreatorFeeV2Args;
}

/** Accounts for the `collectCreatorFeeV2` instruction. */
export interface CollectCreatorFeeV2Accounts {
  /**
   * SAFETY: only PDA seed material for `creator_vault`/`creator_token_account`
   * below; never deserialized.
   */
  creator: naclac.Address | string;
  creatorTokenAccount: naclac.Address | string;
  /**
   * SAFETY: the `seeds`/`bump` constraint already verifies its address;
   * it's a lamport-only PDA (no stored data) — only ever a CPI signer
   * below, never deserialized.
   */
  creatorVault?: naclac.Address | string;
  creatorVaultTokenAccount: naclac.Address | string;
  quoteMint: naclac.Address | string;
  quoteTokenProgram: naclac.Address | string;
  associatedTokenProgram?: naclac.Address | string;
  systemProgram?: naclac.Address | string;
}

/**
 * Builds the `collectCreatorFeeV2` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 * 
 * Sweeps `creator_vault_token_account`'s full SPL-token balance to
 * `creator_token_account` — the SPL-token sibling of `collect_creator_fee`
 * (native SOL). Permissionless: anyone may crank it, funds always land at
 * the fixed `creator_vault_token_account` -> `creator_token_account` pair.
 * Unlike the native path there's no rent-exempt floor to preserve (an SPL
 * token account's rent is a fixed lamport reserve separate from its token
 * balance), so the entire token balance is swept.
 */
export function collectCreatorFeeV2(
  program: any,
  args: CollectCreatorFeeV2Args,
  accounts?: Partial<CollectCreatorFeeV2Accounts>
) {
  const builder = program.methods.collect_creator_fee_v2(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
