// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client/legacy";
import type { UserVolumeAccumulator } from "../../types";

/** 8-byte on-chain discriminator for `UserVolumeAccumulator` accounts. */
export const USERVOLUMEACCUMULATOR_DISCRIMINATOR = new Uint8Array([86, 255, 112, 14, 102, 53, 154, 250]);

/** Fetches a `UserVolumeAccumulator` account. Throws if it does not exist. */
export async function fetchUserVolumeAccumulator(
  program: naclac.LegacyProgram,
  address: naclac.PublicKey | string,
): Promise<UserVolumeAccumulator> {
  const result = await program.account['UserVolumeAccumulator'].fetch(address);
  return result as unknown as UserVolumeAccumulator;
}

/** Fetches a `UserVolumeAccumulator` account. Returns `null` if it does not exist. */
export async function fetchMaybeUserVolumeAccumulator(
  program: naclac.LegacyProgram,
  address: naclac.PublicKey | string,
): Promise<UserVolumeAccumulator | null> {
  try {
    const result = await program.account['UserVolumeAccumulator'].fetch(address);
    return result as unknown as UserVolumeAccumulator;
  } catch {
    return null;
  }
}

/** Fetches multiple `UserVolumeAccumulator` accounts. Missing accounts are `null`. */
export async function fetchAllUserVolumeAccumulator(
  program: naclac.LegacyProgram,
  addresses: Array<naclac.PublicKey | string>,
  opts?: { dropCorrupted?: boolean },
): Promise<Array<UserVolumeAccumulator | null>> {
  const results = await program.account['UserVolumeAccumulator'].fetchMultiple(addresses, opts);
  return results as unknown as Array<UserVolumeAccumulator | null>;
}

/** Fetches all on-chain `UserVolumeAccumulator` accounts owned by this program. */
export async function fetchAllUserVolumeAccumulatorByOwner(
  program: naclac.LegacyProgram,
  options?: { dropCorrupted?: boolean; filters?: any[] },
): Promise<Array<{ publicKey: naclac.PublicKey; account: UserVolumeAccumulator }>> {
  const results = await program.account['UserVolumeAccumulator'].all(options);
  return results as unknown as Array<{ publicKey: naclac.PublicKey; account: UserVolumeAccumulator }>;
}
