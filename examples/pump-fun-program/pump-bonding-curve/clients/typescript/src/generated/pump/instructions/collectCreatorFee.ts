// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `collect_creator_fee` instruction. */
export const COLLECT_CREATOR_FEE_DISCRIMINATOR = new Uint8Array([20, 22, 86, 123, 198, 28, 219, 132]);

/** Instruction arguments for `collectCreatorFee`. */
export interface CollectCreatorFeeArgs {
  creatorVaultBump: number;
}

/** Accounts for the `collectCreatorFee` instruction. */
export interface CollectCreatorFeeAccounts {
  /**
   * SAFETY: only a native-lamport transfer destination and PDA seed
   * material for `creator_vault` below; never deserialized.
   */
  creator: naclac.Address | string;
  /**
   * SAFETY: the `seeds`/`bump` constraint already verifies its address;
   * it's a lamport-only PDA (no stored data) — only ever a lamport source
   * below via a signed System Program transfer, never deserialized.
   */
  creatorVault?: naclac.Address | string;
  systemProgram?: naclac.Address | string;
}

/**
 * Builds the `collectCreatorFee` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 * 
 * Sweeps `creator_vault` down to its rent-exempt minimum, paying the full
 * swept amount to `creator` — the native-SOL sibling of
 * `distribute_creator_fees`'s own rent-exempt-floor sweep, just without a
 * shareholder split (100% to `creator`). Permissionless: anyone may crank
 * it, funds always land at the fixed `creator_vault` -> `creator` PDA pair.
 */
export function collectCreatorFee(
  program: any,
  args: CollectCreatorFeeArgs,
  accounts?: Partial<CollectCreatorFeeAccounts>
) {
  const builder = program.methods.collect_creator_fee(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
