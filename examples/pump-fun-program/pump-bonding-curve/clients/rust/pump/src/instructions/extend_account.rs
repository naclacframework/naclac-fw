// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

#[cfg(feature = "offchain")]
/// Grows a pump-owned account's data buffer to match its currently
/// compiled-in size, funding the extra rent from `user`. `account` stays a
/// raw `AccountInfo` (never a typed `Account<T>`) deliberately: a
/// still-undersized account -- the very thing being extended -- fails
/// `Account<T>`'s own size check before this instruction's `realloc`
/// constraint (let alone its body) ever runs (confirmed empirically:
/// `AccountDataTooSmall` from a typed field against this exact scenario).
/// Recognizes every component this program itself owns; add a new type to
/// the `realloc::any_of` list above when a new component needs the same
/// treatment.
pub struct ExtendAccountAccounts {
    /// SAFETY: never deserialized as a typed component -- that's the whole
    /// point (a still-undersized account would fail a typed load before
    /// this instruction's `realloc` constraint ever ran). The `owner`
    /// constraint is the only real validation of the account itself;
    /// `realloc::any_of` identifies its type from its own raw discriminator
    /// and resizes it to that type's current compiled size.
    pub account: naclac_client::Address,
    pub user: naclac_client::Address,
    pub system_program: naclac_client::Address,
}

#[cfg(feature = "offchain")]
/// Grows a pump-owned account's data buffer to match its currently
/// compiled-in size, funding the extra rent from `user`. `account` stays a
/// raw `AccountInfo` (never a typed `Account<T>`) deliberately: a
/// still-undersized account -- the very thing being extended -- fails
/// `Account<T>`'s own size check before this instruction's `realloc`
/// constraint (let alone its body) ever runs (confirmed empirically:
/// `AccountDataTooSmall` from a typed field against this exact scenario).
/// Recognizes every component this program itself owns; add a new type to
/// the `realloc::any_of` list above when a new component needs the same
/// treatment.
pub fn build_extend_account<'a>(
    provider: &'a naclac_client::NaclacProvider,
    program_id: naclac_client::Address,
    accounts: ExtendAccountAccounts,
) -> naclac_client::InstructionBuilder<'a> {
    let ix_data = crate::sdk_core_offchain::vec![234, 102, 194, 203, 150, 72, 62, 229];
    naclac_client::InstructionBuilder::new(provider, program_id, ix_data)
        .account(naclac_client::AccountMeta { address: accounts.account, is_signer: false, is_writable: true }, "account")
        .account(naclac_client::AccountMeta { address: accounts.user, is_signer: true, is_writable: true }, "user")
        .account(naclac_client::AccountMeta { address: accounts.system_program, is_signer: false, is_writable: false }, "system_program")
}

#[cfg(feature = "cpi")]
/// Grows a pump-owned account's data buffer to match its currently
/// compiled-in size, funding the extra rent from `user`. `account` stays a
/// raw `AccountInfo` (never a typed `Account<T>`) deliberately: a
/// still-undersized account -- the very thing being extended -- fails
/// `Account<T>`'s own size check before this instruction's `realloc`
/// constraint (let alone its body) ever runs (confirmed empirically:
/// `AccountDataTooSmall` from a typed field against this exact scenario).
/// Recognizes every component this program itself owns; add a new type to
/// the `realloc::any_of` list above when a new component needs the same
/// treatment.
pub struct ExtendAccountCpiAccounts<'a> {
    /// SAFETY: never deserialized as a typed component -- that's the whole
    /// point (a still-undersized account would fail a typed load before
    /// this instruction's `realloc` constraint ever ran). The `owner`
    /// constraint is the only real validation of the account itself;
    /// `realloc::any_of` identifies its type from its own raw discriminator
    /// and resizes it to that type's current compiled size.
    pub account: crate::sdk_core_cpi::CpiHandleMut<'a>,
    pub user: crate::sdk_core_cpi::CpiHandleMut<'a>,
    pub system_program: crate::sdk_core_cpi::CpiHandle<'a>,
}

#[cfg(feature = "cpi")]
pub struct ExtendAccountCpiCall<'a> {
    pub accounts: ExtendAccountCpiAccounts<'a>,
    pub signer_seeds: &'a [&'a [&'a [u8]]],
    pub remaining_accounts: &'a [(crate::sdk_core_cpi::AccountInfo, bool, bool)],
}

#[cfg(feature = "cpi")]
pub trait ExtendAccountCpi<'info> {
    /// Grows a pump-owned account's data buffer to match its currently
    /// compiled-in size, funding the extra rent from `user`. `account` stays a
    /// raw `AccountInfo` (never a typed `Account<T>`) deliberately: a
    /// still-undersized account -- the very thing being extended -- fails
    /// `Account<T>`'s own size check before this instruction's `realloc`
    /// constraint (let alone its body) ever runs (confirmed empirically:
    /// `AccountDataTooSmall` from a typed field against this exact scenario).
    /// Recognizes every component this program itself owns; add a new type to
    /// the `realloc::any_of` list above when a new component needs the same
    /// treatment.
    fn extend_account<'a>(
        &self,
        accounts: ExtendAccountCpiAccounts<'a>
    ) -> crate::sdk_core_cpi::Result<()> {
        self.extend_account_with_remaining_accounts(ExtendAccountCpiCall {
        accounts,
        signer_seeds: &[],
        remaining_accounts: &[],
    })
    }

    fn extend_account_signed<'a>(
        &self,
        accounts: ExtendAccountCpiAccounts<'a>,
        signer_seeds: &[&[&[u8]]]
    ) -> crate::sdk_core_cpi::Result<()> {
        self.extend_account_with_remaining_accounts(ExtendAccountCpiCall {
        accounts,
        signer_seeds,
        remaining_accounts: &[],
    })
    }

    fn extend_account_with_remaining_accounts<'a>(
        &self,
        call: ExtendAccountCpiCall<'a>
    ) -> crate::sdk_core_cpi::Result<()>;

}

#[cfg(feature = "cpi")]
impl<'info> ExtendAccountCpi<'info> for crate::sdk_core_cpi::Program<crate::Pump> {
    fn extend_account_with_remaining_accounts<'a>(
        &self,
        call: ExtendAccountCpiCall<'a>
    ) -> crate::sdk_core_cpi::Result<()> {
        let ExtendAccountCpiCall { accounts, signer_seeds, remaining_accounts } = call;
        let mut ix_data = crate::sdk_core_cpi::Vec::new();
        ix_data.extend_from_slice(&[234, 102, 194, 203, 150, 72, 62, 229]);
        #[cfg(feature = "pinocchio")]
        {
            let mut metas_arr = [const { core::mem::MaybeUninit::<crate::sdk_core_cpi::pinocchio::instruction::InstructionAccount>::uninit() }; 32];
            let mut idx = 0;
            metas_arr[idx].write(crate::sdk_core_cpi::pinocchio::instruction::InstructionAccount::writable(accounts.account.info.view.address()));
            idx += 1;
            metas_arr[idx].write(crate::sdk_core_cpi::pinocchio::instruction::InstructionAccount::writable_signer(accounts.user.info.view.address()));
            idx += 1;
            metas_arr[idx].write(crate::sdk_core_cpi::pinocchio::instruction::InstructionAccount::readonly(accounts.system_program.info.view.address()));
            idx += 1;
            for (acc, is_writable, is_signer) in remaining_accounts {
                if idx >= 32 { break; }
                let meta = if *is_writable {
                    if *is_signer {
                        crate::sdk_core_cpi::pinocchio::instruction::InstructionAccount::writable_signer(acc.view.address())
                    } else {
                        crate::sdk_core_cpi::pinocchio::instruction::InstructionAccount::writable(acc.view.address())
                    }
                } else {
                    if *is_signer {
                        crate::sdk_core_cpi::pinocchio::instruction::InstructionAccount::readonly_signer(acc.view.address())
                    } else {
                        crate::sdk_core_cpi::pinocchio::instruction::InstructionAccount::readonly(acc.view.address())
                    }
                };
                metas_arr[idx].write(meta);
                idx += 1;
            }
            let account_metas = unsafe { core::slice::from_raw_parts(metas_arr.as_ptr() as *const crate::sdk_core_cpi::pinocchio::instruction::InstructionAccount, idx) };
            let program_id_address = self.address();
            let instruction = crate::sdk_core_cpi::pinocchio::instruction::InstructionView {
                program_id: program_id_address.as_address(),
                accounts: account_metas,
                data: &ix_data,
            };
            let mut handles_arr = [const { core::mem::MaybeUninit::<crate::sdk_core_cpi::CpiHandle>::uninit() }; 32];
            let mut h_idx = 0;
            handles_arr[h_idx].write(crate::sdk_core_cpi::CpiHandle::from(accounts.account));
            h_idx += 1;
            handles_arr[h_idx].write(crate::sdk_core_cpi::CpiHandle::from(accounts.user));
            h_idx += 1;
            handles_arr[h_idx].write(accounts.system_program);
            h_idx += 1;
            for (acc, _, _) in remaining_accounts {
                if h_idx >= 32 { break; }
                handles_arr[h_idx].write(crate::sdk_core_cpi::ToCpiHandle::to_cpi_handle(acc));
                h_idx += 1;
            }
            let account_handles = unsafe { core::slice::from_raw_parts(handles_arr.as_ptr() as *const crate::sdk_core_cpi::CpiHandle, h_idx) };
            crate::sdk_core_cpi::cpi::invoke_signed_pinocchio_handles(&instruction, account_handles, signer_seeds)
        }
        #[cfg(not(feature = "pinocchio"))]
        {
            use crate::sdk_core_cpi::ToAddress;
            let mut account_metas = crate::sdk_core_cpi::vec![
                ::naclac_lang::solana_program::instruction::AccountMeta::new(accounts.account.address(), false),
                ::naclac_lang::solana_program::instruction::AccountMeta::new(accounts.user.address(), true),
                ::naclac_lang::solana_program::instruction::AccountMeta::new_readonly(accounts.system_program.address(), false),
            ];
            for (acc, is_writable, is_signer) in remaining_accounts {
                let meta = if *is_writable {
                    ::naclac_lang::solana_program::instruction::AccountMeta::new(acc.address(), *is_signer)
                } else {
                    ::naclac_lang::solana_program::instruction::AccountMeta::new_readonly(acc.address(), *is_signer)
                };
                account_metas.push(meta);
            }
            let instruction = ::naclac_lang::solana_program::instruction::Instruction {
                program_id: self.address(),
                accounts: account_metas,
                data: ix_data,
            };
            let mut handles_arr = [const { core::mem::MaybeUninit::<crate::sdk_core_cpi::CpiHandle>::uninit() }; 32];
            let mut h_idx = 0;
            handles_arr[h_idx].write(crate::sdk_core_cpi::CpiHandle::from(accounts.account));
            h_idx += 1;
            handles_arr[h_idx].write(crate::sdk_core_cpi::CpiHandle::from(accounts.user));
            h_idx += 1;
            handles_arr[h_idx].write(accounts.system_program);
            h_idx += 1;
            for (acc, _, _) in remaining_accounts {
                if h_idx >= 32 { break; }
                handles_arr[h_idx].write(crate::sdk_core_cpi::ToCpiHandle::to_cpi_handle(acc));
                h_idx += 1;
            }
            let account_handles = unsafe { core::slice::from_raw_parts(handles_arr.as_ptr() as *const crate::sdk_core_cpi::CpiHandle, h_idx) };
            crate::sdk_core_cpi::cpi::invoke_signed(&instruction, account_handles, signer_seeds)
        }
    }
}

