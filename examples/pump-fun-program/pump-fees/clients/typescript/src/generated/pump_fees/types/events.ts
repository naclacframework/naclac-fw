// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";
import { Shareholder, FeeTier, Fees } from "./typedefs";

/** 8-byte discriminator for `CreateFeeSharingConfigEvent` events in transaction logs. */
export const CREATEFEESHARINGCONFIGEVENT_EVENT_DISCRIMINATOR = new Uint8Array([133, 105, 170, 200, 184, 116, 251, 88]);

/** Auto-generated event interface from the program IDL. */
export interface CreateFeeSharingConfigEvent {
  timestamp: bigint | number;
  mint: naclac.Address | string;
  bonding_curve: naclac.Address | string;
  pool: naclac.Address | string | null;
  sharing_config: naclac.Address | string;
  admin: naclac.Address | string;
  initial_shareholders: Array<Shareholder>;
  status: number;
}

/** Subscribes to `CreateFeeSharingConfigEvent` events. Returns a listener ID for cleanup. */
export function addCreateFeeSharingConfigEventListener(
  program: any,
  callback: (event: CreateFeeSharingConfigEvent, slot: number, signature: string) => void
): number {
  return program.addEventListener("CreateFeeSharingConfigEvent", callback);
}

/** Waits for the next `CreateFeeSharingConfigEvent` event. Resolves `null` if the timeout expires. */
export async function waitForCreateFeeSharingConfigEvent(
  program: any,
  options?: { timeoutMs?: number }
): Promise<CreateFeeSharingConfigEvent | null> {
  return program.waitForEvent("CreateFeeSharingConfigEvent", options) as Promise<CreateFeeSharingConfigEvent | null>;
}

/** Decodes every `CreateFeeSharingConfigEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForCreateFeeSharingConfigEvent`/`addCreateFeeSharingConfigEventListener` — prefer this when you already know which transaction you're checking. */
export function parseCreateFeeSharingConfigEventEvents(
  program: any,
  logs: readonly string[]
): CreateFeeSharingConfigEvent[] {
  return program.parseEvents("CreateFeeSharingConfigEvent", logs) as CreateFeeSharingConfigEvent[];
}

/** 8-byte discriminator for `DonationFeePdaCranked` events in transaction logs. */
export const DONATIONFEEPDACRANKED_EVENT_DISCRIMINATOR = new Uint8Array([30, 208, 107, 93, 177, 0, 223, 78]);

/** Auto-generated event interface from the program IDL. */
export interface DonationFeePdaCranked {
  timestamp: bigint | number;
  amount: bigint | number;
  signer: naclac.Address | string;
  donation_fee_pda: naclac.Address | string;
  config_id: naclac.Address | string;
  base_mint: naclac.Address | string;
  quote_mint: naclac.Address | string;
  creator: naclac.Address | string;
}

/** Subscribes to `DonationFeePdaCranked` events. Returns a listener ID for cleanup. */
export function addDonationFeePdaCrankedListener(
  program: any,
  callback: (event: DonationFeePdaCranked, slot: number, signature: string) => void
): number {
  return program.addEventListener("DonationFeePdaCranked", callback);
}

/** Waits for the next `DonationFeePdaCranked` event. Resolves `null` if the timeout expires. */
export async function waitForDonationFeePdaCranked(
  program: any,
  options?: { timeoutMs?: number }
): Promise<DonationFeePdaCranked | null> {
  return program.waitForEvent("DonationFeePdaCranked", options) as Promise<DonationFeePdaCranked | null>;
}

/** Decodes every `DonationFeePdaCranked` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForDonationFeePdaCranked`/`addDonationFeePdaCrankedListener` — prefer this when you already know which transaction you're checking. */
export function parseDonationFeePdaCrankedEvents(
  program: any,
  logs: readonly string[]
): DonationFeePdaCranked[] {
  return program.parseEvents("DonationFeePdaCranked", logs) as DonationFeePdaCranked[];
}

/** 8-byte discriminator for `DonationFeePdaCreated` events in transaction logs. */
export const DONATIONFEEPDACREATED_EVENT_DISCRIMINATOR = new Uint8Array([94, 20, 137, 239, 35, 77, 225, 235]);

/** Auto-generated event interface from the program IDL. */
export interface DonationFeePdaCreated {
  timestamp: bigint | number;
  created_by: naclac.Address | string;
  donation_fee_pda: naclac.Address | string;
  config_id: naclac.Address | string;
  base_mint: naclac.Address | string;
  quote_mint: naclac.Address | string;
  creator: naclac.Address | string;
}

/** Subscribes to `DonationFeePdaCreated` events. Returns a listener ID for cleanup. */
export function addDonationFeePdaCreatedListener(
  program: any,
  callback: (event: DonationFeePdaCreated, slot: number, signature: string) => void
): number {
  return program.addEventListener("DonationFeePdaCreated", callback);
}

/** Waits for the next `DonationFeePdaCreated` event. Resolves `null` if the timeout expires. */
export async function waitForDonationFeePdaCreated(
  program: any,
  options?: { timeoutMs?: number }
): Promise<DonationFeePdaCreated | null> {
  return program.waitForEvent("DonationFeePdaCreated", options) as Promise<DonationFeePdaCreated | null>;
}

/** Decodes every `DonationFeePdaCreated` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForDonationFeePdaCreated`/`addDonationFeePdaCreatedListener` — prefer this when you already know which transaction you're checking. */
export function parseDonationFeePdaCreatedEvents(
  program: any,
  logs: readonly string[]
): DonationFeePdaCreated[] {
  return program.parseEvents("DonationFeePdaCreated", logs) as DonationFeePdaCreated[];
}

/** 8-byte discriminator for `ExtendFeeConfigEvent` events in transaction logs. */
export const EXTENDFEECONFIGEVENT_EVENT_DISCRIMINATOR = new Uint8Array([226, 203, 224, 35, 153, 10, 88, 51]);

/** Auto-generated event interface from the program IDL. */
export interface ExtendFeeConfigEvent {
  current_size: bigint | number;
  new_size: bigint | number;
  timestamp: bigint | number;
  fee_config: naclac.Address | string;
  user: naclac.Address | string;
}

/** Subscribes to `ExtendFeeConfigEvent` events. Returns a listener ID for cleanup. */
export function addExtendFeeConfigEventListener(
  program: any,
  callback: (event: ExtendFeeConfigEvent, slot: number, signature: string) => void
): number {
  return program.addEventListener("ExtendFeeConfigEvent", callback);
}

/** Waits for the next `ExtendFeeConfigEvent` event. Resolves `null` if the timeout expires. */
export async function waitForExtendFeeConfigEvent(
  program: any,
  options?: { timeoutMs?: number }
): Promise<ExtendFeeConfigEvent | null> {
  return program.waitForEvent("ExtendFeeConfigEvent", options) as Promise<ExtendFeeConfigEvent | null>;
}

/** Decodes every `ExtendFeeConfigEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForExtendFeeConfigEvent`/`addExtendFeeConfigEventListener` — prefer this when you already know which transaction you're checking. */
export function parseExtendFeeConfigEventEvents(
  program: any,
  logs: readonly string[]
): ExtendFeeConfigEvent[] {
  return program.parseEvents("ExtendFeeConfigEvent", logs) as ExtendFeeConfigEvent[];
}

/** 8-byte discriminator for `InitializeFeeConfigEvent` events in transaction logs. */
export const INITIALIZEFEECONFIGEVENT_EVENT_DISCRIMINATOR = new Uint8Array([89, 138, 244, 230, 10, 56, 226, 126]);

/** Auto-generated event interface from the program IDL. */
export interface InitializeFeeConfigEvent {
  timestamp: bigint | number;
  admin: naclac.Address | string;
  fee_config: naclac.Address | string;
}

/** Subscribes to `InitializeFeeConfigEvent` events. Returns a listener ID for cleanup. */
export function addInitializeFeeConfigEventListener(
  program: any,
  callback: (event: InitializeFeeConfigEvent, slot: number, signature: string) => void
): number {
  return program.addEventListener("InitializeFeeConfigEvent", callback);
}

/** Waits for the next `InitializeFeeConfigEvent` event. Resolves `null` if the timeout expires. */
export async function waitForInitializeFeeConfigEvent(
  program: any,
  options?: { timeoutMs?: number }
): Promise<InitializeFeeConfigEvent | null> {
  return program.waitForEvent("InitializeFeeConfigEvent", options) as Promise<InitializeFeeConfigEvent | null>;
}

/** Decodes every `InitializeFeeConfigEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForInitializeFeeConfigEvent`/`addInitializeFeeConfigEventListener` — prefer this when you already know which transaction you're checking. */
export function parseInitializeFeeConfigEventEvents(
  program: any,
  logs: readonly string[]
): InitializeFeeConfigEvent[] {
  return program.parseEvents("InitializeFeeConfigEvent", logs) as InitializeFeeConfigEvent[];
}

/** 8-byte discriminator for `InitializeFeeProgramGlobalEvent` events in transaction logs. */
export const INITIALIZEFEEPROGRAMGLOBALEVENT_EVENT_DISCRIMINATOR = new Uint8Array([40, 233, 156, 78, 95, 0, 8, 199]);

/** Auto-generated event interface from the program IDL. */
export interface InitializeFeeProgramGlobalEvent {
  timestamp: bigint | number;
  claim_rate_limit: bigint | number;
  authority: naclac.Address | string;
  social_claim_authority: naclac.Address | string;
  disable_flags: number;
  padding: string | Uint8Array;
}

/** Subscribes to `InitializeFeeProgramGlobalEvent` events. Returns a listener ID for cleanup. */
export function addInitializeFeeProgramGlobalEventListener(
  program: any,
  callback: (event: InitializeFeeProgramGlobalEvent, slot: number, signature: string) => void
): number {
  return program.addEventListener("InitializeFeeProgramGlobalEvent", callback);
}

/** Waits for the next `InitializeFeeProgramGlobalEvent` event. Resolves `null` if the timeout expires. */
export async function waitForInitializeFeeProgramGlobalEvent(
  program: any,
  options?: { timeoutMs?: number }
): Promise<InitializeFeeProgramGlobalEvent | null> {
  return program.waitForEvent("InitializeFeeProgramGlobalEvent", options) as Promise<InitializeFeeProgramGlobalEvent | null>;
}

/** Decodes every `InitializeFeeProgramGlobalEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForInitializeFeeProgramGlobalEvent`/`addInitializeFeeProgramGlobalEventListener` — prefer this when you already know which transaction you're checking. */
export function parseInitializeFeeProgramGlobalEventEvents(
  program: any,
  logs: readonly string[]
): InitializeFeeProgramGlobalEvent[] {
  return program.parseEvents("InitializeFeeProgramGlobalEvent", logs) as InitializeFeeProgramGlobalEvent[];
}

/** 8-byte discriminator for `ResetFeeSharingConfigEvent` events in transaction logs. */
export const RESETFEESHARINGCONFIGEVENT_EVENT_DISCRIMINATOR = new Uint8Array([203, 204, 151, 226, 120, 55, 214, 243]);

/** Auto-generated event interface from the program IDL. */
export interface ResetFeeSharingConfigEvent {
  timestamp: bigint | number;
  mint: naclac.Address | string;
  sharing_config: naclac.Address | string;
  old_admin: naclac.Address | string;
  old_shareholders: Array<Shareholder>;
  new_admin: naclac.Address | string;
  new_shareholders: Array<Shareholder>;
  old_version: number;
  new_version: number;
}

/** Subscribes to `ResetFeeSharingConfigEvent` events. Returns a listener ID for cleanup. */
export function addResetFeeSharingConfigEventListener(
  program: any,
  callback: (event: ResetFeeSharingConfigEvent, slot: number, signature: string) => void
): number {
  return program.addEventListener("ResetFeeSharingConfigEvent", callback);
}

/** Waits for the next `ResetFeeSharingConfigEvent` event. Resolves `null` if the timeout expires. */
export async function waitForResetFeeSharingConfigEvent(
  program: any,
  options?: { timeoutMs?: number }
): Promise<ResetFeeSharingConfigEvent | null> {
  return program.waitForEvent("ResetFeeSharingConfigEvent", options) as Promise<ResetFeeSharingConfigEvent | null>;
}

/** Decodes every `ResetFeeSharingConfigEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForResetFeeSharingConfigEvent`/`addResetFeeSharingConfigEventListener` — prefer this when you already know which transaction you're checking. */
export function parseResetFeeSharingConfigEventEvents(
  program: any,
  logs: readonly string[]
): ResetFeeSharingConfigEvent[] {
  return program.parseEvents("ResetFeeSharingConfigEvent", logs) as ResetFeeSharingConfigEvent[];
}

/** 8-byte discriminator for `SetAuthorityEvent` events in transaction logs. */
export const SETAUTHORITYEVENT_EVENT_DISCRIMINATOR = new Uint8Array([18, 175, 132, 66, 208, 201, 87, 242]);

/** Auto-generated event interface from the program IDL. */
export interface SetAuthorityEvent {
  timestamp: bigint | number;
  old_authority: naclac.Address | string;
  new_authority: naclac.Address | string;
}

/** Subscribes to `SetAuthorityEvent` events. Returns a listener ID for cleanup. */
export function addSetAuthorityEventListener(
  program: any,
  callback: (event: SetAuthorityEvent, slot: number, signature: string) => void
): number {
  return program.addEventListener("SetAuthorityEvent", callback);
}

/** Waits for the next `SetAuthorityEvent` event. Resolves `null` if the timeout expires. */
export async function waitForSetAuthorityEvent(
  program: any,
  options?: { timeoutMs?: number }
): Promise<SetAuthorityEvent | null> {
  return program.waitForEvent("SetAuthorityEvent", options) as Promise<SetAuthorityEvent | null>;
}

/** Decodes every `SetAuthorityEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForSetAuthorityEvent`/`addSetAuthorityEventListener` — prefer this when you already know which transaction you're checking. */
export function parseSetAuthorityEventEvents(
  program: any,
  logs: readonly string[]
): SetAuthorityEvent[] {
  return program.parseEvents("SetAuthorityEvent", logs) as SetAuthorityEvent[];
}

/** 8-byte discriminator for `SetClaimRateLimitEvent` events in transaction logs. */
export const SETCLAIMRATELIMITEVENT_EVENT_DISCRIMINATOR = new Uint8Array([13, 143, 143, 235, 181, 19, 51, 40]);

/** Auto-generated event interface from the program IDL. */
export interface SetClaimRateLimitEvent {
  timestamp: bigint | number;
  claim_rate_limit: bigint | number;
}

/** Subscribes to `SetClaimRateLimitEvent` events. Returns a listener ID for cleanup. */
export function addSetClaimRateLimitEventListener(
  program: any,
  callback: (event: SetClaimRateLimitEvent, slot: number, signature: string) => void
): number {
  return program.addEventListener("SetClaimRateLimitEvent", callback);
}

/** Waits for the next `SetClaimRateLimitEvent` event. Resolves `null` if the timeout expires. */
export async function waitForSetClaimRateLimitEvent(
  program: any,
  options?: { timeoutMs?: number }
): Promise<SetClaimRateLimitEvent | null> {
  return program.waitForEvent("SetClaimRateLimitEvent", options) as Promise<SetClaimRateLimitEvent | null>;
}

/** Decodes every `SetClaimRateLimitEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForSetClaimRateLimitEvent`/`addSetClaimRateLimitEventListener` — prefer this when you already know which transaction you're checking. */
export function parseSetClaimRateLimitEventEvents(
  program: any,
  logs: readonly string[]
): SetClaimRateLimitEvent[] {
  return program.parseEvents("SetClaimRateLimitEvent", logs) as SetClaimRateLimitEvent[];
}

/** 8-byte discriminator for `SetDisableFlagsEvent` events in transaction logs. */
export const SETDISABLEFLAGSEVENT_EVENT_DISCRIMINATOR = new Uint8Array([5, 8, 179, 65, 49, 55, 145, 126]);

/** Auto-generated event interface from the program IDL. */
export interface SetDisableFlagsEvent {
  timestamp: bigint | number;
  disable_flags: number;
  padding: string | Uint8Array;
}

/** Subscribes to `SetDisableFlagsEvent` events. Returns a listener ID for cleanup. */
export function addSetDisableFlagsEventListener(
  program: any,
  callback: (event: SetDisableFlagsEvent, slot: number, signature: string) => void
): number {
  return program.addEventListener("SetDisableFlagsEvent", callback);
}

/** Waits for the next `SetDisableFlagsEvent` event. Resolves `null` if the timeout expires. */
export async function waitForSetDisableFlagsEvent(
  program: any,
  options?: { timeoutMs?: number }
): Promise<SetDisableFlagsEvent | null> {
  return program.waitForEvent("SetDisableFlagsEvent", options) as Promise<SetDisableFlagsEvent | null>;
}

/** Decodes every `SetDisableFlagsEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForSetDisableFlagsEvent`/`addSetDisableFlagsEventListener` — prefer this when you already know which transaction you're checking. */
export function parseSetDisableFlagsEventEvents(
  program: any,
  logs: readonly string[]
): SetDisableFlagsEvent[] {
  return program.parseEvents("SetDisableFlagsEvent", logs) as SetDisableFlagsEvent[];
}

/** 8-byte discriminator for `SetSocialClaimAuthorityEvent` events in transaction logs. */
export const SETSOCIALCLAIMAUTHORITYEVENT_EVENT_DISCRIMINATOR = new Uint8Array([60, 118, 127, 132, 239, 52, 254, 14]);

/** Auto-generated event interface from the program IDL. */
export interface SetSocialClaimAuthorityEvent {
  timestamp: bigint | number;
  social_claim_authority: naclac.Address | string;
}

/** Subscribes to `SetSocialClaimAuthorityEvent` events. Returns a listener ID for cleanup. */
export function addSetSocialClaimAuthorityEventListener(
  program: any,
  callback: (event: SetSocialClaimAuthorityEvent, slot: number, signature: string) => void
): number {
  return program.addEventListener("SetSocialClaimAuthorityEvent", callback);
}

/** Waits for the next `SetSocialClaimAuthorityEvent` event. Resolves `null` if the timeout expires. */
export async function waitForSetSocialClaimAuthorityEvent(
  program: any,
  options?: { timeoutMs?: number }
): Promise<SetSocialClaimAuthorityEvent | null> {
  return program.waitForEvent("SetSocialClaimAuthorityEvent", options) as Promise<SetSocialClaimAuthorityEvent | null>;
}

/** Decodes every `SetSocialClaimAuthorityEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForSetSocialClaimAuthorityEvent`/`addSetSocialClaimAuthorityEventListener` — prefer this when you already know which transaction you're checking. */
export function parseSetSocialClaimAuthorityEventEvents(
  program: any,
  logs: readonly string[]
): SetSocialClaimAuthorityEvent[] {
  return program.parseEvents("SetSocialClaimAuthorityEvent", logs) as SetSocialClaimAuthorityEvent[];
}

/** 8-byte discriminator for `SocialFeePdaClaimed` events in transaction logs. */
export const SOCIALFEEPDACLAIMED_EVENT_DISCRIMINATOR = new Uint8Array([50, 18, 193, 65, 237, 210, 234, 236]);

/** Auto-generated event interface from the program IDL. */
export interface SocialFeePdaClaimed {
  timestamp: bigint | number;
  user_id: string;
  platform: number;
  social_fee_pda: naclac.Address | string;
  recipient: naclac.Address | string;
  social_claim_authority: naclac.Address | string;
  amount_claimed: bigint | number;
  claimable_before: bigint | number;
  lifetime_claimed: bigint | number;
  recipient_balance_before: bigint | number;
  recipient_balance_after: bigint | number;
  quote_mint: naclac.Address | string;
}

/** Subscribes to `SocialFeePdaClaimed` events. Returns a listener ID for cleanup. */
export function addSocialFeePdaClaimedListener(
  program: any,
  callback: (event: SocialFeePdaClaimed, slot: number, signature: string) => void
): number {
  return program.addEventListener("SocialFeePdaClaimed", callback);
}

/** Waits for the next `SocialFeePdaClaimed` event. Resolves `null` if the timeout expires. */
export async function waitForSocialFeePdaClaimed(
  program: any,
  options?: { timeoutMs?: number }
): Promise<SocialFeePdaClaimed | null> {
  return program.waitForEvent("SocialFeePdaClaimed", options) as Promise<SocialFeePdaClaimed | null>;
}

/** Decodes every `SocialFeePdaClaimed` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForSocialFeePdaClaimed`/`addSocialFeePdaClaimedListener` — prefer this when you already know which transaction you're checking. */
export function parseSocialFeePdaClaimedEvents(
  program: any,
  logs: readonly string[]
): SocialFeePdaClaimed[] {
  return program.parseEvents("SocialFeePdaClaimed", logs) as SocialFeePdaClaimed[];
}

/** 8-byte discriminator for `SocialFeePdaCreated` events in transaction logs. */
export const SOCIALFEEPDACREATED_EVENT_DISCRIMINATOR = new Uint8Array([183, 183, 218, 147, 24, 124, 137, 169]);

/** Auto-generated event interface from the program IDL. */
export interface SocialFeePdaCreated {
  timestamp: bigint | number;
  user_id: string;
  platform: number;
  social_fee_pda: naclac.Address | string;
  created_by: naclac.Address | string;
}

/** Subscribes to `SocialFeePdaCreated` events. Returns a listener ID for cleanup. */
export function addSocialFeePdaCreatedListener(
  program: any,
  callback: (event: SocialFeePdaCreated, slot: number, signature: string) => void
): number {
  return program.addEventListener("SocialFeePdaCreated", callback);
}

/** Waits for the next `SocialFeePdaCreated` event. Resolves `null` if the timeout expires. */
export async function waitForSocialFeePdaCreated(
  program: any,
  options?: { timeoutMs?: number }
): Promise<SocialFeePdaCreated | null> {
  return program.waitForEvent("SocialFeePdaCreated", options) as Promise<SocialFeePdaCreated | null>;
}

/** Decodes every `SocialFeePdaCreated` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForSocialFeePdaCreated`/`addSocialFeePdaCreatedListener` — prefer this when you already know which transaction you're checking. */
export function parseSocialFeePdaCreatedEvents(
  program: any,
  logs: readonly string[]
): SocialFeePdaCreated[] {
  return program.parseEvents("SocialFeePdaCreated", logs) as SocialFeePdaCreated[];
}

/** 8-byte discriminator for `SweepBuybackEvent` events in transaction logs. */
export const SWEEPBUYBACKEVENT_EVENT_DISCRIMINATOR = new Uint8Array([43, 56, 42, 214, 153, 57, 166, 137]);

/** Auto-generated event interface from the program IDL. */
export interface SweepBuybackEvent {
  sol_amount: bigint | number;
  token_amount: bigint | number;
  destination: naclac.Address | string;
  buyback_vault: naclac.Address | string;
  mint: naclac.Address | string;
  index: number;
  padding: string | Uint8Array;
}

/** Subscribes to `SweepBuybackEvent` events. Returns a listener ID for cleanup. */
export function addSweepBuybackEventListener(
  program: any,
  callback: (event: SweepBuybackEvent, slot: number, signature: string) => void
): number {
  return program.addEventListener("SweepBuybackEvent", callback);
}

/** Waits for the next `SweepBuybackEvent` event. Resolves `null` if the timeout expires. */
export async function waitForSweepBuybackEvent(
  program: any,
  options?: { timeoutMs?: number }
): Promise<SweepBuybackEvent | null> {
  return program.waitForEvent("SweepBuybackEvent", options) as Promise<SweepBuybackEvent | null>;
}

/** Decodes every `SweepBuybackEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForSweepBuybackEvent`/`addSweepBuybackEventListener` — prefer this when you already know which transaction you're checking. */
export function parseSweepBuybackEventEvents(
  program: any,
  logs: readonly string[]
): SweepBuybackEvent[] {
  return program.parseEvents("SweepBuybackEvent", logs) as SweepBuybackEvent[];
}

/** 8-byte discriminator for `UpdateAdminEvent` events in transaction logs. */
export const UPDATEADMINEVENT_EVENT_DISCRIMINATOR = new Uint8Array([225, 152, 171, 87, 246, 63, 66, 234]);

/** Auto-generated event interface from the program IDL. */
export interface UpdateAdminEvent {
  timestamp: bigint | number;
  old_admin: naclac.Address | string;
  new_admin: naclac.Address | string;
}

/** Subscribes to `UpdateAdminEvent` events. Returns a listener ID for cleanup. */
export function addUpdateAdminEventListener(
  program: any,
  callback: (event: UpdateAdminEvent, slot: number, signature: string) => void
): number {
  return program.addEventListener("UpdateAdminEvent", callback);
}

/** Waits for the next `UpdateAdminEvent` event. Resolves `null` if the timeout expires. */
export async function waitForUpdateAdminEvent(
  program: any,
  options?: { timeoutMs?: number }
): Promise<UpdateAdminEvent | null> {
  return program.waitForEvent("UpdateAdminEvent", options) as Promise<UpdateAdminEvent | null>;
}

/** Decodes every `UpdateAdminEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForUpdateAdminEvent`/`addUpdateAdminEventListener` — prefer this when you already know which transaction you're checking. */
export function parseUpdateAdminEventEvents(
  program: any,
  logs: readonly string[]
): UpdateAdminEvent[] {
  return program.parseEvents("UpdateAdminEvent", logs) as UpdateAdminEvent[];
}

/** 8-byte discriminator for `UpdateFeeConfigEvent` events in transaction logs. */
export const UPDATEFEECONFIGEVENT_EVENT_DISCRIMINATOR = new Uint8Array([90, 23, 65, 35, 62, 244, 188, 208]);

/** Auto-generated event interface from the program IDL. */
export interface UpdateFeeConfigEvent {
  timestamp: bigint | number;
  admin: naclac.Address | string;
  fee_config: naclac.Address | string;
  fee_tiers: Array<FeeTier>;
  flat_fees: Fees;
}

/** Subscribes to `UpdateFeeConfigEvent` events. Returns a listener ID for cleanup. */
export function addUpdateFeeConfigEventListener(
  program: any,
  callback: (event: UpdateFeeConfigEvent, slot: number, signature: string) => void
): number {
  return program.addEventListener("UpdateFeeConfigEvent", callback);
}

/** Waits for the next `UpdateFeeConfigEvent` event. Resolves `null` if the timeout expires. */
export async function waitForUpdateFeeConfigEvent(
  program: any,
  options?: { timeoutMs?: number }
): Promise<UpdateFeeConfigEvent | null> {
  return program.waitForEvent("UpdateFeeConfigEvent", options) as Promise<UpdateFeeConfigEvent | null>;
}

/** Decodes every `UpdateFeeConfigEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForUpdateFeeConfigEvent`/`addUpdateFeeConfigEventListener` — prefer this when you already know which transaction you're checking. */
export function parseUpdateFeeConfigEventEvents(
  program: any,
  logs: readonly string[]
): UpdateFeeConfigEvent[] {
  return program.parseEvents("UpdateFeeConfigEvent", logs) as UpdateFeeConfigEvent[];
}

/** 8-byte discriminator for `UpdateFeeSharesEvent` events in transaction logs. */
export const UPDATEFEESHARESEVENT_EVENT_DISCRIMINATOR = new Uint8Array([21, 186, 196, 184, 91, 228, 225, 203]);

/** Auto-generated event interface from the program IDL. */
export interface UpdateFeeSharesEvent {
  timestamp: bigint | number;
  mint: naclac.Address | string;
  sharing_config: naclac.Address | string;
  admin: naclac.Address | string;
  new_shareholders: Array<Shareholder>;
  version: number;
}

/** Subscribes to `UpdateFeeSharesEvent` events. Returns a listener ID for cleanup. */
export function addUpdateFeeSharesEventListener(
  program: any,
  callback: (event: UpdateFeeSharesEvent, slot: number, signature: string) => void
): number {
  return program.addEventListener("UpdateFeeSharesEvent", callback);
}

/** Waits for the next `UpdateFeeSharesEvent` event. Resolves `null` if the timeout expires. */
export async function waitForUpdateFeeSharesEvent(
  program: any,
  options?: { timeoutMs?: number }
): Promise<UpdateFeeSharesEvent | null> {
  return program.waitForEvent("UpdateFeeSharesEvent", options) as Promise<UpdateFeeSharesEvent | null>;
}

/** Decodes every `UpdateFeeSharesEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForUpdateFeeSharesEvent`/`addUpdateFeeSharesEventListener` — prefer this when you already know which transaction you're checking. */
export function parseUpdateFeeSharesEventEvents(
  program: any,
  logs: readonly string[]
): UpdateFeeSharesEvent[] {
  return program.parseEvents("UpdateFeeSharesEvent", logs) as UpdateFeeSharesEvent[];
}

/** 8-byte discriminator for `UpdateStableFeeConfigEvent` events in transaction logs. */
export const UPDATESTABLEFEECONFIGEVENT_EVENT_DISCRIMINATOR = new Uint8Array([94, 5, 43, 237, 103, 147, 232, 245]);

/** Auto-generated event interface from the program IDL. */
export interface UpdateStableFeeConfigEvent {
  timestamp: bigint | number;
  admin: naclac.Address | string;
  fee_config: naclac.Address | string;
  stable_fee_tiers: Array<FeeTier>;
  flat_fees: Fees;
}

/** Subscribes to `UpdateStableFeeConfigEvent` events. Returns a listener ID for cleanup. */
export function addUpdateStableFeeConfigEventListener(
  program: any,
  callback: (event: UpdateStableFeeConfigEvent, slot: number, signature: string) => void
): number {
  return program.addEventListener("UpdateStableFeeConfigEvent", callback);
}

/** Waits for the next `UpdateStableFeeConfigEvent` event. Resolves `null` if the timeout expires. */
export async function waitForUpdateStableFeeConfigEvent(
  program: any,
  options?: { timeoutMs?: number }
): Promise<UpdateStableFeeConfigEvent | null> {
  return program.waitForEvent("UpdateStableFeeConfigEvent", options) as Promise<UpdateStableFeeConfigEvent | null>;
}

/** Decodes every `UpdateStableFeeConfigEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForUpdateStableFeeConfigEvent`/`addUpdateStableFeeConfigEventListener` — prefer this when you already know which transaction you're checking. */
export function parseUpdateStableFeeConfigEventEvents(
  program: any,
  logs: readonly string[]
): UpdateStableFeeConfigEvent[] {
  return program.parseEvents("UpdateStableFeeConfigEvent", logs) as UpdateStableFeeConfigEvent[];
}

/** 8-byte discriminator for `UpsertFeeTiersEvent` events in transaction logs. */
export const UPSERTFEETIERSEVENT_EVENT_DISCRIMINATOR = new Uint8Array([171, 89, 169, 187, 122, 186, 33, 204]);

/** Auto-generated event interface from the program IDL. */
export interface UpsertFeeTiersEvent {
  timestamp: bigint | number;
  admin: naclac.Address | string;
  fee_config: naclac.Address | string;
  fee_tiers: Array<FeeTier>;
  offset: number;
}

/** Subscribes to `UpsertFeeTiersEvent` events. Returns a listener ID for cleanup. */
export function addUpsertFeeTiersEventListener(
  program: any,
  callback: (event: UpsertFeeTiersEvent, slot: number, signature: string) => void
): number {
  return program.addEventListener("UpsertFeeTiersEvent", callback);
}

/** Waits for the next `UpsertFeeTiersEvent` event. Resolves `null` if the timeout expires. */
export async function waitForUpsertFeeTiersEvent(
  program: any,
  options?: { timeoutMs?: number }
): Promise<UpsertFeeTiersEvent | null> {
  return program.waitForEvent("UpsertFeeTiersEvent", options) as Promise<UpsertFeeTiersEvent | null>;
}

/** Decodes every `UpsertFeeTiersEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForUpsertFeeTiersEvent`/`addUpsertFeeTiersEventListener` — prefer this when you already know which transaction you're checking. */
export function parseUpsertFeeTiersEventEvents(
  program: any,
  logs: readonly string[]
): UpsertFeeTiersEvent[] {
  return program.parseEvents("UpsertFeeTiersEvent", logs) as UpsertFeeTiersEvent[];
}

/** 8-byte discriminator for `UpsertStableFeeTiersEvent` events in transaction logs. */
export const UPSERTSTABLEFEETIERSEVENT_EVENT_DISCRIMINATOR = new Uint8Array([232, 237, 237, 52, 98, 146, 73, 243]);

/** Auto-generated event interface from the program IDL. */
export interface UpsertStableFeeTiersEvent {
  timestamp: bigint | number;
  admin: naclac.Address | string;
  fee_config: naclac.Address | string;
  stable_fee_tiers: Array<FeeTier>;
  offset: number;
}

/** Subscribes to `UpsertStableFeeTiersEvent` events. Returns a listener ID for cleanup. */
export function addUpsertStableFeeTiersEventListener(
  program: any,
  callback: (event: UpsertStableFeeTiersEvent, slot: number, signature: string) => void
): number {
  return program.addEventListener("UpsertStableFeeTiersEvent", callback);
}

/** Waits for the next `UpsertStableFeeTiersEvent` event. Resolves `null` if the timeout expires. */
export async function waitForUpsertStableFeeTiersEvent(
  program: any,
  options?: { timeoutMs?: number }
): Promise<UpsertStableFeeTiersEvent | null> {
  return program.waitForEvent("UpsertStableFeeTiersEvent", options) as Promise<UpsertStableFeeTiersEvent | null>;
}

/** Decodes every `UpsertStableFeeTiersEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForUpsertStableFeeTiersEvent`/`addUpsertStableFeeTiersEventListener` — prefer this when you already know which transaction you're checking. */
export function parseUpsertStableFeeTiersEventEvents(
  program: any,
  logs: readonly string[]
): UpsertStableFeeTiersEvent[] {
  return program.parseEvents("UpsertStableFeeTiersEvent", logs) as UpsertStableFeeTiersEvent[];
}

/** Removes a previously registered event listener. */
export function removeListener(program: any, listenerId: number) {
  program.removeEventListener(listenerId);
}
