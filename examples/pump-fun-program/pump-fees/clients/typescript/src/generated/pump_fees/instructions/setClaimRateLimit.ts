// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `set_claim_rate_limit` instruction. */
export const SET_CLAIM_RATE_LIMIT_DISCRIMINATOR = new Uint8Array([185, 211, 159, 174, 212, 49, 88, 4]);

/** Instruction arguments for `setClaimRateLimit`. */
export interface SetClaimRateLimitArgs {
  claim_rate_limit: bigint | number;
}

/** Accounts for the `setClaimRateLimit` instruction. */
export interface SetClaimRateLimitAccounts {
  authority: naclac.Address | string;
  fee_program_global?: naclac.Address | string;
}

/**
 * Builds the `setClaimRateLimit` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function setClaimRateLimit(
  program: any,
  args: SetClaimRateLimitArgs,
  accounts?: Partial<SetClaimRateLimitAccounts>
) {
  const builder = program.methods.set_claim_rate_limit(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
