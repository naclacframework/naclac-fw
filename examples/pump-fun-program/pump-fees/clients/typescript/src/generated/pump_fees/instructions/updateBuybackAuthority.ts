// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `update_buyback_authority` instruction. */
export const UPDATE_BUYBACK_AUTHORITY_DISCRIMINATOR = new Uint8Array([66, 98, 113, 202, 121, 37, 219, 107]);

/** Instruction arguments for `updateBuybackAuthority`. */
export interface UpdateBuybackAuthorityArgs {
  index: number;
  buyback_vault_bump: number;
  new_authority: naclac.Address | string;
}

/** Accounts for the `updateBuybackAuthority` instruction. */
export interface UpdateBuybackAuthorityAccounts {
  authority: naclac.Address | string;
  fee_program_global?: naclac.Address | string;
  buyback_vault?: naclac.Address | string;
}

/**
 * Builds the `updateBuybackAuthority` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 * 
 * Gated by the *global* `fee_program_global.authority`, not the vault's own
 * `authority` — an intentional admin-override design (fees-05).
 */
export function updateBuybackAuthority(
  program: any,
  args: UpdateBuybackAuthorityArgs,
  accounts?: Partial<UpdateBuybackAuthorityAccounts>
) {
  const builder = program.methods.update_buyback_authority(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
