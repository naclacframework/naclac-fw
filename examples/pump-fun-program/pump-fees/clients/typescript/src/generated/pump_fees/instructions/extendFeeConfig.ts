// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `extend_fee_config` instruction. */
export const EXTEND_FEE_CONFIG_DISCRIMINATOR = new Uint8Array([68, 179, 244, 90, 173, 56, 17, 217]);

/** Accounts for the `extendFeeConfig` instruction. */
export interface ExtendFeeConfigAccounts {
  user: naclac.Address | string;
  /**
   * SAFETY: only used as PDA seed material for `fee_config`; not
   * deserialized or invoked.
   */
  configProgramId: naclac.Address | string;
  feeConfig?: naclac.Address | string;
  systemProgram?: naclac.Address | string;
}

/**
 * Builds the `extendFeeConfig` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 * 
 * Realloc the `fee_config` PDA to `FeeConfig::CURRENT_SIZE`. Callable by
 * any signer (`user` pays the rent delta, per the real program's IDL —
 * there's no admin/ownership check on this instruction).
 */
export function extendFeeConfig(
  program: any,
  args?: Record<string, never>,
  accounts?: Partial<ExtendFeeConfigAccounts>
) {
  const builder = program.methods.extend_fee_config(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
