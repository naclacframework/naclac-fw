// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client/legacy";
import rawIDL from "./idl/pump_fees.json";
import type { PumpFees } from "./idl/pump_fees";
const IDL = rawIDL as unknown as PumpFees;
import * as instructions from "./instructions";
import * as accounts from "./accounts/legacy";
import * as types from "./types";

/**
 * High-level client for the `pump_fees` program.
 * Uses the classic @solana/web3.js (v1) transaction model.
 */
export class PumpFeesClient {
  public readonly program: naclac.LegacyProgram;
  public readonly instructions = instructions;
  public readonly accounts = accounts;
  public readonly types = types;
  public readonly errors = types.ProgramErrors;

  public get constants() {
    return this.program.constants;
  }

  /** The on-chain program address as a `PublicKey`. */
  public get programId(): naclac.PublicKey {
    return this.program.programId;
  }

  constructor(
    providerOrCluster: naclac.LegacyProvider | "devnet" | "mainnet" | "localnet" | string,
    payer?: naclac.Keypair
  ) {
    let provider: naclac.LegacyProvider;
    if (typeof providerOrCluster === "string") {
      let url = providerOrCluster;
      if (providerOrCluster === "devnet") url = "https://api.devnet.solana.com";
      else if (providerOrCluster === "mainnet") url = "https://api.mainnet-beta.solana.com";
      else if (providerOrCluster === "localnet") url = "http://127.0.0.1:8899";
      const connection = new naclac.Connection(url, "confirmed");
      provider = { connection, payer, publicKey: payer ? payer.publicKey : undefined };
    } else {
      provider = providerOrCluster;
    }
    this.program = new naclac.LegacyProgram(IDL, provider);
  }

  /**
   * Builds the `getFees` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public getFees(args: instructions.GetFeesArgs, accounts?: Partial<instructions.GetFeesAccounts>) {
    return instructions.getFees(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `initializeFeeConfig` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public initializeFeeConfig(args: instructions.InitializeFeeConfigArgs, accounts?: Partial<instructions.InitializeFeeConfigAccounts>) {
    return instructions.initializeFeeConfig(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `createFeeSharingConfig` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public createFeeSharingConfig(args: instructions.CreateFeeSharingConfigArgs, accounts?: Partial<instructions.CreateFeeSharingConfigAccounts>) {
    return instructions.createFeeSharingConfig(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `createDonationFeePda` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public createDonationFeePda(args: instructions.CreateDonationFeePdaArgs, accounts?: Partial<instructions.CreateDonationFeePdaAccounts>) {
    return instructions.createDonationFeePda(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `crankDonationFeePda` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public crankDonationFeePda(args: instructions.CrankDonationFeePdaArgs, accounts?: Partial<instructions.CrankDonationFeePdaAccounts>) {
    return instructions.crankDonationFeePda(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `initializeFeeProgramGlobal` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public initializeFeeProgramGlobal(args: instructions.InitializeFeeProgramGlobalArgs, accounts?: Partial<instructions.InitializeFeeProgramGlobalAccounts>) {
    return instructions.initializeFeeProgramGlobal(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `updateAdmin` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public updateAdmin(args?: Record<string, never>, accounts?: Partial<instructions.UpdateAdminAccounts>) {
    return instructions.updateAdmin(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `extendFeeConfig` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public extendFeeConfig(args?: Record<string, never>, accounts?: Partial<instructions.ExtendFeeConfigAccounts>) {
    return instructions.extendFeeConfig(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `updateFeeConfig` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public updateFeeConfig(args: instructions.UpdateFeeConfigArgs, accounts?: Partial<instructions.UpdateFeeConfigAccounts>) {
    return instructions.updateFeeConfig(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `updateStableFeeConfig` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public updateStableFeeConfig(args: instructions.UpdateStableFeeConfigArgs, accounts?: Partial<instructions.UpdateStableFeeConfigAccounts>) {
    return instructions.updateStableFeeConfig(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `upsertFeeTiers` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public upsertFeeTiers(args: instructions.UpsertFeeTiersArgs, accounts?: Partial<instructions.UpsertFeeTiersAccounts>) {
    return instructions.upsertFeeTiers(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `upsertStableFeeTiers` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public upsertStableFeeTiers(args: instructions.UpsertStableFeeTiersArgs, accounts?: Partial<instructions.UpsertStableFeeTiersAccounts>) {
    return instructions.upsertStableFeeTiers(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `setAuthority` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public setAuthority(args: instructions.SetAuthorityArgs, accounts?: Partial<instructions.SetAuthorityAccounts>) {
    return instructions.setAuthority(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `setClaimRateLimit` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public setClaimRateLimit(args: instructions.SetClaimRateLimitArgs, accounts?: Partial<instructions.SetClaimRateLimitAccounts>) {
    return instructions.setClaimRateLimit(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `setDisableFlags` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public setDisableFlags(args: instructions.SetDisableFlagsArgs, accounts?: Partial<instructions.SetDisableFlagsAccounts>) {
    return instructions.setDisableFlags(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `setSocialClaimAuthority` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public setSocialClaimAuthority(args: instructions.SetSocialClaimAuthorityArgs, accounts?: Partial<instructions.SetSocialClaimAuthorityAccounts>) {
    return instructions.setSocialClaimAuthority(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `initializeBuyback` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public initializeBuyback(args: instructions.InitializeBuybackArgs, accounts?: Partial<instructions.InitializeBuybackAccounts>) {
    return instructions.initializeBuyback(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `sweepBuyback` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public sweepBuyback(args: instructions.SweepBuybackArgs, accounts?: Partial<instructions.SweepBuybackAccounts>) {
    return instructions.sweepBuyback(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `updateBuybackAuthority` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public updateBuybackAuthority(args: instructions.UpdateBuybackAuthorityArgs, accounts?: Partial<instructions.UpdateBuybackAuthorityAccounts>) {
    return instructions.updateBuybackAuthority(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `updateBuybackClaimRateLimit` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public updateBuybackClaimRateLimit(args: instructions.UpdateBuybackClaimRateLimitArgs, accounts?: Partial<instructions.UpdateBuybackClaimRateLimitAccounts>) {
    return instructions.updateBuybackClaimRateLimit(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `revokeFeeSharingAuthority` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public revokeFeeSharingAuthority(args?: Record<string, never>, accounts?: Partial<instructions.RevokeFeeSharingAuthorityAccounts>) {
    return instructions.revokeFeeSharingAuthority(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `transferFeeSharingAuthority` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public transferFeeSharingAuthority(args?: Record<string, never>, accounts?: Partial<instructions.TransferFeeSharingAuthorityAccounts>) {
    return instructions.transferFeeSharingAuthority(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `createSocialFeePda` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public createSocialFeePda(args: instructions.CreateSocialFeePdaArgs, accounts?: Partial<instructions.CreateSocialFeePdaAccounts>) {
    return instructions.createSocialFeePda(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `claimSocialFeePda` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public claimSocialFeePda(args: instructions.ClaimSocialFeePdaArgs, accounts?: Partial<instructions.ClaimSocialFeePdaAccounts>) {
    return instructions.claimSocialFeePda(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `claimSocialFeePdaV2` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public claimSocialFeePdaV2(args: instructions.ClaimSocialFeePdaV2Args, accounts?: Partial<instructions.ClaimSocialFeePdaV2Accounts>) {
    return instructions.claimSocialFeePdaV2(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `resetFeeSharingConfig` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public resetFeeSharingConfig(args: instructions.ResetFeeSharingConfigArgs, accounts?: Partial<instructions.ResetFeeSharingConfigAccounts>) {
    return instructions.resetFeeSharingConfig(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `resetFeeSharingConfigV2` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public resetFeeSharingConfigV2(args: instructions.ResetFeeSharingConfigV2Args, accounts?: Partial<instructions.ResetFeeSharingConfigV2Accounts>) {
    return instructions.resetFeeSharingConfigV2(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `updateFeeShares` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public updateFeeShares(args: instructions.UpdateFeeSharesArgs, accounts?: Partial<instructions.UpdateFeeSharesAccounts>) {
    return instructions.updateFeeShares(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `updateFeeSharesV2` instruction pipeline.
   * Call `.rpc()` to send or `.transaction()` to get a `Transaction` object.
   */
  public updateFeeSharesV2(args: instructions.UpdateFeeSharesV2Args, accounts?: Partial<instructions.UpdateFeeSharesV2Accounts>) {
    return instructions.updateFeeSharesV2(this.program, args ?? {}, accounts);
  }

  /** Derives the PDA for a `bonding_curve` account. Returns `[PublicKey, bumpSeed]`. */
  public getBondingCurvePda(seeds: {
    mint: naclac.PublicKey | string;
  }): [naclac.PublicKey, number] {
    const [pda, bump] = naclac.PublicKey.findProgramAddressSync(
      [
                new Uint8Array([98, 111, 110, 100, 105, 110, 103, 45, 99, 117, 114, 118, 101]),
        new naclac.PublicKey(seeds.mint).toBuffer()
      ],
      new naclac.PublicKey(new Uint8Array([219, 228, 38, 61, 197, 238, 49, 67, 254, 52, 90, 137, 137, 108, 51, 148, 9, 123, 135, 93, 203, 217, 165, 115, 223, 190, 144, 135, 242, 216, 223, 192]))
    );
    return [pda, bump];
  }

  /** Derives the PDA for a `buyback_vault` account. Returns `[PublicKey, bumpSeed]`. */
  public getBuybackVaultPda(seeds: {
    index: number;
  }): [naclac.PublicKey, number] {
    const [pda, bump] = naclac.PublicKey.findProgramAddressSync(
      [
                new Uint8Array([98, 117, 121, 98, 97, 99, 107, 45, 118, 97, 117, 108, 116]),
        new Uint8Array(naclac.getIdlCodec(JSON.parse('"u8"')).encode(seeds.index))
      ],
      this.programId
    );
    return [pda, bump];
  }

  /** Derives the PDA for a `coin_creator_vault_authority` account. Returns `[PublicKey, bumpSeed]`. */
  public getCoinCreatorVaultAuthorityPda(seeds: {
    sharing_config: naclac.PublicKey | string;
  }): [naclac.PublicKey, number] {
    const [pda, bump] = naclac.PublicKey.findProgramAddressSync(
      [
                new Uint8Array([99, 114, 101, 97, 116, 111, 114, 95, 118, 97, 117, 108, 116]),
        new naclac.PublicKey(seeds.sharing_config).toBuffer()
      ],
      new naclac.PublicKey(new Uint8Array([252, 69, 211, 229, 209, 250, 91, 185, 95, 119, 96, 125, 211, 65, 169, 106, 209, 112, 144, 171, 161, 186, 45, 122, 136, 122, 226, 73, 138, 146, 134, 211]))
    );
    return [pda, bump];
  }

  /** Derives the PDA for a `debouncer` account. Returns `[PublicKey, bumpSeed]`. */
  public getDebouncerPda(seeds: {
    config_id: naclac.PublicKey | string;
    quote_mint: naclac.PublicKey | string;
  }): [naclac.PublicKey, number] {
    const [pda, bump] = naclac.PublicKey.findProgramAddressSync(
      [
                new Uint8Array([100, 101, 98, 111, 117, 110, 99, 101, 114, 95, 118, 49]),
        new naclac.PublicKey(seeds.config_id).toBuffer(),
        new naclac.PublicKey(seeds.quote_mint).toBuffer()
      ],
      new naclac.PublicKey(new Uint8Array([23, 118, 164, 191, 192, 201, 243, 51, 32, 226, 179, 109, 83, 249, 158, 252, 119, 104, 39, 62, 96, 34, 156, 219, 131, 24, 131, 100, 36, 174, 205, 212]))
    );
    return [pda, bump];
  }

  /** Derives the PDA for a `debouncer_ata` account. Returns `[PublicKey, bumpSeed]`. */
  public getDebouncerAtaPda(seeds: {
    debouncer: naclac.PublicKey | string;
    token_program: naclac.PublicKey | string;
    quote_mint: naclac.PublicKey | string;
  }): [naclac.PublicKey, number] {
    const [pda, bump] = naclac.PublicKey.findProgramAddressSync(
      [
                new naclac.PublicKey(seeds.debouncer).toBuffer(),
        new naclac.PublicKey(seeds.token_program).toBuffer(),
        new naclac.PublicKey(seeds.quote_mint).toBuffer()
      ],
      this.programId
    );
    return [pda, bump];
  }

  /** Derives the PDA for a `donation_fee_pda` account. Returns `[PublicKey, bumpSeed]`. */
  public getDonationFeePdaPda(seeds: {
    base_mint: naclac.PublicKey | string;
    config_id: naclac.PublicKey | string;
  }): [naclac.PublicKey, number] {
    const [pda, bump] = naclac.PublicKey.findProgramAddressSync(
      [
                new Uint8Array([100, 111, 110, 97, 116, 105, 111, 110, 45, 102, 101, 101, 45, 112, 100, 97]),
        new naclac.PublicKey(seeds.base_mint).toBuffer(),
        new naclac.PublicKey(seeds.config_id).toBuffer()
      ],
      this.programId
    );
    return [pda, bump];
  }

  /** Derives the PDA for a `epoch_tracker` account. Returns `[PublicKey, bumpSeed]`. */
  public getEpochTrackerPda(seeds: {
    config_id: naclac.PublicKey | string;
    quote_mint: naclac.PublicKey | string;
  }): [naclac.PublicKey, number] {
    const [pda, bump] = naclac.PublicKey.findProgramAddressSync(
      [
                new Uint8Array([101, 112, 111, 99, 104, 95, 116, 114, 97, 99, 107, 101, 114, 95, 118, 49]),
        new naclac.PublicKey(seeds.config_id).toBuffer(),
        new naclac.PublicKey(seeds.quote_mint).toBuffer()
      ],
      new naclac.PublicKey(new Uint8Array([23, 118, 164, 191, 192, 201, 243, 51, 32, 226, 179, 109, 83, 249, 158, 252, 119, 104, 39, 62, 96, 34, 156, 219, 131, 24, 131, 100, 36, 174, 205, 212]))
    );
    return [pda, bump];
  }

  /** Derives the PDA for a `fee_config` account. Returns `[PublicKey, bumpSeed]`. */
  public getFeeConfigPda(seeds: {
    config_program_id: naclac.PublicKey | string;
  }): [naclac.PublicKey, number] {
    const [pda, bump] = naclac.PublicKey.findProgramAddressSync(
      [
                new Uint8Array([102, 101, 101, 95, 99, 111, 110, 102, 105, 103]),
        new naclac.PublicKey(seeds.config_program_id).toBuffer()
      ],
      this.programId
    );
    return [pda, bump];
  }

  /** Derives the PDA for a `fee_program_global` account. Returns `[PublicKey, bumpSeed]`. */
  public getFeeProgramGlobalPda(seeds: {
  }): [naclac.PublicKey, number] {
    const [pda, bump] = naclac.PublicKey.findProgramAddressSync(
      [
                new Uint8Array([102, 101, 101, 45, 112, 114, 111, 103, 114, 97, 109, 45, 103, 108, 111, 98, 97, 108])
      ],
      this.programId
    );
    return [pda, bump];
  }

  /** Derives the PDA for a `global` account. Returns `[PublicKey, bumpSeed]`. */
  public getGlobalPda(seeds: {
  }): [naclac.PublicKey, number] {
    const [pda, bump] = naclac.PublicKey.findProgramAddressSync(
      [
                new Uint8Array([103, 108, 111, 98, 97, 108])
      ],
      new naclac.PublicKey(new Uint8Array([219, 228, 38, 61, 197, 238, 49, 67, 254, 52, 90, 137, 137, 108, 51, 148, 9, 123, 135, 93, 203, 217, 165, 115, 223, 190, 144, 135, 242, 216, 223, 192]))
    );
    return [pda, bump];
  }

  /** Derives the PDA for a `pool` account. Returns `[PublicKey, bumpSeed]`. */
  public getPoolPda(seeds: {
    pool_authority: naclac.PublicKey | string;
    base_mint: naclac.PublicKey | string;
  }): [naclac.PublicKey, number] {
    const [pda, bump] = naclac.PublicKey.findProgramAddressSync(
      [
                new Uint8Array([112, 111, 111, 108]),
        new Uint8Array([0, 0]),
        new naclac.PublicKey(seeds.pool_authority).toBuffer(),
        new naclac.PublicKey(seeds.base_mint).toBuffer(),
        new Uint8Array([6, 155, 136, 87, 254, 171, 129, 132, 251, 104, 127, 99, 70, 24, 192, 53, 218, 196, 57, 220, 26, 235, 59, 85, 152, 160, 240, 0, 0, 0, 0, 1])
      ],
      new naclac.PublicKey(new Uint8Array([252, 69, 211, 229, 209, 250, 91, 185, 95, 119, 96, 125, 211, 65, 169, 106, 209, 112, 144, 171, 161, 186, 45, 122, 136, 122, 226, 73, 138, 146, 134, 211]))
    );
    return [pda, bump];
  }

  /** Derives the PDA for a `pool_authority` account. Returns `[PublicKey, bumpSeed]`. */
  public getPoolAuthorityPda(seeds: {
    base_mint: naclac.PublicKey | string;
  }): [naclac.PublicKey, number] {
    const [pda, bump] = naclac.PublicKey.findProgramAddressSync(
      [
                new Uint8Array([112, 111, 111, 108, 45, 97, 117, 116, 104, 111, 114, 105, 116, 121]),
        new naclac.PublicKey(seeds.base_mint).toBuffer()
      ],
      new naclac.PublicKey(new Uint8Array([219, 228, 38, 61, 197, 238, 49, 67, 254, 52, 90, 137, 137, 108, 51, 148, 9, 123, 135, 93, 203, 217, 165, 115, 223, 190, 144, 135, 242, 216, 223, 192]))
    );
    return [pda, bump];
  }

  /** Derives the PDA for a `pump_authority` account. Returns `[PublicKey, bumpSeed]`. */
  public getPumpAuthorityPda(seeds: {
  }): [naclac.PublicKey, number] {
    const [pda, bump] = naclac.PublicKey.findProgramAddressSync(
      [
                new Uint8Array([112, 117, 109, 112, 45, 97, 117, 116, 104, 111, 114, 105, 116, 121])
      ],
      new naclac.PublicKey(new Uint8Array([219, 228, 38, 61, 197, 238, 49, 67, 254, 52, 90, 137, 137, 108, 51, 148, 9, 123, 135, 93, 203, 217, 165, 115, 223, 190, 144, 135, 242, 216, 223, 192]))
    );
    return [pda, bump];
  }

  /** Derives the PDA for a `pump_creator_vault` account. Returns `[PublicKey, bumpSeed]`. */
  public getPumpCreatorVaultPda(seeds: {
    sharing_config: naclac.PublicKey | string;
  }): [naclac.PublicKey, number] {
    const [pda, bump] = naclac.PublicKey.findProgramAddressSync(
      [
                new Uint8Array([99, 114, 101, 97, 116, 111, 114, 45, 118, 97, 117, 108, 116]),
        new naclac.PublicKey(seeds.sharing_config).toBuffer()
      ],
      new naclac.PublicKey(new Uint8Array([219, 228, 38, 61, 197, 238, 49, 67, 254, 52, 90, 137, 137, 108, 51, 148, 9, 123, 135, 93, 203, 217, 165, 115, 223, 190, 144, 135, 242, 216, 223, 192]))
    );
    return [pda, bump];
  }

  /** Derives the PDA for a `pump_fees_authority` account. Returns `[PublicKey, bumpSeed]`. */
  public getPumpFeesAuthorityPda(seeds: {
  }): [naclac.PublicKey, number] {
    const [pda, bump] = naclac.PublicKey.findProgramAddressSync(
      [
                new Uint8Array([112, 117, 109, 112, 45, 102, 101, 101, 115, 45, 97, 117, 116, 104, 111, 114, 105, 116, 121])
      ],
      this.programId
    );
    return [pda, bump];
  }

  /** Derives the PDA for a `pump_global` account. Returns `[PublicKey, bumpSeed]`. */
  public getPumpGlobalPda(seeds: {
  }): [naclac.PublicKey, number] {
    const [pda, bump] = naclac.PublicKey.findProgramAddressSync(
      [
                new Uint8Array([103, 108, 111, 98, 97, 108])
      ],
      new naclac.PublicKey(new Uint8Array([219, 228, 38, 61, 197, 238, 49, 67, 254, 52, 90, 137, 137, 108, 51, 148, 9, 123, 135, 93, 203, 217, 165, 115, 223, 190, 144, 135, 242, 216, 223, 192]))
    );
    return [pda, bump];
  }

  /** Derives the PDA for a `sharing_config` account. Returns `[PublicKey, bumpSeed]`. */
  public getSharingConfigPda(seeds: {
    mint: naclac.PublicKey | string;
  }): [naclac.PublicKey, number] {
    const [pda, bump] = naclac.PublicKey.findProgramAddressSync(
      [
                new Uint8Array([115, 104, 97, 114, 105, 110, 103, 45, 99, 111, 110, 102, 105, 103]),
        new naclac.PublicKey(seeds.mint).toBuffer()
      ],
      this.programId
    );
    return [pda, bump];
  }

  /** Derives the PDA for a `social_fee_pda` account. Returns `[PublicKey, bumpSeed]`. */
  public getSocialFeePdaPda(seeds: {
    user_id: string;
    platform: number;
  }): [naclac.PublicKey, number] {
    const [pda, bump] = naclac.PublicKey.findProgramAddressSync(
      [
                new Uint8Array([115, 111, 99, 105, 97, 108, 45, 102, 101, 101, 45, 112, 100, 97]),
        new Uint8Array(naclac.getIdlCodec(JSON.parse('"string"')).encode(seeds.user_id)),
        new Uint8Array(naclac.getIdlCodec(JSON.parse('"u8"')).encode(seeds.platform))
      ],
      this.programId
    );
    return [pda, bump];
  }

  /** Fetches a `BondingCurve` account. Throws if it does not exist. */
  public fetchBondingCurve(address: naclac.PublicKey | string) {
    return this.program.account['BondingCurve'].fetch(address);
  }

  /** Fetches a `BondingCurve` account. Returns `null` if it does not exist. */
  public async fetchMaybeBondingCurve(address: naclac.PublicKey | string) {
    try { return await this.program.account['BondingCurve'].fetch(address); }
    catch { return null; }
  }

  /** Fetches multiple `BondingCurve` accounts in a single RPC call. */
  public fetchAllBondingCurves(addresses: Array<naclac.PublicKey | string>, opts?: { dropCorrupted?: boolean }) {
    return this.program.account['BondingCurve'].fetchMultiple(addresses, opts);
  }

  /** Fetches all on-chain `BondingCurve` accounts owned by this program. */
  public fetchAllBondingCurveByOwner(options?: { dropCorrupted?: boolean; filters?: any[] }) {
    return this.program.account['BondingCurve'].all(options);
  }

  /** Fetches a `BuybackVault` account. Throws if it does not exist. */
  public fetchBuybackVault(address: naclac.PublicKey | string) {
    return this.program.account['BuybackVault'].fetch(address);
  }

  /** Fetches a `BuybackVault` account. Returns `null` if it does not exist. */
  public async fetchMaybeBuybackVault(address: naclac.PublicKey | string) {
    try { return await this.program.account['BuybackVault'].fetch(address); }
    catch { return null; }
  }

  /** Fetches multiple `BuybackVault` accounts in a single RPC call. */
  public fetchAllBuybackVaults(addresses: Array<naclac.PublicKey | string>, opts?: { dropCorrupted?: boolean }) {
    return this.program.account['BuybackVault'].fetchMultiple(addresses, opts);
  }

  /** Fetches all on-chain `BuybackVault` accounts owned by this program. */
  public fetchAllBuybackVaultByOwner(options?: { dropCorrupted?: boolean; filters?: any[] }) {
    return this.program.account['BuybackVault'].all(options);
  }

  /** Fetches a `DonationFeePda` account. Throws if it does not exist. */
  public fetchDonationFeePda(address: naclac.PublicKey | string) {
    return this.program.account['DonationFeePda'].fetch(address);
  }

  /** Fetches a `DonationFeePda` account. Returns `null` if it does not exist. */
  public async fetchMaybeDonationFeePda(address: naclac.PublicKey | string) {
    try { return await this.program.account['DonationFeePda'].fetch(address); }
    catch { return null; }
  }

  /** Fetches multiple `DonationFeePda` accounts in a single RPC call. */
  public fetchAllDonationFeePdas(addresses: Array<naclac.PublicKey | string>, opts?: { dropCorrupted?: boolean }) {
    return this.program.account['DonationFeePda'].fetchMultiple(addresses, opts);
  }

  /** Fetches all on-chain `DonationFeePda` accounts owned by this program. */
  public fetchAllDonationFeePdaByOwner(options?: { dropCorrupted?: boolean; filters?: any[] }) {
    return this.program.account['DonationFeePda'].all(options);
  }

  /** Fetches a `FeeConfig` account. Throws if it does not exist. */
  public fetchFeeConfig(address: naclac.PublicKey | string) {
    return this.program.account['FeeConfig'].fetch(address);
  }

  /** Fetches a `FeeConfig` account. Returns `null` if it does not exist. */
  public async fetchMaybeFeeConfig(address: naclac.PublicKey | string) {
    try { return await this.program.account['FeeConfig'].fetch(address); }
    catch { return null; }
  }

  /** Fetches multiple `FeeConfig` accounts in a single RPC call. */
  public fetchAllFeeConfigs(addresses: Array<naclac.PublicKey | string>, opts?: { dropCorrupted?: boolean }) {
    return this.program.account['FeeConfig'].fetchMultiple(addresses, opts);
  }

  /** Fetches all on-chain `FeeConfig` accounts owned by this program. */
  public fetchAllFeeConfigByOwner(options?: { dropCorrupted?: boolean; filters?: any[] }) {
    return this.program.account['FeeConfig'].all(options);
  }

  /** Fetches a `FeeProgramGlobal` account. Throws if it does not exist. */
  public fetchFeeProgramGlobal(address: naclac.PublicKey | string) {
    return this.program.account['FeeProgramGlobal'].fetch(address);
  }

  /** Fetches a `FeeProgramGlobal` account. Returns `null` if it does not exist. */
  public async fetchMaybeFeeProgramGlobal(address: naclac.PublicKey | string) {
    try { return await this.program.account['FeeProgramGlobal'].fetch(address); }
    catch { return null; }
  }

  /** Fetches multiple `FeeProgramGlobal` accounts in a single RPC call. */
  public fetchAllFeeProgramGlobals(addresses: Array<naclac.PublicKey | string>, opts?: { dropCorrupted?: boolean }) {
    return this.program.account['FeeProgramGlobal'].fetchMultiple(addresses, opts);
  }

  /** Fetches all on-chain `FeeProgramGlobal` accounts owned by this program. */
  public fetchAllFeeProgramGlobalByOwner(options?: { dropCorrupted?: boolean; filters?: any[] }) {
    return this.program.account['FeeProgramGlobal'].all(options);
  }

  /** Fetches a `Pool` account. Throws if it does not exist. */
  public fetchPool(address: naclac.PublicKey | string) {
    return this.program.account['Pool'].fetch(address);
  }

  /** Fetches a `Pool` account. Returns `null` if it does not exist. */
  public async fetchMaybePool(address: naclac.PublicKey | string) {
    try { return await this.program.account['Pool'].fetch(address); }
    catch { return null; }
  }

  /** Fetches multiple `Pool` accounts in a single RPC call. */
  public fetchAllPools(addresses: Array<naclac.PublicKey | string>, opts?: { dropCorrupted?: boolean }) {
    return this.program.account['Pool'].fetchMultiple(addresses, opts);
  }

  /** Fetches all on-chain `Pool` accounts owned by this program. */
  public fetchAllPoolByOwner(options?: { dropCorrupted?: boolean; filters?: any[] }) {
    return this.program.account['Pool'].all(options);
  }

  /** Fetches a `Global` account. Throws if it does not exist. */
  public fetchGlobal(address: naclac.PublicKey | string) {
    return this.program.account['Global'].fetch(address);
  }

  /** Fetches a `Global` account. Returns `null` if it does not exist. */
  public async fetchMaybeGlobal(address: naclac.PublicKey | string) {
    try { return await this.program.account['Global'].fetch(address); }
    catch { return null; }
  }

  /** Fetches multiple `Global` accounts in a single RPC call. */
  public fetchAllGlobals(addresses: Array<naclac.PublicKey | string>, opts?: { dropCorrupted?: boolean }) {
    return this.program.account['Global'].fetchMultiple(addresses, opts);
  }

  /** Fetches all on-chain `Global` accounts owned by this program. */
  public fetchAllGlobalByOwner(options?: { dropCorrupted?: boolean; filters?: any[] }) {
    return this.program.account['Global'].all(options);
  }

  /** Fetches a `SharingConfig` account. Throws if it does not exist. */
  public fetchSharingConfig(address: naclac.PublicKey | string) {
    return this.program.account['SharingConfig'].fetch(address);
  }

  /** Fetches a `SharingConfig` account. Returns `null` if it does not exist. */
  public async fetchMaybeSharingConfig(address: naclac.PublicKey | string) {
    try { return await this.program.account['SharingConfig'].fetch(address); }
    catch { return null; }
  }

  /** Fetches multiple `SharingConfig` accounts in a single RPC call. */
  public fetchAllSharingConfigs(addresses: Array<naclac.PublicKey | string>, opts?: { dropCorrupted?: boolean }) {
    return this.program.account['SharingConfig'].fetchMultiple(addresses, opts);
  }

  /** Fetches all on-chain `SharingConfig` accounts owned by this program. */
  public fetchAllSharingConfigByOwner(options?: { dropCorrupted?: boolean; filters?: any[] }) {
    return this.program.account['SharingConfig'].all(options);
  }

  /** Fetches a `SocialFeePda` account. Throws if it does not exist. */
  public fetchSocialFeePda(address: naclac.PublicKey | string) {
    return this.program.account['SocialFeePda'].fetch(address);
  }

  /** Fetches a `SocialFeePda` account. Returns `null` if it does not exist. */
  public async fetchMaybeSocialFeePda(address: naclac.PublicKey | string) {
    try { return await this.program.account['SocialFeePda'].fetch(address); }
    catch { return null; }
  }

  /** Fetches multiple `SocialFeePda` accounts in a single RPC call. */
  public fetchAllSocialFeePdas(addresses: Array<naclac.PublicKey | string>, opts?: { dropCorrupted?: boolean }) {
    return this.program.account['SocialFeePda'].fetchMultiple(addresses, opts);
  }

  /** Fetches all on-chain `SocialFeePda` accounts owned by this program. */
  public fetchAllSocialFeePdaByOwner(options?: { dropCorrupted?: boolean; filters?: any[] }) {
    return this.program.account['SocialFeePda'].all(options);
  }

  /** Subscribes to `CreateFeeSharingConfigEvent` events. Returns a listener ID. */
  public onCreateFeeSharingConfigEvent(callback: (event: types.CreateFeeSharingConfigEvent, slot: number, signature: string) => void) {
    return types.addCreateFeeSharingConfigEventListener(this.program, callback);
  }

  /** Awaits the next `CreateFeeSharingConfigEvent` event. Resolves `null` on timeout. */
  public waitForCreateFeeSharingConfigEvent(options?: { timeoutMs?: number }) {
    return this.program.waitForEvent<types.CreateFeeSharingConfigEvent>("CreateFeeSharingConfigEvent", options);
  }

  /** Decodes every `CreateFeeSharingConfigEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForCreateFeeSharingConfigEvent` — prefer this when you already know which transaction you're checking. */
  public parseCreateFeeSharingConfigEventEvents(logs: readonly string[]) {
    return this.program.parseEvents<types.CreateFeeSharingConfigEvent>("CreateFeeSharingConfigEvent", logs);
  }

  /** Subscribes to `DonationFeePdaCranked` events. Returns a listener ID. */
  public onDonationFeePdaCranked(callback: (event: types.DonationFeePdaCranked, slot: number, signature: string) => void) {
    return types.addDonationFeePdaCrankedListener(this.program, callback);
  }

  /** Awaits the next `DonationFeePdaCranked` event. Resolves `null` on timeout. */
  public waitForDonationFeePdaCranked(options?: { timeoutMs?: number }) {
    return this.program.waitForEvent<types.DonationFeePdaCranked>("DonationFeePdaCranked", options);
  }

  /** Decodes every `DonationFeePdaCranked` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForDonationFeePdaCranked` — prefer this when you already know which transaction you're checking. */
  public parseDonationFeePdaCrankedEvents(logs: readonly string[]) {
    return this.program.parseEvents<types.DonationFeePdaCranked>("DonationFeePdaCranked", logs);
  }

  /** Subscribes to `DonationFeePdaCreated` events. Returns a listener ID. */
  public onDonationFeePdaCreated(callback: (event: types.DonationFeePdaCreated, slot: number, signature: string) => void) {
    return types.addDonationFeePdaCreatedListener(this.program, callback);
  }

  /** Awaits the next `DonationFeePdaCreated` event. Resolves `null` on timeout. */
  public waitForDonationFeePdaCreated(options?: { timeoutMs?: number }) {
    return this.program.waitForEvent<types.DonationFeePdaCreated>("DonationFeePdaCreated", options);
  }

  /** Decodes every `DonationFeePdaCreated` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForDonationFeePdaCreated` — prefer this when you already know which transaction you're checking. */
  public parseDonationFeePdaCreatedEvents(logs: readonly string[]) {
    return this.program.parseEvents<types.DonationFeePdaCreated>("DonationFeePdaCreated", logs);
  }

  /** Subscribes to `ExtendFeeConfigEvent` events. Returns a listener ID. */
  public onExtendFeeConfigEvent(callback: (event: types.ExtendFeeConfigEvent, slot: number, signature: string) => void) {
    return types.addExtendFeeConfigEventListener(this.program, callback);
  }

  /** Awaits the next `ExtendFeeConfigEvent` event. Resolves `null` on timeout. */
  public waitForExtendFeeConfigEvent(options?: { timeoutMs?: number }) {
    return this.program.waitForEvent<types.ExtendFeeConfigEvent>("ExtendFeeConfigEvent", options);
  }

  /** Decodes every `ExtendFeeConfigEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForExtendFeeConfigEvent` — prefer this when you already know which transaction you're checking. */
  public parseExtendFeeConfigEventEvents(logs: readonly string[]) {
    return this.program.parseEvents<types.ExtendFeeConfigEvent>("ExtendFeeConfigEvent", logs);
  }

  /** Subscribes to `InitializeFeeConfigEvent` events. Returns a listener ID. */
  public onInitializeFeeConfigEvent(callback: (event: types.InitializeFeeConfigEvent, slot: number, signature: string) => void) {
    return types.addInitializeFeeConfigEventListener(this.program, callback);
  }

  /** Awaits the next `InitializeFeeConfigEvent` event. Resolves `null` on timeout. */
  public waitForInitializeFeeConfigEvent(options?: { timeoutMs?: number }) {
    return this.program.waitForEvent<types.InitializeFeeConfigEvent>("InitializeFeeConfigEvent", options);
  }

  /** Decodes every `InitializeFeeConfigEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForInitializeFeeConfigEvent` — prefer this when you already know which transaction you're checking. */
  public parseInitializeFeeConfigEventEvents(logs: readonly string[]) {
    return this.program.parseEvents<types.InitializeFeeConfigEvent>("InitializeFeeConfigEvent", logs);
  }

  /** Subscribes to `InitializeFeeProgramGlobalEvent` events. Returns a listener ID. */
  public onInitializeFeeProgramGlobalEvent(callback: (event: types.InitializeFeeProgramGlobalEvent, slot: number, signature: string) => void) {
    return types.addInitializeFeeProgramGlobalEventListener(this.program, callback);
  }

  /** Awaits the next `InitializeFeeProgramGlobalEvent` event. Resolves `null` on timeout. */
  public waitForInitializeFeeProgramGlobalEvent(options?: { timeoutMs?: number }) {
    return this.program.waitForEvent<types.InitializeFeeProgramGlobalEvent>("InitializeFeeProgramGlobalEvent", options);
  }

  /** Decodes every `InitializeFeeProgramGlobalEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForInitializeFeeProgramGlobalEvent` — prefer this when you already know which transaction you're checking. */
  public parseInitializeFeeProgramGlobalEventEvents(logs: readonly string[]) {
    return this.program.parseEvents<types.InitializeFeeProgramGlobalEvent>("InitializeFeeProgramGlobalEvent", logs);
  }

  /** Subscribes to `ResetFeeSharingConfigEvent` events. Returns a listener ID. */
  public onResetFeeSharingConfigEvent(callback: (event: types.ResetFeeSharingConfigEvent, slot: number, signature: string) => void) {
    return types.addResetFeeSharingConfigEventListener(this.program, callback);
  }

  /** Awaits the next `ResetFeeSharingConfigEvent` event. Resolves `null` on timeout. */
  public waitForResetFeeSharingConfigEvent(options?: { timeoutMs?: number }) {
    return this.program.waitForEvent<types.ResetFeeSharingConfigEvent>("ResetFeeSharingConfigEvent", options);
  }

  /** Decodes every `ResetFeeSharingConfigEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForResetFeeSharingConfigEvent` — prefer this when you already know which transaction you're checking. */
  public parseResetFeeSharingConfigEventEvents(logs: readonly string[]) {
    return this.program.parseEvents<types.ResetFeeSharingConfigEvent>("ResetFeeSharingConfigEvent", logs);
  }

  /** Subscribes to `SetAuthorityEvent` events. Returns a listener ID. */
  public onSetAuthorityEvent(callback: (event: types.SetAuthorityEvent, slot: number, signature: string) => void) {
    return types.addSetAuthorityEventListener(this.program, callback);
  }

  /** Awaits the next `SetAuthorityEvent` event. Resolves `null` on timeout. */
  public waitForSetAuthorityEvent(options?: { timeoutMs?: number }) {
    return this.program.waitForEvent<types.SetAuthorityEvent>("SetAuthorityEvent", options);
  }

  /** Decodes every `SetAuthorityEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForSetAuthorityEvent` — prefer this when you already know which transaction you're checking. */
  public parseSetAuthorityEventEvents(logs: readonly string[]) {
    return this.program.parseEvents<types.SetAuthorityEvent>("SetAuthorityEvent", logs);
  }

  /** Subscribes to `SetClaimRateLimitEvent` events. Returns a listener ID. */
  public onSetClaimRateLimitEvent(callback: (event: types.SetClaimRateLimitEvent, slot: number, signature: string) => void) {
    return types.addSetClaimRateLimitEventListener(this.program, callback);
  }

  /** Awaits the next `SetClaimRateLimitEvent` event. Resolves `null` on timeout. */
  public waitForSetClaimRateLimitEvent(options?: { timeoutMs?: number }) {
    return this.program.waitForEvent<types.SetClaimRateLimitEvent>("SetClaimRateLimitEvent", options);
  }

  /** Decodes every `SetClaimRateLimitEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForSetClaimRateLimitEvent` — prefer this when you already know which transaction you're checking. */
  public parseSetClaimRateLimitEventEvents(logs: readonly string[]) {
    return this.program.parseEvents<types.SetClaimRateLimitEvent>("SetClaimRateLimitEvent", logs);
  }

  /** Subscribes to `SetDisableFlagsEvent` events. Returns a listener ID. */
  public onSetDisableFlagsEvent(callback: (event: types.SetDisableFlagsEvent, slot: number, signature: string) => void) {
    return types.addSetDisableFlagsEventListener(this.program, callback);
  }

  /** Awaits the next `SetDisableFlagsEvent` event. Resolves `null` on timeout. */
  public waitForSetDisableFlagsEvent(options?: { timeoutMs?: number }) {
    return this.program.waitForEvent<types.SetDisableFlagsEvent>("SetDisableFlagsEvent", options);
  }

  /** Decodes every `SetDisableFlagsEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForSetDisableFlagsEvent` — prefer this when you already know which transaction you're checking. */
  public parseSetDisableFlagsEventEvents(logs: readonly string[]) {
    return this.program.parseEvents<types.SetDisableFlagsEvent>("SetDisableFlagsEvent", logs);
  }

  /** Subscribes to `SetSocialClaimAuthorityEvent` events. Returns a listener ID. */
  public onSetSocialClaimAuthorityEvent(callback: (event: types.SetSocialClaimAuthorityEvent, slot: number, signature: string) => void) {
    return types.addSetSocialClaimAuthorityEventListener(this.program, callback);
  }

  /** Awaits the next `SetSocialClaimAuthorityEvent` event. Resolves `null` on timeout. */
  public waitForSetSocialClaimAuthorityEvent(options?: { timeoutMs?: number }) {
    return this.program.waitForEvent<types.SetSocialClaimAuthorityEvent>("SetSocialClaimAuthorityEvent", options);
  }

  /** Decodes every `SetSocialClaimAuthorityEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForSetSocialClaimAuthorityEvent` — prefer this when you already know which transaction you're checking. */
  public parseSetSocialClaimAuthorityEventEvents(logs: readonly string[]) {
    return this.program.parseEvents<types.SetSocialClaimAuthorityEvent>("SetSocialClaimAuthorityEvent", logs);
  }

  /** Subscribes to `SocialFeePdaClaimed` events. Returns a listener ID. */
  public onSocialFeePdaClaimed(callback: (event: types.SocialFeePdaClaimed, slot: number, signature: string) => void) {
    return types.addSocialFeePdaClaimedListener(this.program, callback);
  }

  /** Awaits the next `SocialFeePdaClaimed` event. Resolves `null` on timeout. */
  public waitForSocialFeePdaClaimed(options?: { timeoutMs?: number }) {
    return this.program.waitForEvent<types.SocialFeePdaClaimed>("SocialFeePdaClaimed", options);
  }

  /** Decodes every `SocialFeePdaClaimed` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForSocialFeePdaClaimed` — prefer this when you already know which transaction you're checking. */
  public parseSocialFeePdaClaimedEvents(logs: readonly string[]) {
    return this.program.parseEvents<types.SocialFeePdaClaimed>("SocialFeePdaClaimed", logs);
  }

  /** Subscribes to `SocialFeePdaCreated` events. Returns a listener ID. */
  public onSocialFeePdaCreated(callback: (event: types.SocialFeePdaCreated, slot: number, signature: string) => void) {
    return types.addSocialFeePdaCreatedListener(this.program, callback);
  }

  /** Awaits the next `SocialFeePdaCreated` event. Resolves `null` on timeout. */
  public waitForSocialFeePdaCreated(options?: { timeoutMs?: number }) {
    return this.program.waitForEvent<types.SocialFeePdaCreated>("SocialFeePdaCreated", options);
  }

  /** Decodes every `SocialFeePdaCreated` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForSocialFeePdaCreated` — prefer this when you already know which transaction you're checking. */
  public parseSocialFeePdaCreatedEvents(logs: readonly string[]) {
    return this.program.parseEvents<types.SocialFeePdaCreated>("SocialFeePdaCreated", logs);
  }

  /** Subscribes to `SweepBuybackEvent` events. Returns a listener ID. */
  public onSweepBuybackEvent(callback: (event: types.SweepBuybackEvent, slot: number, signature: string) => void) {
    return types.addSweepBuybackEventListener(this.program, callback);
  }

  /** Awaits the next `SweepBuybackEvent` event. Resolves `null` on timeout. */
  public waitForSweepBuybackEvent(options?: { timeoutMs?: number }) {
    return this.program.waitForEvent<types.SweepBuybackEvent>("SweepBuybackEvent", options);
  }

  /** Decodes every `SweepBuybackEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForSweepBuybackEvent` — prefer this when you already know which transaction you're checking. */
  public parseSweepBuybackEventEvents(logs: readonly string[]) {
    return this.program.parseEvents<types.SweepBuybackEvent>("SweepBuybackEvent", logs);
  }

  /** Subscribes to `UpdateAdminEvent` events. Returns a listener ID. */
  public onUpdateAdminEvent(callback: (event: types.UpdateAdminEvent, slot: number, signature: string) => void) {
    return types.addUpdateAdminEventListener(this.program, callback);
  }

  /** Awaits the next `UpdateAdminEvent` event. Resolves `null` on timeout. */
  public waitForUpdateAdminEvent(options?: { timeoutMs?: number }) {
    return this.program.waitForEvent<types.UpdateAdminEvent>("UpdateAdminEvent", options);
  }

  /** Decodes every `UpdateAdminEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForUpdateAdminEvent` — prefer this when you already know which transaction you're checking. */
  public parseUpdateAdminEventEvents(logs: readonly string[]) {
    return this.program.parseEvents<types.UpdateAdminEvent>("UpdateAdminEvent", logs);
  }

  /** Subscribes to `UpdateFeeConfigEvent` events. Returns a listener ID. */
  public onUpdateFeeConfigEvent(callback: (event: types.UpdateFeeConfigEvent, slot: number, signature: string) => void) {
    return types.addUpdateFeeConfigEventListener(this.program, callback);
  }

  /** Awaits the next `UpdateFeeConfigEvent` event. Resolves `null` on timeout. */
  public waitForUpdateFeeConfigEvent(options?: { timeoutMs?: number }) {
    return this.program.waitForEvent<types.UpdateFeeConfigEvent>("UpdateFeeConfigEvent", options);
  }

  /** Decodes every `UpdateFeeConfigEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForUpdateFeeConfigEvent` — prefer this when you already know which transaction you're checking. */
  public parseUpdateFeeConfigEventEvents(logs: readonly string[]) {
    return this.program.parseEvents<types.UpdateFeeConfigEvent>("UpdateFeeConfigEvent", logs);
  }

  /** Subscribes to `UpdateFeeSharesEvent` events. Returns a listener ID. */
  public onUpdateFeeSharesEvent(callback: (event: types.UpdateFeeSharesEvent, slot: number, signature: string) => void) {
    return types.addUpdateFeeSharesEventListener(this.program, callback);
  }

  /** Awaits the next `UpdateFeeSharesEvent` event. Resolves `null` on timeout. */
  public waitForUpdateFeeSharesEvent(options?: { timeoutMs?: number }) {
    return this.program.waitForEvent<types.UpdateFeeSharesEvent>("UpdateFeeSharesEvent", options);
  }

  /** Decodes every `UpdateFeeSharesEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForUpdateFeeSharesEvent` — prefer this when you already know which transaction you're checking. */
  public parseUpdateFeeSharesEventEvents(logs: readonly string[]) {
    return this.program.parseEvents<types.UpdateFeeSharesEvent>("UpdateFeeSharesEvent", logs);
  }

  /** Subscribes to `UpdateStableFeeConfigEvent` events. Returns a listener ID. */
  public onUpdateStableFeeConfigEvent(callback: (event: types.UpdateStableFeeConfigEvent, slot: number, signature: string) => void) {
    return types.addUpdateStableFeeConfigEventListener(this.program, callback);
  }

  /** Awaits the next `UpdateStableFeeConfigEvent` event. Resolves `null` on timeout. */
  public waitForUpdateStableFeeConfigEvent(options?: { timeoutMs?: number }) {
    return this.program.waitForEvent<types.UpdateStableFeeConfigEvent>("UpdateStableFeeConfigEvent", options);
  }

  /** Decodes every `UpdateStableFeeConfigEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForUpdateStableFeeConfigEvent` — prefer this when you already know which transaction you're checking. */
  public parseUpdateStableFeeConfigEventEvents(logs: readonly string[]) {
    return this.program.parseEvents<types.UpdateStableFeeConfigEvent>("UpdateStableFeeConfigEvent", logs);
  }

  /** Subscribes to `UpsertFeeTiersEvent` events. Returns a listener ID. */
  public onUpsertFeeTiersEvent(callback: (event: types.UpsertFeeTiersEvent, slot: number, signature: string) => void) {
    return types.addUpsertFeeTiersEventListener(this.program, callback);
  }

  /** Awaits the next `UpsertFeeTiersEvent` event. Resolves `null` on timeout. */
  public waitForUpsertFeeTiersEvent(options?: { timeoutMs?: number }) {
    return this.program.waitForEvent<types.UpsertFeeTiersEvent>("UpsertFeeTiersEvent", options);
  }

  /** Decodes every `UpsertFeeTiersEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForUpsertFeeTiersEvent` — prefer this when you already know which transaction you're checking. */
  public parseUpsertFeeTiersEventEvents(logs: readonly string[]) {
    return this.program.parseEvents<types.UpsertFeeTiersEvent>("UpsertFeeTiersEvent", logs);
  }

  /** Subscribes to `UpsertStableFeeTiersEvent` events. Returns a listener ID. */
  public onUpsertStableFeeTiersEvent(callback: (event: types.UpsertStableFeeTiersEvent, slot: number, signature: string) => void) {
    return types.addUpsertStableFeeTiersEventListener(this.program, callback);
  }

  /** Awaits the next `UpsertStableFeeTiersEvent` event. Resolves `null` on timeout. */
  public waitForUpsertStableFeeTiersEvent(options?: { timeoutMs?: number }) {
    return this.program.waitForEvent<types.UpsertStableFeeTiersEvent>("UpsertStableFeeTiersEvent", options);
  }

  /** Decodes every `UpsertStableFeeTiersEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForUpsertStableFeeTiersEvent` — prefer this when you already know which transaction you're checking. */
  public parseUpsertStableFeeTiersEventEvents(logs: readonly string[]) {
    return this.program.parseEvents<types.UpsertStableFeeTiersEvent>("UpsertStableFeeTiersEvent", logs);
  }

  /** Removes a registered event listener by its ID. */
  public removeEventListener(listenerId: number) {
    return types.removeListener(this.program, listenerId);
  }
}
