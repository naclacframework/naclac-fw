// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client/legacy";
import type { Pool } from "../../types";

/** 8-byte on-chain discriminator for `Pool` accounts. */
export const POOL_DISCRIMINATOR = new Uint8Array([241, 154, 109, 4, 17, 177, 109, 188]);

/** Fetches a `Pool` account. Throws if it does not exist. */
export async function fetchPool(
  program: naclac.LegacyProgram,
  address: naclac.PublicKey | string,
): Promise<Pool> {
  const result = await program.account['Pool'].fetch(address);
  return result as unknown as Pool;
}

/** Fetches a `Pool` account. Returns `null` if it does not exist. */
export async function fetchMaybePool(
  program: naclac.LegacyProgram,
  address: naclac.PublicKey | string,
): Promise<Pool | null> {
  try {
    const result = await program.account['Pool'].fetch(address);
    return result as unknown as Pool;
  } catch {
    return null;
  }
}

/** Fetches multiple `Pool` accounts. Missing accounts are `null`. */
export async function fetchAllPool(
  program: naclac.LegacyProgram,
  addresses: Array<naclac.PublicKey | string>,
  opts?: { dropCorrupted?: boolean },
): Promise<Array<Pool | null>> {
  const results = await program.account['Pool'].fetchMultiple(addresses, opts);
  return results as unknown as Array<Pool | null>;
}

/** Fetches all on-chain `Pool` accounts owned by this program. */
export async function fetchAllPoolByOwner(
  program: naclac.LegacyProgram,
  options?: { dropCorrupted?: boolean; filters?: any[] },
): Promise<Array<{ publicKey: naclac.PublicKey; account: Pool }>> {
  const results = await program.account['Pool'].all(options);
  return results as unknown as Array<{ publicKey: naclac.PublicKey; account: Pool }>;
}
