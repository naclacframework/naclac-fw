// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `update_buyback_claim_rate_limit` instruction. */
export const UPDATE_BUYBACK_CLAIM_RATE_LIMIT_DISCRIMINATOR = new Uint8Array([186, 95, 135, 190, 255, 199, 137, 170]);

/** Instruction arguments for `updateBuybackClaimRateLimit`. */
export interface UpdateBuybackClaimRateLimitArgs {
  index: number;
  buyback_vault_bump: number;
  claim_rate_limit: bigint | number;
}

/** Accounts for the `updateBuybackClaimRateLimit` instruction. */
export interface UpdateBuybackClaimRateLimitAccounts {
  authority: naclac.Address | string;
  fee_program_global?: naclac.Address | string;
  buyback_vault?: naclac.Address | string;
}

/**
 * Builds the `updateBuybackClaimRateLimit` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 * 
 * Gated by the *global* `fee_program_global.authority`, not the vault's own
 * `authority` — an intentional admin-override design (fees-05).
 */
export function updateBuybackClaimRateLimit(
  program: any,
  args: UpdateBuybackClaimRateLimitArgs,
  accounts?: Partial<UpdateBuybackClaimRateLimitAccounts>
) {
  const builder = program.methods.update_buyback_claim_rate_limit(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
