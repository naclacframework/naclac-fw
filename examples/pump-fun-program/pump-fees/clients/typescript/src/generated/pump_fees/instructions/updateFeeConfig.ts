// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";
import * as types from "../types/typedefs";

/** 8-byte discriminator for the `update_fee_config` instruction. */
export const UPDATE_FEE_CONFIG_DISCRIMINATOR = new Uint8Array([104, 184, 103, 242, 88, 151, 107, 20]);

/** Instruction arguments for `updateFeeConfig`. */
export interface UpdateFeeConfigArgs {
  feeTiers: Array<types.FeeTier>;
  flatFees: types.Fees;
}

/** Accounts for the `updateFeeConfig` instruction. */
export interface UpdateFeeConfigAccounts {
  admin: naclac.Address | string;
  /**
   * SAFETY: only used as PDA seed material for `fee_config`; not
   * deserialized or invoked.
   */
  configProgramId: naclac.Address | string;
  feeConfig?: naclac.Address | string;
}

/**
 * Builds the `updateFeeConfig` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 * 
 * Set/Replace fee parameters entirely (only callable by admin)
 */
export function updateFeeConfig(
  program: any,
  args: UpdateFeeConfigArgs,
  accounts?: Partial<UpdateFeeConfigAccounts>
) {
  const builder = program.methods.update_fee_config(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
