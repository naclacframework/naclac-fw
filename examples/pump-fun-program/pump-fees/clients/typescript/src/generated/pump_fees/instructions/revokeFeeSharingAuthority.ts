// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `revoke_fee_sharing_authority` instruction. */
export const REVOKE_FEE_SHARING_AUTHORITY_DISCRIMINATOR = new Uint8Array([18, 233, 158, 39, 185, 207, 58, 104]);

/** Accounts for the `revokeFeeSharingAuthority` instruction. */
export interface RevokeFeeSharingAuthorityAccounts {
}

/**
 * Builds the `revokeFeeSharingAuthority` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 * 
 * Revoke Fee Sharing Authority
 * 
 * confirmed via direct execution against the real bytecode —
 * this instruction always reverts with DeprecatedInstruction, checked
 * before touching any accounts.
 */
export function revokeFeeSharingAuthority(
  program: any,
  args?: Record<string, never>,
  accounts?: Partial<RevokeFeeSharingAuthorityAccounts>
) {
  const builder = program.methods.revoke_fee_sharing_authority(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
