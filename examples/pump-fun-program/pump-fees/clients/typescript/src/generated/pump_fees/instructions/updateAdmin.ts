// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `update_admin` instruction. */
export const UPDATE_ADMIN_DISCRIMINATOR = new Uint8Array([161, 176, 40, 213, 60, 184, 179, 228]);

/** Accounts for the `updateAdmin` instruction. */
export interface UpdateAdminAccounts {
  admin: naclac.Address | string;
  /**
   * SAFETY: only used as PDA seed material for `fee_config`; not
   * deserialized or invoked.
   */
  config_program_id: naclac.Address | string;
  fee_config?: naclac.Address | string;
  /**
   * SAFETY: new admin is passed as an account (not instruction data);
   * its pubkey is only stored, never deserialized or invoked.
   */
  new_admin: naclac.Address | string;
}

/**
 * Builds the `updateAdmin` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 * 
 * Update admin (only callable by admin)
 */
export function updateAdmin(
  program: any,
  args?: Record<string, never>,
  accounts?: Partial<UpdateAdminAccounts>
) {
  const builder = program.methods.update_admin(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
