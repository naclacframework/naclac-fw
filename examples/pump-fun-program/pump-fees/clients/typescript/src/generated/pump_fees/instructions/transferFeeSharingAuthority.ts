// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `transfer_fee_sharing_authority` instruction. */
export const TRANSFER_FEE_SHARING_AUTHORITY_DISCRIMINATOR = new Uint8Array([202, 10, 75, 200, 164, 34, 210, 96]);

/** Accounts for the `transferFeeSharingAuthority` instruction. */
export interface TransferFeeSharingAuthorityAccounts {
}

/**
 * Builds the `transferFeeSharingAuthority` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 * 
 * Transfer Fee Sharing Authority
 * 
 * confirmed via direct execution against the real bytecode —
 * this instruction always reverts with DeprecatedInstruction, checked
 * before touching any accounts.
 */
export function transferFeeSharingAuthority(
  program: any,
  args?: Record<string, never>,
  accounts?: Partial<TransferFeeSharingAuthorityAccounts>
) {
  const builder = program.methods.transfer_fee_sharing_authority(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
