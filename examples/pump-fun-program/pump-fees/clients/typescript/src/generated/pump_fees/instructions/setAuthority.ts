// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `set_authority` instruction. */
export const SET_AUTHORITY_DISCRIMINATOR = new Uint8Array([133, 250, 37, 21, 110, 163, 26, 121]);

/** Instruction arguments for `setAuthority`. */
export interface SetAuthorityArgs {
  new_authority: naclac.Address | string;
}

/** Accounts for the `setAuthority` instruction. */
export interface SetAuthorityAccounts {
  authority: naclac.Address | string;
  fee_program_global?: naclac.Address | string;
}

/**
 * Builds the `setAuthority` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function setAuthority(
  program: any,
  args: SetAuthorityArgs,
  accounts?: Partial<SetAuthorityAccounts>
) {
  const builder = program.methods.set_authority(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
