// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

#[cfg(feature = "offchain")]
#[cfg(feature = "borsh")]
use crate::sdk_core_offchain::borsh::BorshSerialize;
#[cfg(feature = "offchain")]
#[cfg_attr(feature = "borsh", derive(Clone, Debug, BorshSerialize))]
#[cfg_attr(feature = "borsh", borsh(crate = "crate::sdk_core_offchain::borsh"))]
#[cfg_attr(not(feature = "borsh"), derive(Copy, Clone, Debug))]
#[cfg_attr(not(feature = "borsh"), repr(C, packed))]
pub struct InitBoostIxArgs {
    pub boost_vault_authority_bump: u8,
}

#[cfg(feature = "offchain")]
#[cfg(not(feature = "borsh"))]
unsafe impl crate::sdk_core_offchain::bytemuck::Zeroable for InitBoostIxArgs {}
#[cfg(feature = "offchain")]
#[cfg(not(feature = "borsh"))]
unsafe impl crate::sdk_core_offchain::bytemuck::Pod for InitBoostIxArgs {}

#[cfg(feature = "cpi")]
#[cfg(feature = "borsh")]
use crate::sdk_core_cpi::borsh::BorshSerialize;
#[cfg(feature = "cpi")]
#[cfg_attr(feature = "borsh", derive(Clone, Debug, BorshSerialize))]
#[cfg_attr(feature = "borsh", borsh(crate = "crate::sdk_core_cpi::borsh"))]
#[cfg_attr(not(feature = "borsh"), derive(Copy, Clone, Debug))]
#[cfg_attr(not(feature = "borsh"), repr(C, packed))]
pub struct InitBoostCpiIxArgs {
    pub boost_vault_authority_bump: u8,
}

#[cfg(feature = "cpi")]
#[cfg(not(feature = "borsh"))]
unsafe impl crate::sdk_core_cpi::bytemuck::Zeroable for InitBoostCpiIxArgs {}
#[cfg(feature = "cpi")]
#[cfg(not(feature = "borsh"))]
unsafe impl crate::sdk_core_cpi::bytemuck::Pod for InitBoostCpiIxArgs {}

#[cfg(feature = "offchain")]
/// Real behavior confirmed via `probe46.rs`/`probe48.rs`/`probe49.rs`
/// (litesvm, real bytecode) and 5 independent real mainnet `migrate_v2`
/// transactions (`docs/plan/amm-03-boost-mechanism.md`): `init_boost` moves
/// no funds of its own accord and computes no percentage — it reads
/// `pool_quote_token_account`'s and `pool_base_token_account`'s balances
/// *as they stand at call time*, computes `virtual_quote_reserves =
/// floor(quote_balance * base_balance / BOOST_BASE_SUPPLY_DIVISOR)`,
/// transfers that amount out of `pool_quote_token_account` into a
/// freshly-created `boost_vault`, and records it on `pool`. Confirmed with
/// zero error across 5 independent real transactions plus a direct blind
/// litesvm replication using a 9-decimal quote mint (ruling out a
/// quote-decimals dependency).
pub struct InitBoostAccounts {
    /// SAFETY: only used as `InitBoostEvent`'s `bonding_curve` field — the
    /// real program derives this on-chain via `find_program_address` purely
    /// for event logging (not an account it ever reads), which naclac bans
    /// outright; our own `migrate_v2` (the only real caller) already has
    /// this account in its own context and passes it straight through.
    /// Never deserialized.
    pub bonding_curve: naclac_client::Address,
    pub pool: naclac_client::Address,
    pub global_config: naclac_client::Address,
    pub creator: naclac_client::Address,
    pub base_mint: naclac_client::Address,
    pub quote_mint: naclac_client::Address,
    pub pool_base_token_account: naclac_client::Address,
    pub pool_quote_token_account: naclac_client::Address,
    /// SAFETY: `seeds`/`bump` already verifies its address; a bare
    /// signing/seed PDA with no stored data (matches the real program — no
    /// dedicated `BoostVault`-type account exists), used below only as a CPI
    /// signer and as `boost_vault`'s ATA authority.
    pub boost_vault_authority: naclac_client::Address,
    /// SAFETY: `init` + `associated_token::mint`/`::authority` below, plus
    /// the real Associated Token Program's own CPI-level address
    /// verification, fully validate and construct this account — there is
    /// no naclac `Discriminator` to check since this is a raw SPL
    /// `TokenAccount` layout (same reasoning as `create_pool`'s own
    /// freshly-`init`ed ATA fields).
    pub boost_vault: naclac_client::Address,
    pub quote_token_program: naclac_client::Address,
    pub system_program: naclac_client::Address,
    pub associated_token_program: naclac_client::Address,
}

#[cfg(feature = "offchain")]
/// Real behavior confirmed via `probe46.rs`/`probe48.rs`/`probe49.rs`
/// (litesvm, real bytecode) and 5 independent real mainnet `migrate_v2`
/// transactions (`docs/plan/amm-03-boost-mechanism.md`): `init_boost` moves
/// no funds of its own accord and computes no percentage — it reads
/// `pool_quote_token_account`'s and `pool_base_token_account`'s balances
/// *as they stand at call time*, computes `virtual_quote_reserves =
/// floor(quote_balance * base_balance / BOOST_BASE_SUPPLY_DIVISOR)`,
/// transfers that amount out of `pool_quote_token_account` into a
/// freshly-created `boost_vault`, and records it on `pool`. Confirmed with
/// zero error across 5 independent real transactions plus a direct blind
/// litesvm replication using a 9-decimal quote mint (ruling out a
/// quote-decimals dependency).
pub fn build_init_boost<'a>(
    provider: &'a naclac_client::NaclacProvider,
    program_id: naclac_client::Address,
    boost_vault_authority_bump: u8,
    accounts: InitBoostAccounts,
) -> naclac_client::InstructionBuilder<'a> {
    let mut ix_data = crate::sdk_core_offchain::vec![140, 233, 33, 94, 132, 90, 194, 143];
    let args = InitBoostIxArgs {
        boost_vault_authority_bump,
    };
    #[cfg(not(feature = "borsh"))]
    {
        ix_data.extend_from_slice(crate::sdk_core_offchain::bytemuck::bytes_of(&args));
    }
    #[cfg(feature = "borsh")]
    {
        crate::sdk_core_offchain::borsh::BorshSerialize::serialize(&args, &mut ix_data).unwrap();
    }
    naclac_client::InstructionBuilder::new(provider, program_id, ix_data)
        .account(naclac_client::AccountMeta { address: accounts.bonding_curve, is_signer: false, is_writable: false }, "bonding_curve")
        .account(naclac_client::AccountMeta { address: accounts.pool, is_signer: false, is_writable: true }, "pool")
        .account(naclac_client::AccountMeta { address: accounts.global_config, is_signer: false, is_writable: false }, "global_config")
        .account(naclac_client::AccountMeta { address: accounts.creator, is_signer: true, is_writable: true }, "creator")
        .account(naclac_client::AccountMeta { address: accounts.base_mint, is_signer: false, is_writable: false }, "base_mint")
        .account(naclac_client::AccountMeta { address: accounts.quote_mint, is_signer: false, is_writable: false }, "quote_mint")
        .account(naclac_client::AccountMeta { address: accounts.pool_base_token_account, is_signer: false, is_writable: false }, "pool_base_token_account")
        .account(naclac_client::AccountMeta { address: accounts.pool_quote_token_account, is_signer: false, is_writable: true }, "pool_quote_token_account")
        .account(naclac_client::AccountMeta { address: accounts.boost_vault_authority, is_signer: false, is_writable: false }, "boost_vault_authority")
        .account(naclac_client::AccountMeta { address: accounts.boost_vault, is_signer: false, is_writable: true }, "boost_vault")
        .account(naclac_client::AccountMeta { address: accounts.quote_token_program, is_signer: false, is_writable: false }, "quote_token_program")
        .account(naclac_client::AccountMeta { address: accounts.system_program, is_signer: false, is_writable: false }, "system_program")
        .account(naclac_client::AccountMeta { address: accounts.associated_token_program, is_signer: false, is_writable: false }, "associated_token_program")
}

#[cfg(feature = "cpi")]
/// Real behavior confirmed via `probe46.rs`/`probe48.rs`/`probe49.rs`
/// (litesvm, real bytecode) and 5 independent real mainnet `migrate_v2`
/// transactions (`docs/plan/amm-03-boost-mechanism.md`): `init_boost` moves
/// no funds of its own accord and computes no percentage — it reads
/// `pool_quote_token_account`'s and `pool_base_token_account`'s balances
/// *as they stand at call time*, computes `virtual_quote_reserves =
/// floor(quote_balance * base_balance / BOOST_BASE_SUPPLY_DIVISOR)`,
/// transfers that amount out of `pool_quote_token_account` into a
/// freshly-created `boost_vault`, and records it on `pool`. Confirmed with
/// zero error across 5 independent real transactions plus a direct blind
/// litesvm replication using a 9-decimal quote mint (ruling out a
/// quote-decimals dependency).
pub struct InitBoostCpiAccounts<'a> {
    /// SAFETY: only used as `InitBoostEvent`'s `bonding_curve` field — the
    /// real program derives this on-chain via `find_program_address` purely
    /// for event logging (not an account it ever reads), which naclac bans
    /// outright; our own `migrate_v2` (the only real caller) already has
    /// this account in its own context and passes it straight through.
    /// Never deserialized.
    pub bonding_curve: crate::sdk_core_cpi::CpiHandle<'a>,
    pub pool: crate::sdk_core_cpi::CpiHandleMut<'a>,
    pub global_config: crate::sdk_core_cpi::CpiHandle<'a>,
    pub creator: crate::sdk_core_cpi::CpiHandleMut<'a>,
    pub base_mint: crate::sdk_core_cpi::CpiHandle<'a>,
    pub quote_mint: crate::sdk_core_cpi::CpiHandle<'a>,
    pub pool_base_token_account: crate::sdk_core_cpi::CpiHandle<'a>,
    pub pool_quote_token_account: crate::sdk_core_cpi::CpiHandleMut<'a>,
    /// SAFETY: `seeds`/`bump` already verifies its address; a bare
    /// signing/seed PDA with no stored data (matches the real program — no
    /// dedicated `BoostVault`-type account exists), used below only as a CPI
    /// signer and as `boost_vault`'s ATA authority.
    pub boost_vault_authority: crate::sdk_core_cpi::CpiHandle<'a>,
    /// SAFETY: `init` + `associated_token::mint`/`::authority` below, plus
    /// the real Associated Token Program's own CPI-level address
    /// verification, fully validate and construct this account — there is
    /// no naclac `Discriminator` to check since this is a raw SPL
    /// `TokenAccount` layout (same reasoning as `create_pool`'s own
    /// freshly-`init`ed ATA fields).
    pub boost_vault: crate::sdk_core_cpi::CpiHandleMut<'a>,
    pub quote_token_program: crate::sdk_core_cpi::CpiHandle<'a>,
    pub system_program: crate::sdk_core_cpi::CpiHandle<'a>,
    pub associated_token_program: crate::sdk_core_cpi::CpiHandle<'a>,
}

#[cfg(feature = "cpi")]
pub struct InitBoostCpiCall<'a> {
    pub accounts: InitBoostCpiAccounts<'a>,
    pub boost_vault_authority_bump: u8,
    pub signer_seeds: &'a [&'a [&'a [u8]]],
    pub remaining_accounts: &'a [(crate::sdk_core_cpi::AccountInfo, bool, bool)],
}

#[cfg(feature = "cpi")]
pub trait InitBoostCpi<'info> {
    /// Real behavior confirmed via `probe46.rs`/`probe48.rs`/`probe49.rs`
    /// (litesvm, real bytecode) and 5 independent real mainnet `migrate_v2`
    /// transactions (`docs/plan/amm-03-boost-mechanism.md`): `init_boost` moves
    /// no funds of its own accord and computes no percentage — it reads
    /// `pool_quote_token_account`'s and `pool_base_token_account`'s balances
    /// *as they stand at call time*, computes `virtual_quote_reserves =
    /// floor(quote_balance * base_balance / BOOST_BASE_SUPPLY_DIVISOR)`,
    /// transfers that amount out of `pool_quote_token_account` into a
    /// freshly-created `boost_vault`, and records it on `pool`. Confirmed with
    /// zero error across 5 independent real transactions plus a direct blind
    /// litesvm replication using a 9-decimal quote mint (ruling out a
    /// quote-decimals dependency).
    fn init_boost<'a>(
        &self,
        accounts: InitBoostCpiAccounts<'a>,
        boost_vault_authority_bump: u8
    ) -> crate::sdk_core_cpi::Result<()> {
        self.init_boost_with_remaining_accounts(InitBoostCpiCall {
        accounts,
        boost_vault_authority_bump,
        signer_seeds: &[],
        remaining_accounts: &[],
    })
    }

    fn init_boost_signed<'a>(
        &self,
        accounts: InitBoostCpiAccounts<'a>,
        boost_vault_authority_bump: u8,
        signer_seeds: &[&[&[u8]]]
    ) -> crate::sdk_core_cpi::Result<()> {
        self.init_boost_with_remaining_accounts(InitBoostCpiCall {
        accounts,
        boost_vault_authority_bump,
        signer_seeds,
        remaining_accounts: &[],
    })
    }

    fn init_boost_with_remaining_accounts<'a>(
        &self,
        call: InitBoostCpiCall<'a>
    ) -> crate::sdk_core_cpi::Result<()>;

}

#[cfg(feature = "cpi")]
impl<'info> InitBoostCpi<'info> for crate::sdk_core_cpi::Program<crate::PumpAmm> {
    fn init_boost_with_remaining_accounts<'a>(
        &self,
        call: InitBoostCpiCall<'a>
    ) -> crate::sdk_core_cpi::Result<()> {
        let InitBoostCpiCall { accounts, boost_vault_authority_bump, signer_seeds, remaining_accounts } = call;
        let args = InitBoostCpiIxArgs {
            boost_vault_authority_bump,
        };
        let mut ix_data = crate::sdk_core_cpi::Vec::new();
        ix_data.extend_from_slice(&[140, 233, 33, 94, 132, 90, 194, 143]);
        #[cfg(not(feature = "borsh"))]
        {
        ix_data.extend_from_slice(crate::sdk_core_cpi::bytemuck::bytes_of(&args));
        }
        #[cfg(feature = "borsh")]
        {
            crate::sdk_core_cpi::borsh::BorshSerialize::serialize(&args, &mut ix_data).unwrap();
        }
        #[cfg(feature = "pinocchio")]
        {
            let mut metas_arr = [const { core::mem::MaybeUninit::<crate::sdk_core_cpi::pinocchio::instruction::InstructionAccount>::uninit() }; 32];
            let mut idx = 0;
            metas_arr[idx].write(crate::sdk_core_cpi::pinocchio::instruction::InstructionAccount::readonly(accounts.bonding_curve.info.view.address()));
            idx += 1;
            metas_arr[idx].write(crate::sdk_core_cpi::pinocchio::instruction::InstructionAccount::writable(accounts.pool.info.view.address()));
            idx += 1;
            metas_arr[idx].write(crate::sdk_core_cpi::pinocchio::instruction::InstructionAccount::readonly(accounts.global_config.info.view.address()));
            idx += 1;
            metas_arr[idx].write(crate::sdk_core_cpi::pinocchio::instruction::InstructionAccount::writable_signer(accounts.creator.info.view.address()));
            idx += 1;
            metas_arr[idx].write(crate::sdk_core_cpi::pinocchio::instruction::InstructionAccount::readonly(accounts.base_mint.info.view.address()));
            idx += 1;
            metas_arr[idx].write(crate::sdk_core_cpi::pinocchio::instruction::InstructionAccount::readonly(accounts.quote_mint.info.view.address()));
            idx += 1;
            metas_arr[idx].write(crate::sdk_core_cpi::pinocchio::instruction::InstructionAccount::readonly(accounts.pool_base_token_account.info.view.address()));
            idx += 1;
            metas_arr[idx].write(crate::sdk_core_cpi::pinocchio::instruction::InstructionAccount::writable(accounts.pool_quote_token_account.info.view.address()));
            idx += 1;
            metas_arr[idx].write(crate::sdk_core_cpi::pinocchio::instruction::InstructionAccount::readonly(accounts.boost_vault_authority.info.view.address()));
            idx += 1;
            metas_arr[idx].write(crate::sdk_core_cpi::pinocchio::instruction::InstructionAccount::writable(accounts.boost_vault.info.view.address()));
            idx += 1;
            metas_arr[idx].write(crate::sdk_core_cpi::pinocchio::instruction::InstructionAccount::readonly(accounts.quote_token_program.info.view.address()));
            idx += 1;
            metas_arr[idx].write(crate::sdk_core_cpi::pinocchio::instruction::InstructionAccount::readonly(accounts.system_program.info.view.address()));
            idx += 1;
            metas_arr[idx].write(crate::sdk_core_cpi::pinocchio::instruction::InstructionAccount::readonly(accounts.associated_token_program.info.view.address()));
            idx += 1;
            for (acc, is_writable, is_signer) in remaining_accounts {
                if idx >= 32 { break; }
                let meta = if *is_writable {
                    if *is_signer {
                        crate::sdk_core_cpi::pinocchio::instruction::InstructionAccount::writable_signer(acc.view.address())
                    } else {
                        crate::sdk_core_cpi::pinocchio::instruction::InstructionAccount::writable(acc.view.address())
                    }
                } else {
                    if *is_signer {
                        crate::sdk_core_cpi::pinocchio::instruction::InstructionAccount::readonly_signer(acc.view.address())
                    } else {
                        crate::sdk_core_cpi::pinocchio::instruction::InstructionAccount::readonly(acc.view.address())
                    }
                };
                metas_arr[idx].write(meta);
                idx += 1;
            }
            let account_metas = unsafe { core::slice::from_raw_parts(metas_arr.as_ptr() as *const crate::sdk_core_cpi::pinocchio::instruction::InstructionAccount, idx) };
            let program_id_address = self.address();
            let instruction = crate::sdk_core_cpi::pinocchio::instruction::InstructionView {
                program_id: program_id_address.as_address(),
                accounts: account_metas,
                data: &ix_data,
            };
            let mut handles_arr = [const { core::mem::MaybeUninit::<crate::sdk_core_cpi::CpiHandle>::uninit() }; 32];
            let mut h_idx = 0;
            handles_arr[h_idx].write(accounts.bonding_curve);
            h_idx += 1;
            handles_arr[h_idx].write(crate::sdk_core_cpi::CpiHandle::from(accounts.pool));
            h_idx += 1;
            handles_arr[h_idx].write(accounts.global_config);
            h_idx += 1;
            handles_arr[h_idx].write(crate::sdk_core_cpi::CpiHandle::from(accounts.creator));
            h_idx += 1;
            handles_arr[h_idx].write(accounts.base_mint);
            h_idx += 1;
            handles_arr[h_idx].write(accounts.quote_mint);
            h_idx += 1;
            handles_arr[h_idx].write(accounts.pool_base_token_account);
            h_idx += 1;
            handles_arr[h_idx].write(crate::sdk_core_cpi::CpiHandle::from(accounts.pool_quote_token_account));
            h_idx += 1;
            handles_arr[h_idx].write(accounts.boost_vault_authority);
            h_idx += 1;
            handles_arr[h_idx].write(crate::sdk_core_cpi::CpiHandle::from(accounts.boost_vault));
            h_idx += 1;
            handles_arr[h_idx].write(accounts.quote_token_program);
            h_idx += 1;
            handles_arr[h_idx].write(accounts.system_program);
            h_idx += 1;
            handles_arr[h_idx].write(accounts.associated_token_program);
            h_idx += 1;
            for (acc, _, _) in remaining_accounts {
                if h_idx >= 32 { break; }
                handles_arr[h_idx].write(crate::sdk_core_cpi::ToCpiHandle::to_cpi_handle(acc));
                h_idx += 1;
            }
            let account_handles = unsafe { core::slice::from_raw_parts(handles_arr.as_ptr() as *const crate::sdk_core_cpi::CpiHandle, h_idx) };
            crate::sdk_core_cpi::cpi::invoke_signed_pinocchio_handles(&instruction, account_handles, signer_seeds)
        }
        #[cfg(not(feature = "pinocchio"))]
        {
            use crate::sdk_core_cpi::ToAddress;
            let mut account_metas = crate::sdk_core_cpi::vec![
                ::naclac_lang::solana_program::instruction::AccountMeta::new_readonly(accounts.bonding_curve.address(), false),
                ::naclac_lang::solana_program::instruction::AccountMeta::new(accounts.pool.address(), false),
                ::naclac_lang::solana_program::instruction::AccountMeta::new_readonly(accounts.global_config.address(), false),
                ::naclac_lang::solana_program::instruction::AccountMeta::new(accounts.creator.address(), true),
                ::naclac_lang::solana_program::instruction::AccountMeta::new_readonly(accounts.base_mint.address(), false),
                ::naclac_lang::solana_program::instruction::AccountMeta::new_readonly(accounts.quote_mint.address(), false),
                ::naclac_lang::solana_program::instruction::AccountMeta::new_readonly(accounts.pool_base_token_account.address(), false),
                ::naclac_lang::solana_program::instruction::AccountMeta::new(accounts.pool_quote_token_account.address(), false),
                ::naclac_lang::solana_program::instruction::AccountMeta::new_readonly(accounts.boost_vault_authority.address(), false),
                ::naclac_lang::solana_program::instruction::AccountMeta::new(accounts.boost_vault.address(), false),
                ::naclac_lang::solana_program::instruction::AccountMeta::new_readonly(accounts.quote_token_program.address(), false),
                ::naclac_lang::solana_program::instruction::AccountMeta::new_readonly(accounts.system_program.address(), false),
                ::naclac_lang::solana_program::instruction::AccountMeta::new_readonly(accounts.associated_token_program.address(), false),
            ];
            for (acc, is_writable, is_signer) in remaining_accounts {
                let meta = if *is_writable {
                    ::naclac_lang::solana_program::instruction::AccountMeta::new(acc.address(), *is_signer)
                } else {
                    ::naclac_lang::solana_program::instruction::AccountMeta::new_readonly(acc.address(), *is_signer)
                };
                account_metas.push(meta);
            }
            let instruction = ::naclac_lang::solana_program::instruction::Instruction {
                program_id: self.address(),
                accounts: account_metas,
                data: ix_data,
            };
            let mut handles_arr = [const { core::mem::MaybeUninit::<crate::sdk_core_cpi::CpiHandle>::uninit() }; 32];
            let mut h_idx = 0;
            handles_arr[h_idx].write(accounts.bonding_curve);
            h_idx += 1;
            handles_arr[h_idx].write(crate::sdk_core_cpi::CpiHandle::from(accounts.pool));
            h_idx += 1;
            handles_arr[h_idx].write(accounts.global_config);
            h_idx += 1;
            handles_arr[h_idx].write(crate::sdk_core_cpi::CpiHandle::from(accounts.creator));
            h_idx += 1;
            handles_arr[h_idx].write(accounts.base_mint);
            h_idx += 1;
            handles_arr[h_idx].write(accounts.quote_mint);
            h_idx += 1;
            handles_arr[h_idx].write(accounts.pool_base_token_account);
            h_idx += 1;
            handles_arr[h_idx].write(crate::sdk_core_cpi::CpiHandle::from(accounts.pool_quote_token_account));
            h_idx += 1;
            handles_arr[h_idx].write(accounts.boost_vault_authority);
            h_idx += 1;
            handles_arr[h_idx].write(crate::sdk_core_cpi::CpiHandle::from(accounts.boost_vault));
            h_idx += 1;
            handles_arr[h_idx].write(accounts.quote_token_program);
            h_idx += 1;
            handles_arr[h_idx].write(accounts.system_program);
            h_idx += 1;
            handles_arr[h_idx].write(accounts.associated_token_program);
            h_idx += 1;
            for (acc, _, _) in remaining_accounts {
                if h_idx >= 32 { break; }
                handles_arr[h_idx].write(crate::sdk_core_cpi::ToCpiHandle::to_cpi_handle(acc));
                h_idx += 1;
            }
            let account_handles = unsafe { core::slice::from_raw_parts(handles_arr.as_ptr() as *const crate::sdk_core_cpi::CpiHandle, h_idx) };
            crate::sdk_core_cpi::cpi::invoke_signed(&instruction, account_handles, signer_seeds)
        }
    }
}

