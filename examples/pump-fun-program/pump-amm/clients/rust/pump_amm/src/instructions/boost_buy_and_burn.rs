// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

#[cfg(feature = "offchain")]
#[cfg(feature = "borsh")]
use crate::sdk_core_offchain::borsh::BorshSerialize;
#[cfg(feature = "offchain")]
#[cfg_attr(feature = "borsh", derive(Clone, Debug, BorshSerialize))]
#[cfg_attr(feature = "borsh", borsh(crate = "crate::sdk_core_offchain::borsh"))]
#[cfg_attr(not(feature = "borsh"), derive(Copy, Clone, Debug))]
#[cfg_attr(not(feature = "borsh"), repr(C, packed))]
pub struct BoostBuyAndBurnIxArgs {
    pub quote_amount_in: u64,
    pub min_base_amount_burned: u64,
    pub boost_vault_authority_bump: u8,
}

#[cfg(feature = "offchain")]
#[cfg(not(feature = "borsh"))]
unsafe impl crate::sdk_core_offchain::bytemuck::Zeroable for BoostBuyAndBurnIxArgs {}
#[cfg(feature = "offchain")]
#[cfg(not(feature = "borsh"))]
unsafe impl crate::sdk_core_offchain::bytemuck::Pod for BoostBuyAndBurnIxArgs {}

#[cfg(feature = "cpi")]
#[cfg(feature = "borsh")]
use crate::sdk_core_cpi::borsh::BorshSerialize;
#[cfg(feature = "cpi")]
#[cfg_attr(feature = "borsh", derive(Clone, Debug, BorshSerialize))]
#[cfg_attr(feature = "borsh", borsh(crate = "crate::sdk_core_cpi::borsh"))]
#[cfg_attr(not(feature = "borsh"), derive(Copy, Clone, Debug))]
#[cfg_attr(not(feature = "borsh"), repr(C, packed))]
pub struct BoostBuyAndBurnCpiIxArgs {
    pub quote_amount_in: u64,
    pub min_base_amount_burned: u64,
    pub boost_vault_authority_bump: u8,
}

#[cfg(feature = "cpi")]
#[cfg(not(feature = "borsh"))]
unsafe impl crate::sdk_core_cpi::bytemuck::Zeroable for BoostBuyAndBurnCpiIxArgs {}
#[cfg(feature = "cpi")]
#[cfg(not(feature = "borsh"))]
unsafe impl crate::sdk_core_cpi::bytemuck::Pod for BoostBuyAndBurnCpiIxArgs {}

#[cfg(feature = "offchain")]
/// Real formula confirmed via `probe46.rs` (litesvm, real bytecode),
/// `docs/plan/amm-03-boost-mechanism.md`: internally a genuine constant-
/// product buy against the pool using `effective_quote_reserves = real +
/// virtual` as the pricing denominator, with the same "amount − 1"
/// conservative-rounding pattern already confirmed for `buy_exact_sol_in`
/// in the bonding-curve program: `base_out = floor(base_reserves *
/// (net_quote - 1) / (effective_quote_reserves + net_quote - 1))`. 100% of
/// `base_out` is burned (no partial-burn leftover observed).
/// `pool.virtual_quote_reserves` is left completely unchanged — it's a
/// fixed pricing-depth constant set once by `init_boost`, not a running
/// counter.
/// 
/// `quote_amount_in_used` is capped at `boost_vault`'s current balance —
/// this specific capping behavior (vs. e.g. erroring outright) was not
/// empirically exercised (the one real/probe example available always had
/// `quote_amount_in_used == quote_amount_in_requested`), inferred from the
/// event's separate `_requested`/`_used` fields, which would otherwise have
/// no reason to differ.
pub struct BoostBuyAndBurnAccounts {
    /// SAFETY: only used as `BoostBuyAndBurnEvent`'s `bonding_curve` field
    /// (see `init_boost.rs`'s own account of the same name and reasoning —
    /// the real program derives this on-chain purely for event logging,
    /// which naclac bans; the caller computes it off-chain and passes it
    /// in). Never deserialized.
    pub bonding_curve: naclac_client::Address,
    pub pool: naclac_client::Address,
    pub authority: naclac_client::Address,
    pub global_config: naclac_client::Address,
    pub base_mint: naclac_client::Address,
    pub quote_mint: naclac_client::Address,
    pub pool_base_token_account: naclac_client::Address,
    pub pool_quote_token_account: naclac_client::Address,
    /// SAFETY: `seeds`/`bump` already verifies its address; a bare
    /// signing/seed PDA with no stored data, used below only as a CPI
    /// signer (matches `init_boost.rs`'s own account of the same name).
    pub boost_vault_authority: naclac_client::Address,
    pub boost_vault: naclac_client::Address,
    pub base_token_program: naclac_client::Address,
    pub quote_token_program: naclac_client::Address,
}

#[cfg(feature = "offchain")]
/// Real formula confirmed via `probe46.rs` (litesvm, real bytecode),
/// `docs/plan/amm-03-boost-mechanism.md`: internally a genuine constant-
/// product buy against the pool using `effective_quote_reserves = real +
/// virtual` as the pricing denominator, with the same "amount − 1"
/// conservative-rounding pattern already confirmed for `buy_exact_sol_in`
/// in the bonding-curve program: `base_out = floor(base_reserves *
/// (net_quote - 1) / (effective_quote_reserves + net_quote - 1))`. 100% of
/// `base_out` is burned (no partial-burn leftover observed).
/// `pool.virtual_quote_reserves` is left completely unchanged — it's a
/// fixed pricing-depth constant set once by `init_boost`, not a running
/// counter.
/// 
/// `quote_amount_in_used` is capped at `boost_vault`'s current balance —
/// this specific capping behavior (vs. e.g. erroring outright) was not
/// empirically exercised (the one real/probe example available always had
/// `quote_amount_in_used == quote_amount_in_requested`), inferred from the
/// event's separate `_requested`/`_used` fields, which would otherwise have
/// no reason to differ.
pub fn build_boost_buy_and_burn<'a>(
    provider: &'a naclac_client::NaclacProvider,
    program_id: naclac_client::Address,
    quote_amount_in: u64,
    min_base_amount_burned: u64,
    boost_vault_authority_bump: u8,
    accounts: BoostBuyAndBurnAccounts,
) -> naclac_client::InstructionBuilder<'a> {
    let mut ix_data = crate::sdk_core_offchain::vec![105, 68, 6, 175, 0, 7, 35, 162];
    let args = BoostBuyAndBurnIxArgs {
        quote_amount_in,
        min_base_amount_burned,
        boost_vault_authority_bump,
    };
    #[cfg(not(feature = "borsh"))]
    {
        ix_data.extend_from_slice(crate::sdk_core_offchain::bytemuck::bytes_of(&args));
    }
    #[cfg(feature = "borsh")]
    {
        crate::sdk_core_offchain::borsh::BorshSerialize::serialize(&args, &mut ix_data).unwrap();
    }
    naclac_client::InstructionBuilder::new(provider, program_id, ix_data)
        .account(naclac_client::AccountMeta { address: accounts.bonding_curve, is_signer: false, is_writable: true }, "bonding_curve")
        .account(naclac_client::AccountMeta { address: accounts.pool, is_signer: false, is_writable: true }, "pool")
        .account(naclac_client::AccountMeta { address: accounts.authority, is_signer: true, is_writable: true }, "authority")
        .account(naclac_client::AccountMeta { address: accounts.global_config, is_signer: false, is_writable: false }, "global_config")
        .account(naclac_client::AccountMeta { address: accounts.base_mint, is_signer: false, is_writable: true }, "base_mint")
        .account(naclac_client::AccountMeta { address: accounts.quote_mint, is_signer: false, is_writable: false }, "quote_mint")
        .account(naclac_client::AccountMeta { address: accounts.pool_base_token_account, is_signer: false, is_writable: true }, "pool_base_token_account")
        .account(naclac_client::AccountMeta { address: accounts.pool_quote_token_account, is_signer: false, is_writable: true }, "pool_quote_token_account")
        .account(naclac_client::AccountMeta { address: accounts.boost_vault_authority, is_signer: false, is_writable: true }, "boost_vault_authority")
        .account(naclac_client::AccountMeta { address: accounts.boost_vault, is_signer: false, is_writable: true }, "boost_vault")
        .account(naclac_client::AccountMeta { address: accounts.base_token_program, is_signer: false, is_writable: false }, "base_token_program")
        .account(naclac_client::AccountMeta { address: accounts.quote_token_program, is_signer: false, is_writable: false }, "quote_token_program")
}

#[cfg(feature = "cpi")]
/// Real formula confirmed via `probe46.rs` (litesvm, real bytecode),
/// `docs/plan/amm-03-boost-mechanism.md`: internally a genuine constant-
/// product buy against the pool using `effective_quote_reserves = real +
/// virtual` as the pricing denominator, with the same "amount − 1"
/// conservative-rounding pattern already confirmed for `buy_exact_sol_in`
/// in the bonding-curve program: `base_out = floor(base_reserves *
/// (net_quote - 1) / (effective_quote_reserves + net_quote - 1))`. 100% of
/// `base_out` is burned (no partial-burn leftover observed).
/// `pool.virtual_quote_reserves` is left completely unchanged — it's a
/// fixed pricing-depth constant set once by `init_boost`, not a running
/// counter.
/// 
/// `quote_amount_in_used` is capped at `boost_vault`'s current balance —
/// this specific capping behavior (vs. e.g. erroring outright) was not
/// empirically exercised (the one real/probe example available always had
/// `quote_amount_in_used == quote_amount_in_requested`), inferred from the
/// event's separate `_requested`/`_used` fields, which would otherwise have
/// no reason to differ.
pub struct BoostBuyAndBurnCpiAccounts<'a> {
    /// SAFETY: only used as `BoostBuyAndBurnEvent`'s `bonding_curve` field
    /// (see `init_boost.rs`'s own account of the same name and reasoning —
    /// the real program derives this on-chain purely for event logging,
    /// which naclac bans; the caller computes it off-chain and passes it
    /// in). Never deserialized.
    pub bonding_curve: crate::sdk_core_cpi::CpiHandleMut<'a>,
    pub pool: crate::sdk_core_cpi::CpiHandleMut<'a>,
    pub authority: crate::sdk_core_cpi::CpiHandleMut<'a>,
    pub global_config: crate::sdk_core_cpi::CpiHandle<'a>,
    pub base_mint: crate::sdk_core_cpi::CpiHandleMut<'a>,
    pub quote_mint: crate::sdk_core_cpi::CpiHandle<'a>,
    pub pool_base_token_account: crate::sdk_core_cpi::CpiHandleMut<'a>,
    pub pool_quote_token_account: crate::sdk_core_cpi::CpiHandleMut<'a>,
    /// SAFETY: `seeds`/`bump` already verifies its address; a bare
    /// signing/seed PDA with no stored data, used below only as a CPI
    /// signer (matches `init_boost.rs`'s own account of the same name).
    pub boost_vault_authority: crate::sdk_core_cpi::CpiHandleMut<'a>,
    pub boost_vault: crate::sdk_core_cpi::CpiHandleMut<'a>,
    pub base_token_program: crate::sdk_core_cpi::CpiHandle<'a>,
    pub quote_token_program: crate::sdk_core_cpi::CpiHandle<'a>,
}

#[cfg(feature = "cpi")]
pub struct BoostBuyAndBurnCpiCall<'a> {
    pub accounts: BoostBuyAndBurnCpiAccounts<'a>,
    pub quote_amount_in: u64,
    pub min_base_amount_burned: u64,
    pub boost_vault_authority_bump: u8,
    pub signer_seeds: &'a [&'a [&'a [u8]]],
    pub remaining_accounts: &'a [(crate::sdk_core_cpi::AccountInfo, bool, bool)],
}

#[cfg(feature = "cpi")]
pub trait BoostBuyAndBurnCpi<'info> {
    /// Real formula confirmed via `probe46.rs` (litesvm, real bytecode),
    /// `docs/plan/amm-03-boost-mechanism.md`: internally a genuine constant-
    /// product buy against the pool using `effective_quote_reserves = real +
    /// virtual` as the pricing denominator, with the same "amount − 1"
    /// conservative-rounding pattern already confirmed for `buy_exact_sol_in`
    /// in the bonding-curve program: `base_out = floor(base_reserves *
    /// (net_quote - 1) / (effective_quote_reserves + net_quote - 1))`. 100% of
    /// `base_out` is burned (no partial-burn leftover observed).
    /// `pool.virtual_quote_reserves` is left completely unchanged — it's a
    /// fixed pricing-depth constant set once by `init_boost`, not a running
    /// counter.
    /// 
    /// `quote_amount_in_used` is capped at `boost_vault`'s current balance —
    /// this specific capping behavior (vs. e.g. erroring outright) was not
    /// empirically exercised (the one real/probe example available always had
    /// `quote_amount_in_used == quote_amount_in_requested`), inferred from the
    /// event's separate `_requested`/`_used` fields, which would otherwise have
    /// no reason to differ.
    fn boost_buy_and_burn<'a>(
        &self,
        accounts: BoostBuyAndBurnCpiAccounts<'a>,
        quote_amount_in: u64,
        min_base_amount_burned: u64,
        boost_vault_authority_bump: u8
    ) -> crate::sdk_core_cpi::Result<()> {
        self.boost_buy_and_burn_with_remaining_accounts(BoostBuyAndBurnCpiCall {
        accounts,
        quote_amount_in,
        min_base_amount_burned,
        boost_vault_authority_bump,
        signer_seeds: &[],
        remaining_accounts: &[],
    })
    }

    fn boost_buy_and_burn_signed<'a>(
        &self,
        accounts: BoostBuyAndBurnCpiAccounts<'a>,
        quote_amount_in: u64,
        min_base_amount_burned: u64,
        boost_vault_authority_bump: u8,
        signer_seeds: &[&[&[u8]]]
    ) -> crate::sdk_core_cpi::Result<()> {
        self.boost_buy_and_burn_with_remaining_accounts(BoostBuyAndBurnCpiCall {
        accounts,
        quote_amount_in,
        min_base_amount_burned,
        boost_vault_authority_bump,
        signer_seeds,
        remaining_accounts: &[],
    })
    }

    fn boost_buy_and_burn_with_remaining_accounts<'a>(
        &self,
        call: BoostBuyAndBurnCpiCall<'a>
    ) -> crate::sdk_core_cpi::Result<()>;

}

#[cfg(feature = "cpi")]
impl<'info> BoostBuyAndBurnCpi<'info> for crate::sdk_core_cpi::Program<crate::PumpAmm> {
    fn boost_buy_and_burn_with_remaining_accounts<'a>(
        &self,
        call: BoostBuyAndBurnCpiCall<'a>
    ) -> crate::sdk_core_cpi::Result<()> {
        let BoostBuyAndBurnCpiCall { accounts, quote_amount_in, min_base_amount_burned, boost_vault_authority_bump, signer_seeds, remaining_accounts } = call;
        let args = BoostBuyAndBurnCpiIxArgs {
            quote_amount_in,
            min_base_amount_burned,
            boost_vault_authority_bump,
        };
        let mut ix_data = crate::sdk_core_cpi::Vec::new();
        ix_data.extend_from_slice(&[105, 68, 6, 175, 0, 7, 35, 162]);
        #[cfg(not(feature = "borsh"))]
        {
        ix_data.extend_from_slice(crate::sdk_core_cpi::bytemuck::bytes_of(&args));
        }
        #[cfg(feature = "borsh")]
        {
            crate::sdk_core_cpi::borsh::BorshSerialize::serialize(&args, &mut ix_data).unwrap();
        }
        #[cfg(feature = "pinocchio")]
        {
            let mut metas_arr = [const { core::mem::MaybeUninit::<crate::sdk_core_cpi::pinocchio::instruction::InstructionAccount>::uninit() }; 32];
            let mut idx = 0;
            metas_arr[idx].write(crate::sdk_core_cpi::pinocchio::instruction::InstructionAccount::writable(accounts.bonding_curve.info.view.address()));
            idx += 1;
            metas_arr[idx].write(crate::sdk_core_cpi::pinocchio::instruction::InstructionAccount::writable(accounts.pool.info.view.address()));
            idx += 1;
            metas_arr[idx].write(crate::sdk_core_cpi::pinocchio::instruction::InstructionAccount::writable_signer(accounts.authority.info.view.address()));
            idx += 1;
            metas_arr[idx].write(crate::sdk_core_cpi::pinocchio::instruction::InstructionAccount::readonly(accounts.global_config.info.view.address()));
            idx += 1;
            metas_arr[idx].write(crate::sdk_core_cpi::pinocchio::instruction::InstructionAccount::writable(accounts.base_mint.info.view.address()));
            idx += 1;
            metas_arr[idx].write(crate::sdk_core_cpi::pinocchio::instruction::InstructionAccount::readonly(accounts.quote_mint.info.view.address()));
            idx += 1;
            metas_arr[idx].write(crate::sdk_core_cpi::pinocchio::instruction::InstructionAccount::writable(accounts.pool_base_token_account.info.view.address()));
            idx += 1;
            metas_arr[idx].write(crate::sdk_core_cpi::pinocchio::instruction::InstructionAccount::writable(accounts.pool_quote_token_account.info.view.address()));
            idx += 1;
            metas_arr[idx].write(crate::sdk_core_cpi::pinocchio::instruction::InstructionAccount::writable(accounts.boost_vault_authority.info.view.address()));
            idx += 1;
            metas_arr[idx].write(crate::sdk_core_cpi::pinocchio::instruction::InstructionAccount::writable(accounts.boost_vault.info.view.address()));
            idx += 1;
            metas_arr[idx].write(crate::sdk_core_cpi::pinocchio::instruction::InstructionAccount::readonly(accounts.base_token_program.info.view.address()));
            idx += 1;
            metas_arr[idx].write(crate::sdk_core_cpi::pinocchio::instruction::InstructionAccount::readonly(accounts.quote_token_program.info.view.address()));
            idx += 1;
            for (acc, is_writable, is_signer) in remaining_accounts {
                if idx >= 32 { break; }
                let meta = if *is_writable {
                    if *is_signer {
                        crate::sdk_core_cpi::pinocchio::instruction::InstructionAccount::writable_signer(acc.view.address())
                    } else {
                        crate::sdk_core_cpi::pinocchio::instruction::InstructionAccount::writable(acc.view.address())
                    }
                } else {
                    if *is_signer {
                        crate::sdk_core_cpi::pinocchio::instruction::InstructionAccount::readonly_signer(acc.view.address())
                    } else {
                        crate::sdk_core_cpi::pinocchio::instruction::InstructionAccount::readonly(acc.view.address())
                    }
                };
                metas_arr[idx].write(meta);
                idx += 1;
            }
            let account_metas = unsafe { core::slice::from_raw_parts(metas_arr.as_ptr() as *const crate::sdk_core_cpi::pinocchio::instruction::InstructionAccount, idx) };
            let program_id_address = self.address();
            let instruction = crate::sdk_core_cpi::pinocchio::instruction::InstructionView {
                program_id: program_id_address.as_address(),
                accounts: account_metas,
                data: &ix_data,
            };
            let mut handles_arr = [const { core::mem::MaybeUninit::<crate::sdk_core_cpi::CpiHandle>::uninit() }; 32];
            let mut h_idx = 0;
            handles_arr[h_idx].write(crate::sdk_core_cpi::CpiHandle::from(accounts.bonding_curve));
            h_idx += 1;
            handles_arr[h_idx].write(crate::sdk_core_cpi::CpiHandle::from(accounts.pool));
            h_idx += 1;
            handles_arr[h_idx].write(crate::sdk_core_cpi::CpiHandle::from(accounts.authority));
            h_idx += 1;
            handles_arr[h_idx].write(accounts.global_config);
            h_idx += 1;
            handles_arr[h_idx].write(crate::sdk_core_cpi::CpiHandle::from(accounts.base_mint));
            h_idx += 1;
            handles_arr[h_idx].write(accounts.quote_mint);
            h_idx += 1;
            handles_arr[h_idx].write(crate::sdk_core_cpi::CpiHandle::from(accounts.pool_base_token_account));
            h_idx += 1;
            handles_arr[h_idx].write(crate::sdk_core_cpi::CpiHandle::from(accounts.pool_quote_token_account));
            h_idx += 1;
            handles_arr[h_idx].write(crate::sdk_core_cpi::CpiHandle::from(accounts.boost_vault_authority));
            h_idx += 1;
            handles_arr[h_idx].write(crate::sdk_core_cpi::CpiHandle::from(accounts.boost_vault));
            h_idx += 1;
            handles_arr[h_idx].write(accounts.base_token_program);
            h_idx += 1;
            handles_arr[h_idx].write(accounts.quote_token_program);
            h_idx += 1;
            for (acc, _, _) in remaining_accounts {
                if h_idx >= 32 { break; }
                handles_arr[h_idx].write(crate::sdk_core_cpi::ToCpiHandle::to_cpi_handle(acc));
                h_idx += 1;
            }
            let account_handles = unsafe { core::slice::from_raw_parts(handles_arr.as_ptr() as *const crate::sdk_core_cpi::CpiHandle, h_idx) };
            crate::sdk_core_cpi::cpi::invoke_signed_pinocchio_handles(&instruction, account_handles, signer_seeds)
        }
        #[cfg(not(feature = "pinocchio"))]
        {
            use crate::sdk_core_cpi::ToAddress;
            let mut account_metas = crate::sdk_core_cpi::vec![
                ::naclac_lang::solana_program::instruction::AccountMeta::new(accounts.bonding_curve.address(), false),
                ::naclac_lang::solana_program::instruction::AccountMeta::new(accounts.pool.address(), false),
                ::naclac_lang::solana_program::instruction::AccountMeta::new(accounts.authority.address(), true),
                ::naclac_lang::solana_program::instruction::AccountMeta::new_readonly(accounts.global_config.address(), false),
                ::naclac_lang::solana_program::instruction::AccountMeta::new(accounts.base_mint.address(), false),
                ::naclac_lang::solana_program::instruction::AccountMeta::new_readonly(accounts.quote_mint.address(), false),
                ::naclac_lang::solana_program::instruction::AccountMeta::new(accounts.pool_base_token_account.address(), false),
                ::naclac_lang::solana_program::instruction::AccountMeta::new(accounts.pool_quote_token_account.address(), false),
                ::naclac_lang::solana_program::instruction::AccountMeta::new(accounts.boost_vault_authority.address(), false),
                ::naclac_lang::solana_program::instruction::AccountMeta::new(accounts.boost_vault.address(), false),
                ::naclac_lang::solana_program::instruction::AccountMeta::new_readonly(accounts.base_token_program.address(), false),
                ::naclac_lang::solana_program::instruction::AccountMeta::new_readonly(accounts.quote_token_program.address(), false),
            ];
            for (acc, is_writable, is_signer) in remaining_accounts {
                let meta = if *is_writable {
                    ::naclac_lang::solana_program::instruction::AccountMeta::new(acc.address(), *is_signer)
                } else {
                    ::naclac_lang::solana_program::instruction::AccountMeta::new_readonly(acc.address(), *is_signer)
                };
                account_metas.push(meta);
            }
            let instruction = ::naclac_lang::solana_program::instruction::Instruction {
                program_id: self.address(),
                accounts: account_metas,
                data: ix_data,
            };
            let mut handles_arr = [const { core::mem::MaybeUninit::<crate::sdk_core_cpi::CpiHandle>::uninit() }; 32];
            let mut h_idx = 0;
            handles_arr[h_idx].write(crate::sdk_core_cpi::CpiHandle::from(accounts.bonding_curve));
            h_idx += 1;
            handles_arr[h_idx].write(crate::sdk_core_cpi::CpiHandle::from(accounts.pool));
            h_idx += 1;
            handles_arr[h_idx].write(crate::sdk_core_cpi::CpiHandle::from(accounts.authority));
            h_idx += 1;
            handles_arr[h_idx].write(accounts.global_config);
            h_idx += 1;
            handles_arr[h_idx].write(crate::sdk_core_cpi::CpiHandle::from(accounts.base_mint));
            h_idx += 1;
            handles_arr[h_idx].write(accounts.quote_mint);
            h_idx += 1;
            handles_arr[h_idx].write(crate::sdk_core_cpi::CpiHandle::from(accounts.pool_base_token_account));
            h_idx += 1;
            handles_arr[h_idx].write(crate::sdk_core_cpi::CpiHandle::from(accounts.pool_quote_token_account));
            h_idx += 1;
            handles_arr[h_idx].write(crate::sdk_core_cpi::CpiHandle::from(accounts.boost_vault_authority));
            h_idx += 1;
            handles_arr[h_idx].write(crate::sdk_core_cpi::CpiHandle::from(accounts.boost_vault));
            h_idx += 1;
            handles_arr[h_idx].write(accounts.base_token_program);
            h_idx += 1;
            handles_arr[h_idx].write(accounts.quote_token_program);
            h_idx += 1;
            for (acc, _, _) in remaining_accounts {
                if h_idx >= 32 { break; }
                handles_arr[h_idx].write(crate::sdk_core_cpi::ToCpiHandle::to_cpi_handle(acc));
                h_idx += 1;
            }
            let account_handles = unsafe { core::slice::from_raw_parts(handles_arr.as_ptr() as *const crate::sdk_core_cpi::CpiHandle, h_idx) };
            crate::sdk_core_cpi::cpi::invoke_signed(&instruction, account_handles, signer_seeds)
        }
    }
}

