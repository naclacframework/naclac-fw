// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for `CreatePoolEvent` events in transaction logs. */
export const CREATEPOOLEVENT_EVENT_DISCRIMINATOR = new Uint8Array([177, 49, 12, 210, 160, 118, 167, 116]);

/**
 * Field-for-field mirror of the real `pump_amm::CreatePoolEvent` (confirmed
 * against `reference/pump-rust-client/idls/pump_amm.json`).
 */
export interface CreatePoolEvent {
  timestamp: bigint | number;
  creator: naclac.Address | string;
  base_mint: naclac.Address | string;
  quote_mint: naclac.Address | string;
  base_amount_in: bigint | number;
  quote_amount_in: bigint | number;
  pool_base_amount: bigint | number;
  pool_quote_amount: bigint | number;
  minimum_liquidity: bigint | number;
  initial_liquidity: bigint | number;
  lp_token_amount_out: bigint | number;
  pool: naclac.Address | string;
  lp_mint: naclac.Address | string;
  user_base_token_account: naclac.Address | string;
  user_quote_token_account: naclac.Address | string;
  coin_creator: naclac.Address | string;
  index: number;
  base_mint_decimals: number;
  quote_mint_decimals: number;
  pool_bump: number;
  is_mayhem_mode: naclac.Bool;
  padding: string | Uint8Array;
}

/** Subscribes to `CreatePoolEvent` events. Returns a listener ID for cleanup. */
export function addCreatePoolEventListener(
  program: any,
  callback: (event: CreatePoolEvent, slot: number, signature: string) => void
): number {
  return program.addEventListener("CreatePoolEvent", callback);
}

/** Waits for the next `CreatePoolEvent` event. Resolves `null` if the timeout expires. */
export async function waitForCreatePoolEvent(
  program: any,
  options?: { timeoutMs?: number }
): Promise<CreatePoolEvent | null> {
  return program.waitForEvent("CreatePoolEvent", options) as Promise<CreatePoolEvent | null>;
}

/** Decodes every `CreatePoolEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForCreatePoolEvent`/`addCreatePoolEventListener` — prefer this when you already know which transaction you're checking. */
export function parseCreatePoolEventEvents(
  program: any,
  logs: readonly string[]
): CreatePoolEvent[] {
  return program.parseEvents("CreatePoolEvent", logs) as CreatePoolEvent[];
}

/** 8-byte discriminator for `InitBoostEvent` events in transaction logs. */
export const INITBOOSTEVENT_EVENT_DISCRIMINATOR = new Uint8Array([174, 124, 74, 249, 4, 81, 246, 17]);

/**
 * Field-for-field mirror of the real `pump_amm::InitBoostEvent`. Field
 * order (like `CreatePoolEvent` above) is alignment-optimized, not the
 * real IDL's own listing order — `i128` (16-byte alignment) leads so no
 * padding gets inserted before it.
 */
export interface InitBoostEvent {
  virtual_quote_reserves: bigint | number;
  timestamp: bigint | number;
  real_quote_reserves_after: bigint | number;
  mint: naclac.Address | string;
  bonding_curve: naclac.Address | string;
  pool: naclac.Address | string;
}

/** Subscribes to `InitBoostEvent` events. Returns a listener ID for cleanup. */
export function addInitBoostEventListener(
  program: any,
  callback: (event: InitBoostEvent, slot: number, signature: string) => void
): number {
  return program.addEventListener("InitBoostEvent", callback);
}

/** Waits for the next `InitBoostEvent` event. Resolves `null` if the timeout expires. */
export async function waitForInitBoostEvent(
  program: any,
  options?: { timeoutMs?: number }
): Promise<InitBoostEvent | null> {
  return program.waitForEvent("InitBoostEvent", options) as Promise<InitBoostEvent | null>;
}

/** Decodes every `InitBoostEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForInitBoostEvent`/`addInitBoostEventListener` — prefer this when you already know which transaction you're checking. */
export function parseInitBoostEventEvents(
  program: any,
  logs: readonly string[]
): InitBoostEvent[] {
  return program.parseEvents("InitBoostEvent", logs) as InitBoostEvent[];
}

/** 8-byte discriminator for `BoostBuyAndBurnEvent` events in transaction logs. */
export const BOOSTBUYANDBURNEVENT_EVENT_DISCRIMINATOR = new Uint8Array([63, 69, 28, 22, 48, 92, 194, 185]);

/** Field-for-field mirror of the real `pump_amm::BoostBuyAndBurnEvent`. */
export interface BoostBuyAndBurnEvent {
  virtual_quote_reserves: bigint | number;
  timestamp: bigint | number;
  quote_amount_in_requested: bigint | number;
  quote_amount_in_used: bigint | number;
  base_amount_burned: bigint | number;
  real_quote_reserves_after: bigint | number;
  base_reserves_after: bigint | number;
  boost_vault_remaining: bigint | number;
  mint: naclac.Address | string;
  bonding_curve: naclac.Address | string;
  pool: naclac.Address | string;
  authority: naclac.Address | string;
}

/** Subscribes to `BoostBuyAndBurnEvent` events. Returns a listener ID for cleanup. */
export function addBoostBuyAndBurnEventListener(
  program: any,
  callback: (event: BoostBuyAndBurnEvent, slot: number, signature: string) => void
): number {
  return program.addEventListener("BoostBuyAndBurnEvent", callback);
}

/** Waits for the next `BoostBuyAndBurnEvent` event. Resolves `null` if the timeout expires. */
export async function waitForBoostBuyAndBurnEvent(
  program: any,
  options?: { timeoutMs?: number }
): Promise<BoostBuyAndBurnEvent | null> {
  return program.waitForEvent("BoostBuyAndBurnEvent", options) as Promise<BoostBuyAndBurnEvent | null>;
}

/** Decodes every `BoostBuyAndBurnEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForBoostBuyAndBurnEvent`/`addBoostBuyAndBurnEventListener` — prefer this when you already know which transaction you're checking. */
export function parseBoostBuyAndBurnEventEvents(
  program: any,
  logs: readonly string[]
): BoostBuyAndBurnEvent[] {
  return program.parseEvents("BoostBuyAndBurnEvent", logs) as BoostBuyAndBurnEvent[];
}

/** Removes a previously registered event listener. */
export function removeListener(program: any, listenerId: number) {
  program.removeEventListener(listenerId);
}
