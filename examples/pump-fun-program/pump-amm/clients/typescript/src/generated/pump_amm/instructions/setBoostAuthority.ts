// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `set_boost_authority` instruction. */
export const SET_BOOST_AUTHORITY_DISCRIMINATOR = new Uint8Array([227, 149, 76, 42, 130, 39, 234, 205]);

/** Accounts for the `setBoostAuthority` instruction. */
export interface SetBoostAuthorityAccounts {
  admin: naclac.Address | string;
  global_config?: naclac.Address | string;
  /**
   * SAFETY: the new `boost_authority` value is this account's own
   * address (real `set_boost_authority` takes zero args, matching this)
   * — never deserialized, only its address is read.
   */
  boost_authority: naclac.Address | string;
  system_program?: naclac.Address | string;
}

/**
 * Builds the `setBoostAuthority` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function setBoostAuthority(
  program: any,
  args?: Record<string, never>,
  accounts?: Partial<SetBoostAuthorityAccounts>
) {
  const builder = program.methods.set_boost_authority(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
