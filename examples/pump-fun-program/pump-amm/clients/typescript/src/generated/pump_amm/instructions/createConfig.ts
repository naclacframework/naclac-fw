// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `create_config` instruction. */
export const CREATE_CONFIG_DISCRIMINATOR = new Uint8Array([201, 207, 243, 114, 75, 111, 47, 189]);

/** Accounts for the `createConfig` instruction. */
export interface CreateConfigAccounts {
  admin?: naclac.Address | string;
  globalConfig?: naclac.Address | string;
  systemProgram?: naclac.Address | string;
}

/**
 * Builds the `createConfig` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 * 
 * Creates the singleton `GlobalConfig`, gated by the project's own
 * `ADMIN_PUBKEY` (see its doc comment for why this diverges from real
 * `pump_amm`'s hardcoded admin). Leaves `boost_enabled`/`boost_authority`
 * at their zero-init defaults -- `toggle_boost`/`set_boost_authority`
 * (both gated on `global_config.admin`, set here) enable them separately,
 * matching real `create_config`'s own args, which don't include boost
 * fields at all.
 */
export function createConfig(
  program: any,
  args?: Record<string, never>,
  accounts?: Partial<CreateConfigAccounts>
) {
  const builder = program.methods.create_config(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
