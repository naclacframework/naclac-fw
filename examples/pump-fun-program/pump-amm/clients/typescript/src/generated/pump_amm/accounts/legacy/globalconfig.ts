// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client/legacy";
import type { GlobalConfig } from "../../types";

/** 8-byte on-chain discriminator for `GlobalConfig` accounts. */
export const GLOBALCONFIG_DISCRIMINATOR = new Uint8Array([149, 8, 156, 202, 160, 252, 176, 217]);

/** Fetches a `GlobalConfig` account. Throws if it does not exist. */
export async function fetchGlobalConfig(
  program: naclac.LegacyProgram,
  address: naclac.PublicKey | string,
): Promise<GlobalConfig> {
  const result = await program.account['GlobalConfig'].fetch(address);
  return result as unknown as GlobalConfig;
}

/** Fetches a `GlobalConfig` account. Returns `null` if it does not exist. */
export async function fetchMaybeGlobalConfig(
  program: naclac.LegacyProgram,
  address: naclac.PublicKey | string,
): Promise<GlobalConfig | null> {
  try {
    const result = await program.account['GlobalConfig'].fetch(address);
    return result as unknown as GlobalConfig;
  } catch {
    return null;
  }
}

/** Fetches multiple `GlobalConfig` accounts. Missing accounts are `null`. */
export async function fetchAllGlobalConfig(
  program: naclac.LegacyProgram,
  addresses: Array<naclac.PublicKey | string>,
  opts?: { dropCorrupted?: boolean },
): Promise<Array<GlobalConfig | null>> {
  const results = await program.account['GlobalConfig'].fetchMultiple(addresses, opts);
  return results as unknown as Array<GlobalConfig | null>;
}

/** Fetches all on-chain `GlobalConfig` accounts owned by this program. */
export async function fetchAllGlobalConfigByOwner(
  program: naclac.LegacyProgram,
  options?: { dropCorrupted?: boolean; filters?: any[] },
): Promise<Array<{ publicKey: naclac.PublicKey; account: GlobalConfig }>> {
  const results = await program.account['GlobalConfig'].all(options);
  return results as unknown as Array<{ publicKey: naclac.PublicKey; account: GlobalConfig }>;
}
