// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client";

/** 8-byte discriminator for the `toggle_boost` instruction. */
export const TOGGLE_BOOST_DISCRIMINATOR = new Uint8Array([117, 161, 160, 74, 223, 137, 118, 99]);

/** Instruction arguments for `toggleBoost`. */
export interface ToggleBoostArgs {
  enabled: naclac.Bool;
}

/** Accounts for the `toggleBoost` instruction. */
export interface ToggleBoostAccounts {
  admin: naclac.Address | string;
  globalConfig?: naclac.Address | string;
}

/**
 * Builds the `toggleBoost` instruction pipeline.
 * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
 */
export function toggleBoost(
  program: any,
  args: ToggleBoostArgs,
  accounts?: Partial<ToggleBoostAccounts>
) {
  const builder = program.methods.toggle_boost(args ?? {});
  if (accounts) {
    return builder.accounts(accounts);
  }
  return builder;
}
