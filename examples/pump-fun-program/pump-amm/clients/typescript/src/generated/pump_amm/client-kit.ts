// 🛑 DO NOT EDIT - AUTO-GENERATED BY NACLAC
// Re-run `naclac generate` to refresh this file.

import * as naclac from "@naclac-fw/client/kit";
import rawIDL from "./idl/pump_amm.json";
import type { PumpAmm } from "./idl/pump_amm";
const IDL = rawIDL as unknown as PumpAmm;
import * as instructions from "./instructions";
import * as accounts from "./accounts/kit";
import * as types from "./types";

/**
 * High-level client for the `pump_amm` program.
 * Uses the modern @solana/kit (v2) transaction model.
 */
export class PumpAmmClient {
  public readonly program: naclac.Program;
  public readonly instructions = instructions;
  public readonly accounts = accounts;
  public readonly types = types;
  public readonly errors = types.ProgramErrors;

  public get constants() {
    return this.program.constants;
  }

  /** The on-chain program address. */
  public get programId(): naclac.Address {
    return this.program.idl.address as naclac.Address;
  }

  constructor(
    providerOrCluster: naclac.NaclacProvider | "devnet" | "mainnet" | "localnet",
    payer?: naclac.KeyPairSigner
  ) {
    let provider: naclac.NaclacProvider;
    if (typeof providerOrCluster === "string") {
      if (!payer) throw new Error("[Naclac] A payer KeyPairSigner is required when specifying a cluster string.");
      provider = naclac.createProvider(providerOrCluster, payer);
    } else {
      provider = providerOrCluster;
    }
    this.program = new naclac.Program(IDL, provider);
  }

  /**
   * Builds the `createConfig` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public createConfig(args?: Record<string, never>, accounts?: Partial<instructions.CreateConfigAccounts>) {
    return instructions.createConfig(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `createPool` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public createPool(args: instructions.CreatePoolArgs, accounts?: Partial<instructions.CreatePoolAccounts>) {
    return instructions.createPool(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `transferCreatorFeesToPump` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public transferCreatorFeesToPump(args: instructions.TransferCreatorFeesToPumpArgs, accounts?: Partial<instructions.TransferCreatorFeesToPumpAccounts>) {
    return instructions.transferCreatorFeesToPump(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `transferCreatorFeesToPumpV2` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public transferCreatorFeesToPumpV2(args: instructions.TransferCreatorFeesToPumpV2Args, accounts?: Partial<instructions.TransferCreatorFeesToPumpV2Accounts>) {
    return instructions.transferCreatorFeesToPumpV2(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `initBoost` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public initBoost(args: instructions.InitBoostArgs, accounts?: Partial<instructions.InitBoostAccounts>) {
    return instructions.initBoost(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `boostBuyAndBurn` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public boostBuyAndBurn(args: instructions.BoostBuyAndBurnArgs, accounts?: Partial<instructions.BoostBuyAndBurnAccounts>) {
    return instructions.boostBuyAndBurn(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `toggleBoost` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public toggleBoost(args: instructions.ToggleBoostArgs, accounts?: Partial<instructions.ToggleBoostAccounts>) {
    return instructions.toggleBoost(this.program, args ?? {}, accounts);
  }

  /**
   * Builds the `setBoostAuthority` instruction pipeline.
   * Call `.rpc()` to send or `.instruction()` to get the raw instruction.
   */
  public setBoostAuthority(args?: Record<string, never>, accounts?: Partial<instructions.SetBoostAuthorityAccounts>) {
    return instructions.setBoostAuthority(this.program, args ?? {}, accounts);
  }

  /** Derives the PDA for a `boost_vault_authority` account. */
  public async getBoostVaultAuthorityPda(seeds: {
    pool: naclac.Address | string;
  }): Promise<readonly [naclac.Address, number]> {
    return naclac.getProgramDerivedAddress({
      programAddress: this.programId,
      seeds: [
                new Uint8Array([98, 111, 111, 115, 116, 95, 118, 97, 117, 108, 116]),
        new Uint8Array(naclac.getAddressEncoder().encode(typeof seeds.pool === 'string' ? naclac.address(seeds.pool) : seeds.pool))
      ]
    });
  }

  /** Derives the PDA for a `coin_creator_vault_authority` account. */
  public async getCoinCreatorVaultAuthorityPda(seeds: {
    coin_creator: naclac.Address | string;
  }): Promise<readonly [naclac.Address, number]> {
    return naclac.getProgramDerivedAddress({
      programAddress: this.programId,
      seeds: [
                new Uint8Array([99, 114, 101, 97, 116, 111, 114, 95, 118, 97, 117, 108, 116]),
        new Uint8Array(naclac.getAddressEncoder().encode(typeof seeds.coin_creator === 'string' ? naclac.address(seeds.coin_creator) : seeds.coin_creator))
      ]
    });
  }

  /** Derives the PDA for a `global_config` account. */
  public async getGlobalConfigPda(seeds: {
  }): Promise<readonly [naclac.Address, number]> {
    return naclac.getProgramDerivedAddress({
      programAddress: this.programId,
      seeds: [
                new Uint8Array([103, 108, 111, 98, 97, 108, 95, 99, 111, 110, 102, 105, 103])
      ]
    });
  }

  /** Derives the PDA for a `lp_mint` account. */
  public async getLpMintPda(seeds: {
    pool: naclac.Address | string;
  }): Promise<readonly [naclac.Address, number]> {
    return naclac.getProgramDerivedAddress({
      programAddress: this.programId,
      seeds: [
                new Uint8Array([112, 111, 111, 108, 95, 108, 112, 95, 109, 105, 110, 116]),
        new Uint8Array(naclac.getAddressEncoder().encode(typeof seeds.pool === 'string' ? naclac.address(seeds.pool) : seeds.pool))
      ]
    });
  }

  /** Derives the PDA for a `pool` account. */
  public async getPoolPda(seeds: {
    args: types.CreatePoolArgs;
    creator: naclac.Address | string;
    base_mint: naclac.Address | string;
    quote_mint: naclac.Address | string;
  }): Promise<readonly [naclac.Address, number]> {
    return naclac.getProgramDerivedAddress({
      programAddress: this.programId,
      seeds: [
                new Uint8Array([112, 111, 111, 108]),
        new Uint8Array(naclac.getIdlCodec(JSON.parse('"u16"')).encode(seeds.args.index)),
        new Uint8Array(naclac.getAddressEncoder().encode(typeof seeds.creator === 'string' ? naclac.address(seeds.creator) : seeds.creator)),
        new Uint8Array(naclac.getAddressEncoder().encode(typeof seeds.base_mint === 'string' ? naclac.address(seeds.base_mint) : seeds.base_mint)),
        new Uint8Array(naclac.getAddressEncoder().encode(typeof seeds.quote_mint === 'string' ? naclac.address(seeds.quote_mint) : seeds.quote_mint))
      ]
    });
  }

  /** Derives the PDA for a `pump_creator_vault` account. */
  public async getPumpCreatorVaultPda(seeds: {
    coin_creator: naclac.Address | string;
  }): Promise<readonly [naclac.Address, number]> {
    return naclac.getProgramDerivedAddress({
      programAddress: naclac.address("FoN4cWC8wuVYK3Dd2ge1WVTLpPUvj4CcWXZsq4wmadwD"),
      seeds: [
                new Uint8Array([99, 114, 101, 97, 116, 111, 114, 45, 118, 97, 117, 108, 116]),
        new Uint8Array(naclac.getAddressEncoder().encode(typeof seeds.coin_creator === 'string' ? naclac.address(seeds.coin_creator) : seeds.coin_creator))
      ]
    });
  }

  /** Fetches a `GlobalConfig` account. Throws if it does not exist. */
  public fetchGlobalConfig(address: naclac.Address | string) {
    return accounts.fetchGlobalConfig(this.program.provider.rpc as any, naclac.address(address as string));
  }

  /** Fetches a `GlobalConfig` account. Returns a MaybeAccount (check `.exists`) if it does not exist. */
  public fetchMaybeGlobalConfig(address: naclac.Address | string) {
    return accounts.fetchMaybeGlobalConfig(this.program.provider.rpc as any, naclac.address(address as string));
  }

  /** Fetches multiple `GlobalConfig` accounts by address. Throws if any do not exist. */
  public fetchAllGlobalConfigs(addresses: Array<naclac.Address | string>) {
    return accounts.fetchAllGlobalConfig(this.program.provider.rpc as any, addresses.map((a) => naclac.address(a as string)));
  }

  /** Fetches multiple `GlobalConfig` accounts. Missing accounts have `.exists = false`. */
  public fetchAllMaybeGlobalConfigs(addresses: Array<naclac.Address | string>) {
    return accounts.fetchAllMaybeGlobalConfig(this.program.provider.rpc as any, addresses.map((a) => naclac.address(a as string)));
  }

  /** Fetches a `Pool` account. Throws if it does not exist. */
  public fetchPool(address: naclac.Address | string) {
    return accounts.fetchPool(this.program.provider.rpc as any, naclac.address(address as string));
  }

  /** Fetches a `Pool` account. Returns a MaybeAccount (check `.exists`) if it does not exist. */
  public fetchMaybePool(address: naclac.Address | string) {
    return accounts.fetchMaybePool(this.program.provider.rpc as any, naclac.address(address as string));
  }

  /** Fetches multiple `Pool` accounts by address. Throws if any do not exist. */
  public fetchAllPools(addresses: Array<naclac.Address | string>) {
    return accounts.fetchAllPool(this.program.provider.rpc as any, addresses.map((a) => naclac.address(a as string)));
  }

  /** Fetches multiple `Pool` accounts. Missing accounts have `.exists = false`. */
  public fetchAllMaybePools(addresses: Array<naclac.Address | string>) {
    return accounts.fetchAllMaybePool(this.program.provider.rpc as any, addresses.map((a) => naclac.address(a as string)));
  }

  /** Subscribes to `CreatePoolEvent` events. Returns a listener ID. */
  public onCreatePoolEvent(callback: (event: types.CreatePoolEvent, slot: number, signature: string) => void) {
    return types.addCreatePoolEventListener(this.program, callback);
  }

  /** Awaits the next `CreatePoolEvent` event. Resolves `null` on timeout. */
  public waitForCreatePoolEvent(options?: { timeoutMs?: number }) {
    return this.program.waitForEvent<types.CreatePoolEvent>("CreatePoolEvent", options);
  }

  /** Decodes every `CreatePoolEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForCreatePoolEvent` — prefer this when you already know which transaction you're checking. */
  public parseCreatePoolEventEvents(logs: readonly string[]) {
    return this.program.parseEvents<types.CreatePoolEvent>("CreatePoolEvent", logs);
  }

  /** Subscribes to `InitBoostEvent` events. Returns a listener ID. */
  public onInitBoostEvent(callback: (event: types.InitBoostEvent, slot: number, signature: string) => void) {
    return types.addInitBoostEventListener(this.program, callback);
  }

  /** Awaits the next `InitBoostEvent` event. Resolves `null` on timeout. */
  public waitForInitBoostEvent(options?: { timeoutMs?: number }) {
    return this.program.waitForEvent<types.InitBoostEvent>("InitBoostEvent", options);
  }

  /** Decodes every `InitBoostEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForInitBoostEvent` — prefer this when you already know which transaction you're checking. */
  public parseInitBoostEventEvents(logs: readonly string[]) {
    return this.program.parseEvents<types.InitBoostEvent>("InitBoostEvent", logs);
  }

  /** Subscribes to `BoostBuyAndBurnEvent` events. Returns a listener ID. */
  public onBoostBuyAndBurnEvent(callback: (event: types.BoostBuyAndBurnEvent, slot: number, signature: string) => void) {
    return types.addBoostBuyAndBurnEventListener(this.program, callback);
  }

  /** Awaits the next `BoostBuyAndBurnEvent` event. Resolves `null` on timeout. */
  public waitForBoostBuyAndBurnEvent(options?: { timeoutMs?: number }) {
    return this.program.waitForEvent<types.BoostBuyAndBurnEvent>("BoostBuyAndBurnEvent", options);
  }

  /** Decodes every `BoostBuyAndBurnEvent` event found in an already-fetched list of transaction log lines (e.g. `.rpc()`'s returned `logs`). Race-free, unlike `waitForBoostBuyAndBurnEvent` — prefer this when you already know which transaction you're checking. */
  public parseBoostBuyAndBurnEventEvents(logs: readonly string[]) {
    return this.program.parseEvents<types.BoostBuyAndBurnEvent>("BoostBuyAndBurnEvent", logs);
  }

  /** Removes a registered event listener by its ID. */
  public removeEventListener(listenerId: number) {
    return types.removeListener(this.program, listenerId);
  }
  /** Fetches ALL `GlobalConfig` accounts owned by this program. */
  public fetchAllGlobalConfigByOwner(options?: { commitment?: any; filters?: unknown[] }) {
    return accounts.fetchAllGlobalConfigByOwner(this.program.provider.rpc as any, this.programId, options);
  }

  /** Fetches ALL `Pool` accounts owned by this program. */
  public fetchAllPoolByOwner(options?: { commitment?: any; filters?: unknown[] }) {
    return accounts.fetchAllPoolByOwner(this.program.provider.rpc as any, this.programId, options);
  }

}
